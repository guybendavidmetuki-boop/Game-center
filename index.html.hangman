<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>איש תלוי</title>
  <style>
    :root {
      --bg: #e9fbff;
      --bg-soft: #fff7cf;
      --panel: rgba(255, 255, 255, 0.78);
      --ink: #14304a;
      --accent: #ff7a59;
      --accent-2: #19b7c6;
      --accent-3: #ffd166;
      --good: #189a62;
      --bad: #d94c66;
      --line: rgba(25, 183, 198, 0.22);
      --shadow: rgba(20, 48, 74, 0.14);
      --theme-a: rgba(255, 209, 102, 0.68);
      --theme-b: rgba(25, 183, 198, 0.24);
      --theme-c: rgba(255, 143, 171, 0.2);
      --theme-d: #fff7cf;
      --theme-panel: rgba(255, 255, 255, 0.88);
      --theme-glow: rgba(25, 183, 198, 0.18);
      --mode-strip: linear-gradient(135deg, #1bbfd0, #58d9c7, #ffd166);
      --glass-border: rgba(255, 255, 255, 0.52);
      --deep-shadow: 0 24px 60px rgba(20, 48, 74, 0.14);
      --soft-shadow: 0 14px 28px rgba(20, 48, 74, 0.08);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: "Trebuchet MS", "Arial", sans-serif;
      background:
        radial-gradient(circle at top right, var(--theme-a) 0%, transparent 40%),
        radial-gradient(circle at bottom left, var(--theme-b) 0%, transparent 42%),
        radial-gradient(circle at 78% 72%, var(--theme-c) 0%, transparent 36%),
        linear-gradient(145deg, var(--bg) 0%, var(--theme-d) 100%);
      color: var(--ink);
      display: block;
      padding: 0;
      transition: background 0.45s ease;
      position: relative;
      overflow-x: hidden;
    }

    body.night-mode {
      --bg: #0f172a;
      --bg-soft: #1e293b;
      --panel: rgba(15, 23, 42, 0.84);
      --ink: #eef7ff;
      --line: rgba(125, 211, 252, 0.24);
      --shadow: rgba(2, 8, 23, 0.42);
      --theme-d: #172554;
      --theme-panel: rgba(15, 23, 42, 0.88);
      color: #eef7ff;
    }

    body::before,
    body::after {
      content: "";
      position: fixed;
      inset: auto;
      width: 32vw;
      height: 32vw;
      border-radius: 50%;
      pointer-events: none;
      filter: blur(28px);
      opacity: 0.62;
      z-index: 0;
      animation: backgroundFloat 12s ease-in-out infinite;
    }

    body::before {
      top: 6vh;
      left: 4vw;
      background: radial-gradient(circle, var(--theme-a), transparent 68%);
    }

    body::after {
      right: 5vw;
      bottom: 6vh;
      background: radial-gradient(circle, var(--theme-c), transparent 68%);
      animation-delay: -5s;
    }

    .game {
      position: relative;
      z-index: 1;
      width: 100%;
      min-height: 100vh;
      background:
        radial-gradient(circle at top left, var(--theme-a), transparent 42%),
        radial-gradient(circle at bottom right, var(--theme-b), transparent 40%),
        linear-gradient(180deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.48));
      border: none;
      border-radius: 0;
      box-shadow: none;
      overflow: hidden;
      backdrop-filter: blur(10px);
      transition: background 0.45s ease, box-shadow 0.35s ease;
    }

    .game::before {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.18), transparent 18%, transparent 82%, rgba(255, 255, 255, 0.08)),
        radial-gradient(circle at 10% 12%, rgba(255, 255, 255, 0.16), transparent 22%),
        radial-gradient(circle at 88% 16%, rgba(255, 255, 255, 0.14), transparent 18%);
      z-index: 0;
    }

    .start-screen {
      position: absolute;
      inset: 0;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 24px;
      overflow-y: auto;
      background:
        radial-gradient(circle at 15% 18%, var(--theme-a) 0 14%, transparent 15%),
        radial-gradient(circle at 82% 14%, var(--theme-b) 0 15%, transparent 16%),
        radial-gradient(circle at 78% 78%, var(--theme-c) 0 15%, transparent 16%),
        radial-gradient(circle at 20% 78%, var(--theme-glow) 0 13%, transparent 14%),
        linear-gradient(135deg, var(--bg), var(--theme-d) 52%, rgba(255, 255, 255, 0.92));
      z-index: 30;
    }

    .start-screen.hidden {
      display: none;
    }

    .home-overlay {
      position: absolute;
      inset: 0;
      z-index: 34;
      display: grid;
      place-items: center;
      padding: 24px;
      background:
        radial-gradient(circle at 18% 20%, rgba(255, 122, 89, 0.2), transparent 28%),
        radial-gradient(circle at 82% 18%, rgba(25, 183, 198, 0.2), transparent 30%),
        linear-gradient(135deg, rgba(255, 248, 235, 0.92), rgba(236, 251, 255, 0.95), rgba(255, 241, 246, 0.94));
      transition: opacity 0.28s ease;
    }

    .home-overlay.hidden {
      opacity: 0;
      pointer-events: none;
    }

    .home-card {
      width: min(760px, 100%);
      padding: 30px;
      border-radius: 34px;
      background:
        radial-gradient(circle at top right, rgba(255, 209, 102, 0.22), transparent 32%),
        linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94), rgba(255, 245, 249, 0.96));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 34px 64px rgba(20, 48, 74, 0.16), inset 0 1px 0 rgba(255, 255, 255, 0.7);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .home-card::after {
      content: "";
      position: absolute;
      inset: auto -30px -36px auto;
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255, 209, 102, 0.22), transparent 72%);
      pointer-events: none;
    }

    .home-mascot {
      width: 96px;
      height: 96px;
      margin: 0 auto 14px;
      display: grid;
      place-items: center;
      border-radius: 30px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.9), rgba(101, 208, 255, 0.92), rgba(255, 143, 171, 0.9));
      font-size: 3rem;
      box-shadow: 0 24px 42px rgba(20, 48, 74, 0.14);
      animation: mascotFloat 4.5s ease-in-out infinite;
    }

    .home-title {
      margin: 0 0 8px;
      color: #0e5872;
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 900;
    }

    .home-subtitle {
      margin: 0 auto 18px;
      max-width: 38ch;
      color: rgba(20, 48, 74, 0.74);
      font-size: 1rem;
      line-height: 1.6;
    }

    .home-guide-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      margin: 0 0 18px;
      text-align: right;
    }

    .home-guide-step {
      padding: 12px 14px;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.06);
    }

    .home-guide-step strong {
      display: block;
      margin-bottom: 4px;
      color: #0e5872;
      font-size: 0.9rem;
    }

    .home-guide-step span {
      color: rgba(20, 48, 74, 0.72);
      font-size: 0.84rem;
      line-height: 1.45;
    }

    .home-enter {
      min-width: 220px;
      min-height: 54px;
      font-size: 1rem;
    }

    .start-card {
      width: min(980px, 100%);
      max-height: calc(100vh - 48px);
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.28), rgba(255, 255, 255, 0.12));
      border: none;
      border-radius: 36px;
      padding: 30px;
      box-shadow: var(--deep-shadow);
      position: relative;
      overflow-x: hidden;
      overflow-y: auto;
      backdrop-filter: blur(12px);
    }

    .start-card::before {
      content: none;
      position: absolute;
      inset: -40% auto auto -10%;
      width: 220px;
      height: 220px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(25, 183, 198, 0.14), transparent 68%);
      pointer-events: none;
    }

    .start-card::after {
      content: none;
      position: absolute;
      inset: auto -6% -18% auto;
      width: 200px;
      height: 200px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255, 122, 89, 0.16), transparent 70%);
      pointer-events: none;
    }

    .start-title {
      margin: 0 0 10px;
      font-size: clamp(2.2rem, 5vw, 4rem);
      color: #0e5872;
      letter-spacing: 0.03em;
      text-shadow: 0 8px 24px rgba(25, 183, 198, 0.16);
    }

    .start-subtitle {
      margin: 0 0 24px;
      color: rgba(20, 48, 74, 0.72);
      font-size: 1.05rem;
      max-width: 54ch;
    }

    .start-highlights {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      margin: 0 0 22px;
    }

    .future-games-nav {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin: 0 0 16px;
    }

    .future-game-pill {
      min-height: 40px;
      padding: 8px 12px;
      border-radius: 999px;
      border: 1px solid rgba(25, 183, 198, 0.16);
      background: rgba(255, 255, 255, 0.82);
      color: #0e5872;
      font-size: 0.82rem;
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.06);
    }

    .future-game-pill.active {
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #ffd166);
      color: #fff;
    }

    .future-game-pill.locked {
      opacity: 0.58;
    }

    .highlight-card {
      position: relative;
      overflow: hidden;
      min-height: 86px;
      padding: 14px 14px 12px;
      border-radius: 20px;
      background:
        radial-gradient(circle at top left, rgba(255, 255, 255, 0.92), transparent 48%),
        linear-gradient(135deg, rgba(255, 255, 255, 0.92), rgba(243, 251, 255, 0.85));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 16px 30px rgba(20, 48, 74, 0.08);
    }

    .highlight-card::after {
      content: "";
      position: absolute;
      inset: auto -22px -30px auto;
      width: 84px;
      height: 84px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255, 209, 102, 0.24), transparent 70%);
      pointer-events: none;
    }

    .highlight-icon {
      display: inline-grid;
      place-items: center;
      width: 36px;
      height: 36px;
      margin-bottom: 10px;
      border-radius: 14px;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.18), rgba(255, 209, 102, 0.2));
      font-size: 1.15rem;
    }

    .highlight-title {
      margin: 0 0 4px;
      color: #0e5872;
      font-size: 0.95rem;
      font-weight: 800;
    }

    .highlight-text {
      margin: 0;
      color: rgba(20, 48, 74, 0.72);
      font-size: 0.82rem;
      line-height: 1.4;
    }

    .start-topline,
    .hero-topline {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 16px;
    }

    .start-dashboard {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      gap: 10px;
      margin-bottom: 18px;
    }

    .start-dashboard-card {
      padding: 12px 14px;
      border-radius: 18px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.94), rgba(240, 251, 255, 0.88));
      border: 1px solid rgba(25, 183, 198, 0.14);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.06);
      text-align: center;
    }

    .start-dashboard-value {
      display: block;
      color: #0e5872;
      font-size: 1.25rem;
      font-weight: 800;
    }

    .start-dashboard-label {
      display: block;
      margin-top: 4px;
      color: rgba(20, 48, 74, 0.7);
      font-size: 0.8rem;
      font-weight: 700;
    }

    .avatar-showcase,
    .hero-avatar {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .avatar-pill {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
      font-weight: 700;
      color: #0e5872;
      min-height: 42px;
    }

    .avatar-pill strong {
      font-size: 1.2rem;
    }

    .mode-chip,
    .mode-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 9px 14px;
      border-radius: 999px;
      color: #fff;
      background: var(--mode-strip);
      box-shadow: 0 14px 28px rgba(25, 183, 198, 0.2);
      font-weight: 700;
    }

    .settings-grid {
      display: grid;
      grid-template-columns: repeat(12, minmax(0, 1fr));
      gap: 16px;
      margin-bottom: 18px;
    }

    .setting-card {
      background:
        radial-gradient(circle at top right, rgba(255, 255, 255, 0.54), transparent 32%),
        linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(246, 253, 255, 0.84));
      border: 1px solid var(--glass-border);
      border-radius: 24px;
      padding: 16px;
      box-shadow: var(--soft-shadow), inset 0 1px 0 rgba(255, 255, 255, 0.7);
    }

    .settings-grid > .setting-card {
      grid-column: span 6;
    }

    .setting-label {
      display: block;
      margin-bottom: 10px;
      font-weight: 700;
      color: #0e5872;
    }

    .select,
    .text-input {
      width: 100%;
      border: 1px solid rgba(25, 183, 198, 0.22);
      background: #fff;
      border-radius: 14px;
      padding: 12px 14px;
      font: inherit;
      font-size: 1rem;
      color: var(--ink);
    }

    .select.ui-field-flash,
    .text-input.ui-field-flash,
    .category-chip.ui-field-flash,
    .avatar-picker.ui-field-flash,
    .style-picker.ui-field-flash {
      animation: fieldFlash 0.68s cubic-bezier(0.18, 0.88, 0.22, 1);
    }

    .mini-add-word {
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px dashed rgba(25, 183, 198, 0.18);
    }

    .mini-add-actions {
      display: flex;
      gap: 8px;
      margin-top: 8px;
      flex-wrap: wrap;
    }

    .mini-add-button {
      min-width: 118px;
      min-height: 44px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.34), rgba(25, 183, 198, 0.18), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .mini-remove-button {
      min-width: 118px;
      min-height: 44px;
      background: linear-gradient(135deg, rgba(255, 143, 171, 0.26), rgba(255, 209, 102, 0.2), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .mini-add-note {
      margin-top: 8px;
      min-height: 22px;
      font-size: 0.86rem;
      color: rgba(20, 48, 74, 0.72);
    }

    .mini-add-note.success {
      color: var(--good);
      font-weight: 700;
    }

    .mini-add-note.error {
      color: var(--bad);
      font-weight: 700;
    }

    .avatar-picker {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 8px;
    }

    .avatar-option {
      min-width: 64px;
      min-height: 58px;
      padding: 8px 10px;
      font-size: 1rem;
      background: rgba(255, 255, 255, 0.94);
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 8px 16px rgba(20, 48, 74, 0.08);
      display: inline-flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 2px;
      font-family: "Trebuchet MS", "Arial", "Apple Color Emoji", "Segoe UI Emoji", sans-serif;
    }

    .avatar-option.active {
      background: linear-gradient(135deg, #ff7a59, #ffd166);
      color: #fff;
      transform: translateY(-1px);
    }

    .avatar-option.avatar-bob .avatar-emoji,
    .avatar-pill.avatar-bob strong {
      animation: avatarBob 2.7s ease-in-out infinite;
    }

    .avatar-option.avatar-sway .avatar-emoji,
    .avatar-pill.avatar-sway strong {
      animation: avatarSway 2.4s ease-in-out infinite;
      transform-origin: center bottom;
    }

    .avatar-option.avatar-blink .avatar-emoji,
    .avatar-pill.avatar-blink strong {
      animation: avatarBlink 3s steps(1) infinite;
    }

    .avatar-option.avatar-glow .avatar-emoji,
    .avatar-pill.avatar-glow strong {
      animation: avatarGlow 2.2s ease-in-out infinite;
    }

    .avatar-emoji {
      font-size: 1.35rem;
      line-height: 1;
    }

    .avatar-label {
      font-size: 0.68rem;
      font-weight: 800;
      line-height: 1;
    }

    .leaderboard-list {
      display: grid;
      gap: 8px;
      margin-top: 4px;
    }

    .leaderboard-item {
      display: flex;
      justify-content: space-between;
      gap: 8px;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.86rem;
    }

    .leaderboard-item strong {
      color: #0e5872;
    }

    .leaderboard-empty {
      color: rgba(20, 48, 74, 0.6);
      font-size: 0.88rem;
      margin: 0;
    }

    .helper-actions {
      display: flex;
      gap: 8px;
      margin-top: 10px;
      flex-wrap: wrap;
    }

    .category-multi {
      margin-top: 14px;
      padding: 14px 12px 0;
      border-top: 1px dashed rgba(25, 183, 198, 0.18);
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.72), rgba(238, 251, 255, 0.8));
    }

    .category-multi-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
    }

    .category-multi-note {
      margin: 0 0 10px;
      color: rgba(20, 48, 74, 0.68);
      font-size: 0.82rem;
      line-height: 1.4;
    }

    .category-clear-button {
      min-height: 34px;
      padding: 6px 12px;
      font-size: 0.8rem;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.22), rgba(25, 183, 198, 0.18), rgba(255, 255, 255, 0.96));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 8px 16px rgba(20, 48, 74, 0.06);
    }

    .category-clear-button.hidden {
      display: none;
    }

    .category-multi .setting-label {
      margin-top: 12px;
      margin-bottom: 6px;
    }

    .category-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
      max-height: 180px;
      overflow-y: auto;
      padding-inline-end: 4px;
    }

    .category-chip {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 9px 10px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.86rem;
      cursor: pointer;
    }

    .category-chip input {
      margin: 0;
    }

    .mode-records {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .mode-record {
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.84);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.86rem;
    }

    .profile-list,
    .player-stats-list,
    .daily-card,
    .import-export-box {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .profile-item,
    .player-stat-item,
    .daily-card,
    .import-export-box {
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.84);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.86rem;
    }

    .mission-list,
    .style-picker,
    .pack-list {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .mission-item,
    .pack-item {
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.84);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.85rem;
    }

    .mission-item.done {
      border-color: rgba(24, 154, 98, 0.26);
      background: rgba(232, 255, 241, 0.92);
    }

    .daily-wheel {
      margin-top: 8px;
      padding: 6px;
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.22), rgba(255, 122, 89, 0.18), rgba(255, 255, 255, 0.92));
      border: 1px solid rgba(255, 183, 77, 0.22);
    }

    #daily-helper-card .daily-card,
    #daily-helper-card .streak-card,
    #daily-helper-card .mission-item {
      padding: 6px 8px;
      font-size: 0.72rem;
    }

    .daily-wheel-wrap {
      position: relative;
      width: 102px;
      margin: 0 auto 4px;
      padding-top: 10px;
    }

    .daily-wheel-pointer {
      position: absolute;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 0;
      height: 0;
      border-left: 8px solid transparent;
      border-right: 8px solid transparent;
      border-top: 12px solid #0e5872;
      z-index: 2;
      filter: drop-shadow(0 4px 6px rgba(20, 48, 74, 0.2));
    }

    .daily-wheel-disc {
      position: relative;
      width: 92px;
      height: 92px;
      margin: 0 auto 4px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      font-size: 1.25rem;
      font-weight: 700;
      color: #0e5872;
      background:
        conic-gradient(
          from -30deg,
          #ffd166 0 16.66%,
          #65d0ff 16.66% 33.32%,
          #ff8fab 33.32% 49.98%,
          #8fe388 49.98% 66.64%,
          #c4b5fd 66.64% 83.3%,
          #ffb347 83.3% 100%
        );
      box-shadow: inset 0 0 0 8px rgba(255, 255, 255, 0.66), 0 10px 16px rgba(255, 183, 77, 0.14);
      transition: transform 2.2s cubic-bezier(0.18, 0.86, 0.18, 1);
    }

    .daily-wheel-disc::after {
      content: attr(data-center-label);
      position: absolute;
      inset: 50%;
      width: 34px;
      height: 34px;
      transform: translate(-50%, -50%);
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: rgba(255, 255, 255, 0.86);
      box-shadow: inset 0 0 0 4px rgba(255, 255, 255, 0.7), 0 8px 16px rgba(20, 48, 74, 0.1);
      color: #0e5872;
      font-size: 0.88rem;
    }

    .daily-wheel-disc.spinning {
      filter: saturate(1.08) brightness(1.02);
    }

    .daily-wheel-prizes {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 3px 5px;
      margin: 4px 0 0;
      padding: 0;
      list-style: none;
      font-size: 0.64rem;
      color: #0e5872;
    }

    .daily-wheel-prizes li {
      padding: 3px 5px;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.64);
      border: 1px solid rgba(255, 183, 77, 0.14);
    }

    .daily-wheel-prizes li.active {
      background: linear-gradient(135deg, rgba(255, 232, 153, 0.94), rgba(255, 179, 71, 0.9));
      border-color: rgba(255, 183, 77, 0.42);
      box-shadow: 0 10px 18px rgba(255, 183, 77, 0.18);
      font-weight: 700;
    }

    .daily-wheel-note {
      margin: 0;
      text-align: center;
      color: rgba(20, 48, 74, 0.76);
      font-size: 0.66rem;
      line-height: 1.18;
    }

    .profile-summary {
      margin-top: 12px;
      padding: 12px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      color: #0e5872;
      font-size: 0.86rem;
      line-height: 1.6;
    }

    .pack-item.active {
      border-color: rgba(255, 122, 89, 0.28);
      background: rgba(255, 246, 234, 0.94);
    }

    .style-picker {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
    }

    .style-swatch {
      min-height: 42px;
      border-radius: 14px;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      color: #0e5872;
      background: linear-gradient(135deg, #fff, #eefcff);
    }

    .style-swatch.active {
      color: #fff;
      box-shadow: 0 12px 24px rgba(20, 48, 74, 0.14);
      transform: translateY(-1px);
    }

    .style-swatch[data-style-key="cheerful"] {
      background: linear-gradient(135deg, #ff7a59, #ffd166);
      color: #fff;
    }

    .style-swatch[data-style-key="ocean"] {
      background: linear-gradient(135deg, #19b7c6, #65d0ff);
      color: #fff;
    }

    .style-swatch[data-style-key="sunset"] {
      background: linear-gradient(135deg, #f97316, #ef4444, #fdba74);
      color: #fff;
    }

    .streak-card {
      padding: 8px 10px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.84);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.78rem;
      margin-top: 8px;
    }

    .profile-panel {
      margin-top: 4px;
      padding: 10px;
      border-radius: 22px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.16);
      display: grid;
      grid-template-columns: minmax(164px, 208px) minmax(0, 1fr);
      gap: 10px;
      overflow: hidden;
    }

    .profile-panel.open {
      display: grid;
    }

    .profile-sidebar {
      display: grid;
      gap: 8px;
      align-content: start;
    }

    .profile-list {
      margin-top: 0;
    }

    .profile-tab {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: flex-start;
      gap: 8px;
      text-align: right;
      padding: 12px 12px;
      border-radius: 18px;
      border: 1px solid rgba(25, 183, 198, 0.14);
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(233, 248, 255, 0.92));
      color: #0e5872;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.06);
      overflow-wrap: anywhere;
      word-break: break-word;
    }

    .profile-tab-avatar {
      font-size: 1.2rem;
      line-height: 1;
      flex: 0 0 auto;
    }

    .profile-tab-text {
      min-width: 0;
      flex: 1 1 auto;
    }

    .profile-tab.active {
      background: linear-gradient(135deg, rgba(101, 208, 255, 0.96), rgba(255, 209, 102, 0.96));
      border-color: rgba(25, 183, 198, 0.32);
      box-shadow: 0 16px 28px rgba(34, 197, 194, 0.18);
      transform: translateX(-2px);
    }

    .profile-tab-name {
      display: block;
      font-weight: 800;
      font-size: 0.9rem;
    }

    .profile-tab-meta {
      display: block;
      margin-top: 4px;
      font-size: 0.73rem;
      color: rgba(20, 48, 74, 0.76);
    }

    .profile-detail {
      min-height: 220px;
      padding: 14px;
      border-radius: 24px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.98), rgba(239, 252, 255, 0.95));
      border: 1px solid rgba(25, 183, 198, 0.16);
      color: #0e5872;
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.5);
      overflow-wrap: anywhere;
      word-break: break-word;
    }

    .profile-detail-empty {
      display: grid;
      place-items: center;
      min-height: 180px;
      text-align: center;
      font-size: 0.92rem;
      color: rgba(20, 48, 74, 0.72);
    }

    .profile-hero {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 12px;
    }

    .profile-hero-avatar {
      width: 54px;
      height: 54px;
      border-radius: 24px;
      display: grid;
      place-items: center;
      font-size: 1.55rem;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.92), rgba(101, 208, 255, 0.9));
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.12);
    }

    .profile-hero-title {
      margin: 0;
      font-size: 1.15rem;
      font-weight: 800;
    }

    .profile-hero-subtitle {
      margin: 6px 0 0;
      font-size: 0.82rem;
      color: rgba(20, 48, 74, 0.74);
      overflow-wrap: anywhere;
      word-break: break-word;
    }

    .profile-rank-row {
      display: grid;
      gap: 8px;
      margin-bottom: 12px;
    }

    .profile-rank-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
      font-size: 0.84rem;
      color: rgba(20, 48, 74, 0.76);
    }

    .profile-rank-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      border-radius: 999px;
      font-weight: 800;
      color: #0e5872;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.3), rgba(101, 208, 255, 0.32), rgba(255, 255, 255, 0.92));
      border: 1px solid rgba(25, 183, 198, 0.18);
    }

    .profile-progress {
      position: relative;
      height: 12px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .profile-progress-fill {
      position: absolute;
      inset: 0 auto 0 0;
      border-radius: inherit;
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #ffd166);
    }

    .profile-stat-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
      margin-bottom: 10px;
    }

    .profile-stat-card,
    .profile-meta-card {
      padding: 10px 12px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      overflow-wrap: anywhere;
      word-break: break-word;
    }

    .profile-stat-card strong,
    .profile-meta-card strong {
      display: block;
      margin-bottom: 5px;
      font-size: 0.76rem;
      color: rgba(20, 48, 74, 0.66);
    }

    .profile-stat-value {
      font-size: 1.42rem;
      font-weight: 800;
    }

    .profile-meta-grid {
      display: grid;
      gap: 8px;
      margin-bottom: 10px;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .profile-actions {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .profile-achievement-list {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 8px;
    }

    .profile-achievement-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 7px 10px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.18), rgba(101, 208, 255, 0.2), rgba(255, 255, 255, 0.92));
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.8rem;
      font-weight: 700;
    }

    @media (max-width: 760px) {
      .profile-panel.open {
        grid-template-columns: 1fr;
      }

      #profiles-card .profile-panel {
        grid-template-columns: 1fr;
      }

      .profile-stat-grid {
        grid-template-columns: 1fr 1fr;
      }
    }

    .tiny-button {
      min-height: 40px;
      padding: 8px 12px;
      font-size: 0.82rem;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.18), rgba(125, 211, 252, 0.2), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .daily-card strong,
    .import-export-box strong {
      color: #0e5872;
    }

    .bonus-meta {
      color: rgba(20, 48, 74, 0.68);
      font-size: 0.78rem;
    }

    .shop-coins {
      font-weight: 700;
      color: #0e5872;
      margin: 0 0 10px;
    }

    .shop-items {
      display: grid;
      gap: 8px;
    }

    .shop-item {
      display: flex;
      justify-content: space-between;
      gap: 8px;
      align-items: center;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.85rem;
    }

    .shop-buy {
      min-height: 40px;
      padding: 8px 12px;
      font-size: 0.82rem;
      background: linear-gradient(135deg, rgba(255, 122, 89, 0.22), rgba(255, 209, 102, 0.28), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .special-mode-pills {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 8px;
      margin-top: 12px;
    }

    .special-mode-pill {
      min-height: 46px;
      padding: 10px 8px;
      font-size: 0.92rem;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .special-mode-pill.active {
      color: #fff;
      background: linear-gradient(135deg, #7f8cff, #65d0ff);
      box-shadow: 0 14px 26px rgba(95, 136, 255, 0.24);
      transform: translateY(-1px);
    }

    .end-summary {
      margin: 0 0 18px;
      padding: 16px;
      border-radius: 22px;
      background: rgba(255, 255, 255, 0.14);
      border: 1px solid rgba(255, 255, 255, 0.16);
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .end-summary-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .end-summary-chip {
      padding: 10px 12px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.12);
      border: 1px solid rgba(255, 255, 255, 0.14);
      font-weight: 700;
      text-align: right;
    }

    .end-summary-cinematic {
      display: grid;
      gap: 14px;
    }

    .end-summary-hero {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 12px;
      align-items: center;
      padding: 14px 16px;
      border-radius: 22px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.18), rgba(255, 255, 255, 0.08));
      border: 1px solid rgba(255, 255, 255, 0.22);
      box-shadow: 0 16px 28px rgba(10, 20, 40, 0.12);
      animation: softRise 0.7s ease both;
    }

    .end-summary-hero-avatar {
      width: 64px;
      height: 64px;
      border-radius: 24px;
      display: grid;
      place-items: center;
      font-size: 2rem;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.86), rgba(101, 208, 255, 0.82));
      box-shadow: 0 16px 28px rgba(10, 20, 40, 0.15);
    }

    .end-summary-hero-text strong,
    .end-summary-hero-text span {
      display: block;
    }

    .end-summary-hero-text strong {
      font-size: 1rem;
      margin-bottom: 6px;
    }

    .end-summary-hero-text span {
      color: rgba(255, 255, 255, 0.88);
      font-size: 0.88rem;
      line-height: 1.5;
    }

    .end-summary-medal {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 14px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.22), rgba(255, 255, 255, 0.14));
      border: 1px solid rgba(255, 255, 255, 0.18);
      font-weight: 800;
    }

    .end-summary-stats {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
    }

    .end-summary-stat {
      padding: 12px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.11);
      border: 1px solid rgba(255, 255, 255, 0.14);
      text-align: center;
      animation: softRise 0.75s ease both;
    }

    .end-summary-stat strong {
      display: block;
      font-size: 1.35rem;
      margin-bottom: 4px;
    }

    .end-summary-stat span {
      display: block;
      font-size: 0.8rem;
      color: rgba(255, 255, 255, 0.82);
    }

    .end-achievement-list {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px;
      margin: 8px 0 0;
    }

    .end-achievement-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.16);
      border: 1px solid rgba(255, 255, 255, 0.18);
      font-size: 0.84rem;
      font-weight: 700;
    }

    .party-ranking {
      display: grid;
      gap: 8px;
      margin: 0 0 18px;
    }

    .party-rank-item {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      align-items: center;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.14);
      border: 1px solid rgba(255, 255, 255, 0.16);
      color: #fff;
      font-size: 0.9rem;
    }

    .party-rank-item.top {
      background: rgba(255, 209, 102, 0.22);
      border-color: rgba(255, 209, 102, 0.34);
    }

    .ghost-button {
      min-height: 46px;
      padding: 10px 12px;
      font-size: 0.92rem;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.18), rgba(255, 209, 102, 0.18), rgba(255, 255, 255, 0.96));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .mode-pills {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .mode-pill {
      flex: 1 1 160px;
      background: linear-gradient(180deg, #fff 0%, #effcff 100%);
      border: 1px solid var(--line);
      color: var(--ink);
      font-weight: 700;
      min-height: 46px;
      padding: 10px 8px;
      font-size: 0.92rem;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .mode-pill.active {
      background: linear-gradient(135deg, #1bbfd0, #58d9c7, #ffd166);
      color: #fff;
      box-shadow: 0 14px 30px rgba(25, 183, 198, 0.26);
    }

    .custom-settings.hidden {
      display: none;
    }

    .single-player-settings.hidden {
      display: none;
    }

    .hidden {
      display: none !important;
    }

    .start-actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 14px;
      margin-top: 22px;
      flex-wrap: wrap;
    }

    .start-note {
      color: rgba(20, 48, 74, 0.7);
      font-size: 0.95rem;
      background: rgba(255, 255, 255, 0.62);
      padding: 12px 16px;
      border-radius: 16px;
      border: 1px solid rgba(25, 183, 198, 0.14);
      transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease, color 0.18s ease;
    }

    .start-note.alert {
      color: #9d2541;
      background: linear-gradient(135deg, rgba(255, 224, 230, 0.96), rgba(255, 243, 246, 0.92));
      border-color: rgba(217, 76, 102, 0.32);
      box-shadow: 0 10px 24px rgba(217, 76, 102, 0.12);
      transform: scale(1.02);
    }

    .start-button {
      min-width: 190px;
      min-height: 50px;
      padding: 12px 18px;
      font-size: 0.92rem;
      background: linear-gradient(135deg, #ff8a5b, #ffd166 55%, #6bd8ff);
      color: #0d4862;
      border: 1px solid rgba(255, 255, 255, 0.56);
      font-weight: 800;
      box-shadow: 0 18px 32px rgba(20, 48, 74, 0.12);
      transform-origin: center;
    }

    .start-button:hover {
      transform: translateY(-2px) scale(1.01);
    }

    .helper-panels {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(235px, 1fr));
      gap: 14px;
      margin-top: 16px;
      align-items: stretch;
    }

    .helper-card {
      background:
        radial-gradient(circle at top right, rgba(255, 255, 255, 0.34), transparent 30%),
        rgba(255, 255, 255, 0.74);
      border: 1px solid rgba(255, 255, 255, 0.56);
      border-radius: 26px;
      padding: 16px 18px;
      height: 100%;
      display: flex;
      flex-direction: column;
      box-shadow: var(--soft-shadow);
      backdrop-filter: blur(10px);
    }

    .helper-title {
      margin: 0 0 10px;
      font-size: 0.95rem;
      color: #0e5872;
    }

    #achievements-card,
    #leaderboard-card,
    #profiles-card,
    #daily-helper-card {
      margin-top: 0;
      width: 100%;
      height: 100%;
      min-height: 100%;
      justify-self: stretch;
      align-self: stretch;
      padding: 14px 16px;
      border-radius: 22px;
    }

    #achievements-card .helper-title,
    #leaderboard-card .helper-title,
    #profiles-card .helper-title,
    #daily-helper-card .helper-title {
      margin-bottom: 6px;
      font-size: 0.95rem;
    }

    #achievements-card .helper-actions,
    #leaderboard-card .helper-actions,
    #profiles-card .helper-actions,
    #daily-helper-card .helper-actions {
      margin-top: 6px;
    }

    #leaderboard-card .leaderboard-item,
    #leaderboard-card .mode-record {
      padding: 6px 7px;
      font-size: 0.86rem;
      line-height: normal;
    }

    #leaderboard-card .leaderboard-list,
    #leaderboard-card .mode-records {
      gap: 5px;
      margin-top: 5px;
    }

    #profiles-card .profile-panel {
      grid-template-columns: 1fr;
      gap: 10px;
      padding: 10px;
      align-items: start;
    }

    .profile-sidebar {
      padding: 8px;
      border-radius: 16px;
      background: linear-gradient(180deg, rgba(227, 247, 255, 0.96), rgba(255, 248, 217, 0.9));
      border: 1px solid rgba(25, 183, 198, 0.14);
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.48);
    }

    #profiles-card .profile-list {
      display: grid;
      grid-template-columns: 1fr;
      gap: 6px;
      margin-top: 0;
    }

    #profiles-card .profile-tab {
      min-width: 0;
      width: 100%;
    }

    #profiles-card .profile-detail {
      min-height: 260px;
    }

    #profiles-card .profile-meta-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    @media (min-width: 1180px) {
      .start-screen {
        justify-content: center;
        padding: 24px;
      }

      .start-card {
        width: min(1180px, 100%);
        margin: 0 auto;
      }
    }

    .helper-list {
      margin: 0;
      padding-inline-start: 18px;
      color: rgba(20, 48, 74, 0.74);
      font-size: 0.92rem;
      line-height: 1.55;
    }

    .achievement-badges {
      display: grid;
      gap: 10px;
    }

    #achievements-card .achievement-badges {
      gap: 8px;
    }

    .achievement-section {
      padding: 10px 12px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.14);
    }

    .achievement-section-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin: 0 0 8px;
      color: #0e5872;
      font-size: 0.85rem;
      font-weight: 800;
    }

    .achievement-tabs {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 10px;
    }

    .achievement-tab {
      min-height: 36px;
      padding: 6px 12px;
      border-radius: 999px;
      border: 1px solid rgba(25, 183, 198, 0.16);
      background: rgba(255, 255, 255, 0.86);
      color: #0e5872;
      font-size: 0.78rem;
      font-weight: 800;
    }

    .achievement-tab.active {
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.95), rgba(255, 209, 102, 0.95));
      color: #fff;
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.12);
    }

    .achievement-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 9px 12px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(236, 251, 255, 0.94));
      border: 1px solid rgba(25, 183, 198, 0.2);
      font-size: 0.85rem;
      color: #0e5872;
      font-weight: 700;
      box-shadow: 0 10px 20px rgba(20, 48, 74, 0.08);
      transition: transform 0.22s ease, box-shadow 0.22s ease, background 0.22s ease;
    }

    #achievements-card .badge {
      padding: 9px 12px;
      font-size: 0.85rem;
      box-shadow: 0 10px 20px rgba(20, 48, 74, 0.08);
    }

    .badge.unlocked {
      color: #085d49;
      background: linear-gradient(135deg, rgba(126, 231, 135, 0.98), rgba(255, 237, 128, 0.96));
      border-color: rgba(56, 191, 117, 0.36);
      box-shadow: 0 14px 28px rgba(82, 200, 120, 0.22);
      transform: translateY(-1px);
    }

    .badge-check {
      display: inline-grid;
      place-items: center;
      width: 20px;
      height: 20px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.86);
      color: #119a63;
      font-size: 0.82rem;
      font-weight: 900;
      box-shadow: 0 4px 10px rgba(17, 154, 99, 0.18);
    }

    #time-setting {
      width: 100%;
      padding: 12px 14px;
      justify-content: flex-start;
      gap: 10px;
    }

    #time-setting .setting-label {
      margin-bottom: 8px;
      font-size: 0.9rem;
    }

    #time-select {
      width: 100%;
      min-width: 0;
      padding: 10px 12px;
      font-size: 0.95rem;
      min-height: 46px;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    .time-pills {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
      margin-top: 2px;
    }

    .difficulty-pills {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
      margin-top: 2px;
    }

    .time-pill {
      min-height: 46px;
      padding: 10px 8px;
      font-size: 0.92rem;
      font-weight: 700;
      color: #0e5872;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .time-pill.active {
      color: #fff;
      background: linear-gradient(135deg, #ff7a59, #ffb347);
      box-shadow: 0 14px 26px rgba(255, 122, 89, 0.26);
      transform: translateY(-1px);
    }

    .difficulty-pill {
      min-height: 46px;
      padding: 10px 8px;
      font-size: 0.92rem;
      font-weight: 700;
      color: #0e5872;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .difficulty-pill.active {
      color: #fff;
      box-shadow: 0 14px 26px rgba(25, 183, 198, 0.24);
      transform: translateY(-1px);
    }

    .difficulty-pill[data-difficulty="easy"].active {
      background: linear-gradient(135deg, #52c878, #8fe388);
    }

    .difficulty-pill[data-difficulty="medium"].active {
      background: linear-gradient(135deg, #19b7c6, #65d0ff);
    }

    .difficulty-pill[data-difficulty="hard"].active {
      background: linear-gradient(135deg, #ff7a59, #ff4d6d);
    }

    .badge.locked {
      opacity: 0.62;
      filter: grayscale(0.25);
    }

    .end-screen {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 28px;
      background: rgba(20, 48, 74, 0.2);
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.25s ease;
      z-index: 20;
    }

    .end-screen.visible {
      opacity: 1;
      pointer-events: auto;
    }

    .end-screen.win {
      background: linear-gradient(180deg, rgba(24, 154, 98, 0.9), rgba(16, 122, 77, 0.92));
    }

    .end-screen.lose {
      background: linear-gradient(180deg, rgba(217, 76, 102, 0.9), rgba(173, 38, 63, 0.92));
    }

    .end-card {
      width: min(620px, 100%);
      text-align: center;
      color: #fff;
      padding: 38px 30px;
      border-radius: 32px;
      background: rgba(255, 255, 255, 0.14);
      border: 1px solid rgba(255, 255, 255, 0.25);
      box-shadow: 0 24px 48px rgba(0, 0, 0, 0.18);
      backdrop-filter: blur(10px);
      animation: endCardIn 0.45s cubic-bezier(0.2, 0.8, 0.2, 1);
      overflow: hidden;
      position: relative;
    }

    .end-card::before {
      content: "";
      position: absolute;
      inset: -30% auto auto -12%;
      width: 180px;
      height: 180px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,255,255,0.12), transparent 70%);
      pointer-events: none;
    }

    .end-screen.win .end-card {
      background:
        radial-gradient(circle at top center, rgba(255, 255, 255, 0.18), transparent 45%),
        rgba(255, 255, 255, 0.14);
      box-shadow: 0 28px 58px rgba(4, 82, 39, 0.28);
    }

    .end-screen.lose .end-card {
      background:
        radial-gradient(circle at top center, rgba(255, 255, 255, 0.14), transparent 45%),
        rgba(255, 255, 255, 0.12);
      box-shadow: 0 28px 58px rgba(101, 8, 27, 0.28);
    }

    .end-icon {
      width: 84px;
      height: 84px;
      margin: 0 auto 16px;
      display: grid;
      place-items: center;
      font-size: 2.5rem;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.16);
      border: 1px solid rgba(255, 255, 255, 0.22);
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.18);
    }

    .end-title {
      margin: 0 0 12px;
      font-size: clamp(2rem, 5vw, 3.1rem);
      line-height: 1.1;
    }

    .end-subtitle {
      margin: 0 0 22px;
      font-size: 1.05rem;
      color: rgba(255, 255, 255, 0.92);
    }

    .end-word {
      margin: 0 0 24px;
      font-size: 1.2rem;
      font-weight: 700;
      letter-spacing: 0.03em;
    }

    .end-button {
      min-width: 180px;
      min-height: 46px;
      padding: 10px 14px;
      font-size: 0.92rem;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.2), rgba(125, 140, 255, 0.22), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .end-actions {
      display: flex;
      justify-content: center;
      gap: 10px;
      flex-wrap: wrap;
      margin-top: 10px;
    }

    .end-home-button {
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.22), rgba(125, 211, 252, 0.2), rgba(255, 209, 102, 0.18));
      color: #fff;
      border: 1px solid rgba(255, 255, 255, 0.28);
      box-shadow: 0 14px 26px rgba(10, 20, 40, 0.12);
    }

    .hero {
      padding: 30px 30px 20px;
      background:
        radial-gradient(circle at top left, var(--theme-a), transparent 35%),
        radial-gradient(circle at top right, rgba(255, 255, 255, 0.34), transparent 20%),
        linear-gradient(135deg, rgba(25, 183, 198, 0.26), rgba(255, 122, 89, 0.18), rgba(255, 255, 255, 0.42));
      border-bottom: 1px solid rgba(255, 255, 255, 0.45);
    }

    h1 {
      margin: 0 0 8px;
      font-size: clamp(2rem, 5vw, 3.6rem);
      color: #0e5872;
      letter-spacing: 0.02em;
    }

    .subtitle {
      margin: 0;
      font-size: 1rem;
      color: rgba(20, 48, 74, 0.74);
    }

    .content {
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 24px;
      padding: 24px 28px 28px;
    }

    .background-bar {
      padding: 0 28px 28px;
    }

    .background-card {
      background: rgba(255, 255, 255, 0.78);
      border: 1px solid rgba(25, 183, 198, 0.16);
      border-radius: 22px;
      padding: 16px 18px;
      box-shadow: 0 14px 28px rgba(20, 48, 74, 0.08);
    }

    .background-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }

    .background-label {
      font-weight: 700;
      color: #0e5872;
    }

    .background-note {
      color: rgba(20, 48, 74, 0.66);
      font-size: 0.84rem;
    }

    .background-pills {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .background-tabs {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 12px;
    }

    .background-options {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .background-pill {
      min-height: 40px;
      padding: 8px 12px;
      border-radius: 999px;
      border: 1px solid rgba(25, 183, 198, 0.16);
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.18), rgba(255, 209, 102, 0.18), rgba(255, 255, 255, 0.96));
      color: #0e5872;
      font-size: 0.86rem;
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .background-pill.active {
      color: #fff;
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #ffd166);
      box-shadow: 0 14px 26px rgba(25, 183, 198, 0.24);
      transform: translateY(-1px);
    }

    .background-pill[data-bg-style="sky"] {
      background: #8ed8ff;
      color: #0e5872;
    }

    .background-pill[data-bg-style="sun"] {
      background: #ffd84d;
      color: #7a4f00;
    }

    .background-pill[data-bg-style="mint"] {
      background: #76e3b4;
      color: #065f46;
    }

    .background-pill[data-bg-style="rose"] {
      background: #ff8fab;
      color: #7a2040;
    }

    .background-pill[data-bg-style="lavender"] {
      background: #b9a7ff;
      color: #3f2d7a;
    }

    .background-pill[data-bg-style="peach"] {
      background: #ffb38a;
      color: #7a3d16;
    }

    .background-pill[data-bg-style="lime"] {
      background: #b7ef4a;
      color: #466400;
    }

    .background-pill[data-bg-style="ice"] {
      background: #b9f2ff;
      color: #0c5f73;
    }

    .background-pill[data-bg-style="coral"] {
      background: #ff7f6f;
      color: #6f1f17;
    }

    .background-pill[data-bg-style="white"] {
      background: #ffffff;
      color: #14304a;
      border-color: rgba(20, 48, 74, 0.18);
    }

    .background-pill[data-bg-style="black"] {
      background: #111111;
      color: #f8fbff;
      border-color: rgba(255, 255, 255, 0.16);
    }

    .background-pill[data-bg-style="candy"] {
      background: linear-gradient(135deg, #ff8fab, #ffd166, #8ed8ff);
      color: #14304a;
    }

    .background-pill[data-bg-style="ocean"] {
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #7dd3fc);
      color: #083b5c;
    }

    .background-pill[data-bg-style="sunset"] {
      background: linear-gradient(135deg, #ff7a59, #ffb347, #ffd166);
      color: #6f2f00;
    }

    .background-pill[data-bg-style="forest"] {
      background: linear-gradient(135deg, #34d399, #4ade80, #bef264);
      color: #0f5132;
    }

    .background-pill[data-bg-style="berry"] {
      background: linear-gradient(135deg, #fb7185, #f472b6, #c084fc);
      color: #5b214d;
    }

    .background-pill[data-bg-style="aurora"] {
      background: linear-gradient(135deg, #22c55e, #06b6d4, #8b5cf6);
      color: #14304a;
    }

    .background-pill[data-bg-style="fire"] {
      background: linear-gradient(135deg, #ff5a36, #ff9f1c, #ffd166);
      color: #5f2400;
    }

    .background-pill[data-bg-style="dream"] {
      background: linear-gradient(135deg, #7dd3fc, #c084fc, #f9a8d4);
      color: #4a245e;
    }

    .background-pill[data-bg-style="tropical"] {
      background: linear-gradient(135deg, #00c2a8, #8ac926, #ffca3a);
      color: #124437;
    }

    .background-pill[data-bg-style="night-pop"] {
      background: linear-gradient(135deg, #1d3557, #4361ee, #4cc9f0);
      color: #eef9ff;
    }

    .story-card,
    .online-room-card {
      margin-top: 10px;
      padding: 12px 14px;
      border-radius: 18px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.94), rgba(240, 251, 255, 0.88));
      border: 1px solid rgba(25, 183, 198, 0.14);
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.06);
    }

    .story-card-title,
    .online-room-title {
      margin: 0 0 6px;
      color: #0e5872;
      font-size: 0.92rem;
      font-weight: 800;
    }

    .story-card-text,
    .online-room-text {
      margin: 0;
      color: rgba(20, 48, 74, 0.72);
      line-height: 1.5;
      font-size: 0.82rem;
    }

    .story-progress {
      margin-top: 10px;
      height: 12px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .story-progress-fill {
      height: 100%;
      border-radius: inherit;
      background: linear-gradient(135deg, #ff7a59, #ffd166, #19b7c6);
    }

    .story-map {
      position: relative;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 12px;
      margin-top: 10px;
    }

    .story-map::before {
      content: "";
      position: absolute;
      inset: 44px 22% 44px 22%;
      border-radius: 999px;
      background:
        linear-gradient(180deg, rgba(25, 183, 198, 0.18), rgba(255, 209, 102, 0.2), rgba(255, 122, 89, 0.18));
      z-index: 0;
      filter: blur(1px);
    }

    .story-journey-head {
      grid-column: 1 / -1;
      position: relative;
      z-index: 1;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
      padding: 12px 14px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.06);
    }

    .story-mini-map {
      display: flex;
      align-items: center;
      gap: 8px;
      flex-wrap: wrap;
    }

    .story-mini-stop {
      width: 30px;
      height: 30px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      font-size: 0.85rem;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
      position: relative;
    }

    .story-mini-stop::after {
      content: "";
      position: absolute;
      inset-inline-start: calc(100% + 2px);
      top: 50%;
      width: 12px;
      height: 3px;
      transform: translateY(-50%);
      border-radius: 999px;
      background: rgba(25, 183, 198, 0.24);
    }

    .story-mini-stop:last-child::after {
      display: none;
    }

    .story-mini-stop.done {
      background: linear-gradient(135deg, rgba(82, 200, 120, 0.9), rgba(161, 240, 144, 0.88));
      color: #0f5132;
    }

    .story-mini-stop.active {
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.94), rgba(255, 122, 89, 0.92));
      color: #7a3300;
    }

    .story-node {
      position: relative;
      z-index: 1;
      padding: 14px 14px 16px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      min-height: 126px;
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.06);
      overflow: hidden;
    }

    .story-node::before {
      content: "";
      position: absolute;
      inset: auto 18px -1px 18px;
      height: 4px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
    }

    .story-node::after {
      content: "";
      position: absolute;
      inset-inline-start: -14px;
      top: 34px;
      width: 28px;
      height: 28px;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.95), rgba(255, 122, 89, 0.92));
      border: 4px solid rgba(255, 255, 255, 0.88);
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.12);
    }

    .story-node.active {
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.34), rgba(255, 122, 89, 0.2), rgba(255, 255, 255, 0.94));
      border-color: rgba(255, 122, 89, 0.24);
    }

    .story-node.done {
      background: linear-gradient(135deg, rgba(82, 200, 120, 0.22), rgba(25, 183, 198, 0.18), rgba(255, 255, 255, 0.94));
      border-color: rgba(24, 154, 98, 0.24);
    }

    .story-node.done::after {
      background: linear-gradient(135deg, rgba(82, 200, 120, 0.96), rgba(25, 183, 198, 0.92));
    }

    .story-node-title {
      display: block;
      color: #0e5872;
      font-weight: 800;
      margin-bottom: 6px;
    }

    .story-node-flag {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin-bottom: 8px;
      padding: 6px 10px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.12);
      font-size: 0.74rem;
      font-weight: 800;
      color: #0e5872;
    }

    .story-node-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-top: 10px;
    }

    .story-node-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 9px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.84);
      font-size: 0.73rem;
      font-weight: 700;
      color: rgba(20, 48, 74, 0.8);
    }

    .story-node-text {
      display: block;
      color: rgba(20, 48, 74, 0.72);
      font-size: 0.8rem;
      line-height: 1.4;
    }

    .story-node-state {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin-top: 10px;
      color: #0e5872;
      font-weight: 700;
      font-size: 0.76rem;
    }

    .trophy-card {
      padding: 12px 14px;
      border-radius: 20px;
      background:
        radial-gradient(circle at top left, rgba(255, 209, 102, 0.18), transparent 38%),
        rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.84rem;
      color: #0e5872;
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.08);
    }

    .trophy-card strong {
      display: block;
      margin-bottom: 4px;
    }

    .trophy-gallery {
      display: grid;
      gap: 12px;
    }

    .trophy-shelf {
      position: relative;
      padding: 14px 14px 18px;
      border-radius: 22px;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(248, 244, 229, 0.94));
      border: 1px solid rgba(255, 209, 102, 0.2);
      box-shadow: 0 16px 28px rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .trophy-shelf::after {
      content: "";
      position: absolute;
      inset: auto 18px 10px 18px;
      height: 10px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(170, 120, 70, 0.35), rgba(255, 209, 102, 0.3));
    }

    .trophy-shelf-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 12px;
      font-weight: 800;
      color: #0e5872;
    }

    .trophy-pedestal-row {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      align-items: end;
    }

    .trophy-pedestal {
      padding: 12px 10px;
      border-radius: 18px;
      text-align: center;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      min-height: 118px;
    }

    .trophy-pedestal strong {
      display: block;
      margin-bottom: 4px;
    }

    .trophy-pedestal .icon {
      display: block;
      font-size: 1.7rem;
      margin-bottom: 8px;
    }

    .tournament-tree {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 16px;
      margin-top: 8px;
      position: relative;
    }

    .tournament-tree-column {
      padding: 12px;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.06);
      position: relative;
      min-height: 220px;
    }

    .tournament-tree-column::after {
      content: "";
      position: absolute;
      top: 50%;
      inset-inline-end: -16px;
      width: 16px;
      height: 2px;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.36), rgba(255, 209, 102, 0.34));
      transform: translateY(-50%);
    }

    .tournament-tree-column:last-child::after {
      display: none;
    }

    .tournament-tree-title {
      display: block;
      margin-bottom: 8px;
      color: #0e5872;
      font-weight: 800;
      font-size: 0.86rem;
    }

    .tournament-tree-match {
      padding: 12px 12px;
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(239, 252, 255, 0.92));
      border: 1px solid rgba(25, 183, 198, 0.14);
      margin-bottom: 10px;
      font-size: 0.82rem;
      color: #0e5872;
      position: relative;
      overflow: hidden;
    }

    .tournament-tree-match::after {
      content: "";
      position: absolute;
      inset-inline-end: -10px;
      top: 50%;
      width: 10px;
      height: 2px;
      background: rgba(25, 183, 198, 0.24);
      transform: translateY(-50%);
    }

    .tournament-tree-column:last-child .tournament-tree-match::after {
      display: none;
    }

    .tournament-tree-match.active {
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.3), rgba(255, 122, 89, 0.2), rgba(255, 255, 255, 0.95));
      border-color: rgba(255, 122, 89, 0.24);
    }

    .tournament-tree-match.done {
      background: linear-gradient(135deg, rgba(82, 200, 120, 0.2), rgba(25, 183, 198, 0.18), rgba(255, 255, 255, 0.94));
      border-color: rgba(24, 154, 98, 0.24);
    }

    .tournament-tree-name {
      display: block;
      font-weight: 800;
      margin-bottom: 4px;
    }

    .tournament-tree-meta {
      display: block;
      font-size: 0.76rem;
      color: rgba(20, 48, 74, 0.72);
      line-height: 1.45;
    }

    .tournament-tree-crown {
      min-height: 160px;
      display: grid;
      place-items: center;
      text-align: center;
      border-radius: 22px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.24), rgba(255, 122, 89, 0.18), rgba(255, 255, 255, 0.94));
      border: 1px solid rgba(255, 209, 102, 0.24);
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.56);
    }

    .tournament-tree-crown strong {
      display: block;
      margin-bottom: 6px;
      font-size: 1rem;
      color: #0e5872;
    }

    .tournament-tree-crown span {
      display: block;
      color: rgba(20, 48, 74, 0.76);
      line-height: 1.5;
    }

    .cosmetic-grid {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .cosmetic-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      padding: 10px 12px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      font-size: 0.84rem;
    }

    .cosmetic-item-preview {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 42px;
      min-height: 42px;
      border-radius: 14px;
      font-size: 1.35rem;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.24), rgba(101, 208, 255, 0.24));
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.56);
    }

    .cosmetic-item strong {
      display: block;
      color: #0e5872;
      margin-bottom: 2px;
    }

    .cosmetic-item span {
      color: rgba(20, 48, 74, 0.7);
      font-size: 0.78rem;
    }

    .xp-summary {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .xp-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.28), rgba(101, 208, 255, 0.28), rgba(255, 255, 255, 0.95));
      border: 1px solid rgba(25, 183, 198, 0.16);
      color: #0e5872;
      font-weight: 800;
    }

    .guide-card {
      display: grid;
      gap: 10px;
      margin-top: 8px;
    }

    .guide-bubble {
      padding: 12px 14px;
      border-radius: 20px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.92));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.07);
      color: rgba(20, 48, 74, 0.78);
      line-height: 1.55;
      font-size: 0.86rem;
    }

    .guide-bubble strong {
      color: #0e5872;
    }

    .progress-mini-map {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 8px;
      margin-top: 8px;
    }

    .progress-mini-node {
      padding: 10px 8px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.14);
      text-align: center;
      font-size: 0.78rem;
      color: #0e5872;
    }

    .progress-mini-node strong {
      display: block;
      font-size: 1rem;
      margin-bottom: 4px;
    }

    .online-room-code {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 148px;
      margin-top: 8px;
      padding: 10px 14px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(67, 97, 238, 0.22), rgba(76, 201, 240, 0.22), rgba(255, 255, 255, 0.94));
      color: #0e5872;
      font-size: 1rem;
      font-weight: 800;
      letter-spacing: 0.08em;
    }

    .favorite-word-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin: 4px 6px 0 0;
      padding: 7px 10px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(250, 244, 220, 0.94));
      border: 1px solid rgba(255, 209, 102, 0.22);
      color: #0e5872;
      font-size: 0.78rem;
      font-weight: 700;
    }

    .topic-stat-bar {
      margin-top: 8px;
      padding: 10px 12px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(25, 183, 198, 0.14);
    }

    .topic-stat-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 8px;
      font-size: 0.82rem;
      color: #0e5872;
      font-weight: 700;
    }

    .topic-stat-track {
      height: 10px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .topic-stat-fill {
      height: 100%;
      border-radius: inherit;
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #ffd166);
    }

    .prize-box-grid {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    .prize-box {
      padding: 12px 14px;
      border-radius: 18px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(250, 244, 220, 0.92));
      border: 1px solid rgba(255, 209, 102, 0.22);
      box-shadow: 0 12px 22px rgba(20, 48, 74, 0.06);
      color: #0e5872;
      font-size: 0.83rem;
    }

    .prize-box strong {
      display: block;
      margin-bottom: 4px;
    }

    .profile-graph-list {
      display: grid;
      gap: 8px;
      margin-top: 10px;
    }

    .profile-graph-row {
      display: grid;
      gap: 5px;
      font-size: 0.8rem;
      color: #0e5872;
    }

    .profile-graph-row span {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }

    .profile-graph-track {
      height: 10px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .profile-graph-fill {
      height: 100%;
      border-radius: inherit;
      background: linear-gradient(135deg, #ff7a59, #ffd166, #19b7c6);
    }

    .profile-history-timeline {
      display: grid;
      gap: 8px;
      margin-top: 10px;
    }

    .profile-history-dot {
      position: relative;
      padding: 10px 12px 10px 24px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
      color: rgba(20, 48, 74, 0.8);
      font-size: 0.82rem;
    }

    .profile-history-dot::before {
      content: "";
      position: absolute;
      inset-inline-start: 10px;
      top: 50%;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: linear-gradient(135deg, #19b7c6, #ffd166);
      transform: translateY(-50%);
    }

    .profile-tag-list {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 10px;
    }

    .profile-tag {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 7px 10px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(250, 244, 220, 0.9));
      border: 1px solid rgba(255, 209, 102, 0.22);
      font-size: 0.79rem;
      font-weight: 700;
      color: #0e5872;
    }

    .game.special-kids .hero,
    .game.special-kids .panel,
    .game.special-kids .helper-card,
    .game.special-kids .setting-card {
      background:
        radial-gradient(circle at top left, rgba(255, 241, 118, 0.18), transparent 36%),
        linear-gradient(135deg, rgba(255, 255, 255, 0.94), rgba(245, 255, 235, 0.94), rgba(235, 250, 255, 0.92));
      border-color: rgba(126, 231, 135, 0.24);
    }

    .game.special-kids .mode-pill.active,
    .game.special-kids .special-mode-pill.active,
    .game.special-kids .time-pill.active,
    .game.special-kids .difficulty-pill.active {
      background: linear-gradient(135deg, #8fe388, #65d0ff, #ffd166);
      color: #0e5872;
    }

    .profile-modal {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 24px;
      background: rgba(20, 48, 74, 0.42);
      z-index: 26;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.22s ease;
    }

    .profile-modal.visible {
      opacity: 1;
      pointer-events: auto;
    }

    .profile-modal-card {
      width: min(760px, 100%);
      max-height: min(82vh, 760px);
      overflow: auto;
      padding: 24px;
      border-radius: 28px;
      background:
        radial-gradient(circle at top right, rgba(255, 209, 102, 0.18), transparent 34%),
        linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(238, 251, 255, 0.96));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 30px 60px rgba(20, 48, 74, 0.18);
    }

    .profile-modal-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 16px;
      flex-wrap: wrap;
    }

    .profile-modal-title {
      margin: 0;
      color: #0e5872;
      font-size: 1.5rem;
      font-weight: 900;
    }

    .profile-modal-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 12px;
    }

    .profile-modal-section {
      padding: 14px;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(25, 183, 198, 0.14);
    }

    .profile-modal-section.full {
      grid-column: 1 / -1;
    }

    .profile-history-list {
      display: grid;
      gap: 8px;
      margin-top: 8px;
    }

    @keyframes mascotFloat {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    @keyframes avatarBob {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-4px); }
    }

    @keyframes avatarSway {
      0%, 100% { transform: rotate(0deg); }
      50% { transform: rotate(-8deg); }
    }

    @keyframes avatarBlink {
      0%, 92%, 100% { opacity: 1; transform: scaleY(1); }
      94% { opacity: 0.72; transform: scaleY(0.18); }
    }

    @keyframes avatarGlow {
      0%, 100% { filter: drop-shadow(0 0 0 rgba(255, 209, 102, 0)); }
      50% { filter: drop-shadow(0 0 10px rgba(255, 209, 102, 0.7)); }
    }

    .profile-history-item {
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(240, 251, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.12);
      color: #0e5872;
      font-size: 0.82rem;
      font-weight: 700;
    }

    .ambient-layer {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
      z-index: 0;
    }

    .ambient-particle {
      position: absolute;
      border-radius: 999px;
      opacity: 0.18;
      filter: blur(2px);
      animation: ambientFloat 14s linear infinite;
    }

    .panel {
      background:
        radial-gradient(circle at top right, rgba(255, 255, 255, 0.38), transparent 28%),
        var(--panel);
      border: 1px solid rgba(255, 255, 255, 0.52);
      border-radius: 26px;
      padding: 20px;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.55), var(--soft-shadow);
      transition: transform 0.25s ease, border-color 0.25s ease, background 0.3s ease, box-shadow 0.25s ease;
      backdrop-filter: blur(10px);
    }

    .status {
      display: grid;
      gap: 12px;
      margin-bottom: 20px;
    }

    .status-card {
      border-radius: 20px;
      padding: 14px 16px;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(240, 251, 255, 0.84));
      border: 1px solid rgba(255, 255, 255, 0.58);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      box-shadow: 0 12px 24px rgba(20, 48, 74, 0.06);
    }

    .status-label {
      font-size: 0.85rem;
      color: rgba(20, 48, 74, 0.62);
      margin-bottom: 6px;
    }

    .status-value {
      font-size: 1.5rem;
      font-weight: 700;
    }

    .status-value.timer-warning {
      color: var(--bad);
    }

    .word {
      position: relative;
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      justify-content: center;
      margin: 24px 0 28px;
      min-height: 78px;
      padding: 16px 18px;
      border-radius: 28px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 252, 233, 0.54), rgba(235, 248, 255, 0.54));
      border: 1px solid rgba(255, 255, 255, 0.5);
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.66);
    }

    .word.burst::after {
      content: "";
      position: absolute;
      inset: -16px;
      border-radius: 28px;
      background:
        radial-gradient(circle at 20% 30%, rgba(255, 122, 89, 0.42) 0 8px, transparent 9px),
        radial-gradient(circle at 80% 25%, rgba(25, 183, 198, 0.45) 0 7px, transparent 8px),
        radial-gradient(circle at 65% 80%, rgba(255, 209, 102, 0.48) 0 9px, transparent 10px),
        radial-gradient(circle at 35% 75%, rgba(24, 154, 98, 0.4) 0 7px, transparent 8px);
      pointer-events: none;
      animation: burstGlow 0.7s ease;
    }

    .letter-slot {
      width: 50px;
      height: 64px;
      border-bottom: 5px solid var(--accent);
      display: grid;
      place-items: center;
      font-size: 2.1rem;
      font-weight: 700;
      color: #0e5872;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(255, 248, 221, 0.72), rgba(237, 252, 255, 0.76));
      border-radius: 16px 16px 8px 8px;
      box-shadow: 0 14px 24px rgba(25, 183, 198, 0.1);
    }

    .hint {
      margin: 0 0 18px;
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      padding: 0 14px;
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(255, 249, 214, 0.86));
      border: 1px solid rgba(255, 209, 102, 0.24);
      transition: max-height 0.28s ease, opacity 0.22s ease, padding 0.22s ease, transform 0.22s ease;
      transform: translateY(-6px);
      line-height: 1.5;
      color: #0e5872;
      font-weight: 700;
    }

    .hint.open {
      max-height: 120px;
      opacity: 1;
      padding: 12px 14px;
      transform: translateY(0);
    }

    .letter-slot.reveal {
      animation: popIn 0.72s cubic-bezier(0.18, 0.82, 0.2, 1);
      background: linear-gradient(180deg, rgba(255, 255, 255, 1), rgba(255, 249, 206, 0.92));
      box-shadow: 0 0 0 12px rgba(255, 209, 102, 0), 0 22px 36px rgba(25, 183, 198, 0.2);
    }

    .keyboard {
      display: grid;
      gap: 10px;
      padding: 14px;
      border-radius: 24px;
      background: linear-gradient(135deg, rgba(255,255,255,0.5), rgba(255,248,231,0.32));
      border: 1px solid rgba(255,255,255,0.46);
    }

    .keyboard-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(40px, 1fr));
      gap: 10px;
    }

    button {
      border: none;
      border-radius: 14px;
      padding: 12px 10px;
      font: inherit;
      font-size: 1.15rem;
      cursor: pointer;
      transition: transform 0.15s ease, background 0.15s ease, opacity 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease, filter 0.15s ease;
    }

    button.ui-tap,
    .avatar-option.ui-tap,
    .mode-pill.ui-tap,
    .time-pill.ui-tap,
    .difficulty-pill.ui-tap,
    .special-mode-pill.ui-tap,
    .shop-buy.ui-tap,
    .style-swatch.ui-tap,
    .category-chip.ui-tap,
    .select.ui-tap,
    .text-input.ui-tap {
      animation: uiTapPop 0.52s cubic-bezier(0.18, 0.88, 0.22, 1);
    }

    button:hover:not(:disabled),
    .category-chip:hover,
    .avatar-option:hover,
    .style-swatch:hover,
    .select:hover,
    .text-input:hover {
      transform: translateY(-2px) scale(1.01);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.12);
      border-color: rgba(25, 183, 198, 0.28);
      filter: saturate(1.06);
    }

    button:focus-visible,
    .category-chip:focus-within,
    .avatar-option:focus-visible,
    .style-swatch:focus-visible,
    .select:focus-visible,
    .text-input:focus-visible {
      outline: none;
      transform: translateY(-1px) scale(1.01);
      box-shadow: 0 0 0 4px rgba(255, 209, 102, 0.24), 0 14px 24px rgba(20, 48, 74, 0.12);
      border-color: rgba(255, 122, 89, 0.38);
    }

    .key {
      background: linear-gradient(180deg, #ffffff 0%, #f7fdff 52%, #edfaff 100%);
      color: var(--ink);
      border: 1px solid rgba(255, 255, 255, 0.62);
      box-shadow: 0 10px 18px rgba(25, 183, 198, 0.1);
      min-height: 52px;
      font-weight: 800;
    }

    .key:hover:not(:disabled),
    .reset:hover {
      transform: translateY(-2px) scale(1.01);
    }

    .key:disabled {
      cursor: default;
      opacity: 0.88;
    }

    .key.correct {
      background: linear-gradient(135deg, rgba(126, 231, 135, 0.34), rgba(232, 255, 241, 0.96));
      color: var(--good);
      border-color: rgba(47, 125, 74, 0.28);
    }

    .key.wrong {
      background: linear-gradient(135deg, rgba(255, 196, 196, 0.46), rgba(255, 236, 236, 0.96));
      color: var(--bad);
      border-color: rgba(179, 58, 47, 0.24);
    }

    .key.guess-hit {
      animation: keyCelebrate 0.82s cubic-bezier(0.16, 0.84, 0.2, 1);
    }

    .key.guess-miss {
      animation: keyWobble 0.62s ease;
    }

    .message {
      min-height: 56px;
      display: grid;
      place-items: center;
      text-align: center;
      padding: 14px 16px;
      border-radius: 20px;
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.28), rgba(25, 183, 198, 0.16), rgba(255,255,255,0.7));
      margin-bottom: 18px;
      font-weight: 700;
      border: 1px solid rgba(255,255,255,0.54);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.06);
    }

    .message.win {
      color: var(--good);
      background: rgba(47, 125, 74, 0.12);
    }

    .message.lose {
      color: var(--bad);
      background: rgba(179, 58, 47, 0.12);
    }

    .message.guess-hit {
      animation: messageGlow 1s ease;
    }

    .message.guess-miss {
      animation: messageShake 0.68s ease;
      background: linear-gradient(135deg, rgba(255, 185, 185, 0.34), rgba(217, 76, 102, 0.16));
    }

    .reset {
      width: 100%;
      background: linear-gradient(135deg, rgba(255, 122, 89, 0.2), rgba(255, 209, 102, 0.24), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      margin-top: 12px;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .secondary-button {
      width: 100%;
      margin-top: 10px;
      background: linear-gradient(135deg, rgba(25, 183, 198, 0.18), rgba(109, 211, 255, 0.2), rgba(255, 255, 255, 0.95));
      color: #0e5872;
      border: 1px solid rgba(25, 183, 198, 0.16);
      font-weight: 700;
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.08);
    }

    .pause-button {
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
    }

    .pause-button.paused {
      background: linear-gradient(135deg, #52c878, #8fe388);
      color: #fff;
      box-shadow: 0 14px 26px rgba(82, 200, 120, 0.24);
    }

    .secondary-button:disabled {
      cursor: default;
      opacity: 0.58;
      box-shadow: none;
    }

    .bonus-button {
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
    }

    .bonus-button.life {
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(239, 252, 255, 0.94));
    }

    .bonus-button:disabled {
      opacity: 0.55;
    }

    .hangman-box {
      display: grid;
      place-items: center;
      min-height: 340px;
      padding: 18px;
      border-radius: 28px;
      background:
        radial-gradient(circle at bottom center, rgba(255, 209, 102, 0.22), transparent 30%),
        linear-gradient(180deg, rgba(255,255,255,0.68), rgba(244,252,255,0.44));
      border: 1px solid rgba(255,255,255,0.52);
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.72), 0 18px 30px rgba(20, 48, 74, 0.08);
    }

    svg {
      width: 100%;
      max-width: 260px;
      overflow: visible;
    }

    .frame {
      filter: drop-shadow(0 14px 18px rgba(20, 48, 74, 0.15));
    }

    .frame-wood {
      stroke: #8d6a43;
      stroke-linecap: round;
      fill: none;
    }

    .frame-rope {
      stroke: #c8b38d;
      stroke-linecap: round;
      fill: none;
    }

    .body-line {
      stroke: #203245;
      stroke-linecap: round;
      stroke-linejoin: round;
      fill: none;
    }

    .body-fill {
      fill: rgba(32, 50, 69, 0.08);
      stroke: #203245;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .whatsapp-share-btn {
      background: linear-gradient(135deg, #25d366, #128c7e) !important;
      color: #fff !important;
      border: none !important;
    }

    .whatsapp-share-btn:hover {
      background: linear-gradient(135deg, #20ba5a, #0e6e60) !important;
      transform: translateY(-2px);
    }

    /* ── TOPIC HINT CARD ── */
    .topic-hint-card {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 16px;
      margin: 10px 0 4px;
      border-radius: 999px;
      background: rgba(255,255,255,0.82);
      border: 1px solid rgba(25,183,198,0.22);
      box-shadow: 0 6px 18px rgba(20,48,74,0.08);
      width: fit-content;
      align-self: center;
      animation: topicPop 0.4s cubic-bezier(0.34,1.56,0.64,1) both;
    }

    .topic-hint-icon {
      font-size: 1.5rem;
      line-height: 1;
      animation: topicSpin 0.5s ease both;
    }

    .topic-hint-label {
      font-weight: 800;
      font-size: 0.88rem;
      color: var(--ink);
      opacity: 0.75;
    }

    @keyframes topicPop {
      from { transform: scale(0.7); opacity: 0; }
      to   { transform: scale(1);   opacity: 1; }
    }

    @keyframes topicSpin {
      from { transform: rotate(-20deg) scale(0.7); }
      to   { transform: rotate(0deg)   scale(1); }
    }


    .part {
      opacity: 0;
    }

    .part.visible {
      opacity: 1;
    }

    .part.visible .draw-path {
      animation: drawLine 0.55s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    }

    .part.visible .draw-circle {
      animation: drawCircle 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    }

    .part.visible .draw-face {
      animation: fadeInFace 0.4s 0.5s ease forwards;
      opacity: 0;
    }

    @keyframes drawLine {
      from { stroke-dashoffset: 500; opacity: 1; }
      to   { stroke-dashoffset: 0;   opacity: 1; }
    }

    @keyframes drawCircle {
      from { stroke-dashoffset: 145; opacity: 1; }
      to   { stroke-dashoffset: 0;   opacity: 1; }
    }

    @keyframes fadeInFace {
      from { opacity: 0; transform: scale(0.7); }
      to   { opacity: 1; transform: scale(1); }
    }

    /* rope swing when wrong */
    .hangman-box.guess-miss .hangman-figure {
      animation: ropeSwing 0.7s ease-in-out;
      transform-origin: 175px 68px;
    }

    @keyframes ropeSwing {
      0%   { transform: rotate(0deg); }
      25%  { transform: rotate(6deg); }
      75%  { transform: rotate(-6deg); }
      100% { transform: rotate(0deg); }
    }

    .hangman-box.guess-miss {
      animation: hangmanShake 0.7s ease;
      filter: saturate(0.9);
    }

    .hangman-figure.explode [data-piece] {
      transform-box: fill-box;
      transform-origin: center;
      animation-duration: 0.95s;
      animation-fill-mode: forwards;
      animation-timing-function: cubic-bezier(0.2, 0.78, 0.22, 1);
    }

    .hangman-figure.explode [data-piece="head"] {
      animation-name: fallHead;
    }

    .hangman-figure.explode [data-piece="body"] {
      animation-name: fallBody;
    }

    .hangman-figure.explode [data-piece="arm-left"] {
      animation-name: fallArmLeft;
    }

    .hangman-figure.explode [data-piece="arm-right"] {
      animation-name: fallArmRight;
    }

    .hangman-figure.explode [data-piece="leg-left"] {
      animation-name: fallLegLeft;
    }

    .hangman-figure.explode [data-piece="leg-right"] {
      animation-name: fallLegRight;
    }

    .celebration {
      position: absolute;
      inset: 0;
      pointer-events: none;
      overflow: hidden;
      border-radius: 22px;
    }

    .loading-screen {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 24px;
      background:
        radial-gradient(circle at 22% 24%, rgba(255, 209, 102, 0.28), transparent 18%),
        radial-gradient(circle at 82% 22%, rgba(25, 183, 198, 0.28), transparent 18%),
        linear-gradient(145deg, rgba(255, 255, 255, 0.84), rgba(241, 252, 255, 0.9));
      z-index: 25;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.22s ease;
    }

    .loading-screen.visible {
      opacity: 1;
      pointer-events: auto;
    }

    .night-intro-screen {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 24px;
      background:
        radial-gradient(circle at 20% 18%, rgba(125, 140, 255, 0.16), transparent 22%),
        radial-gradient(circle at 82% 22%, rgba(76, 201, 240, 0.14), transparent 24%),
        linear-gradient(180deg, rgba(2, 8, 23, 0.95), rgba(15, 23, 42, 0.96));
      z-index: 29;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.24s ease;
    }

    .night-intro-screen.visible {
      opacity: 1;
      pointer-events: auto;
    }

    .night-intro-card {
      width: min(520px, 100%);
      padding: 34px 28px;
      border-radius: 30px;
      text-align: center;
      color: #eef7ff;
      background:
        radial-gradient(circle at top center, rgba(255, 255, 255, 0.08), transparent 42%),
        rgba(15, 23, 42, 0.74);
      border: 1px solid rgba(125, 211, 252, 0.18);
      box-shadow: 0 28px 58px rgba(2, 8, 23, 0.4);
      backdrop-filter: blur(12px);
    }

    .night-intro-icon {
      width: 84px;
      height: 84px;
      margin: 0 auto 18px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(67, 97, 238, 0.3), rgba(76, 201, 240, 0.24));
      font-size: 2.5rem;
      box-shadow: 0 16px 32px rgba(15, 23, 42, 0.28);
    }

    .night-intro-title {
      margin: 0 0 10px;
      font-size: clamp(2rem, 5vw, 3rem);
      color: #f8fbff;
    }

    .night-intro-subtitle {
      margin: 0 0 18px;
      color: rgba(226, 232, 240, 0.9);
      font-size: 1rem;
      line-height: 1.6;
    }

    .night-intro-key {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 190px;
      min-height: 52px;
      padding: 10px 18px;
      border-radius: 999px;
      background: linear-gradient(135deg, rgba(34, 197, 94, 0.22), rgba(76, 201, 240, 0.22), rgba(255, 255, 255, 0.08));
      border: 1px solid rgba(125, 211, 252, 0.24);
      color: #f8fbff;
      font-size: 1rem;
      font-weight: 800;
      box-shadow: 0 16px 32px rgba(2, 8, 23, 0.3);
    }

    .loading-card {
      width: min(360px, 100%);
      text-align: center;
      border-radius: 28px;
      padding: 26px 22px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 24px 40px rgba(20, 48, 74, 0.14);
    }

    .loading-spinner {
      width: 66px;
      height: 66px;
      margin: 0 auto 16px;
      border-radius: 50%;
      border: 6px solid rgba(25, 183, 198, 0.12);
      border-top-color: #ff7a59;
      border-right-color: #19b7c6;
      animation: spinLoader 0.9s linear infinite;
    }

    .loading-screen[data-mode="party"] .loading-spinner {
      animation-duration: 0.65s;
      border-top-color: #ff8fab;
      border-right-color: #ffd166;
    }

    .loading-screen[data-mode="competition"] .loading-spinner {
      animation-duration: 0.58s;
      border-top-color: #5f88ff;
      border-right-color: #19b7c6;
    }

    .loading-screen[data-mode="survival"] .loading-spinner {
      animation-duration: 1.25s;
      border-top-color: #34d399;
      border-right-color: #bef264;
    }

    .loading-screen[data-mode="two"] .loading-spinner {
      border-top-color: #ff9f68;
      border-right-color: #7dd3fc;
    }

    .loading-screen[data-mode="online"] .loading-spinner {
      animation-duration: 0.62s;
      border-top-color: #4cc9f0;
      border-right-color: #4361ee;
    }

    .loading-title {
      margin: 0 0 8px;
      font-size: 1.35rem;
      color: #0e5872;
      font-weight: 700;
    }

    .loading-subtitle {
      margin: 0;
      color: rgba(20, 48, 74, 0.72);
    }

    .game.mode-single .panel:hover {
      transform: translateY(-2px);
    }

    .game.mode-competition .message.guess-hit {
      animation-duration: 0.7s;
    }

    .game.mode-competition .word.burst::after {
      transform: scale(1.12);
      filter: saturate(1.15);
    }

    .game.mode-online .message {
      background: linear-gradient(135deg, rgba(76, 201, 240, 0.18), rgba(67, 97, 238, 0.18));
    }

    .game.mode-story .message,
    .game.special-story .message {
      background: linear-gradient(135deg, rgba(255, 209, 102, 0.2), rgba(255, 122, 89, 0.16), rgba(25, 183, 198, 0.12));
    }

    .game.mode-practice .message {
      background: linear-gradient(135deg, rgba(153, 228, 196, 0.22), rgba(25, 183, 198, 0.1));
    }

    .game.mode-practice .panel {
      border-color: rgba(24, 154, 98, 0.18);
    }

    .game.mode-survival .message {
      background: linear-gradient(135deg, rgba(255, 122, 89, 0.18), rgba(255, 209, 102, 0.18));
    }

    .game.mode-survival .panel {
      border-color: rgba(255, 122, 89, 0.24);
    }

    body::before,
    body::after {
      content: "";
      position: fixed;
      inset: auto;
      width: 220px;
      height: 220px;
      border-radius: 50%;
      pointer-events: none;
      z-index: -1;
      filter: blur(10px);
      opacity: 0.6;
      animation: backgroundFloat 11s ease-in-out infinite;
    }

    body[data-weather="rain"]::before {
      background: repeating-linear-gradient(180deg, rgba(101, 208, 255, 0.18) 0 10px, transparent 10px 18px);
      width: 160px;
      height: 260px;
      border-radius: 24px;
      animation: rainShift 2.6s linear infinite;
    }

    body[data-weather="sun"]::after {
      background: radial-gradient(circle, rgba(255, 209, 102, 0.42), transparent 68%);
      animation: sunPulse 3.2s ease-in-out infinite;
    }

    body[data-weather="wind"]::before {
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
      width: 220px;
      height: 22px;
      border-radius: 999px;
      top: 18%;
      animation: windSlide 4s linear infinite;
    }

    body::before {
      top: 8%;
      left: 4%;
      background: radial-gradient(circle, rgba(255, 209, 102, 0.36), transparent 68%);
    }

    body::after {
      right: 3%;
      bottom: 6%;
      background: radial-gradient(circle, rgba(25, 183, 198, 0.28), transparent 68%);
      animation-delay: -4s;
    }

    .game.mode-party .message {
      background: linear-gradient(135deg, rgba(255, 143, 171, 0.18), rgba(101, 208, 255, 0.16));
    }

    .turn-banner {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 10px;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.16);
      color: #0e5872;
      font-weight: 700;
    }

    .live-board {
      display: grid;
      gap: 10px;
      margin-bottom: 14px;
      padding: 12px;
      border-radius: 20px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.92), rgba(240, 251, 255, 0.9));
      border: 1px solid rgba(25, 183, 198, 0.16);
      box-shadow: 0 14px 24px rgba(20, 48, 74, 0.07);
    }

    .live-board.hidden {
      display: none !important;
    }

    .live-board-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
      color: #0e5872;
      font-weight: 800;
    }

    .live-board-subtitle {
      color: rgba(20, 48, 74, 0.68);
      font-size: 0.82rem;
      font-weight: 700;
    }

    .live-board-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .live-board-card {
      padding: 10px 12px;
      border-radius: 18px;
      background: rgba(255, 255, 255, 0.88);
      border: 1px solid rgba(25, 183, 198, 0.14);
    }

    .live-board-card.active {
      background: linear-gradient(135deg, rgba(101, 208, 255, 0.3), rgba(255, 209, 102, 0.28), rgba(255, 255, 255, 0.96));
      border-color: rgba(25, 183, 198, 0.26);
    }

    .live-board-name {
      display: block;
      color: #0e5872;
      font-size: 0.9rem;
      font-weight: 800;
    }

    .live-board-meta {
      display: block;
      margin-top: 4px;
      color: rgba(20, 48, 74, 0.68);
      font-size: 0.76rem;
      font-weight: 700;
    }

    .live-board-score {
      display: block;
      margin-top: 8px;
      color: #0e5872;
      font-size: 1.4rem;
      font-weight: 800;
    }

    .live-board-track {
      position: relative;
      height: 12px;
      border-radius: 999px;
      background: rgba(20, 48, 74, 0.08);
      overflow: hidden;
    }

    .live-board-fill {
      position: absolute;
      inset: 0 auto 0 0;
      border-radius: inherit;
      background: linear-gradient(135deg, #19b7c6, #65d0ff, #ffd166);
    }

    .live-board-list {
      display: grid;
      gap: 8px;
    }

    .tournament-bracket {
      display: grid;
      gap: 8px;
      margin-top: 12px;
    }

    .tournament-match {
      padding: 10px 12px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.82);
      border: 1px solid rgba(25, 183, 198, 0.14);
    }

    .tournament-match strong {
      display: block;
      color: #0e5872;
      margin-bottom: 4px;
      font-size: 0.84rem;
    }

    .tournament-match span {
      display: block;
      color: rgba(20, 48, 74, 0.78);
      font-size: 0.82rem;
    }

    .live-board-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      padding: 9px 10px;
      border-radius: 15px;
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid rgba(25, 183, 198, 0.12);
      font-weight: 700;
      color: #0e5872;
    }

    .game.mode-two .hangman-box.guess-miss {
      animation-duration: 0.82s;
    }

    .game.mode-two .status-card {
      box-shadow: 0 10px 18px rgba(20, 48, 74, 0.06);
    }

    .confetti-piece {
      position: absolute;
      top: 38%;
      width: 10px;
      height: 18px;
      border-radius: 999px;
      opacity: 0;
      transform: translate(-50%, -50%) scale(0.4) rotate(0deg);
    }

    .confetti-piece.pop {
      animation: confettiLaunch 0.95s cubic-bezier(0.18, 0.88, 0.22, 1) forwards;
    }

    @keyframes popIn {
      0% {
        transform: scale(0.42) translateY(10px) rotate(-8deg);
        opacity: 0;
        box-shadow: 0 0 0 0 rgba(255, 209, 102, 0.65);
      }
      38% {
        transform: scale(1.3) translateY(-10px) rotate(5deg);
        box-shadow: 0 0 0 18px rgba(255, 209, 102, 0.24);
      }
      62% {
        transform: scale(0.94) translateY(0) rotate(-2deg);
      }
      100% {
        transform: scale(1);
        opacity: 1;
        box-shadow: 0 0 0 0 rgba(255, 209, 102, 0);
      }
    }

    @keyframes keyCelebrate {
      0% {
        transform: scale(1);
      }
      24% {
        transform: scale(1.24) rotate(-8deg);
        box-shadow: 0 0 0 12px rgba(255, 209, 102, 0.24);
      }
      48% {
        transform: scale(1.05) rotate(7deg);
      }
      72% {
        transform: scale(1.15) rotate(-3deg);
      }
      100% {
        transform: scale(1);
        box-shadow: 0 0 0 0 rgba(255, 209, 102, 0);
      }
    }

    @keyframes keyWobble {
      0%,
      100% {
        transform: translateX(0);
      }
      18% {
        transform: translateX(-6px) rotate(-5deg) scale(0.98);
      }
      38% {
        transform: translateX(6px) rotate(4deg) scale(1.02);
      }
      58% {
        transform: translateX(-4px) rotate(-3deg);
      }
      78% {
        transform: translateX(4px) rotate(2deg);
      }
    }

    @keyframes messageGlow {
      0% {
        transform: scale(1);
        box-shadow: 0 0 0 rgba(24, 154, 98, 0);
      }
      35% {
        transform: scale(1.04);
        box-shadow: 0 0 0 16px rgba(24, 154, 98, 0.18);
      }
      65% {
        transform: scale(1.015);
        box-shadow: 0 0 0 8px rgba(255, 209, 102, 0.14);
      }
      100% {
        transform: scale(1);
        box-shadow: 0 0 0 rgba(24, 154, 98, 0);
      }
    }

    @keyframes messageShake {
      0%,
      100% {
        transform: translateX(0);
      }
      15% {
        transform: translateX(-7px) rotate(-1deg);
      }
      35% {
        transform: translateX(8px) rotate(1deg);
      }
      55% {
        transform: translateX(-5px) rotate(-0.5deg);
      }
      75% {
        transform: translateX(5px) rotate(0.5deg);
      }
    }

    @keyframes hangmanShake {
      0%,
      100% {
        transform: translateX(0);
      }
      20% {
        transform: translateX(-10px) rotate(-1.5deg);
      }
      45% {
        transform: translateX(10px) rotate(1.4deg);
      }
      70% {
        transform: translateX(-6px) rotate(-0.9deg);
      }
    }

    @keyframes burstGlow {
      0% {
        opacity: 0;
        transform: scale(0.78);
      }
      28% {
        opacity: 1;
      }
      100% {
        opacity: 0;
        transform: scale(1.18);
      }
    }

    @keyframes confettiLaunch {
      0% {
        opacity: 0;
        transform: translate(var(--x-start), var(--y-start)) scale(0.3) rotate(0deg);
      }
      12% {
        opacity: 1;
      }
      100% {
        opacity: 0;
        transform: translate(var(--x-end), var(--y-end)) scale(1) rotate(var(--spin));
      }
    }

    @keyframes endCardIn {
      0% {
        opacity: 0;
        transform: translateY(18px) scale(0.92);
      }
      100% {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    @keyframes spinLoader {
      to {
        transform: rotate(360deg);
      }
    }

    @keyframes spinWheel {
      0% {
        transform: rotate(0deg) scale(0.96);
      }
      70% {
        transform: rotate(900deg) scale(1.06);
      }
      100% {
        transform: rotate(1080deg) scale(1);
      }
    }

    @keyframes backgroundFloat {
      0%, 100% {
        transform: translate3d(0, 0, 0) scale(1);
      }
      50% {
        transform: translate3d(12px, -18px, 0) scale(1.08);
      }
    }

    @keyframes ambientFloat {
      0% {
        transform: translate3d(0, 18px, 0) scale(0.92);
      }
      50% {
        transform: translate3d(14px, -28px, 0) scale(1.08);
      }
      100% {
        transform: translate3d(-8px, -60px, 0) scale(0.96);
      }
    }

    @keyframes rainShift {
      from {
        transform: translateY(-20px);
      }
      to {
        transform: translateY(20px);
      }
    }

    @keyframes sunPulse {
      0%, 100% {
        transform: scale(1);
        opacity: 0.55;
      }
      50% {
        transform: scale(1.08);
        opacity: 0.78;
      }
    }

    @keyframes windSlide {
      from {
        transform: translateX(-30px);
        opacity: 0;
      }
      20%, 80% {
        opacity: 0.7;
      }
      to {
        transform: translateX(120vw);
        opacity: 0;
      }
    }

    @keyframes uiTapPop {
      0% {
        transform: scale(1);
      }
      30% {
        transform: scale(1.12) translateY(-4px) rotate(-1.5deg);
        box-shadow: 0 0 0 14px rgba(25, 183, 198, 0.16);
      }
      55% {
        transform: scale(1.04) translateY(-1px) rotate(1deg);
      }
      100% {
        transform: scale(1);
        box-shadow: 0 0 0 0 rgba(25, 183, 198, 0);
      }
    }

    @keyframes fieldFlash {
      0% {
        transform: scale(1);
        box-shadow: 0 0 0 0 rgba(255, 209, 102, 0);
      }
      28% {
        transform: scale(1.025);
        box-shadow: 0 0 0 14px rgba(255, 209, 102, 0.22);
        border-color: rgba(255, 122, 89, 0.42);
        background: rgba(255, 250, 220, 0.96);
      }
      55% {
        transform: scale(1.012);
        box-shadow: 0 0 0 8px rgba(25, 183, 198, 0.12);
      }
      100% {
        transform: scale(1);
        box-shadow: 0 0 0 0 rgba(255, 209, 102, 0);
        background: #fff;
      }
    }

    @keyframes fallHead {
      0% {
        transform: translate(0, 0) rotate(0deg) scale(1);
      }
      100% {
        transform: translate(34px, 138px) rotate(132deg) scale(0.96);
      }
    }

    @keyframes fallBody {
      0% {
        transform: translate(0, 0) rotate(0deg);
      }
      100% {
        transform: translate(22px, 116px) rotate(98deg);
      }
    }

    @keyframes fallArmLeft {
      0% {
        transform: translate(0, 0) rotate(0deg);
      }
      100% {
        transform: translate(-26px, 136px) rotate(-168deg);
      }
    }

    @keyframes fallArmRight {
      0% {
        transform: translate(0, 0) rotate(0deg);
      }
      100% {
        transform: translate(42px, 126px) rotate(188deg);
      }
    }

    @keyframes fallLegLeft {
      0% {
        transform: translate(0, 0) rotate(0deg);
      }
      100% {
        transform: translate(-14px, 126px) rotate(-118deg);
      }
    }

    @keyframes fallLegRight {
      0% {
        transform: translate(0, 0) rotate(0deg);
      }
      100% {
        transform: translate(30px, 134px) rotate(142deg);
      }
    }

    .hint {
      text-align: center;
      color: rgba(20, 48, 74, 0.76);
      margin-top: 8px;
    }

    @media (max-width: 900px) {
      body {
        padding: 14px;
        place-items: stretch;
      }

      .game {
        width: 100%;
      }

      .content {
        grid-template-columns: 1fr;
      }

      .status {
        grid-template-columns: repeat(4, minmax(0, 1fr));
        align-items: stretch;
      }

      .helper-panels {
        grid-template-columns: 1fr;
      }

      .start-highlights {
        grid-template-columns: 1fr;
      }

      .home-guide-grid {
        grid-template-columns: 1fr;
      }

      .start-dashboard {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .time-pills {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
    }

    @media (max-width: 760px) {
      body {
        padding: 10px;
        min-height: 100dvh;
      }

      .game {
        min-height: calc(100dvh - 20px);
        border-radius: 22px;
      }

      .start-screen,
      .end-screen {
        padding: 12px;
      }

      .start-card,
      .end-card {
        border-radius: 24px;
        padding: 22px 16px;
      }

      .start-card {
        max-height: calc(100dvh - 24px);
      }

      .start-title {
        font-size: clamp(2rem, 10vw, 3rem);
      }

      .start-subtitle {
        font-size: 0.96rem;
        margin-bottom: 18px;
      }

      .highlight-card {
        min-height: auto;
      }

      .settings-grid {
        grid-template-columns: 1fr;
        gap: 12px;
        margin-bottom: 14px;
      }

      .settings-grid > .setting-card {
        grid-column: auto;
      }

      .setting-card {
        padding: 14px;
        border-radius: 18px;
      }

      .mode-pills {
        flex-direction: row;
        gap: 8px;
      }

      .mode-pill,
      .time-pill,
      .start-button,
      .end-button,
      .reset,
      .secondary-button {
        min-height: 52px;
        font-size: 1rem;
      }

      .mode-pill {
        flex: 1 1 0;
        min-height: 42px;
        padding: 9px 8px;
        font-size: 0.92rem;
      }

      .start-actions {
        flex-direction: column;
        align-items: stretch;
        margin-top: 18px;
      }

      .start-note {
        width: 100%;
        font-size: 0.92rem;
      }

      .hero {
        padding: 12px 12px 8px;
      }

      .start-topline,
      .hero-topline {
        align-items: stretch;
      }

      h1 {
        font-size: clamp(1.55rem, 8vw, 2.1rem);
        margin-bottom: 4px;
      }

      .subtitle {
        font-size: 0.84rem;
      }

      .hero,
      .content {
        padding-inline: 10px;
      }

      .content {
        gap: 10px;
        padding-top: 10px;
        padding-bottom: 10px;
      }

      .panel {
        border-radius: 18px;
        padding: 10px;
      }

      .end-summary-grid {
        grid-template-columns: 1fr;
      }

      .profile-panel,
      #profiles-card .profile-panel {
        grid-template-columns: 1fr;
      }

      .profile-meta-grid {
        grid-template-columns: 1fr;
      }

      .live-board-grid {
        grid-template-columns: 1fr;
      }

      .hangman-box {
        min-height: 148px;
      }

      svg {
        max-width: 132px;
      }

      .status {
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 8px;
        margin-bottom: 10px;
      }

      .status-card {
        padding: 8px;
        border-radius: 14px;
        text-align: center;
        min-width: 0;
      }

      .status-label {
        font-size: 0.72rem;
        margin-bottom: 4px;
      }

      .status-value {
        font-size: 0.96rem;
      }

      .message {
        min-height: 42px;
        font-size: 0.88rem;
        margin-bottom: 10px;
        padding: 8px;
      }

      .word {
        gap: 6px;
        margin: 10px 0 12px;
        min-height: 44px;
      }

      .letter-slot {
        width: 28px;
        height: 36px;
        font-size: 1.1rem;
        border-radius: 10px 10px 5px 5px;
      }

      .hint {
        font-size: 0.82rem;
        margin-top: 4px;
      }

      .keyboard {
        gap: 6px;
      }

      .keyboard-row {
        grid-template-columns: repeat(6, minmax(0, 1fr));
        gap: 6px;
      }

      .key {
        min-height: 38px;
        padding: 8px 4px;
        font-size: 0.98rem;
      }

      .reset,
      .secondary-button {
        min-height: 42px;
        margin-top: 8px;
        padding: 10px 8px;
        font-size: 0.92rem;
      }
    }

    @media (max-width: 420px) {
      .start-card,
      .end-card,
      .panel {
        padding: 12px;
      }

      .start-title {
        font-size: 2rem;
      }

      .word {
        gap: 6px;
      }

      .letter-slot {
        width: 26px;
        height: 34px;
        font-size: 1rem;
      }

      .keyboard {
        gap: 6px;
      }

      .key {
        min-height: 36px;
        font-size: 0.92rem;
      }

      .mode-pill {
        min-height: 40px;
        font-size: 0.86rem;
      }

      .helper-card {
        padding: 12px;
      }

      #time-setting {
        padding: 12px;
      }

      .helper-list {
        font-size: 0.85rem;
      }

      .start-dashboard {
        grid-template-columns: 1fr;
      }
    }

    .games-hub-bar {
      position: fixed;
      top: 14px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 120;
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.8);
      border: 1px solid rgba(160, 221, 233, 0.78);
      box-shadow: 0 16px 35px rgba(32, 92, 114, 0.14);
      backdrop-filter: blur(14px);
      overflow: hidden;
    }

    .games-hub-bar::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(90deg, rgba(255,255,255,0.08), transparent 25%, transparent 75%, rgba(255,255,255,0.08));
      pointer-events: none;
    }

    .games-hub-link {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 118px;
      padding: 10px 16px;
      border-radius: 999px;
      color: #0d607e;
      font-weight: 800;
      font-size: 0.98rem;
      text-decoration: none;
      transition: transform 0.22s ease, box-shadow 0.22s ease, background 0.22s ease;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(233, 250, 255, 0.96));
      border: 1px solid rgba(179, 228, 240, 0.92);
      box-shadow: 0 10px 24px rgba(70, 170, 196, 0.14);
      position: relative;
      z-index: 1;
    }

    .games-hub-link:hover {
      transform: translateY(-2px);
      box-shadow: 0 14px 28px rgba(70, 170, 196, 0.22);
    }

    .games-hub-link.active {
      color: #fff;
      background: linear-gradient(135deg, #18c6d9, #ffcf57);
      box-shadow: 0 16px 30px rgba(255, 180, 88, 0.24);
      border-color: transparent;
    }

    @media (max-width: 700px) {
      .games-hub-bar {
        width: calc(100% - 26px);
        justify-content: center;
        gap: 8px;
        padding: 8px 10px;
      }

      .games-hub-link {
        min-width: 0;
        flex: 1 1 0;
        padding: 9px 10px;
        font-size: 0.86rem;
      }
    }
  </style>
</head>
  <body>
  <main class="game mode-single" id="game-shell">
    <nav class="games-hub-bar" aria-label="מעבר בין משחקים">
      <a class="games-hub-link" href="../index.html">🕹️ בית</a>
      <a class="games-hub-link active" href="index.html">🎯 איש תלוי</a>
      <a class="games-hub-link" href="../wordle-game/index.html">🔤 וורדל</a>
    </nav>
    <div class="ambient-layer" id="ambient-layer"></div>
    <div class="home-overlay" id="home-overlay">
      <div class="home-card">
        <div class="home-mascot" id="guide-mascot">🧙</div>
        <h2 class="home-title">ברוכים הבאים לעולם של איש תלוי</h2>
        <p class="home-subtitle">כאן מחכים מסע סיפור, טורניר, פרופילים, גביעים, אתגרים, XP, קוסמטיקה ומוזיקה. נכנסים למסך הבית ומתחילים לבנות את המסע שלכם.</p>
        <div class="home-guide-grid">
          <div class="home-guide-step">
            <strong>1. בוחרים מצב</strong>
            <span>יחיד, מסיבה, הישרדות, סיפור, טורניר ועוד.</span>
          </div>
          <div class="home-guide-step">
            <strong>2. צוברים התקדמות</strong>
            <span>XP, דרגות, הישגים, גביעים ואתגרים שמתחלפים.</span>
          </div>
          <div class="home-guide-step">
            <strong>3. משדרגים את החוויה</strong>
            <span>מוזיקה, רקעים, אוואטרים ופריטים קוסמטיים.</span>
          </div>
        </div>
        <button class="start-button home-enter" id="home-enter-button" type="button">היכנסו למסך הבית</button>
      </div>
    </div>
    <div class="start-screen" id="start-screen">
      <div class="start-card">
        <h2 class="start-title">איש תלוי</h2>
        <p class="start-subtitle">בחרו מצב משחק, נושא ורמת קושי, ואז נתחיל עם 3 דקות על השעון.</p>
        <div class="future-games-nav">
          <button class="future-game-pill active" type="button">🎯 איש תלוי</button>
          <button class="future-game-pill locked" type="button" disabled>🧠 טריוויה בקרוב</button>
          <button class="future-game-pill locked" type="button" disabled>🎨 ציור וניחוש בקרוב</button>
          <button class="future-game-pill locked" type="button" disabled>🕹️ משחק מילים בקרוב</button>
        </div>
        <div class="start-highlights">
          <article class="highlight-card">
            <div class="highlight-icon">🎮</div>
            <p class="highlight-title">כיף גדול בכל מצב</p>
            <p class="highlight-text">לא רק משחק רגיל, אלא כמה דרכים שונות לשחק, לצבור ניקוד ולנצח.</p>
          </article>
          <article class="highlight-card">
            <div class="highlight-icon">🎉</div>
            <p class="highlight-title">משחקים, מנצחים, חוגגים</p>
            <p class="highlight-text">אתגר יומי, אתגר שבועי, גלגל פרסים, הישגים ומטבעות לבונוסים.</p>
          </article>
          <article class="highlight-card">
            <div class="highlight-icon">👥</div>
            <p class="highlight-title">לבד או עם חברים</p>
            <p class="highlight-text">כפתורים גדולים יותר, חלוקה מסודרת וחוויה חיה גם במסכים קטנים.</p>
          </article>
        </div>
        <div class="start-topline">
          <div class="avatar-showcase" id="avatar-showcase"></div>
          <div class="mode-chip" id="mode-chip">🙂 שחקן יחיד</div>
        </div>
        <div class="start-dashboard" id="start-dashboard"></div>

        <div class="settings-grid">
          <section class="setting-card">
            <label class="setting-label">מצב משחק</label>
            <div class="mode-pills">
              <button class="mode-pill active" id="single-player-btn" type="button">שחקן יחיד</button>
              <button class="mode-pill" id="two-player-btn" type="button">שני שחקנים</button>
              <button class="mode-pill" id="competition-mode-btn" type="button">תחרות</button>
              <button class="mode-pill" id="online-mode-btn" type="button">אונליין</button>
              <button class="mode-pill" id="tournament-mode-btn" type="button">טורניר</button>
              <button class="mode-pill" id="party-mode-btn" type="button">מסיבה</button>
              <button class="mode-pill" id="survival-mode-btn" type="button">הישרדות</button>
              <button class="mode-pill" id="practice-mode-btn" type="button">אימון</button>
            </div>
          </section>

          <section class="setting-card single-player-settings" id="single-category-setting">
            <label class="setting-label" for="category-select">נושא ראשי</label>
            <select class="select" id="category-select"></select>
            <div class="category-multi">
              <div class="category-multi-head">
                <label class="setting-label" id="category-multi-title">או בחרו כמה קטגוריות יחד</label>
                <button class="category-clear-button hidden" id="clear-categories-button" type="button">נקה בחירה</button>
              </div>
              <p class="category-multi-note" id="category-multi-note">אפשר לסמן כמה נושאים, ואז המשחק יבחר מילים רק מתוכם.</p>
              <div class="category-grid" id="category-grid"></div>
            </div>
            <div class="mini-add-word">
              <label class="setting-label" for="add-word-input">הוסף מילה למאגר</label>
              <input class="text-input" id="add-word-input" type="text" maxlength="24" placeholder="למשל: טורטיה">
              <div class="mini-add-actions">
                <button class="mini-add-button" id="add-word-button" type="button">שמור למאגר</button>
                <button class="mini-add-button" id="favorite-word-button" type="button">שמור למועדפים</button>
                <button class="mini-remove-button" id="remove-word-button" type="button">הסר מילה</button>
              </div>
              <div class="mini-add-note" id="add-word-note"></div>
            </div>
          </section>

          <section class="setting-card single-player-settings" id="single-difficulty-setting">
            <label class="setting-label" for="difficulty-select">רמת קושי</label>
            <select class="select sr-only" id="difficulty-select" aria-hidden="true" tabindex="-1">
              <option value="easy">קל</option>
              <option value="medium" selected>בינוני</option>
              <option value="hard">קשה</option>
              <option value="favorites">מילים אהובות</option>
            </select>
            <div class="difficulty-pills" id="difficulty-pills">
              <button class="difficulty-pill" data-difficulty="easy" type="button">קל</button>
              <button class="difficulty-pill active" data-difficulty="medium" type="button">בינוני</button>
              <button class="difficulty-pill" data-difficulty="hard" type="button">קשה</button>
              <button class="difficulty-pill hidden" data-difficulty="favorites" type="button">מילים אהובות</button>
            </div>
            <div class="special-mode-pills" id="special-mode-pills">
              <button class="special-mode-pill active" data-special-mode="normal" type="button">רגיל</button>
              <button class="special-mode-pill" data-special-mode="kids" type="button">ילדים</button>
              <button class="special-mode-pill" data-special-mode="story" type="button">סיפור</button>
              <button class="special-mode-pill" data-special-mode="challenge" type="button">אתגר</button>
              <button class="special-mode-pill" data-special-mode="extreme" type="button">קשה באמת</button>
            </div>
            <div id="single-avatar-setting">
              <label class="setting-label" style="margin-top: 12px;">אוואטר לשחקן</label>
              <div class="avatar-picker" id="single-avatar-picker"></div>
            </div>
          </section>

          <section class="setting-card custom-settings hidden" id="custom-settings">
            <div id="party-player-config" class="hidden">
              <label class="setting-label" for="party-player-count-input">כמה משתתפים במסיבה (מ-2 עד 10)</label>
              <input class="text-input" id="party-player-count-input" type="number" min="2" max="10" value="2" placeholder="למשל: 4">
              <div id="party-players-fields"></div>
            </div>
            <div id="fixed-player-config">
              <label class="setting-label" for="player-one-input">שם שחקן 1</label>
              <input class="text-input" id="player-one-input" type="text" maxlength="16" placeholder="למשל: דניאל">
              <div class="avatar-picker" id="player-one-avatar-picker"></div>
              <label class="setting-label" for="player-two-input" style="margin-top: 12px;">שם שחקן 2</label>
              <input class="text-input" id="player-two-input" type="text" maxlength="16" placeholder="למשל: נועם">
              <div class="avatar-picker" id="player-two-avatar-picker"></div>
            </div>
            <div id="two-player-word-settings">
              <label class="setting-label" for="custom-word-input" style="margin-top: 12px;">מילה לשחקן השני</label>
              <input class="text-input" id="custom-word-input" type="text" maxlength="24" placeholder="למשל: צ'יפס">
              <label class="setting-label" for="custom-topic-input" style="margin-top: 12px;">נושא מותאם</label>
              <input class="text-input" id="custom-topic-input" type="text" maxlength="20" placeholder="למשל: אוכל">
            </div>
            <div id="bonus-owner-setting" style="margin-top: 12px;">
              <label class="setting-label" for="bonus-owner-select">מי קונה את הבונוסים?</label>
              <select class="select" id="bonus-owner-select">
                <option value="player1">שחקן 1</option>
                <option value="player2">שחקן 2</option>
              </select>
            </div>
          </section>
        </div>

        <div class="start-actions">
          <div class="start-note" id="start-note">במצב שחקן יחיד תיבחר מילה אקראית לפי ההגדרות.</div>
          <div class="helper-actions">
            <button class="ghost-button" id="night-mode-button" type="button">מצב לילה</button>
            <button class="start-button" id="start-button" type="button">התחל משחק</button>
          </div>
        </div>

        <div class="helper-panels">
          <section class="setting-card helper-card" id="time-setting">
            <label class="setting-label" for="time-select">זמן</label>
            <select class="select sr-only" id="time-select" aria-hidden="true" tabindex="-1">
              <option value="60">דקה</option>
              <option value="180" selected>3 דקות</option>
              <option value="300">5 דקות</option>
            </select>
            <div class="time-pills" id="time-pills">
              <button class="time-pill" data-time="60" type="button">1 דקה</button>
              <button class="time-pill active" data-time="180" type="button">3 דקות</button>
              <button class="time-pill" data-time="300" type="button">5 דקות</button>
            </div>
          </section>

          <section class="setting-card helper-card hidden" id="party-rounds-setting">
            <label class="setting-label" for="party-rounds-input">מספר סיבובים (מ-1 עד 50)</label>
            <input class="text-input" id="party-rounds-input" type="number" min="1" max="50" value="3" placeholder="למשל: 5">
          </section>

          <section class="helper-card">
            <h3 class="helper-title">הוראות קצרות</h3>
            <ul class="helper-list">
              <li>נחשו אותיות לפני שהזמן או הטעויות נגמרים.</li>
              <li>רמז אחד בלבד בכל משחק, והוא עולה בטעות אחת.</li>
              <li>במצב תחרות צוברים כמה שיותר מילים לפני שהזמן נגמר.</li>
            </ul>
          </section>

          <section class="helper-card" id="achievements-card">
            <h3 class="helper-title">הישגים</h3>
            <div class="achievement-badges" id="achievement-badges"></div>
            <div class="helper-actions">
              <button class="ghost-button" id="reset-data-button" type="button">איפוס נתונים</button>
            </div>
          </section>

          <section class="helper-card" id="leaderboard-card">
            <h3 class="helper-title">טבלת שיאים</h3>
            <div class="leaderboard-list" id="leaderboard-list"></div>
            <div class="mode-records" id="mode-records"></div>
          </section>

          <section class="helper-card" id="profiles-card">
            <h3 class="helper-title">פרופילים אישיים</h3>
            <label class="setting-label" for="profile-name-input">שם לפרופיל</label>
            <input class="text-input" id="profile-name-input" type="text" maxlength="16" placeholder="למשל: גיא">
            <label class="setting-label" style="margin-top: 10px;">אוואטר לפרופיל</label>
            <div class="avatar-picker" id="profile-avatar-picker"></div>
            <label class="setting-label" style="margin-top: 10px;">סגנון אישי</label>
            <div class="style-picker" id="style-picker"></div>
            <div class="helper-actions">
              <button class="ghost-button" id="save-profile-button" type="button">שמור פרופיל</button>
              <button class="ghost-button" id="profiles-button" type="button">רענן פרופילים</button>
            </div>
            <div class="profile-panel" id="profile-panel">
              <div class="profile-sidebar">
                <div class="profile-list" id="profile-list"></div>
              </div>
              <div class="profile-detail" id="profile-detail"></div>
            </div>
          </section>

          <section class="helper-card" id="daily-helper-card">
            <h3 class="helper-title" id="daily-helper-title">גלגל ופרסים יומיים</h3>
            <div class="daily-card" id="daily-card"></div>
            <div class="daily-card" id="weekly-card"></div>
            <div class="streak-card" id="login-streak-card"></div>
            <div class="mission-list" id="daily-missions-list"></div>
            <div class="daily-wheel">
              <div class="daily-wheel-wrap">
                <div class="daily-wheel-pointer" aria-hidden="true"></div>
                <div class="daily-wheel-disc" id="daily-wheel-disc" data-center-label="🎡"></div>
              </div>
              <ul class="daily-wheel-prizes" id="daily-wheel-prizes"></ul>
              <p class="daily-wheel-note" id="daily-wheel-note">גלגל פרס יומי בעלות 3 מטבעות. אפשר לסובב פעם אחת ביום.</p>
              <div class="helper-actions">
                <button class="ghost-button" id="daily-wheel-button" type="button">סובב ב-3 מטבעות</button>
              </div>
            </div>
            <div class="helper-actions">
              <button class="ghost-button" id="daily-challenge-button" type="button">שחק אתגר יומי</button>
              <button class="ghost-button" id="weekly-challenge-button" type="button">שחק אתגר שבועי</button>
            </div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מסע סיפור</h3>
            <div class="story-card" id="story-card"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">דמות מדריכה</h3>
            <div class="guide-card" id="guide-card"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מפת הסיפור</h3>
            <div class="story-map" id="story-map"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מיני מפה של התקדמות</h3>
            <div class="progress-mini-map" id="progress-mini-map"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">אונליין</h3>
            <div class="online-room-card" id="online-room-card"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">משימות שבועיות</h3>
            <div class="mission-list" id="weekly-missions-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">אתגרים חודשיים</h3>
            <div class="mission-list" id="monthly-missions-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מאגר וסטטיסטיקות</h3>
            <div class="player-stats-list" id="player-stats-list"></div>
            <div class="profile-summary" id="profile-summary"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">שיאים לפי פרופיל</h3>
            <div class="leaderboard-list" id="profile-leaderboard-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">סטטיסטיקות לפי נושא</h3>
            <div class="leaderboard-list" id="topic-stats-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">חבילות נושאים</h3>
            <div class="pack-list" id="pack-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">חדר גביעים</h3>
            <div class="player-stats-list" id="trophy-room"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">תיבות פרס</h3>
            <div class="prize-box-grid" id="prize-box-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">עץ טורניר</h3>
            <div class="tournament-tree" id="tournament-tree"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">חנות קוסמטית</h3>
            <p class="shop-coins" id="cosmetic-shop-coins">מטבעות: 0</p>
            <div class="cosmetic-grid" id="cosmetic-shop-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">חנות בונוסים</h3>
            <p class="shop-coins" id="shop-coins">מטבעות: 0</p>
            <div class="shop-items">
              <div class="shop-item">
                <span>הקפאת זמן • מחיר: 3 מטבעות<div class="bonus-meta">עוצר את השעון ל-10 שניות</div></span>
                <button class="shop-buy" id="buy-freeze-button" type="button">קנה 3</button>
              </div>
              <div class="shop-item">
                <span>חיים נוספים • מחיר: 4 מטבעות<div class="bonus-meta">מוחק טעות אחת שכבר קרתה</div></span>
                <button class="shop-buy" id="buy-life-button" type="button">קנה 4</button>
              </div>
              <div class="shop-item">
                <span>דלג מילה • מחיר: 5 מטבעות<div class="bonus-meta">מעביר למילה חדשה בלי הפסד</div></span>
                <button class="shop-buy" id="buy-skip-button" type="button">קנה 5</button>
              </div>
              <div class="shop-item">
                <span>מגן טעות • מחיר: 4 מטבעות<div class="bonus-meta">טעות אחת לא תיספר</div></span>
                <button class="shop-buy" id="buy-shield-button" type="button">קנה 4</button>
              </div>
            </div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">ניהול מאגר מילים</h3>
            <input class="text-input" id="word-search-input" type="text" maxlength="24" placeholder="חפש מילה במאגר">
            <div class="leaderboard-list" id="word-manager-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מילים אהובות</h3>
            <div class="leaderboard-list" id="favorite-words-list"></div>
          </section>

          <section class="helper-card">
            <h3 class="helper-title">מוזיקה</h3>
            <label class="setting-label" for="music-style-select">בחרו סגנון מוזיקה</label>
            <select class="select" id="music-style-select">
              <option value="calm">רגועה</option>
              <option value="playful">קופצנית</option>
              <option value="dreamy">חלומית</option>
              <option value="arcade">ארקייד</option>
              <option value="story">סיפורית</option>
            </select>
            <div class="helper-actions">
              <button class="ghost-button" id="music-toggle-button" type="button">הפעל מוזיקה</button>
              <button class="ghost-button" id="music-test-button" type="button">בדוק צליל</button>
            </div>
            <div class="profile-summary" id="music-status">כרגע שקט.</div>
          </section>
        </div>

        <section class="background-bar">
          <div class="background-card">
            <div class="background-head">
              <span class="background-label">בחרו רקע</span>
              <span class="background-note">אוטומטי יתאים את עצמו לפי מצב המשחק והסגנון האישי</span>
            </div>
            <div class="background-tabs" id="background-tabs">
              <button class="background-pill active" data-bg-tab="theme" type="button">אוטומטי</button>
              <button class="background-pill" data-bg-tab="solid" type="button">צבעים אחידים</button>
              <button class="background-pill" data-bg-tab="gradient" type="button">מעברי צבע</button>
            </div>
            <div class="background-options" id="background-pills">
              <button class="background-pill active" data-bg-style="theme" type="button">אוטומטי</button>
              <button class="background-pill hidden" data-bg-style="sky" type="button">תכלת</button>
              <button class="background-pill hidden" data-bg-style="sun" type="button">צהוב</button>
              <button class="background-pill hidden" data-bg-style="mint" type="button">מנטה</button>
              <button class="background-pill hidden" data-bg-style="rose" type="button">ורוד</button>
              <button class="background-pill hidden" data-bg-style="lavender" type="button">לבנדר</button>
              <button class="background-pill hidden" data-bg-style="peach" type="button">אפרסק</button>
              <button class="background-pill hidden" data-bg-style="lime" type="button">ליים</button>
              <button class="background-pill hidden" data-bg-style="ice" type="button">קרח</button>
              <button class="background-pill hidden" data-bg-style="coral" type="button">קורל</button>
              <button class="background-pill hidden" data-bg-style="white" type="button">לבן</button>
              <button class="background-pill hidden" data-bg-style="black" type="button">שחור</button>
              <button class="background-pill hidden" data-bg-style="candy" type="button">ממתק</button>
              <button class="background-pill hidden" data-bg-style="ocean" type="button">ים</button>
              <button class="background-pill hidden" data-bg-style="sunset" type="button">שקיעה</button>
              <button class="background-pill hidden" data-bg-style="forest" type="button">יער</button>
              <button class="background-pill hidden" data-bg-style="berry" type="button">ברי</button>
              <button class="background-pill hidden" data-bg-style="aurora" type="button">אורורה</button>
              <button class="background-pill hidden" data-bg-style="fire" type="button">אש</button>
              <button class="background-pill hidden" data-bg-style="dream" type="button">חלום</button>
              <button class="background-pill hidden" data-bg-style="tropical" type="button">טרופי</button>
              <button class="background-pill hidden" data-bg-style="night-pop" type="button">לילה</button>
            </div>
          </div>
        </section>
      </div>
    </div>

    <div class="loading-screen" id="loading-screen">
      <div class="loading-card">
        <div class="loading-spinner"></div>
        <h3 class="loading-title" id="loading-title">מכינים את הסיבוב...</h3>
        <p class="loading-subtitle" id="loading-subtitle">עוד רגע מתחילים.</p>
      </div>
    </div>

    <div class="night-intro-screen" id="night-intro-screen">
      <div class="night-intro-card">
        <div class="night-intro-icon">🌙</div>
        <h3 class="night-intro-title">מצב לילה</h3>
        <p class="night-intro-subtitle">המסך החשיך, האווירה התחלפה, ועכשיו אפשר להיכנס למשחק בשקט. לחצו על כל מקש במקלדת כדי לחזור למסך הפתיחה.</p>
        <div class="night-intro-key">לחצו על כל מקש כדי לשחק</div>
      </div>
    </div>

    <div class="end-screen" id="end-screen">
      <div class="end-card">
        <div class="end-icon" id="end-icon">🎉</div>
        <h2 class="end-title" id="end-title"></h2>
        <p class="end-subtitle" id="end-subtitle"></p>
        <p class="end-word" id="end-word"></p>
        <div class="end-summary" id="end-summary"></div>
        <div class="party-ranking" id="party-ranking"></div>
        <div class="end-achievement-list" id="end-achievement"></div>
        <div class="end-actions">
          <button class="end-button" id="end-button" type="button">שחק שוב</button>
          <button class="end-button end-home-button" id="end-home-button" type="button">חזרה למסך הבית</button>
          <button class="end-button whatsapp-share-btn" id="whatsapp-share-btn" type="button">📲 שתף לוואטסאפ</button>
        </div>
      </div>
    </div>

    <div class="profile-modal" id="profile-modal">
      <div class="profile-modal-card">
        <div class="profile-modal-head">
          <h3 class="profile-modal-title" id="profile-modal-title">פרופיל מלא</h3>
          <button class="ghost-button" id="close-profile-modal-button" type="button">סגור</button>
        </div>
        <div id="profile-modal-content"></div>
      </div>
    </div>

    <section class="hero">
      <div class="hero-topline">
        <div class="hero-avatar" id="hero-avatar"></div>
        <div class="mode-badge" id="mode-badge">🙂 שחקן יחיד</div>
      </div>
      <h1>איש תלוי</h1>
      <p class="subtitle" id="hero-subtitle">נחש את המילה לפני שהציור יושלם.</p>
    </section>

    <section class="content">
      <aside class="panel">
        <div class="hangman-box">
          <svg viewBox="0 0 240 280" aria-label="ציור איש תלוי">
            <g class="frame">
              <ellipse cx="110" cy="245" rx="74" ry="10" fill="rgba(20, 48, 74, 0.08)" />
              <path class="frame-wood" d="M34 242 H162" stroke-width="10" />
              <path class="frame-wood" d="M72 242 V28" stroke-width="11" />
              <path class="frame-wood" d="M70 28 H176" stroke-width="11" />
              <path class="frame-wood" d="M102 28 L72 64" stroke-width="7" />
              <path class="frame-wood" d="M175 28 V52" stroke-width="6" />
              <path class="frame-rope" d="M175 52 V68" stroke-width="4" />
            </g>

            <g class="hangman-figure" id="hangman-figure">

              <!-- ראש -->
              <g class="part" data-step="1" data-piece="head">
                <circle class="body-fill draw-circle" cx="175" cy="92" r="22" stroke-width="4.5"
                  stroke-dasharray="145" stroke-dashoffset="145" />
                <!-- פנים עצובות שמופיעות אחרי הראש -->
                <g class="draw-face">
                  <circle fill="#203245" cx="168" cy="88" r="2.5" />
                  <circle fill="#203245" cx="182" cy="88" r="2.5" />
                  <path class="body-line" d="M168 100 q7 -5 14 0" stroke-width="2.5" />
                </g>
              </g>

              <!-- גוף -->
              <g class="part" data-step="2" data-piece="body">
                <path class="body-line draw-path" stroke-dasharray="500" stroke-dashoffset="500"
                  d="M175 114 C174 125 172 138 173 152 C174 166 176 180 176 192" stroke-width="6" />
              </g>

              <!-- יד שמאל -->
              <g class="part" data-step="3" data-piece="arm-left">
                <path class="body-line draw-path" stroke-dasharray="500" stroke-dashoffset="500"
                  d="M173 132 C162 138 152 146 145 157 C142 162 140 166 138 171" stroke-width="5" />
              </g>

              <!-- יד ימין -->
              <g class="part" data-step="4" data-piece="arm-right">
                <path class="body-line draw-path" stroke-dasharray="500" stroke-dashoffset="500"
                  d="M176 132 C188 138 198 146 205 157 C208 162 210 166 212 171" stroke-width="5" />
              </g>

              <!-- רגל שמאל -->
              <g class="part" data-step="5" data-piece="leg-left">
                <path class="body-line draw-path" stroke-dasharray="500" stroke-dashoffset="500"
                  d="M176 192 C170 204 164 216 160 230 C158 235 157 238 156 240" stroke-width="5" />
              </g>

              <!-- רגל ימין -->
              <g class="part" data-step="6" data-piece="leg-right">
                <path class="body-line draw-path" stroke-dasharray="500" stroke-dashoffset="500"
                  d="M176 192 C184 204 190 216 194 230 C196 235 197 238 198 240" stroke-width="5" />
              </g>

            </g>
          </svg>
        </div>

        <!-- כרטיס נושא חזותי -->
        <div class="topic-hint-card" id="topic-hint-card">
          <div class="topic-hint-icon" id="topic-hint-icon">🎲</div>
          <div class="topic-hint-label" id="topic-hint-label">כללי</div>
        </div>

        <div class="status">
          <div class="status-card">
            <div class="status-label">טעויות</div>
            <div class="status-value" id="wrong-count">0 / 6</div>
          </div>
          <div class="status-card">
            <div class="status-label">זמן</div>
            <div class="status-value" id="timer">03:00</div>
          </div>
          <div class="status-card">
            <div class="status-label">ניקוד</div>
            <div class="status-value" id="score">0</div>
          </div>
          <div class="status-card">
            <div class="status-label">אותיות שנוסו</div>
            <div class="status-value" id="used-letters">-</div>
          </div>
        </div>

        <button class="reset" id="reset-button" type="button">משחק חדש</button>
        <button class="secondary-button pause-button" id="pause-button" type="button">עצור משחק</button>
        <button class="secondary-button" id="hint-button" type="button">רמז +1 טעות</button>
        <button class="secondary-button bonus-button" id="freeze-button" type="button">הקפאת זמן</button>
        <button class="secondary-button bonus-button life" id="life-button" type="button">חיים נוספים</button>
        <button class="secondary-button bonus-button" id="skip-button" type="button">דלג מילה</button>
        <button class="secondary-button bonus-button life" id="shield-button" type="button">מגן טעות</button>
        <button class="secondary-button" id="switch-mode-button" type="button">החלף מצב</button>
      </aside>

      <section class="panel">
        <div class="turn-banner" id="turn-banner">תור: שחקן 1</div>
        <div class="live-board hidden" id="live-board"></div>
        <div class="message" id="message">בחר אות מהמקלדת כדי להתחיל.</div>
        <div class="word" id="word"></div>
        <div class="hint" id="hint"></div>
        <div class="keyboard" id="keyboard"></div>
        <div class="celebration" id="celebration"></div>
      </section>
    </section>

  </main>

  <script>
    const wordBank = {
      animals: `
        כלב חתול סוס חמור פרה עז כבשה גמל אריה נמר ברדלס זאב שועל דוב פיל גירפה זברה קוף גורילה שימפנזה היפופוטם קרנף קנגורו קואלה פנדה סנאי קיפוד ארנב אוגר עכבר חולדה עטלף חמוס לוטרה דביבון בואש טלה גדי סייח גור חתלתול כלבלב יעל תיש ביזון אייל צבי איילה חזיר ראם למור שפן נוטריה
      `,
      birds: `
        יונה עורב דרור נשר בז ינשוף חסידה פלמינגו ברבור ברווז אווז תרנגול תרנגולת טווס יען שחף סנונית נקר חוגלה תוכי קנרית בולבול סיס חופמי אנפה זרזיר עגור דיה חוגלה שליו חוגית רחם קאקי נחליאלי חוחית אדוםחזה זמיר סבכי קורמורן שחורכיפה
      `,
      sea: `
        דג כריש לוויתן דולפין תמנון דיונון מדוזה סרטן צדפה חילזון צבצלופח סוסוןים חסילון לובסטר טונה סלמון מקרל ברקודה שפמנון אמנון דניס מורנה שושנתים אלמוג אצה פנינה צדף כוכבים מלפפוןים קיפודים כלביים ראשתן לווייתן פינגווין כלבים טריגון לברק בקלה דגיגון
      `,
      insects: `
        נמלה דבורה צרעה זבוב יתוש פרפר עש חגב חיפושית פרתמשהרבנו גמלשלמה עכביש עקרב תיקן טרמיט כינה פרעוש שפירית חרגול תולעת זחל דבורהמלכה פשפש חדקונית צרצר עשש יתושון כדרורית צרעתעץ נמלתאש חיפושיתן
      `,
      fruits: `
        תפוח אגס בננה תפוז קלמנטינה לימון ליים אשכולית פומלה מנגו אבוקדו אפרסק שזיף משמש דובדבן דובדבניה ענב תאנה רימון תמר אננס פסיפלורה קיווי מלון אבטיח תות פטל אוכמניה חמוצית קוקוס גויאבה פפאיה ליצי חרוב סברס חבוש שסק נקטרינה קרמבולה
      `,
      vegetables: `
        עגבניה מלפפון חסה כרוב כרובית ברוקולי גזר בטטה תפוחאדמה בצל שום קישוא דלעת צנון סלק פלפל חציל תרד סלרי פטרוזיליה כוסברה שמיר לוף מנגולד אפונה שעועית תירס לפת ארטישוק כרישה קולרבי צ'ילי זוקיני קייל גרגיר עולש
      `,
      food: `
        לחם חלה פיתה בגט לחמניה בורקס פיצה פסטה ספגטי לזניה קוסקוס אורז פתיתים מרק סלט שניצל קציצה המבורגר נקניקייה סביח פלאפל שווארמה חביתה ביצה טוסט גבינה יוגורט חמאה ריבה דבש עוגה עוגייה ביסקוויט וופל שוקולד סוכרייה גלידה פופקורן קורנפלקס דייסה פנקייק ופלבלגי רוגלך קרואסון סופגנייה מלוואח ג'חנון חומוס טחינה קבב ציפס
      `,
      drinks: `
        מים סודה מיץ לימונדה תפוזינה שוקו קפה תה אספרסו קפוצינו לאטה מילקשייק שייק בירה יין ויסקי וודקה רום ברנדי קוניאק סיידר משקהאנרגיה חליטה נענע קקאו גזוז צוף
      `,
      home: `
        בית דירה בניין חדר מסדרון מדרגות גג מרפסת חצר שער גינה דלת חלון תריס מנעול מפתח קיר תקרה רצפה שטיח וילון מראה מדף ארון מגירה מיטה כרית שמיכה סדין מזרן מנורה נורה שקע מתג מטען מאוורר מזגן תנור רדיאטור דוד כיור ברז אסלה מקלחת אמבטיה סבון מגבת סל כביסה
      `,
      furniture: `
        כיסא שולחן ספה כורסה שרפרף ספסל שידה מזנון קומודה ספרייה ויטרינה שולחןקפה שולחןכתיבה כוננית ארגז הדום ערסל שזלונג פוף מזרון מראה מסגרת שידתלילה שולחןאוכל מתלה קולב ארונית כספת דלפק דוכן סולם מדףקיר
      `,
      kitchen: `
        מטבח צלחת קערה כוס ספל מזלג סכין כף כפית מצקת מחבת סיר קומקום תרמוס מסננת פומפייה קרשחיתוך מגרסה קולפן פותחן בקבוק פותחןקופסאות כיריים תנור אפייה מיקרוגל מקרר מקפיא טוסטר בלנדר מיקסר מעבדמזון סחטנית תבנית קופסה צנצנת מלחייה פלפלייה
      `,
      clothes: `
        חולצה מכנסיים גינס חצאית שמלה מעיל סוודר קפוצון זקט חליפה עניבה פיגמה גרביים נעליים סנדלים כפכפים מגפיים כובע צעיף כפפות חגורה ארנק אריג וסט חולצתטישרט סריג כותונת גופייה סינר בגדים כתר משקפיים שעוןצמיד תיקגב תיקיד צמיד עגיל שרשרת טבעת בושם
      `,
      school: `
        ביתספר כיתה מחברת ספר מחק עיפרון עט טוש צבע סרגל ילקוט לוח גיר טבלה שיעור מורה תלמידה תלמיד מנהל מבחן ציון תעודה הפסקה תרגיל שאלה תשובה מחקר הרצאה מעבדה ספרייה מחברתספירלה דפדפת מחשבון גיאומטריה מחוגה מפה אטלס מילון דבק מספריים מדבקה קלמר מחברתכיס
      `,
      office: `
        משרד שולחןעבודה מחשב מסך מקלדת עכבר טלפון מדפסת סורק פקס קלסר תיקייה מעטפה בול מכתב מסמך חוזה חשבונית קבלה יומן לוחשנה פגישה מזכירה מנהלת עובד לקוח ארכיון סיכה שדכן מחורר נייר גריסה חותמת מדבקה כרטיסביקור
      `,
      technology: `
        טלפון סמארטפון טאבלט מחשב נייד אוזניות רמקול מצלמה עדשה חצובה רחפן רובוט שבב קוד תוכנה אפליקציה דפדפן אתר סיסמה שרת ענן נתב מודם כבל מטען סוללה מסךמגע מדפסתתלתממד שעוןחכם קונסולה משחקון בינהמלאכותית אלגוריתם קובץ תיקייה חיישן מקלדת עכברון צג מסדנתונים אנטנה משדר
      `,
      nature: `
        ים אגם נהר נחל מפל מעיין הר גבעה עמק מדבר דיונה יער חורש שדה אחו מערה צוק חוף אי מפרץ לגונה שביל סלע אבן חול בוץ קרקע אדמה ענף גזע שורש עלה פרח ניצן עשב שיח עץ יערה אורן ברוש אלון דקל תאנה חרוב שקד זית גפן חמנייה כלנית רקפת נרקיס סביון קמומיל ביצה ערבה פסגה חורבה
      `,
      weather: `
        שמש ירח כוכב ענן גשם ברד שלג רוח סערה סופה ערפל טל לחות קשת ברק רעם זריחה שקיעה דמדומים לילה בוקר צהריים ערב חום קור שרב קרה טיפטוף
      `,
      places: `
        עיר כפר מושב קיבוץ שכונה רחוב סמטה כיכר פארק גן תחנה שוק קניון חנות מסעדה ביתקפה מאפייה ספרייה מוזיאון גלריה תאטרון קולנוע אצטדיון בריכה חוף נמל שדהתעופה תחנתרכבת תחנתאוטובוס מרפאה ביתחולים ביתמרקחת בנק דואר אוניברסיטה מכללה מלון אכסניה מקדש כנסייה מסגד ביתכנסת מגדל טיילת אנדרטה
      `,
      transport: `
        מכונית אוטובוס רכבת אופניים קורקינט אופנוע משאית טנדר מונית מיניבוס רכבל ספינה סירה יאכטה מטוס מסוק צוללת טרקטור מלגזה אמבולנס ניידת כבאית קרוואן קרון עגלה קטר מכולה גלגל דוושה הגה מנוע בלם מזוודה מסלול המראה רמזור צומת כביש מנהרה גשר
      `,
      sports: `
        כדורגל כדורסל טניס כדורעף כדוריד שחייה ריצה הליכה קפיצה התעמלות אגרוף ג'ודו קראטה טאקוונדו סיף רכיבה גלישה סקי סנובורד חתירה טיפוס יוגה פילאטיס מרתון טריאתלון אופניים שחמט ביליארד באולינג מטקה מחניים תופסת מחבואים דמקה ריקוד פינגפונג
      `,
      music: `
        מוזיקה שיר מנגינה קצב תו צליל מקהלה זמר זמרת מלחין מנצח תזמורת גיטרה פסנתר תוף חליל כינור צ'לו סקסופון חצוצרה טרומבון נבל עוד דרבוקה קלרינט אקורדיון מפוחית מיקרופון רמקול במה הופעה קונצרט
      `,
      body: `
        ראש פנים עין אוזן אף פה שן לשון צוואר כתף יד כףיד אצבע ציפורן חזה גב בטן מותן רגל ברך קרסול עקב עור דם עצם שריר לב מוח ריאה קיבה כבד כליה מצח סנטר לחי שיער ריס גבה מרפק ירך אגן
      `,
      health: `
        רופא אחות מטופל חיסון תרופה כדור סירופ תחבושת פלסטר מדחום חום כאב שיעול צינון שפעת אלרגיה ניתוח מרשם בדיקה טיפול החלמה מנוחה ויטמין מרפא זריקה
      `,
      jobs: `
        רופא מורה נהג טבח אופה נגר צבעי חשמלאי אינסטלטור חקלאי גנן שוטר כבאי חייל טייס דייל שחקן זמר סופר משורר עיתונאי צלם מעצב מהנדס אדריכל מתכנת מנהל מזכיר רוקח קצב דייג דוור ספר שען צורף פסל חוקר מרצה מוכר יזם
      `,
      games: `
        בובה פאזל קובייה קלפים לוחמשחק קוביקלפים כדור פורפרה עפיפון בימבה רכבתצעצוע מכוניתצעצוע דומינו לגו מרבלס יויו בועותסבון גולות בובתיד רובוטצעצוע תחפושת מסכה חרבצעצוע משרוקית תוףצעצוע גיטרתצעצוע פליימוביל קלאס
      `,
      materials: `
        עץ ברזל פלדה זהב כסף נחושת אלומיניום זכוכית בד צמר כותנה משי עור נייר קרטון פלסטיק גומי חרס קרמיקה בטון לבנים גבס שיש גרניט חול חצץ דבק צבע דיו חבל
      `,
      concepts: `
        זמן רגע שעה יום שבוע חודש שנה עבר עתיד הווה זיכרון מחשבה רעיון חלום סוד תקווה פחד אומץ שמחה עצב כעס אהבה שנאה קנאה שלווה מזל גורל בחירה אמת שקר שקט רעש סדר בלגן קצב דמיון השראה הבטחה
      `,
      colors: `
        אדום כחול ירוק צהוב כתום סגול ורוד שחור לבן אפור חום טורקיז זהב כסף ארגמן תכלת בז קרם בורדו חרדל זית
      `
    };

    const categoryLabels = {
      animals: "חיות",
      birds: "ציפורים",
      sea: "ים",
      insects: "חרקים",
      fruits: "פירות",
      vegetables: "ירקות",
      food: "אוכל",
      drinks: "משקאות",
      home: "בית",
      furniture: "רהיטים",
      kitchen: "מטבח",
      clothes: "בגדים",
      school: "בית ספר",
      office: "משרד",
      technology: "טכנולוגיה",
      nature: "טבע",
      weather: "מזג אוויר",
      places: "מקומות",
      transport: "תחבורה",
      sports: "ספורט",
      music: "מוזיקה",
      body: "גוף האדם",
      health: "בריאות",
      jobs: "מקצועות",
      games: "משחקים",
      materials: "חומרים",
      concepts: "מושגים",
      colors: "צבעים"
    };

    const categoryIcons = {
      animals: "🐾",
      birds: "🕊️",
      sea: "🌊",
      insects: "🪲",
      fruits: "🍎",
      vegetables: "🥕",
      food: "🍔",
      drinks: "🥤",
      home: "🏠",
      furniture: "🛋️",
      kitchen: "🍳",
      clothes: "👕",
      school: "🎒",
      office: "📎",
      technology: "💻",
      nature: "🌿",
      weather: "⛅",
      places: "📍",
      transport: "🚗",
      sports: "🏅",
      music: "🎵",
      body: "🧠",
      health: "🩺",
      jobs: "🧰",
      games: "🎲",
      materials: "🧱",
      concepts: "💡",
      colors: "🎨"
    };

    const difficultyLabels = {
      easy: "קל",
      medium: "בינוני",
      hard: "קשה",
      favorites: "מילים אהובות"
    };

    const achievementDefinitions = [
      { id: "first_win", icon: "🏆", label: "ניצחון ראשון", coins: 2 },
      { id: "no_hint", icon: "🧠", label: "ללא רמז", coins: 2 },
      { id: "flawless", icon: "✨", label: "ללא טעויות", coins: 3 },
      { id: "training_finish", icon: "🎯", label: "אימון הושלם", coins: 1 },
      { id: "competition_master", icon: "🔥", label: "3 מילים בתחרות", coins: 4 },
      { id: "tournament_winner", icon: "👑", label: "זכייה בטורניר", coins: 4 },
      { id: "party_star", icon: "🎈", label: "מנצח מסיבה", coins: 3 },
      { id: "survivor", icon: "🛡️", label: "3 מילים בהישרדות", coins: 4 },
      { id: "word_collector", icon: "📚", label: "5 מילים נוספו למאגר", coins: 2 },
      { id: "daily_hero", icon: "🌞", label: "אתגר יומי הושלם", coins: 3 },
      { id: "weekly_hero", icon: "🗓️", label: "אתגר שבועי הושלם", coins: 5 }
    ];

    const modeDetails = {
      single: {
        icon: "🙂",
        label: "שחקן יחיד",
        subtitle: "מילה אקראית, נושא שתבחרו והרבה מקום לחגיגות.",
        loadingTitle: "מגרילים מילה חדשה...",
        loadingSubtitle: "הסיבוב האישי שלכם מתחיל ממש עכשיו."
      },
      two: {
        icon: "👥",
        label: "שני שחקנים",
        subtitle: "שחקן אחד מזין מילה ונושא, והשני צריך לגלות אותה.",
        loadingTitle: "מסתירים את המילה...",
        loadingSubtitle: "תנו לשחקן השני את המכשיר והתחילו לנחש."
      },
      competition: {
        icon: "🏁",
        label: "תחרות",
        subtitle: "ממהרים, פותרים וצוברים כמה שיותר מילים בזמן.",
        loadingTitle: "מכינים סיבוב תחרותי...",
        loadingSubtitle: "השעון רץ, אז בואו נכבוש כמה שיותר מילים."
      },
      online: {
        icon: "🛰️",
        label: "אונליין",
        subtitle: "יוצרים חדר אמיתי, שולחים קישור, ומשחקים יחד משני מכשירים.",
        loadingTitle: "מחברים לחדר האונליין...",
        loadingSubtitle: "הקוד מוכן, והדו-קרב עומד להתחיל."
      },
      tournament: {
        icon: "👑",
        label: "טורניר",
        subtitle: "שני שחקנים מתחרים סיבוב אחרי סיבוב עד להכרעת האלוף.",
        loadingTitle: "פותחים טורניר חדש...",
        loadingSubtitle: "עוד רגע מתחיל סיבוב טורניר אמיתי."
      },
      party: {
        icon: "🎈",
        label: "מסיבה",
        subtitle: "עוברים תור תור, צוברים נקודות, ובסוף מכריזים על המנצח.",
        loadingTitle: "מסדרים סבב מסיבה...",
        loadingSubtitle: "עוד רגע רואים מי יכבוש את המקום הראשון."
      },
      survival: {
        icon: "🛡️",
        label: "הישרדות",
        subtitle: "ממשיכים מילה אחרי מילה עד הטעות הסופית.",
        loadingTitle: "נכנסים למצב הישרדות...",
        loadingSubtitle: "כמה רחוק תצליחו להחזיק מעמד?"
      },
      practice: {
        icon: "🌿",
        label: "אימון",
        subtitle: "מצב רגוע בלי לחץ זמן, רק כדי להשתפר וליהנות.",
        loadingTitle: "פותחים מצב אימון...",
        loadingSubtitle: "לוקחים נשימה ומתחילים לנחש בקצב נעים."
      }
    };

    const avatarPersonalities = {
      "🙂": { title: "רגוע", line: "אוהב משחקים נקיים ושקטים." },
      "😎": { title: "אלוף קר רוח", line: "נשאר קול גם כשנהיה קשה." },
      "🤠": { title: "הרפתקן", line: "רץ קדימה ומחפש סיבוב נועז." },
      "🦊": { title: "חד ומהיר", line: "תמיד מחפש מהלך חכם." },
      "🐯": { title: "לוחם", line: "נכנס חזק ורוצה לנצח." },
      "🐼": { title: "נעים וחכם", line: "שקט מבחוץ, חכם מבפנים." },
      "🦄": { title: "קסום", line: "אוהב רגעי חגיגה וצבע." },
      "🐸": { title: "קופצני", line: "מכניס אנרגיה לכל סיבוב." },
      "🐵": { title: "שובב", line: "מחפש תמיד דרך מפתיעה לנצח." },
      "🦁": { title: "מלכותי", line: "בא להוביל את הטבלה." },
      "🐨": { title: "חלומי", line: "אוהב קצב רגוע ומסע יציב." },
      "🐙": { title: "מתוחכם", line: "חושב על כמה מהלכים קדימה." },
      "🐲": { title: "אגדי", line: "נכנס חזק ורוצה רגע של תהילה." },
      "🦋": { title: "קליל", line: "מביא צבע, תנועה וחגיגה למסך." }
    };

    const storyChapters = [
      { title: "התחלה שקטה", text: "מתחילים במילים מוכרות וקלות יותר.", categories: ["animals", "fruits", "food"], difficulty: "easy" },
      { title: "העיר מתעוררת", text: "המסע עובר למילים יותר יומיומיות ומעט ארוכות יותר.", categories: ["home", "places", "technology"], difficulty: "medium" },
      { title: "המסיבה הגדולה", text: "עכשיו נכנסים לנושאים צבעוניים ומהירים יותר.", categories: ["music", "sports", "games"], difficulty: "medium" },
      { title: "השלב הקשוח", text: "הסיום מביא מילים קשות יותר ואתגר אמיתי.", categories: ["concepts", "materials", "body", "health"], difficulty: "hard" }
    ];

    const finalLetterMap = {
      ך: "כ",
      ם: "מ",
      ן: "נ",
      ף: "פ",
      ץ: "צ"
    };

    const alphabet = ["א", "ב", "ג", "ד", "ה", "ו", "ז", "ח", "ט", "י", "כ", "ל", "מ", "נ", "ס", "ע", "פ", "צ", "ק", "ר", "ש", "ת"];
    const avatarChoices = ["🙂", "😎", "🤠", "🦊", "🐯", "🐼", "🦄", "🐸", "🐵", "🦁", "🐨", "🐙", "🐲", "🦋"];
    const avatarLabels = {
      "🙂": "חיוך",
      "😎": "קול",
      "🤠": "מערב",
      "🦊": "שועל",
      "🐯": "טיגריס",
      "🐼": "פנדה",
      "🦄": "קסם",
      "🐸": "קפיץ",
      "🐵": "שובב",
      "🦁": "מלך",
      "🐨": "קואלה",
      "🐙": "תמנון",
      "🐲": "דרקון",
      "🦋": "פרפר"
    };
    const avatarAnimationClasses = {
      "🙂": "avatar-bob",
      "😎": "avatar-glow",
      "🤠": "avatar-sway",
      "🦊": "avatar-sway",
      "🐯": "avatar-glow",
      "🐼": "avatar-blink",
      "🦄": "avatar-glow",
      "🐸": "avatar-bob",
      "🐵": "avatar-bob",
      "🦁": "avatar-sway",
      "🐨": "avatar-blink",
      "🐙": "avatar-sway",
      "🐲": "avatar-glow",
      "🦋": "avatar-bob"
    };
    const personalStyles = {
      cheerful: { label: "שמח", accent: "#ff7a59", accent2: "#19b7c6", accent3: "#ffd166" },
      ocean: { label: "ים", accent: "#0ea5e9", accent2: "#14b8a6", accent3: "#7dd3fc" },
      sunset: { label: "שקיעה", accent: "#f97316", accent2: "#ef4444", accent3: "#fdba74" }
    };
    const baseAvatarChoices = ["🙂", "😎", "🤠", "🦊", "🐯", "🐼", "🦄", "🐸", "🐵", "🦁"];
    const baseBackgroundChoices = [
      "theme",
      "sky", "sun", "mint", "rose", "lavender", "peach", "lime", "ice", "coral", "white", "black",
      "candy", "ocean", "sunset", "forest", "berry", "aurora", "fire", "dream", "tropical", "night-pop"
    ];
    const cosmeticItems = [
      { id: "avatar_koala", kind: "avatar", unlockKey: "🐨", label: "🐨 אוואטר קואלה", description: "אוואטר רגוע ומתוק למסעות שקטים.", price: 5, preview: "🐨", unlock: () => unlockCosmeticAvatar("🐨") },
      { id: "avatar_octopus", kind: "avatar", unlockKey: "🐙", label: "🐙 אוואטר תמנון", description: "אוואטר חכם ומתוחכם למשחקים תחרותיים.", price: 5, preview: "🐙", unlock: () => unlockCosmeticAvatar("🐙") },
      { id: "avatar_dragon", kind: "avatar", unlockKey: "🐲", label: "🐲 אוואטר דרקון", description: "אוואטר אגדי עם נוכחות חזקה.", price: 6, preview: "🐲", unlock: () => unlockCosmeticAvatar("🐲") },
      { id: "avatar_butterfly", kind: "avatar", unlockKey: "🦋", label: "🦋 אוואטר פרפר", description: "אוואטר קליל, צבעוני וחגיגי.", price: 6, preview: "🦋", unlock: () => unlockCosmeticAvatar("🦋") },
      { id: "bg_sunset", kind: "background", unlockKey: "sunset", label: "🌇 רקע שקיעה", description: "פותח את רקע השקיעה גם לפרופילים.", price: 4, preview: "🌇", unlock: () => unlockCosmeticBackground("sunset") },
      { id: "bg_forest", kind: "background", unlockKey: "forest", label: "🌿 רקע יער", description: "רקע טבע ירוק ומרגיע.", price: 4, preview: "🌿", unlock: () => unlockCosmeticBackground("forest") },
      { id: "bg_night_pop", kind: "background", unlockKey: "night-pop", label: "🌌 רקע לילה", description: "רקע כחול עמוק עם ברק עדין.", price: 5, preview: "🌌", unlock: () => unlockCosmeticBackground("night-pop") },
      { id: "bg_aurora", kind: "background", unlockKey: "aurora", label: "✨ רקע אורורה", description: "רקע זוהר וחלומי למסכים חגיגיים.", price: 5, preview: "✨", unlock: () => unlockCosmeticBackground("aurora") },
      { id: "bg_fire", kind: "background", unlockKey: "fire", label: "🔥 רקע אש", description: "רקע דרמטי למי שאוהב קצב ותנועה.", price: 5, preview: "🔥", unlock: () => unlockCosmeticBackground("fire") },
      { id: "bg_tropical", kind: "background", unlockKey: "tropical", label: "🌴 רקע טרופי", description: "רקע קיצי עם צבעים חיים ומרעננים.", price: 4, preview: "🌴", unlock: () => unlockCosmeticBackground("tropical") }
    ];
    const topicPacks = {
      starter: {
        label: "התחלה",
        price: 0,
        entries: [
          { word: "בלון", category: "games" },
          { word: "פיצה", category: "food" },
          { word: "גור", category: "animals" },
          { word: "קובייה", category: "games" },
          { word: "עוגייה", category: "food" },
          { word: "אייל", category: "animals" }
        ]
      },
      green: {
        label: "טבע ירוק",
        price: 3,
        entries: [
          { word: "ערפל", category: "weather" },
          { word: "מפל", category: "nature" },
          { word: "צדפה", category: "sea" },
          { word: "בריזה", category: "weather" },
          { word: "שונית", category: "sea" },
          { word: "נחל", category: "nature" }
        ]
      },
      home: {
        label: "בית יומיומי",
        price: 3,
        entries: [
          { word: "מרפסת", category: "home" },
          { word: "כורסה", category: "furniture" },
          { word: "מסננת", category: "kitchen" },
          { word: "מדף", category: "home" },
          { word: "שרפרף", category: "furniture" },
          { word: "מצקת", category: "kitchen" }
        ]
      },
      school: {
        label: "לימודים ועבודה",
        price: 4,
        entries: [
          { word: "מחברת", category: "school" },
          { word: "קלסר", category: "office" },
          { word: "אדריכל", category: "jobs" },
          { word: "סרגל", category: "school" },
          { word: "מדפסת", category: "office" },
          { word: "צלם", category: "jobs" },
          { word: "מחשבון", category: "school" }
        ]
      },
      city: {
        label: "מקומות ותנועה",
        price: 4,
        entries: [
          { word: "גשר", category: "places" },
          { word: "קורקינט", category: "transport" },
          { word: "מסך", category: "technology" },
          { word: "מנהרה", category: "places" },
          { word: "מונית", category: "transport" },
          { word: "שרת", category: "technology" },
          { word: "תחנה", category: "places" }
        ]
      },
      body: {
        label: "גוף ובריאות",
        price: 4,
        entries: [
          { word: "שריר", category: "body" },
          { word: "חיסון", category: "health" },
          { word: "מדליה", category: "sports" },
          { word: "מרפק", category: "body" },
          { word: "תחבושת", category: "health" },
          { word: "אליפות", category: "sports" },
          { word: "קרסול", category: "body" }
        ]
      },
      tasty: {
        label: "טעמים",
        price: 3,
        entries: [
          { word: "מנגו", category: "fruits" },
          { word: "קישוא", category: "vegetables" },
          { word: "לימונדה", category: "drinks" },
          { word: "תמר", category: "fruits" },
          { word: "כרובית", category: "vegetables" },
          { word: "מילקשייק", category: "drinks" }
        ]
      },
      style: {
        label: "צבע ולבוש",
        price: 3,
        entries: [
          { word: "צעיף", category: "clothes" },
          { word: "טורקיז", category: "colors" },
          { word: "בד", category: "materials" },
          { word: "מעיל", category: "clothes" },
          { word: "ארגמן", category: "colors" },
          { word: "קרטון", category: "materials" }
        ]
      },
      smart: {
        label: "חשיבה ויצירה",
        price: 4,
        entries: [
          { word: "גיטרה", category: "music" },
          { word: "דמיון", category: "concepts" },
          { word: "פאזל", category: "games" },
          { word: "תזמורת", category: "music" },
          { word: "רעיון", category: "concepts" },
          { word: "מבוך", category: "games" },
          { word: "קצב", category: "music" }
        ]
      },
      wild: {
        label: "עולם החי",
        price: 3,
        entries: [
          { word: "פלמינגו", category: "birds" },
          { word: "פרפר", category: "insects" },
          { word: "קואלה", category: "animals" },
          { word: "ינשוף", category: "birds" },
          { word: "חגב", category: "insects" },
          { word: "ברדלס", category: "animals" }
        ]
      },
      summer: {
        label: "קיץ גדול",
        price: 4,
        entries: [
          { word: "גלידה", category: "food" },
          { word: "שמשייה", category: "weather" },
          { word: "אבטיח", category: "fruits" },
          { word: "כפכף", category: "clothes" },
          { word: "חוף", category: "sea" },
          { word: "מצוף", category: "sea" }
        ]
      },
      winter: {
        label: "חורף נעים",
        price: 4,
        entries: [
          { word: "גשם", category: "weather" },
          { word: "מרק", category: "food" },
          { word: "מעילון", category: "clothes" },
          { word: "שלולית", category: "weather" },
          { word: "תה", category: "drinks" },
          { word: "מטרייה", category: "weather" }
        ]
      }
    };
    const keyboardRows = [
      ["ק", "ר", "א", "ט", "ו", "י", "פ"],
      ["ש", "ד", "ג", "כ", "ע", "ח", "ל"],
      ["ז", "ס", "ב", "ה", "נ", "מ", "צ", "ת"]
    ];
    const maxWrong = 6;
    let customWordEntries = [];
    let words = [];
    let wordTopicLookup = new Map();
    let descriptiveHint = "";

    const topicLookup = Object.fromEntries(
      Object.entries(categoryLabels).map(([key, label]) => [sanitizeTopicValue(label), key])
    );

    const gameShellEl = document.getElementById("game-shell");
    const startScreenEl = document.getElementById("start-screen");
    const loadingScreenEl = document.getElementById("loading-screen");
    const nightIntroScreenEl = document.getElementById("night-intro-screen");
    const loadingTitleEl = document.getElementById("loading-title");
    const loadingSubtitleEl = document.getElementById("loading-subtitle");
    const singlePlayerBtn = document.getElementById("single-player-btn");
    const twoPlayerBtn = document.getElementById("two-player-btn");
    const competitionModeBtn = document.getElementById("competition-mode-btn");
    const onlineModeBtn = document.getElementById("online-mode-btn");
    const tournamentModeBtn = document.getElementById("tournament-mode-btn");
    const partyModeBtn = document.getElementById("party-mode-btn");
    const survivalModeBtn = document.getElementById("survival-mode-btn");
    const practiceModeBtn = document.getElementById("practice-mode-btn");
    const singleCategorySettingEl = document.getElementById("single-category-setting");
    const singleDifficultySettingEl = document.getElementById("single-difficulty-setting");
    const singleAvatarSettingEl = document.getElementById("single-avatar-setting");
    const timeSettingEl = document.getElementById("time-setting");
    const partyRoundsSettingEl = document.getElementById("party-rounds-setting");
    const categorySelectEl = document.getElementById("category-select");
    const categoryGridEl = document.getElementById("category-grid");
    const categoryMultiTitleEl = document.getElementById("category-multi-title");
    const categoryMultiNoteEl = document.getElementById("category-multi-note");
    const clearCategoriesButtonEl = document.getElementById("clear-categories-button");
    const addWordInputEl = document.getElementById("add-word-input");
    const addWordButtonEl = document.getElementById("add-word-button");
    const favoriteWordButtonEl = document.getElementById("favorite-word-button");
    const removeWordButtonEl = document.getElementById("remove-word-button");
    const addWordNoteEl = document.getElementById("add-word-note");
    const difficultySelectEl = document.getElementById("difficulty-select");
    const difficultyPillsEl = document.getElementById("difficulty-pills");
    const specialModePillsEl = document.getElementById("special-mode-pills");
    const singleAvatarPickerEl = document.getElementById("single-avatar-picker");
    const playerOneAvatarPickerEl = document.getElementById("player-one-avatar-picker");
    const playerTwoAvatarPickerEl = document.getElementById("player-two-avatar-picker");
    const timeSelectEl = document.getElementById("time-select");
    const partyRoundsInputEl = document.getElementById("party-rounds-input");
    const timePillsEl = document.getElementById("time-pills");
    const customSettingsEl = document.getElementById("custom-settings");
    const partyPlayerConfigEl = document.getElementById("party-player-config");
    const partyPlayerCountInputEl = document.getElementById("party-player-count-input");
    const partyPlayersFieldsEl = document.getElementById("party-players-fields");
    const fixedPlayerConfigEl = document.getElementById("fixed-player-config");
    const bonusOwnerSettingEl = document.getElementById("bonus-owner-setting");
    const bonusOwnerSelectEl = document.getElementById("bonus-owner-select");
    const twoPlayerWordSettingsEl = document.getElementById("two-player-word-settings");
    const playerOneInputEl = document.getElementById("player-one-input");
    const playerTwoInputEl = document.getElementById("player-two-input");
    const customWordInputEl = document.getElementById("custom-word-input");
    const customTopicInputEl = document.getElementById("custom-topic-input");
    const startButtonEl = document.getElementById("start-button");
    const nightModeButtonEl = document.getElementById("night-mode-button");
    const startNoteEl = document.getElementById("start-note");
    const avatarShowcaseEl = document.getElementById("avatar-showcase");
    const startDashboardEl = document.getElementById("start-dashboard");
    const homeOverlayEl = document.getElementById("home-overlay");
    const homeEnterButtonEl = document.getElementById("home-enter-button");
    const guideMascotEl = document.getElementById("guide-mascot");
    const modeChipEl = document.getElementById("mode-chip");
    const achievementBadgesEl = document.getElementById("achievement-badges");
    const leaderboardListEl = document.getElementById("leaderboard-list");
    const modeRecordsEl = document.getElementById("mode-records");
    const resetDataButtonEl = document.getElementById("reset-data-button");
    const saveProfileButtonEl = document.getElementById("save-profile-button");
    const profileNameInputEl = document.getElementById("profile-name-input");
    const profileAvatarPickerEl = document.getElementById("profile-avatar-picker");
    const shopCoinsEl = document.getElementById("shop-coins");
    const buyFreezeButtonEl = document.getElementById("buy-freeze-button");
    const buyLifeButtonEl = document.getElementById("buy-life-button");
    const buySkipButtonEl = document.getElementById("buy-skip-button");
    const buyShieldButtonEl = document.getElementById("buy-shield-button");
    const profileListEl = document.getElementById("profile-list");
    const profilePanelEl = document.getElementById("profile-panel");
    const profilesButtonEl = document.getElementById("profiles-button");
    const closeProfilesButtonEl = document.getElementById("close-profiles-button");
    const playerStatsListEl = document.getElementById("player-stats-list");
    const profileSummaryEl = document.getElementById("profile-summary");
    const dailyCardEl = document.getElementById("daily-card");
    const weeklyCardEl = document.getElementById("weekly-card");
    const profileDetailEl = document.getElementById("profile-detail");
    const loginStreakCardEl = document.getElementById("login-streak-card");
    const dailyMissionsListEl = document.getElementById("daily-missions-list");
    const dailyWheelDiscEl = document.getElementById("daily-wheel-disc");
    const dailyWheelPrizesEl = document.getElementById("daily-wheel-prizes");
    const dailyWheelNoteEl = document.getElementById("daily-wheel-note");
    const dailyWheelButtonEl = document.getElementById("daily-wheel-button");
    const dailyChallengeButtonEl = document.getElementById("daily-challenge-button");
    const weeklyChallengeButtonEl = document.getElementById("weekly-challenge-button");
    const storyCardEl = document.getElementById("story-card");
    const guideCardEl = document.getElementById("guide-card");
    const storyMapEl = document.getElementById("story-map");
    const progressMiniMapEl = document.getElementById("progress-mini-map");
    const onlineRoomCardEl = document.getElementById("online-room-card");
    const weeklyMissionsListEl = document.getElementById("weekly-missions-list");
    const monthlyMissionsListEl = document.getElementById("monthly-missions-list");
    const profileLeaderboardListEl = document.getElementById("profile-leaderboard-list");
    const topicStatsListEl = document.getElementById("topic-stats-list");
    const trophyRoomEl = document.getElementById("trophy-room");
    const prizeBoxListEl = document.getElementById("prize-box-list");
    const tournamentTreeEl = document.getElementById("tournament-tree");
    const cosmeticShopCoinsEl = document.getElementById("cosmetic-shop-coins");
    const cosmeticShopListEl = document.getElementById("cosmetic-shop-list");
    const wordSearchInputEl = document.getElementById("word-search-input");
    const wordManagerListEl = document.getElementById("word-manager-list");
    const favoriteWordsListEl = document.getElementById("favorite-words-list");
    const musicStyleSelectEl = document.getElementById("music-style-select");
    const musicToggleButtonEl = document.getElementById("music-toggle-button");
    const musicTestButtonEl = document.getElementById("music-test-button");
    const musicStatusEl = document.getElementById("music-status");
    const stylePickerEl = document.getElementById("style-picker");
    const packListEl = document.getElementById("pack-list");
    const heroAvatarEl = document.getElementById("hero-avatar");
    const modeBadgeEl = document.getElementById("mode-badge");
    const heroSubtitleEl = document.getElementById("hero-subtitle");
    const wordEl = document.getElementById("word");
    const keyboardEl = document.getElementById("keyboard");
    const messageEl = document.getElementById("message");
    const hintEl = document.getElementById("hint");
    const wrongCountEl = document.getElementById("wrong-count");
    const timerEl = document.getElementById("timer");
    const scoreEl = document.getElementById("score");
    const usedLettersEl = document.getElementById("used-letters");
    const resetButton = document.getElementById("reset-button");
    const pauseButtonEl = document.getElementById("pause-button");
    const hintButtonEl = document.getElementById("hint-button");
    const freezeButtonEl = document.getElementById("freeze-button");
    const lifeButtonEl = document.getElementById("life-button");
    const skipButtonEl = document.getElementById("skip-button");
    const shieldButtonEl = document.getElementById("shield-button");
    const switchModeButtonEl = document.getElementById("switch-mode-button");
    const turnBannerEl = document.getElementById("turn-banner");
    const liveBoardEl = document.getElementById("live-board");
    const endScreenEl = document.getElementById("end-screen");
    const endIconEl = document.getElementById("end-icon");
    const endTitleEl = document.getElementById("end-title");
    const endSubtitleEl = document.getElementById("end-subtitle");
    const endWordEl = document.getElementById("end-word");
    const endSummaryEl = document.getElementById("end-summary");
    const partyRankingEl = document.getElementById("party-ranking");
    const endAchievementEl = document.getElementById("end-achievement");
    const endButtonEl = document.getElementById("end-button");
    const endHomeButtonEl = document.getElementById("end-home-button");
    const profileModalEl = document.getElementById("profile-modal");
    const profileModalTitleEl = document.getElementById("profile-modal-title");
    const profileModalContentEl = document.getElementById("profile-modal-content");
    const closeProfileModalButtonEl = document.getElementById("close-profile-modal-button");
    const backgroundTabsEl = document.getElementById("background-tabs");
    const backgroundPillsEl = document.getElementById("background-pills");
    const ambientLayerEl = document.getElementById("ambient-layer");
    const parts = [...document.querySelectorAll(".part")];
    const hangmanFigureEl = document.getElementById("hangman-figure");
    const hangmanBoxEl = document.querySelector(".hangman-box");
    const celebrationEl = document.getElementById("celebration");

    let secretWord = "";
    let hint = "";
    let guessedLetters = new Set();
    let wrongGuesses = 0;
    let gameOver = false;
    let lastGuessResult = null;
    let lastGuessedLetter = "";
    let endScreenTimer = null;
    let timerInterval = null;
    let loadingTimer = null;
    let remainingTime = 180;
    let currentMode = "single";
    let audioContext = null;
    let masterGainNode = null;
    let addWordNoteTimer = null;
    let usedHint = false;
    let hintVisible = false;
    let isPaused = false;
    let freezeTimerId = null;
    let freezeAvailable = false;
    let extraLifeAvailable = false;
    let streakCount = 0;
    let competitionScore = 0;
    let leaderboard = [];
    let modeRecords = {};
    let gameTimeLimit = 180;
    let unlockedAchievements = new Set();
    let achievementStore = {};
    let activeAchievementProfileKey = "regular";
    let lastUnlockedAchievements = [];
    let activeTopicKey = "all";
    let activeTopicLabel = "כללי";
    let usedWordKeys = new Set();
    let activeTurn = 0;
    let partyScores = [0, 0];
    let partyRoundCount = 0;
    let partyPlayers = [];
    let pendingEndAction = null;
    let bonusCoins = 0;
    let bonusInventory = { freeze: 0, life: 0, skip: 0, shield: 0 };
    let bonusOwners = { freeze: "", life: "", skip: "", shield: "" };
    let shieldActive = false;
    let playerProfiles = [];
    let selectedProfileTabIndex = 0;
    let profileDetailVisible = true;
    let playerStats = {};
    let dailyChallengeState = { dateKey: "", wordKey: "" };
    let weeklyChallengeState = { weekKey: "", wordKey: "" };
    let loginStreakState = { lastDate: "", streak: 0 };
    let dailyMissionsState = { dateKey: "", completed: {} };
    let dailyWheelState = { dateKey: "", prize: "", claimed: false };
    let dailyWheelRotation = 0;
    let dailyWheelSpinning = false;
    let unlockedTopicPacks = ["starter"];
    let selectedStyle = "cheerful";
    let selectedBackgroundStyle = "theme";
    let selectedBackgroundTab = "theme";
    let selectedTopicPack = "";
    let recentWordKeys = [];
    let nightModeEnabled = false;
    let nightIntroVisible = false;
    let playerHistory = {};
    let storyProgress = { chapter: 0, completed: 0 };
    let localOnlineRoomCode = "";
    let onlineSession = {
      roomId: "",
      role: "host",
      joinUrl: "",
      active: false,
      created: false,
      hostName: "",
      guestName: "",
      guestJoined: false
    };
    let onlinePollId = null;
    let tournamentBracket = [];
    let audioPrimed = false;
    let audioUnlocking = false;
    let weeklyMissionsState = { weekKey: "", completed: {} };
    let favoriteWords = [];
    let wordSearchTerm = "";
    let playerXp = {};
    let cosmeticInventory = { avatars: ["🙂", "😎", "🤠", "🦊", "🐯", "🐼", "🦄", "🐸", "🐵", "🦁"], backgrounds: [...baseBackgroundChoices] };
    let homeIntroSeen = false;
    let topicStats = {};
    let monthlyMissionsState = { monthKey: "", completed: {} };
    let prizeBoxState = { claimed: {} };
    let musicEnabled = false;
    let musicStyle = "calm";
    let musicLoopTimer = null;
    let musicNoteIndex = 0;

    const DAILY_WHEEL_PRIZES = [
      { label: "2 מטבעות", shortLabel: "🪙 2", apply: () => awardCoins(2) },
      { label: "3 מטבעות", shortLabel: "🪙 3", apply: () => awardCoins(3) },
      { label: "הקפאת זמן", shortLabel: "⏸️ זמן", apply: () => { bonusInventory.freeze += 1; saveBonusStore(); renderShop(); } },
      { label: "חיים נוספים", shortLabel: "❤️ חיים", apply: () => { bonusInventory.life += 1; saveBonusStore(); renderShop(); } },
      { label: "דילוג מילה", shortLabel: "⏭️ דילוג", apply: () => { bonusInventory.skip += 1; saveBonusStore(); renderShop(); } },
      { label: "מגן טעות", shortLabel: "🛡️ מגן", apply: () => { bonusInventory.shield += 1; saveBonusStore(); renderShop(); } }
    ];

    let currentSelection = {
      mode: "single",
      category: "all",
      selectedCategories: [],
      difficulty: "medium",
      specialMode: "normal",
      dailyChallenge: false,
      weeklyChallenge: false,
      timeLimit: 180,
      partyRounds: 3,
      partyPlayerCount: 2,
      playerOneName: "",
      playerTwoName: "",
      bonusBuyer: "player1",
      singleAvatar: "🙂",
      playerOneAvatar: "😎",
      playerTwoAvatar: "🦊",
      customWord: "",
      customTopic: ""
    };

    function normalizeLetter(letter) {
      return finalLetterMap[letter] || letter;
    }

    function resetOnlineSession() {
      if (onlinePollId) {
        window.clearInterval(onlinePollId);
        onlinePollId = null;
      }
      onlineSession = {
        roomId: "",
        role: "host",
        joinUrl: "",
        active: false,
        created: false,
        hostName: "",
        guestName: "",
        guestJoined: false
      };
      localOnlineRoomCode = "";
    }

    const PUBLIC_SERVER_ORIGIN = "https://gleeful-crepe-89cce5.netlify.app";

    function getHangmanServerBase() {
      if (window.location.protocol === "file:") {
        return PUBLIC_SERVER_ORIGIN;
      }
      return window.location.origin;
    }

    async function hangmanApi(path, options = {}) {
      const base = getHangmanServerBase();
      if (!base) {
        throw new Error("server_required");
      }
      const response = await fetch(`${base}${path}`, {
        headers: {
          "Content-Type": "application/json",
          ...(options.headers || {})
        },
        ...options
      });
      let payload = null;
      try {
        payload = await response.json();
      } catch {
        payload = null;
      }
      if (!response.ok) {
        throw new Error(payload?.error || "hangman_request_failed");
      }
      return payload;
    }

    function pickOnlineRoomEntry() {
      const pool = getFilteredWords();
      const choice = pool[Math.floor(Math.random() * pool.length)];
      return choice || null;
    }

    function syncFromOnlineState(state) {
      if (!state) {
        return;
      }
      localOnlineRoomCode = state.roomId || localOnlineRoomCode;
      onlineSession.roomId = state.roomId || onlineSession.roomId;
      onlineSession.guestJoined = Boolean(state.guestJoined);
      onlineSession.hostName = state.hostName || currentSelection.playerOneName || "שחקן 1";
      onlineSession.guestName = state.guestName || currentSelection.playerTwoName || "שחקן 2";
      currentSelection.playerOneName = onlineSession.hostName;
      currentSelection.playerTwoName = onlineSession.guestName;
      if (state.hostAvatar) {
        currentSelection.playerOneAvatar = state.hostAvatar;
      }
      if (state.guestAvatar) {
        currentSelection.playerTwoAvatar = state.guestAvatar;
      }
      secretWord = state.word || secretWord;
      activeTopicLabel = state.topic || activeTopicLabel || "אונליין";
      activeTopicKey = state.category || activeTopicKey || "all";
      hint = currentSelection.specialMode === "extreme" ? "נושא: מוסתר" : `נושא: ${activeTopicLabel}`;
      descriptiveHint = state.hint || descriptiveHint;
      guessedLetters = new Set(Array.isArray(state.guessedLetters) ? state.guessedLetters : []);
      wrongGuesses = Number(state.wrongGuesses || 0);
      competitionScore = Number(state.correctHits || 0);
      if (typeof state.remainingTime === "number") {
        remainingTime = Math.max(0, state.remainingTime);
      }
      gameOver = Boolean(state.finished);
      lastGuessResult = null;
      lastGuessedLetter = "";
      renderWord();
      renderKeyboard();
      updateStats();
      renderAvatars();
      renderOnlineRoomCard();
      renderAmbientLayer();
      if (state.finished) {
        if (state.won) {
          messageEl.textContent = "החדר הושלם בהצלחה.";
          messageEl.className = "message win";
        } else {
          messageEl.textContent = `החדר הסתיים. המילה הייתה: ${state.answer || secretWord}`;
          messageEl.className = "message lose";
        }
      } else {
        messageEl.textContent = `חדר אונליין פעיל • ${onlineSession.hostName} מול ${onlineSession.guestName}.`;
        messageEl.className = "message";
      }
    }

    function startOnlinePolling() {
      if (onlinePollId || !onlineSession.roomId) {
        return;
      }
      onlinePollId = window.setInterval(async () => {
        if (!onlineSession.roomId) {
          return;
        }
        try {
          const state = await hangmanApi(`/api/hangman/rooms/${onlineSession.roomId}/state`);
          const wasFinished = gameOver;
          syncFromOnlineState(state);
          if (state.finished && !wasFinished) {
            if (state.won) {
              finishOnlineWin();
            } else {
              finishOnlineLoss(state.answer || secretWord);
            }
          }
        } catch {
          // keep current state if the local server pauses for a moment
        }
      }, 1200);
    }

    async function createOnlineRoom() {
      const choice = pickOnlineRoomEntry();
      if (!choice) {
        showStartValidation("לא מצאתי מילה מתאימה לחדר האונליין הזה.", categorySelectEl);
        return;
      }
      try {
        const payload = await hangmanApi("/api/hangman/rooms/create", {
          method: "POST",
          body: JSON.stringify({
            hostName: currentSelection.playerOneName || "שחקן 1",
            guestName: currentSelection.playerTwoName || "שחקן 2",
            hostAvatar: currentSelection.playerOneAvatar || "😎",
            guestAvatar: currentSelection.playerTwoAvatar || "🦊",
            word: choice.word,
            topic: choice.topic,
            hint: buildHint(choice),
            category: choice.category,
            timeLimit: currentSelection.timeLimit
          })
        });
        onlineSession = {
          roomId: payload.roomId,
          role: "host",
          joinUrl: payload.joinUrl,
          active: false,
          created: true,
          hostName: currentSelection.playerOneName || "שחקן 1",
          guestName: currentSelection.playerTwoName || "שחקן 2",
          guestJoined: false
        };
        localOnlineRoomCode = payload.roomId;
        renderOnlineRoomCard();
        setStartNote(`חדר אונליין נוצר. שלחו את הקישור לחדר ${payload.roomId}.`);
        startOnlinePolling();
      } catch (error) {
        showStartValidation("לא הצלחתי ליצור חדר אונליין. פתחו את המשחק דרך מרכז המשחקים או דרך הקישור הציבורי.", startButtonEl);
      }
    }

    async function copyOnlineRoomLink() {
      if (!onlineSession.joinUrl) {
        return;
      }
      try {
        await navigator.clipboard.writeText(onlineSession.joinUrl);
        setStartNote("הקישור לחדר הועתק.");
      } catch {
        setStartNote(onlineSession.joinUrl);
      }
    }

    async function applyIncomingOnlineRoom() {
      const params = new URLSearchParams(window.location.search);
      const roomId = params.get("hangRoom");
      if (!roomId) {
        return false;
      }
      currentSelection.mode = "online";
      try {
        const joinData = await hangmanApi(`/api/hangman/rooms/${roomId}/join`, {
          method: "POST",
          body: JSON.stringify({
            guestName: currentSelection.playerTwoName || "אורח",
            guestAvatar: currentSelection.playerTwoAvatar || "🦊"
          })
        });
        onlineSession = {
          roomId,
          role: "guest",
          joinUrl: `${window.location.origin}${window.location.pathname}?hangRoom=${roomId}`,
          active: true,
          created: true,
          hostName: joinData.hostName || "שחקן 1",
          guestName: joinData.guestName || "שחקן 2",
          guestJoined: true
        };
        localOnlineRoomCode = roomId;
        currentSelection.playerOneName = joinData.hostName || currentSelection.playerOneName;
        currentSelection.playerTwoName = joinData.guestName || currentSelection.playerTwoName;
        currentSelection.playerOneAvatar = joinData.hostAvatar || currentSelection.playerOneAvatar;
        currentSelection.playerTwoAvatar = joinData.guestAvatar || currentSelection.playerTwoAvatar;
        currentSelection.timeLimit = Number(joinData.timeLimit || currentSelection.timeLimit || 180);
        applySelectionToControls();
        homeIntroSeen = true;
        homeOverlayEl?.classList.add("hidden");
        hideStartScreen();
        transitionWithLoading(async () => {
          guessedLetters = new Set();
          wrongGuesses = 0;
          gameOver = false;
          usedHint = false;
          hintVisible = false;
          clearTimer();
          const state = await hangmanApi(`/api/hangman/rooms/${roomId}/state`);
          syncFromOnlineState(state);
          renderOnlineRoomCard();
          startOnlinePolling();
        }, {
          duration: 140,
          title: "מצטרפים לחדר אונליין...",
          subtitle: `עוד רגע תתחילו לשחק בחדר ${roomId}.`
        });
        window.history.replaceState({}, "", window.location.pathname);
        return true;
      } catch {
        setStartNote("לא הצלחתי להצטרף לחדר האונליין. בדקו שהקישור תקין.", true);
        renderOnlineRoomCard();
        return true;
      }
    }

    async function sendOnlineGuess(letter) {
      if (!onlineSession.roomId || gameOver) {
        return;
      }
      try {
        const state = await hangmanApi(`/api/hangman/rooms/${onlineSession.roomId}/guess`, {
          method: "POST",
          body: JSON.stringify({ letter })
        });
        syncFromOnlineState(state);
        if (state.hit) {
          replayAnimation(wordEl, "burst");
          launchConfetti(26 + getDifficultyConfettiBonus());
          playSound("correct");
        } else {
          replayAnimation(hangmanBoxEl, "guess-miss");
          playSound("wrong");
        }
        if (state.finished) {
          if (state.won) {
            finishOnlineWin();
          } else {
            finishOnlineLoss(state.answer || secretWord);
          }
        }
      } catch {
        messageEl.textContent = "לא הצלחתי לשלוח את האות לשרת.";
        messageEl.className = "message lose";
      }
    }

    function finishOnlineWin() {
      if (gameOver) {
        // state already marked; keep ui coherent
      }
      gameOver = true;
      clearTimer();
      window.clearInterval(onlinePollId);
      onlinePollId = null;
      messageEl.textContent = "ניחשתם את המילה בחדר האונליין!";
      messageEl.className = "message win";
      renderWord();
      renderKeyboard();
      updateStats();
      playSound("win");
      launchConfetti(42 + getDifficultyConfettiBonus());
      showEndScreen("win");
    }

    function finishOnlineLoss(answer = secretWord) {
      gameOver = true;
      clearTimer();
      window.clearInterval(onlinePollId);
      onlinePollId = null;
      secretWord = answer || secretWord;
      messageEl.textContent = `החדר הסתיים. המילה הייתה: ${secretWord}`;
      messageEl.className = "message lose";
      renderWord();
      renderKeyboard();
      updateStats();
      playSound("lose");
      replayAnimation(hangmanBoxEl, "guess-miss");
      replayAnimation(hangmanFigureEl, "explode");
      window.clearTimeout(endScreenTimer);
      endScreenTimer = window.setTimeout(() => showEndScreen("lose"), 500);
    }

    function isTypingField(target) {
      return target instanceof HTMLElement &&
        (target.tagName === "INPUT" || target.tagName === "TEXTAREA" || target.isContentEditable);
    }

    function isHebrewLetter(letter) {
      return /[א-ת]/.test(normalizeLetter(letter));
    }

    function wordLength(word) {
      return [...word].filter((letter) => /[א-ת]/.test(letter)).length;
    }

    function wordIncludesLetter(word, letter) {
      const normalizedGuess = normalizeLetter(letter);
      return [...word].some((currentLetter) => isHebrewLetter(currentLetter) && normalizeLetter(currentLetter) === normalizedGuess);
    }

    function wordSolved(word) {
      return [...word].every((letter) => !isHebrewLetter(letter) || guessedLetters.has(normalizeLetter(letter)));
    }

    function buildHint(choice) {
      const descriptions = {
        food: "משהו שאפשר לאכול, בדרך כלל כשממש רעבים.",
        drinks: "משהו ששותים ולא אוכלים.",
        animals: "יצור חי שאפשר לפגוש בטבע או ליד אנשים.",
        birds: "יצור עם כנפיים שלא תמיד נשאר על הקרקע.",
        fruits: "משהו מתוק יחסית שגדל על עץ או שיח.",
        vegetables: "משהו שמוצאים הרבה בסלט או במטבח.",
        technology: "משהו שקשור למסכים, חשמל או מחשבים.",
        sports: "משהו שפוגשים בזמן תחרות, משחק או אימון.",
        music: "משהו שקשור לצליל, קצב או הופעה.",
        home: "משהו שפוגשים בדרך כלל בתוך הבית.",
        places: "מקום שאפשר להגיע אליו או לבקר בו.",
        weather: "משהו שמרגישים בחוץ ולא בתוך הבית."
      };
      const length = wordLength(choice.word);
      const firstLetter = choice.word[0];
      const lastLetter = choice.word[choice.word.length - 1];
      const baseDescription = descriptions[choice.category] || `המילה שייכת לנושא ${choice.topic || categoryLabels[choice.category] || "כללי"}.`;
      return `${baseDescription} יש בה ${length} אותיות, היא מתחילה ב-${firstLetter} ומסתיימת ב-${lastLetter}.`;
    }

    function formatTime(totalSeconds) {
      const minutes = String(Math.floor(totalSeconds / 60)).padStart(2, "0");
      const seconds = String(totalSeconds % 60).padStart(2, "0");
      return `${minutes}:${seconds}`;
    }

    function updateTimerDisplay() {
      if (isPaused) {
        timerEl.textContent = "מושהה";
        timerEl.classList.remove("timer-warning");
        return;
      }

      if (currentSelection.mode === "practice") {
      timerEl.textContent = "אימון";
      timerEl.classList.remove("timer-warning");
      return;
    }

      if (currentSelection.mode === "survival") {
        timerEl.textContent = "ללא";
        timerEl.classList.remove("timer-warning");
        return;
      }

      timerEl.textContent = formatTime(remainingTime);
      timerEl.classList.toggle("timer-warning", remainingTime <= 30);
    }

    function clearTimer() {
      window.clearInterval(timerInterval);
      timerInterval = null;
    }

    function runTimerInterval() {
      clearTimer();

      if (currentSelection.mode === "practice" || currentSelection.mode === "survival" || isPaused) {
        updateTimerDisplay();
        return;
      }

      timerInterval = window.setInterval(() => {
        remainingTime -= 1;
        updateTimerDisplay();

        if (remainingTime <= 0) {
          clearTimer();
          if (currentSelection.mode === "competition" || currentSelection.mode === "online") {
            finishCompetition();
          } else if (currentSelection.mode === "party") {
            finishParty();
          } else {
            finishLoss("נגמר הזמן!");
          }
        }
      }, 1000);
    }

    function startTimer() {
      remainingTime = gameTimeLimit;
      updateTimerDisplay();
      runTimerInterval();
    }

    function setPaused(nextPaused) {
      if (gameOver || startScreenEl.classList.contains("hidden") === false) {
        return;
      }

      isPaused = nextPaused;

      if (isPaused) {
        clearTimer();
        messageEl.textContent = "המשחק מושהה. לחצו על המשך כדי לחזור.";
        messageEl.className = "message";
      } else {
        messageEl.textContent = "חזרנו למשחק. אפשר להמשיך לנחש.";
        messageEl.className = "message guess-hit";
        runTimerInterval();
      }

      updateTimerDisplay();
      renderKeyboard();
      updateStats();
      updatePauseButton();
      updateBonusButtons();
    }

    function ensureAudioContext() {
      if (!audioContext) {
        const AudioCtx = window.AudioContext || window.webkitAudioContext;
        if (AudioCtx) {
          audioContext = new AudioCtx();
        }
      }

      if (audioContext && !masterGainNode) {
        masterGainNode = audioContext.createGain();
        masterGainNode.gain.value = 0.95;
        masterGainNode.connect(audioContext.destination);
      }

      if (audioContext?.state === "suspended") {
        audioContext.resume();
      }
    }

    function getAudioOutputNode() {
      if (!audioContext) {
        return null;
      }
      return masterGainNode || audioContext.destination;
    }

    function primeAudio() {
      ensureAudioContext();

      if (!audioContext) {
        return;
      }

      const finishPrime = () => {
        if (!audioContext || audioPrimed) {
          return;
        }
        const outputNode = getAudioOutputNode();
        if (!outputNode) {
          return;
        }
        const now = audioContext.currentTime;
        const oscillator = audioContext.createOscillator();
        const gainNode = audioContext.createGain();
        oscillator.type = "sine";
        oscillator.frequency.setValueAtTime(523, now);
        gainNode.gain.setValueAtTime(0.0001, now);
        gainNode.gain.linearRampToValueAtTime(0.045, now + 0.02);
        gainNode.gain.linearRampToValueAtTime(0.0001, now + 0.12);
        oscillator.connect(gainNode);
        gainNode.connect(outputNode);
        oscillator.start(now);
        oscillator.stop(now + 0.14);
        audioPrimed = true;
      };

      if (audioContext.state === "suspended") {
        if (audioUnlocking) {
          return;
        }
        audioUnlocking = true;
        audioContext.resume()
          .then(() => {
            audioUnlocking = false;
            finishPrime();
          })
          .catch(() => {
            audioUnlocking = false;
          });
        return;
      }

      finishPrime();
    }

    function playImmediateTone(freq = 660, duration = 0.18, volume = 0.18, type = "triangle") {
      ensureAudioContext();
      if (!audioContext) {
        return false;
      }

      const runTone = () => {
        if (!audioContext || audioContext.state !== "running") {
          return false;
        }
        const outputNode = getAudioOutputNode();
        if (!outputNode) {
          return false;
        }
        const now = audioContext.currentTime;
        const oscillator = audioContext.createOscillator();
        const gainNode = audioContext.createGain();
        oscillator.type = type;
        oscillator.frequency.setValueAtTime(freq, now);
        gainNode.gain.setValueAtTime(0.0001, now);
        gainNode.gain.linearRampToValueAtTime(volume, now + 0.02);
        gainNode.gain.linearRampToValueAtTime(0.0001, now + duration);
        oscillator.connect(gainNode);
        gainNode.connect(outputNode);
        oscillator.start(now);
        oscillator.stop(now + duration + 0.03);
        return true;
      };

      if (audioContext.state !== "running") {
        audioContext.resume().then(() => {
          runTone();
        }).catch(() => {});
        return false;
      }

      return runTone();
    }

    function playSound(type, attempt = 0) {
      ensureAudioContext();

      if (!audioContext) {
        return;
      }

      if (audioContext.state !== "running") {
        primeAudio();
        if (attempt < 2) {
          window.setTimeout(() => playSound(type, attempt + 1), 80);
        }
        return;
      }

      const now = audioContext.currentTime;
      const notesByType = {
        correct: [
          { freq: 523, duration: 0.07, offset: 0, type: "sine" },
          { freq: 659, duration: 0.08, offset: 0.04, type: "triangle" },
          { freq: 784, duration: 0.1, offset: 0.08, type: "sine" },
          { freq: 880, duration: 0.12, offset: 0.13, type: "sine" }
        ],
        wrong: [
          { freq: 330, duration: 0.07, offset: 0, type: "sine" },
          { freq: 262, duration: 0.1, offset: 0.045, type: "triangle" },
          { freq: 220, duration: 0.14, offset: 0.095, type: "sine" }
        ],
        win: [
          { freq: 523, duration: 0.07, offset: 0, type: "sine" },
          { freq: 659, duration: 0.08, offset: 0.045, type: "triangle" },
          { freq: 784, duration: 0.1, offset: 0.09, type: "sine" },
          { freq: 988, duration: 0.12, offset: 0.145, type: "sine" },
          { freq: 1175, duration: 0.16, offset: 0.205, type: "triangle" }
        ],
        lose: [
          { freq: 294, duration: 0.12, offset: 0, type: "sine" },
          { freq: 220, duration: 0.15, offset: 0.07, type: "triangle" },
          { freq: 175, duration: 0.18, offset: 0.14, type: "sine" },
          { freq: 147, duration: 0.22, offset: 0.23, type: "sine" }
        ],
        validation: [
          { freq: 392, duration: 0.07, offset: 0, type: "sine" },
          { freq: 330, duration: 0.1, offset: 0.045, type: "triangle" },
          { freq: 262, duration: 0.16, offset: 0.1, type: "sine" }
        ],
        ui_soft: [
          { freq: 440, duration: 0.045, offset: 0, type: "sine" },
          { freq: 523, duration: 0.06, offset: 0.025, type: "sine" }
        ],
        ui_warm: [
          { freq: 392, duration: 0.045, offset: 0, type: "sine" },
          { freq: 523, duration: 0.065, offset: 0.025, type: "sine" }
        ],
        ui_cool: [
          { freq: 494, duration: 0.045, offset: 0, type: "sine" },
          { freq: 659, duration: 0.065, offset: 0.022, type: "sine" }
        ],
        ui_bright: [
          { freq: 523, duration: 0.045, offset: 0, type: "sine" },
          { freq: 698, duration: 0.06, offset: 0.022, type: "triangle" }
        ],
        ui_topic_nature: [
          { freq: 392, duration: 0.05, offset: 0, type: "sine" },
          { freq: 523, duration: 0.065, offset: 0.03, type: "sine" }
        ],
        ui_topic_food: [
          { freq: 349, duration: 0.045, offset: 0, type: "sine" },
          { freq: 440, duration: 0.05, offset: 0.022, type: "sine" },
          { freq: 523, duration: 0.06, offset: 0.05, type: "triangle" }
        ],
        ui_topic_tech: [
          { freq: 523, duration: 0.035, offset: 0, type: "triangle" },
          { freq: 659, duration: 0.05, offset: 0.02, type: "sine" }
        ],
        ui_topic_sport: [
          { freq: 330, duration: 0.045, offset: 0, type: "sine" },
          { freq: 494, duration: 0.05, offset: 0.022, type: "triangle" },
          { freq: 659, duration: 0.06, offset: 0.05, type: "sine" }
        ],
        ui_topic_home: [
          { freq: 294, duration: 0.05, offset: 0, type: "sine" },
          { freq: 392, duration: 0.06, offset: 0.03, type: "sine" }
        ]
      };

      const notes = notesByType[type] || [];
      const pitchMultiplier = getAvatarPitchMultiplier();
      const modePitchMultiplier = currentSelection.mode === "party"
        ? 1.08
        : (currentSelection.mode === "competition"
          ? 0.98
          : (currentSelection.mode === "survival" ? 0.92 : (currentSelection.mode === "practice" ? 1.04 : 1)));
      const outputNode = getAudioOutputNode();

      if (!outputNode) {
        return;
      }

      notes.forEach((note) => {
        const oscillator = audioContext.createOscillator();
        const gainNode = audioContext.createGain();
        oscillator.type = note.type || "sine";
        oscillator.frequency.setValueAtTime(note.freq * pitchMultiplier * modePitchMultiplier, now + note.offset);
        gainNode.gain.setValueAtTime(0.0001, now + note.offset);
        const targetVolume = type === "correct"
          ? (currentSelection.specialMode === "kids" ? 0.26 : 0.22)
          : (type === "win"
            ? 0.24
            : (type === "lose"
              ? 0.18
              : (type === "validation"
                ? 0.13
                : (type.startsWith("ui_") ? 0.12 : 0.16))));
        gainNode.gain.exponentialRampToValueAtTime(targetVolume, now + note.offset + 0.008);
        gainNode.gain.exponentialRampToValueAtTime(0.0001, now + note.offset + note.duration);
        oscillator.connect(gainNode);
        gainNode.connect(outputNode);
        oscillator.start(now + note.offset);
        oscillator.stop(now + note.offset + note.duration + 0.02);
      });
    }

    function getTopicSoundType(categoryKey) {
      if (["nature", "weather", "sea", "birds", "animals", "insects"].includes(categoryKey)) {
        return "ui_topic_nature";
      }

      if (["food", "drinks", "fruits", "vegetables"].includes(categoryKey)) {
        return "ui_topic_food";
      }

      if (["technology", "office", "school"].includes(categoryKey)) {
        return "ui_topic_tech";
      }

      if (["sports", "health", "body"].includes(categoryKey)) {
        return "ui_topic_sport";
      }

      if (["home", "furniture", "kitchen", "clothes", "materials"].includes(categoryKey)) {
        return "ui_topic_home";
      }

      return "ui_bright";
    }

    function playUiSoundForElement(element) {
      if (!element || element.disabled) {
        return;
      }

      if (element.matches(".key")) {
        return;
      }

      if (element.matches("#hint-button, #freeze-button, #life-button, #skip-button, #shield-button")) {
        return;
      }

      if (element.matches(".category-chip")) {
        const input = element.querySelector("[data-category-chip]");
        playSound(getTopicSoundType(input?.dataset.categoryChip || ""));
        return;
      }

      if (element.matches(".mode-pill, .special-mode-pill")) {
        playSound("ui_bright");
        return;
      }

      if (element.matches(".time-pill, .difficulty-pill")) {
        playSound("ui_cool");
        return;
      }

      if (element.matches(".shop-buy")) {
        playSound("ui_warm");
        return;
      }

      if (element.matches(".avatar-option, .style-swatch")) {
        playSound("ui_soft");
        return;
      }

      if (element.matches("#start-button, #end-button, #reset-button, #switch-mode-button")) {
        playSound("ui_warm");
        return;
      }

      playSound("ui_soft");
    }

    function playBackgroundMusicStep() {
      if (!musicEnabled) {
        return;
      }
      ensureAudioContext();
      if (!audioContext) {
        return;
      }
      const outputNode = getAudioOutputNode();
      if (!outputNode || audioContext.state !== "running") {
        return;
      }
      const sequenceLibrary = {
        calm: {
          single: [[262, 330, 392], [294, 370, 440], [247, 330, 392], [262, 349, 440]],
          story: [[294, 370, 440], [330, 392, 494], [262, 349, 440], [294, 392, 523]],
          party: [[330, 392, 494], [349, 440, 523], [294, 392, 494], [330, 440, 523]],
          tournament: [[220, 277, 330], [247, 311, 392], [262, 330, 392], [247, 294, 370]],
          online: [[294, 370, 440], [330, 415, 523], [277, 349, 440], [294, 392, 494]],
          survival: [[220, 277, 330], [247, 294, 349], [196, 247, 294], [220, 262, 330]]
        },
        playful: {
          single: [[330, 392, 523], [392, 494, 587], [349, 440, 523], [392, 523, 659]],
          story: [[349, 440, 523], [392, 494, 587], [440, 523, 659], [392, 523, 587]],
          party: [[392, 523, 659], [440, 554, 698], [392, 523, 659], [494, 659, 784]],
          tournament: [[262, 330, 392], [330, 415, 523], [349, 440, 554], [392, 523, 659]],
          online: [[349, 440, 554], [392, 494, 659], [440, 554, 698], [392, 523, 659]],
          survival: [[247, 294, 370], [262, 330, 392], [294, 349, 440], [330, 392, 494]]
        },
        dreamy: {
          single: [[220, 262, 330], [196, 247, 294], [220, 277, 330], [196, 262, 311]],
          story: [[247, 294, 370], [220, 277, 330], [262, 330, 392], [247, 311, 370]],
          party: [[262, 330, 392], [247, 311, 370], [220, 277, 330], [262, 349, 415]],
          tournament: [[196, 220, 262], [220, 277, 330], [247, 294, 349], [220, 262, 311]],
          online: [[247, 330, 392], [220, 294, 349], [247, 311, 392], [262, 330, 415]],
          survival: [[196, 220, 247], [220, 262, 294], [185, 220, 277], [196, 247, 294]]
        },
        arcade: {
          single: [[392, 523, 659], [440, 587, 698], [392, 523, 659], [523, 659, 784]],
          story: [[440, 587, 698], [494, 659, 784], [440, 587, 698], [523, 698, 880]],
          party: [[523, 659, 784], [587, 698, 880], [523, 659, 784], [659, 784, 988]],
          tournament: [[330, 440, 523], [392, 494, 659], [440, 554, 698], [523, 659, 784]],
          online: [[392, 494, 659], [440, 587, 698], [392, 523, 659], [494, 659, 880]],
          survival: [[294, 392, 440], [330, 440, 523], [349, 440, 587], [392, 523, 659]]
        },
        story: {
          single: [[262, 330, 392], [294, 349, 440], [330, 392, 494], [294, 370, 440]],
          story: [[294, 370, 440], [330, 392, 494], [349, 440, 523], [392, 494, 587]],
          party: [[330, 392, 440], [349, 440, 523], [392, 494, 587], [330, 440, 523]],
          tournament: [[247, 294, 330], [277, 349, 392], [294, 370, 440], [330, 392, 494]],
          online: [[294, 349, 392], [330, 392, 494], [349, 440, 523], [392, 523, 587]],
          survival: [[220, 262, 294], [247, 294, 330], [262, 330, 392], [220, 277, 330]]
        }
      };
      const sequences = sequenceLibrary[musicStyle] || sequenceLibrary.calm;
      const key = currentSelection.specialMode === "story" ? "story" : (sequences[currentSelection.mode] ? currentSelection.mode : "single");
      const sequence = sequences[key];
      const chord = sequence[musicNoteIndex % sequence.length];
      musicNoteIndex += 1;
      const now = audioContext.currentTime;
      const baseVolume = musicStyle === "playful" ? 0.055 : (musicStyle === "arcade" ? 0.065 : 0.045);
      const hold = musicStyle === "dreamy" ? 0.78 : (musicStyle === "story" ? 0.74 : (musicStyle === "arcade" ? 0.46 : 0.58));
      chord.forEach((freq, index) => {
        const oscillator = audioContext.createOscillator();
        const gainNode = audioContext.createGain();
        oscillator.type = musicStyle === "dreamy"
          ? "sine"
          : (musicStyle === "arcade" ? "square" : (index === 0 ? "triangle" : "sine"));
        oscillator.frequency.setValueAtTime(freq, now);
        gainNode.gain.setValueAtTime(0.0001, now);
        gainNode.gain.exponentialRampToValueAtTime(baseVolume / (index === 0 ? 1 : 1.45), now + 0.03 + index * 0.01);
        gainNode.gain.exponentialRampToValueAtTime(0.0001, now + hold + index * 0.03);
        oscillator.connect(gainNode);
        gainNode.connect(outputNode);
        oscillator.start(now + index * 0.01);
        oscillator.stop(now + hold + index * 0.03 + 0.04);
      });
    }

    function stopBackgroundMusic() {
      window.clearInterval(musicLoopTimer);
      musicLoopTimer = null;
    }

    function startBackgroundMusic() {
      stopBackgroundMusic();
      if (!musicEnabled) {
        renderMusicState();
        return;
      }
      ensureAudioContext();
      const startLoop = () => {
        playBackgroundMusicStep();
        musicLoopTimer = window.setInterval(playBackgroundMusicStep, musicStyle === "dreamy" ? 820 : (musicStyle === "playful" ? 520 : (musicStyle === "arcade" ? 360 : (musicStyle === "story" ? 760 : 620))));
        renderMusicState();
      };
      primeAudio();
      if (audioContext?.state !== "running") {
        audioContext?.resume()
          .then(() => {
            startLoop();
          })
          .catch(() => {
            renderMusicState();
          });
        return;
      }
      startLoop();
    }

    function toggleBackgroundMusic() {
      musicEnabled = !musicEnabled;
      saveMusicPreference();
      if (musicEnabled) {
        startBackgroundMusic();
      } else {
        stopBackgroundMusic();
        renderMusicState();
      }
    }

    function testBackgroundMusic() {
      ensureAudioContext();
      const testFrequencies = musicStyle === "playful"
        ? [523, 659, 784, 988]
        : (musicStyle === "dreamy" ? [330, 392, 494, 587] : (musicStyle === "arcade" ? [587, 784, 988, 1175] : (musicStyle === "story" ? [294, 370, 440, 523] : [440, 554, 659, 784])));
      const runTest = () => {
        playImmediateTone(testFrequencies[0], 0.24, 0.24, musicStyle === "arcade" ? "square" : "triangle");
        window.setTimeout(() => playImmediateTone(testFrequencies[1], 0.24, 0.22, musicStyle === "dreamy" ? "sine" : "triangle"), 180);
        window.setTimeout(() => playImmediateTone(testFrequencies[2], 0.28, 0.2, musicStyle === "story" ? "sine" : "triangle"), 360);
        window.setTimeout(() => playImmediateTone(testFrequencies[3], 0.34, 0.18, "sine"), 560);
      };
      primeAudio();
      if (audioContext?.state !== "running") {
        audioContext?.resume()
          .then(() => {
            runTest();
          })
          .catch(() => {});
      } else {
        runTest();
      }
      renderMusicState();
    }

    function sanitizeCustomWord(word) {
      return word
        .trim()
        .replace(/\s+/g, " ")
        .replace(/[^א-תךםןףץ'\-\s]/g, "");
    }

    function sanitizeCustomTopic(topic) {
      return topic
        .trim()
        .replace(/\s+/g, " ")
        .replace(/[^א-תךםןףץ'\-\s]/g, "");
    }

    function sanitizePlayerName(name) {
      return name
        .trim()
        .replace(/\s+/g, " ")
        .replace(/[^א-תךםןףץA-Za-z0-9'\-\s]/g, "");
    }

    function clampPartyPlayerCount(value) {
      return Math.max(2, Math.min(10, Number(value) || 2));
    }

    function clampPartyRounds(value) {
      return Math.max(1, Math.min(50, Number(value) || 3));
    }

    function sanitizeStoredWord(word) {
      return sanitizeCustomWord(word).replace(/\s+/g, "");
    }

    function sanitizeLookupValue(value) {
      return [...value.trim()]
        .map((char) => normalizeLetter(char))
        .filter((char) => /[א-ת]/.test(char))
        .join("");
    }

    function sanitizeTopicValue(value) {
      return value.trim().replace(/\s+/g, " ");
    }

    function rebuildWordCollections() {
      const baseWords = Object.entries(wordBank).flatMap(([category, source]) =>
        source
          .trim()
          .split(/\s+/)
          .map((word) => ({ word, category, topic: categoryLabels[category] || "כללי" }))
      );
      const unlockedPackWords = unlockedTopicPacks
        .filter((key) => topicPacks[key])
        .flatMap((key) => topicPacks[key].entries || [])
        .map((entry) => ({
          word: entry.word,
          category: entry.category,
          topic: categoryLabels[entry.category] || "כללי"
        }));

      words = baseWords.concat(unlockedPackWords, customWordEntries.filter((entry) => !entry.favoriteOnly));
      wordTopicLookup = new Map();

      words.forEach((entry) => {
        const key = sanitizeLookupValue(entry.word);
        const existingTopics = wordTopicLookup.get(key) || new Set();
        existingTopics.add(sanitizeTopicValue(entry.topic));
        wordTopicLookup.set(key, existingTopics);
      });
    }

    function getCurrentProfileName() {
      return profileNameInputEl?.value?.trim() || currentSelection.playerOneName || "שחקן יחיד";
    }

    function getXpInfo(name) {
      const xp = Math.max(0, Number(playerXp[name] || 0));
      const level = Math.floor(xp / 40) + 1;
      const currentFloor = (level - 1) * 40;
      const nextFloor = level * 40;
      const inLevel = xp - currentFloor;
      return {
        xp,
        level,
        currentFloor,
        nextFloor,
        inLevel,
        need: Math.max(0, nextFloor - xp),
        progressPercent: Math.max(8, Math.min(100, Math.round((inLevel / 40) * 100)))
      };
    }

    function awardXp(name, amount) {
      if (!name || amount <= 0) {
        return;
      }
      playerXp[name] = Math.max(0, Number(playerXp[name] || 0) + amount);
      savePlayerXp();
      renderProfiles();
      renderProfileSummary();
      renderStartDashboard();
      if (profileModalEl?.classList.contains("visible") && selectedProfileTabIndex >= 0) {
        openFullProfile(selectedProfileTabIndex);
      }
    }

    function unlockCosmeticAvatar(avatar) {
      if (!cosmeticInventory.avatars.includes(avatar)) {
        cosmeticInventory.avatars.push(avatar);
        saveCosmeticInventory();
      }
    }

    function unlockCosmeticBackground(backgroundKey) {
      if (!cosmeticInventory.backgrounds.includes(backgroundKey)) {
        cosmeticInventory.backgrounds.push(backgroundKey);
        saveCosmeticInventory();
      }
    }

    function loadPlayerXp() {
      try {
        playerXp = JSON.parse(localStorage.getItem("hangman_player_xp") || "{}") || {};
      } catch {
        playerXp = {};
      }
    }

    function savePlayerXp() {
      try {
        localStorage.setItem("hangman_player_xp", JSON.stringify(playerXp));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadCosmeticInventory() {
      try {
        const saved = JSON.parse(localStorage.getItem("hangman_cosmetics") || "{}") || {};
        cosmeticInventory = {
          avatars: Array.isArray(saved.avatars) && saved.avatars.length
            ? [...new Set([...baseAvatarChoices, ...saved.avatars])]
            : [...baseAvatarChoices],
          backgrounds: Array.isArray(saved.backgrounds) && saved.backgrounds.length
            ? [...new Set([...baseBackgroundChoices, ...saved.backgrounds])]
            : [...baseBackgroundChoices]
        };
      } catch {
        cosmeticInventory = { avatars: [...baseAvatarChoices], backgrounds: [...baseBackgroundChoices] };
      }
    }

    function saveCosmeticInventory() {
      try {
        localStorage.setItem("hangman_cosmetics", JSON.stringify(cosmeticInventory));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadHomeIntroSeen() {
      homeIntroSeen = false;
    }

    function saveHomeIntroSeen() {
      // מסך הברוכים הבאים נשאר חד-פעמי רק לסשן הנוכחי,
      // כדי שיופיע מחדש בכל פתיחה/רענון של המשחק.
    }

    function loadTopicStats() {
      try {
        topicStats = JSON.parse(localStorage.getItem("hangman_topic_stats") || "{}") || {};
      } catch {
        topicStats = {};
      }
    }

    function saveTopicStats() {
      try {
        localStorage.setItem("hangman_topic_stats", JSON.stringify(topicStats));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadMonthlyMissionsState() {
      try {
        monthlyMissionsState = JSON.parse(localStorage.getItem("hangman_monthly_missions") || "{}") || {};
      } catch {
        monthlyMissionsState = { monthKey: "", completed: {} };
      }
    }

    function saveMonthlyMissionsState() {
      try {
        localStorage.setItem("hangman_monthly_missions", JSON.stringify(monthlyMissionsState));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadPrizeBoxState() {
      try {
        prizeBoxState = JSON.parse(localStorage.getItem("hangman_prize_boxes") || "{}") || {};
      } catch {
        prizeBoxState = { claimed: {} };
      }
    }

    function savePrizeBoxState() {
      try {
        localStorage.setItem("hangman_prize_boxes", JSON.stringify(prizeBoxState));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadCustomWords() {
      try {
        const saved = JSON.parse(localStorage.getItem("hangman_custom_words") || "[]");
        customWordEntries = Array.isArray(saved)
          ? saved
            .map((entry) => ({
              word: sanitizeStoredWord(entry.word || ""),
              category: entry.category,
              topic: categoryLabels[entry.category] || sanitizeTopicValue(entry.topic || ""),
              favoriteOnly: Boolean(entry.favoriteOnly)
            }))
            .filter((entry) => entry.word && categoryLabels[entry.category])
          : [];
      } catch {
        customWordEntries = [];
      }
    }

    function saveCustomWords() {
      try {
        localStorage.setItem("hangman_custom_words", JSON.stringify(customWordEntries));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function setAddWordNote(message = "", type = "") {
      window.clearTimeout(addWordNoteTimer);
      addWordNoteEl.textContent = message;
      addWordNoteEl.className = `mini-add-note${type ? ` ${type}` : ""}`;

      if (message && type === "success") {
        addWordNoteTimer = window.setTimeout(() => {
          addWordNoteEl.textContent = "";
          addWordNoteEl.className = "mini-add-note";
        }, 5000);
      }
    }

    function hasFavoriteWords() {
      return favoriteWords.length > 0;
    }

    function ensureFavoriteDifficultyAvailability() {
      if (!hasFavoriteWords() && currentSelection.difficulty === "favorites") {
        currentSelection.difficulty = "medium";
      }
      if (difficultySelectEl) {
        difficultySelectEl.value = currentSelection.difficulty;
      }
    }

    function saveWordToFavoritesOnly() {
      const category = categorySelectEl.value;
      const newWord = sanitizeStoredWord(addWordInputEl.value);

      if (!category || category === "all") {
        setAddWordNote("בחרו קודם נושא ספציפי למילה החדשה.", "error");
        categorySelectEl.focus();
        return;
      }

      if (wordLength(newWord) < 2) {
        setAddWordNote("כתבו מילה תקינה בעברית כדי לשמור למועדפים.", "error");
        addWordInputEl.focus();
        return;
      }

      const lookupKey = sanitizeLookupValue(newWord);
      const favoriteKey = `${category}:${lookupKey}`;
      const expectedTopic = sanitizeTopicValue(categoryLabels[category] || "");
      const knownTopics = wordTopicLookup.get(lookupKey);

      if (!knownTopics?.has(expectedTopic)) {
        customWordEntries.push({
          word: newWord,
          category,
          topic: categoryLabels[category],
          favoriteOnly: true
        });
        saveCustomWords();
        rebuildWordCollections();
      }

      if (!favoriteWords.includes(favoriteKey)) {
        favoriteWords.unshift(favoriteKey);
        favoriteWords = favoriteWords.slice(0, 20);
        saveFavoriteWords();
      }

      ensureFavoriteDifficultyAvailability();
      addWordInputEl.value = "";
      renderWordManager();
      renderFavoriteWords();
      renderDifficultyPills();
      setAddWordNote(`המילה "${newWord}" נשמרה רק במילים האהובות.`, "success");
    }

    function addWordToBank() {
      const category = categorySelectEl.value;
      const newWord = sanitizeStoredWord(addWordInputEl.value);

      if (!category || category === "all") {
        setAddWordNote("בחרו קודם נושא ספציפי למילה החדשה.", "error");
        categorySelectEl.focus();
        return;
      }

      if (wordLength(newWord) < 2) {
        setAddWordNote("כתבו מילה תקינה בעברית כדי להוסיף למאגר.", "error");
        addWordInputEl.focus();
        return;
      }

      const lookupKey = sanitizeLookupValue(newWord);
      const expectedTopic = sanitizeTopicValue(categoryLabels[category] || "");
      const knownTopics = wordTopicLookup.get(lookupKey);

      if (knownTopics?.has(expectedTopic)) {
        setAddWordNote("מילה זו כבר במאגר.", "error");
        return;
      }

      customWordEntries.push({
        word: newWord,
        category,
        topic: categoryLabels[category],
        favoriteOnly: false
      });
      saveCustomWords();
      rebuildWordCollections();
      if (customWordEntries.length >= 5) {
        unlockAchievements(["word_collector"]);
      }
      completeDailyMission("add_word");
      advanceWeeklyMission("weekly_add_2");
      advanceMonthlyMission("monthly_add_8");
      addWordInputEl.value = "";
      renderWordManager();
      setAddWordNote(`המילה "${newWord}" נוספה לנושא ${categoryLabels[category]}.`, "success");
    }

    function loadLeaderboard() {
      try {
        leaderboard = JSON.parse(localStorage.getItem("hangman_leaderboard") || "[]");
        if (!Array.isArray(leaderboard)) {
          leaderboard = [];
        }
      } catch {
        leaderboard = [];
      }
    }

    function saveLeaderboard() {
      try {
        localStorage.setItem("hangman_leaderboard", JSON.stringify(leaderboard));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadModeRecords() {
      try {
        modeRecords = JSON.parse(localStorage.getItem("hangman_mode_records") || "{}") || {};
      } catch {
        modeRecords = {};
      }
    }

    function saveModeRecords() {
      try {
        localStorage.setItem("hangman_mode_records", JSON.stringify(modeRecords));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadBonusStore() {
      try {
        const saved = JSON.parse(localStorage.getItem("hangman_bonus_store") || "{}");
        bonusCoins = Number(saved.coins) || 0;
        bonusInventory = {
          freeze: Number(saved.freeze) || 0,
          life: Number(saved.life) || 0,
          skip: Number(saved.skip) || 0,
          shield: Number(saved.shield) || 0
        };
        bonusOwners = {
          freeze: String(saved.freezeOwner || ""),
          life: String(saved.lifeOwner || ""),
          skip: String(saved.skipOwner || ""),
          shield: String(saved.shieldOwner || "")
        };
      } catch {
        bonusCoins = 0;
        bonusInventory = { freeze: 0, life: 0, skip: 0, shield: 0 };
        bonusOwners = { freeze: "", life: "", skip: "", shield: "" };
      }
    }

    function saveBonusStore() {
      try {
        localStorage.setItem("hangman_bonus_store", JSON.stringify({
          coins: bonusCoins,
          freeze: bonusInventory.freeze,
          life: bonusInventory.life,
          skip: bonusInventory.skip,
          shield: bonusInventory.shield,
          freezeOwner: bonusOwners.freeze,
          lifeOwner: bonusOwners.life,
          skipOwner: bonusOwners.skip,
          shieldOwner: bonusOwners.shield
        }));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function loadProfiles() {
      try {
        const saved = JSON.parse(localStorage.getItem("hangman_profiles") || "[]");
        playerProfiles = Array.isArray(saved) ? saved : [];
      } catch {
        playerProfiles = [];
      }
    }

    function saveProfiles() {
      try {
        localStorage.setItem("hangman_profiles", JSON.stringify(playerProfiles));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadPlayerStats() {
      try {
        playerStats = JSON.parse(localStorage.getItem("hangman_player_stats") || "{}") || {};
      } catch {
        playerStats = {};
      }
    }

    function savePlayerStats() {
      try {
        localStorage.setItem("hangman_player_stats", JSON.stringify(playerStats));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadPlayerHistory() {
      try {
        playerHistory = JSON.parse(localStorage.getItem("hangman_player_history") || "{}") || {};
      } catch {
        playerHistory = {};
      }
    }

    function savePlayerHistory() {
      try {
        localStorage.setItem("hangman_player_history", JSON.stringify(playerHistory));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadStoryProgress() {
      try {
        storyProgress = JSON.parse(localStorage.getItem("hangman_story_progress") || '{"chapter":0,"completed":0}') || { chapter: 0, completed: 0 };
      } catch {
        storyProgress = { chapter: 0, completed: 0 };
      }
    }

    function saveStoryProgress() {
      try {
        localStorage.setItem("hangman_story_progress", JSON.stringify(storyProgress));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadFavoriteWords() {
      try {
        favoriteWords = JSON.parse(localStorage.getItem("hangman_favorite_words") || "[]") || [];
      } catch {
        favoriteWords = [];
      }
    }

    function saveFavoriteWords() {
      try {
        localStorage.setItem("hangman_favorite_words", JSON.stringify(favoriteWords));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadMusicPreference() {
      try {
        musicEnabled = localStorage.getItem("hangman_music_enabled") === "1";
        musicStyle = localStorage.getItem("hangman_music_style") || "calm";
      } catch {
        musicEnabled = false;
        musicStyle = "calm";
      }
    }

    function saveMusicPreference() {
      try {
        localStorage.setItem("hangman_music_enabled", musicEnabled ? "1" : "0");
        localStorage.setItem("hangman_music_style", musicStyle);
      } catch {
        // Ignore storage failures.
      }
    }

    function getTodayKey() {
      return new Date().toISOString().slice(0, 10);
    }

    function getWeekKey() {
      const today = new Date();
      const day = today.getUTCDay() || 7;
      today.setUTCDate(today.getUTCDate() - day + 1);
      return today.toISOString().slice(0, 10);
    }

    function loadDailyChallengeState() {
      try {
        dailyChallengeState = JSON.parse(localStorage.getItem("hangman_daily_challenge") || "{}") || {};
      } catch {
        dailyChallengeState = {};
      }
    }

    function saveDailyChallengeState() {
      try {
        localStorage.setItem("hangman_daily_challenge", JSON.stringify(dailyChallengeState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadWeeklyChallengeState() {
      try {
        weeklyChallengeState = JSON.parse(localStorage.getItem("hangman_weekly_challenge") || "{}") || {};
      } catch {
        weeklyChallengeState = { weekKey: "", wordKey: "" };
      }
    }

    function saveWeeklyChallengeState() {
      try {
        localStorage.setItem("hangman_weekly_challenge", JSON.stringify(weeklyChallengeState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadLoginStreakState() {
      try {
        loginStreakState = JSON.parse(localStorage.getItem("hangman_login_streak") || "{}") || {};
      } catch {
        loginStreakState = { lastDate: "", streak: 0 };
      }
    }

    function saveLoginStreakState() {
      try {
        localStorage.setItem("hangman_login_streak", JSON.stringify(loginStreakState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadDailyMissionsState() {
      try {
        dailyMissionsState = JSON.parse(localStorage.getItem("hangman_daily_missions") || "{}") || {};
      } catch {
        dailyMissionsState = { dateKey: "", completed: {} };
      }
    }

    function saveDailyMissionsState() {
      try {
        localStorage.setItem("hangman_daily_missions", JSON.stringify(dailyMissionsState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadWeeklyMissionsState() {
      try {
        weeklyMissionsState = JSON.parse(localStorage.getItem("hangman_weekly_missions") || "{}") || {};
      } catch {
        weeklyMissionsState = { weekKey: "", completed: {} };
      }
    }

    function saveWeeklyMissionsState() {
      try {
        localStorage.setItem("hangman_weekly_missions", JSON.stringify(weeklyMissionsState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadDailyWheelState() {
      try {
        dailyWheelState = JSON.parse(localStorage.getItem("hangman_daily_wheel") || "{}") || {};
      } catch {
        dailyWheelState = { dateKey: "", prize: "", claimed: false };
      }
    }

    function saveDailyWheelState() {
      try {
        localStorage.setItem("hangman_daily_wheel", JSON.stringify(dailyWheelState));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadTopicPacks() {
      try {
        unlockedTopicPacks = JSON.parse(localStorage.getItem("hangman_topic_packs") || '["starter"]');
        if (!Array.isArray(unlockedTopicPacks) || !unlockedTopicPacks.length) {
          unlockedTopicPacks = ["starter"];
        }
      } catch {
        unlockedTopicPacks = ["starter"];
      }
    }

    function saveTopicPacks() {
      try {
        localStorage.setItem("hangman_topic_packs", JSON.stringify(unlockedTopicPacks));
      } catch {
        // Ignore storage failures.
      }
    }

    function loadRecentWords() {
      try {
        recentWordKeys = JSON.parse(localStorage.getItem("hangman_recent_words") || "[]");
        if (!Array.isArray(recentWordKeys)) {
          recentWordKeys = [];
        }
      } catch {
        recentWordKeys = [];
      }
    }

    function saveRecentWords() {
      try {
        localStorage.setItem("hangman_recent_words", JSON.stringify(recentWordKeys.slice(0, 60)));
      } catch {
        // Ignore storage failures.
      }
    }

    function rememberWord(entry) {
      if (!entry) {
        return;
      }
      const key = `${entry.category}:${sanitizeLookupValue(entry.word)}`;
      recentWordKeys = [key, ...recentWordKeys.filter((item) => item !== key)].slice(0, 60);
      saveRecentWords();
    }

    function loadNightMode() {
      try {
        nightModeEnabled = localStorage.getItem("hangman_night_mode") === "true";
      } catch {
        nightModeEnabled = false;
      }
      document.body.classList.toggle("night-mode", nightModeEnabled);
      if (nightModeButtonEl) {
        nightModeButtonEl.textContent = nightModeEnabled ? "בטל מצב לילה" : "מצב לילה";
      }
    }

    function saveNightMode() {
      try {
        localStorage.setItem("hangman_night_mode", String(nightModeEnabled));
      } catch {
        // Ignore storage failures.
      }
    }

    function getPackWordCount(packKey) {
      const pack = topicPacks[packKey];
      if (!pack) {
        return 0;
      }

      return Array.isArray(pack.entries) ? pack.entries.length : 0;
    }

    function getUnlockedCategories() {
      return Object.keys(categoryLabels);
    }

    function ensureUnlockedCategorySelection() {
      const unlockedCategories = getUnlockedCategories();

      if (!unlockedCategories.includes(currentSelection.category)) {
        currentSelection.category = "all";
      }

      currentSelection.selectedCategories = (currentSelection.selectedCategories || [])
        .filter((key) => unlockedCategories.includes(key));
    }

    function loadPersonalStyle() {
      try {
        selectedStyle = localStorage.getItem("hangman_personal_style") || "cheerful";
      } catch {
        selectedStyle = "cheerful";
      }
    }

    function savePersonalStyle() {
      try {
        localStorage.setItem("hangman_personal_style", selectedStyle);
      } catch {
        // Ignore storage failures.
      }
    }

    function loadBackgroundStyle() {
      try {
        selectedBackgroundStyle = localStorage.getItem("hangman_background_style") || "theme";
        if (selectedBackgroundStyle !== "theme" && !["sky", "sun", "mint", "rose", "lavender", "peach", "lime", "ice", "coral", "white", "black"].includes(selectedBackgroundStyle) && !cosmeticInventory.backgrounds.includes(selectedBackgroundStyle)) {
          selectedBackgroundStyle = "theme";
        }
        selectedBackgroundTab = selectedBackgroundStyle === "theme"
          ? "theme"
          : (["sky", "sun", "mint", "rose", "lavender", "peach", "lime", "ice", "coral", "white", "black"].includes(selectedBackgroundStyle) ? "solid" : "gradient");
      } catch {
        selectedBackgroundStyle = "theme";
        selectedBackgroundTab = "theme";
      }
    }

    function saveBackgroundStyle() {
      try {
        localStorage.setItem("hangman_background_style", selectedBackgroundStyle);
      } catch {
        // Ignore storage failures.
      }
    }

    function getAchievementProfileKey(profileName = "") {
      const cleaned = sanitizePlayerName(profileName || "");
      return cleaned || "regular";
    }

    function setActiveAchievementProfile(profileName = "") {
      activeAchievementProfileKey = getAchievementProfileKey(profileName);
      unlockedAchievements = new Set(achievementStore[activeAchievementProfileKey] || []);
      renderAchievementBadges();
    }

    function maybeAdvanceLoginStreak() {
      const today = getTodayKey();
      const yesterday = new Date(Date.now() - 86400000).toISOString().slice(0, 10);
      if (loginStreakState.lastDate === today) {
        return;
      }

      loginStreakState.streak = loginStreakState.lastDate === yesterday
        ? (Number(loginStreakState.streak) || 0) + 1
        : 1;
      loginStreakState.lastDate = today;
      awardCoins(loginStreakState.streak >= 7 ? 3 : 1);
      saveLoginStreakState();
    }

    function getDailyMissions() {
      return [
        { id: "win_one", label: "לנצח משחק אחד", reward: 2 },
        { id: "use_hint", label: "לפתוח רמז פעם אחת", reward: 1 },
        { id: "add_word", label: "להוסיף מילה אחת למאגר", reward: 1 }
      ];
    }

    function renderDailyMissions() {
      if (!dailyMissionsListEl) {
        return;
      }

      ensureDailyMissionsDate();
      const missions = getDailyMissions();
      dailyMissionsListEl.innerHTML = missions.map((mission) => {
        const isDone = Boolean(dailyMissionsState.completed[mission.id]);
        return `
          <div class="mission-item${isDone ? " done" : ""}">
            <strong>${isDone ? "✓ " : ""}${mission.label}</strong><br>
            ${isDone ? "המשימה הושלמה היום." : `פרס: ${mission.reward} מטבעות`}
          </div>
        `;
      }).join("");
    }

    function renderLoginStreak() {
      if (!loginStreakCardEl) {
        return;
      }

      loginStreakCardEl.innerHTML = `<strong>רצף כניסה</strong><br>${loginStreakState.streak || 0} ימים ברצף`;
    }

    function renderDailyWheel() {
      if (!dailyWheelButtonEl || !dailyWheelNoteEl || !dailyWheelDiscEl) {
        return;
      }

      const today = getTodayKey();
      const isToday = dailyWheelState.dateKey === today;
      const claimed = isToday && dailyWheelState.claimed;
      const claimedPrizeIndex = DAILY_WHEEL_PRIZES.findIndex((item) => item.label === dailyWheelState.prize);
      const targetAngle = claimedPrizeIndex >= 0 ? ((360 - claimedPrizeIndex * 60) % 360) : 0;
      const displayAngle = dailyWheelSpinning ? dailyWheelRotation : targetAngle;
      const prizeText = dailyWheelSpinning
        ? "מסובבים את הגלגל..."
        : claimed
          ? `היום כבר קיבלת: ${dailyWheelState.prize}`
          : "גלגל פרס יומי עם 6 מקטעים. עלות: 3 מטבעות. אפשר לסובב פעם אחת ביום.";

      dailyWheelNoteEl.textContent = prizeText;
      dailyWheelButtonEl.disabled = claimed || bonusCoins < 3 || dailyWheelSpinning;
      dailyWheelButtonEl.textContent = claimed ? "הגלגל היומי נוצל" : (dailyWheelSpinning ? "מסתובב..." : "סובב ב-3 מטבעות");
      dailyWheelDiscEl.dataset.centerLabel = claimed && !dailyWheelSpinning ? "🎁" : "🎡";
      dailyWheelDiscEl.style.transform = `rotate(${displayAngle}deg)`;

      if (dailyWheelPrizesEl) {
        dailyWheelPrizesEl.innerHTML = DAILY_WHEEL_PRIZES.map((prize, index) => `
          <li class="${claimed && !dailyWheelSpinning && claimedPrizeIndex === index ? "active" : ""}">
            ${prize.shortLabel}
          </li>
        `).join("");
      }
    }

    function spinDailyWheel() {
      const today = getTodayKey();
      if (dailyWheelSpinning) {
        return;
      }

      if (dailyWheelState.dateKey === today && dailyWheelState.claimed) {
        setAddWordNote("כבר סובבתם את הגלגל היום.", "error");
        renderDailyWheel();
        return;
      }

      if (bonusCoins < 3) {
        setAddWordNote("צריך 3 מטבעות כדי לסובב את הגלגל היומי.", "error");
        renderDailyWheel();
        return;
      }

      bonusCoins -= 3;
      saveBonusStore();
      renderShop();
      const prizeIndex = hashString(`${today}-${selectedStyle}-${playerProfiles.length}`) % DAILY_WHEEL_PRIZES.length;
      const prize = DAILY_WHEEL_PRIZES[prizeIndex];
      const currentAngle = ((dailyWheelRotation % 360) + 360) % 360;
      const targetAngle = (360 - prizeIndex * 60) % 360;
      const delta = (targetAngle - currentAngle + 360) % 360;

      dailyWheelSpinning = true;
      dailyWheelRotation += 2160 + delta;
      replayAnimation(dailyWheelDiscEl, "spinning");
      renderDailyWheel();

      window.setTimeout(() => {
        prize.apply();
        dailyWheelSpinning = false;
        dailyWheelState = { dateKey: today, prize: prize.label, claimed: true };
        saveDailyWheelState();
        setAddWordNote(`הגלגל היומי העניק לך ${prize.label}.`, "success");
        renderDailyWheel();
      }, 2250);
    }

    function ensureDailyMissionsDate() {
      const today = getTodayKey();
      if (dailyMissionsState.dateKey !== today) {
        dailyMissionsState = { dateKey: today, completed: {} };
        saveDailyMissionsState();
      }
    }

    function completeDailyMission(id) {
      ensureDailyMissionsDate();
      if (dailyMissionsState.completed[id]) {
        return;
      }
      const mission = getDailyMissions().find((item) => item.id === id);
      if (!mission) {
        return;
      }
      dailyMissionsState.completed[id] = true;
      saveDailyMissionsState();
      awardCoins(mission.reward);
      renderDailyMissions();
    }

    function getBonusBuyerName() {
      if (currentSelection.mode === "party") {
        ensurePartyPlayers();
        if (currentSelection.bonusBuyer?.startsWith("party:")) {
          const selectedIndex = Number(currentSelection.bonusBuyer.split(":")[1]);
          return partyPlayers[selectedIndex]?.name || "משתתף 1";
        }
        return partyPlayers[activeTurn]?.name || "משתתף 1";
      }

      if (currentSelection.mode === "competition" || currentSelection.mode === "online") {
        return currentSelection.bonusBuyer === "player2"
          ? (currentSelection.playerTwoName || "שחקן 2")
          : (currentSelection.playerOneName || "שחקן 1");
      }

      if (currentSelection.mode === "two") {
        return currentSelection.bonusBuyer === "player2"
          ? (currentSelection.playerTwoName || "שחקן 2")
          : (currentSelection.playerOneName || "שחקן 1");
      }

      return currentSelection.playerOneName || "השחקן";
    }

    function canCurrentPlayerUseBonus(type) {
      const owner = bonusOwners[type];
      if (!owner) {
        return true;
      }

      if (!["competition", "party", "two"].includes(currentSelection.mode)) {
        return true;
      }

      return owner === getBonusBuyerName();
    }

    function getModeLabel(mode) {
      return modeDetails[mode]?.label || "משחק";
    }

    function getProfileRank(stats, achievementCount = 0) {
      const score = (stats.wins || 0) * 3 + (stats.survivalBest || 0) + achievementCount * 2;
      if (score >= 40) {
        return { label: "מאסטר", score };
      }
      if (score >= 24) {
        return { label: "אלוף", score };
      }
      if (score >= 12) {
        return { label: "מקצוען", score };
      }
      return { label: "מתחיל", score };
    }

    function getProfileRankProgress(stats, achievementCount = 0) {
      const { score, label } = getProfileRank(stats, achievementCount);
      const levels = [
        { label: "מתחיל", min: 0, max: 12 },
        { label: "מקצוען", min: 12, max: 24 },
        { label: "אלוף", min: 24, max: 40 },
        { label: "מאסטר", min: 40, max: 40 }
      ];
      const currentLevel = levels.find((level) => level.label === label) || levels[0];

      if (label === "מאסטר") {
        return {
          label,
          score,
          nextLabel: "שיא אישי חדש",
          progressPercent: 100,
          progressText: `${score} נקודות דרגה`
        };
      }

      const range = Math.max(1, currentLevel.max - currentLevel.min);
      const progressPercent = Math.max(0, Math.min(100, Math.round(((score - currentLevel.min) / range) * 100)));
      const nextLevel = levels[levels.findIndex((level) => level.label === label) + 1] || levels[levels.length - 1];
      return {
        label,
        score,
        nextLabel: nextLevel.label,
        progressPercent,
        progressText: `עוד ${Math.max(0, nextLevel.min - score)} נק' ל-${nextLevel.label}`
      };
    }

    function getSelectedCategoryKeys() {
      const unlockedCategories = getUnlockedCategories();
      const selected = Array.isArray(currentSelection.selectedCategories)
        ? currentSelection.selectedCategories.filter((value) => unlockedCategories.includes(value))
        : [];

      if (selected.length) {
        return selected;
      }

      if (currentSelection.category && currentSelection.category !== "all" && unlockedCategories.includes(currentSelection.category)) {
        return [currentSelection.category];
      }

      return unlockedCategories;
    }

    function renderCategoryGrid() {
      if (!categoryGridEl) {
        return;
      }

      const unlockedCategories = getUnlockedCategories();
      const selectedCategories = Array.isArray(currentSelection.selectedCategories)
        ? currentSelection.selectedCategories.filter((value) => unlockedCategories.includes(value))
        : [];

      if (categoryMultiTitleEl) {
        categoryMultiTitleEl.textContent = selectedCategories.length
          ? `בחירת קטגוריות מרובות (${selectedCategories.length} נבחרו)`
          : "בחירת קטגוריות מרובות";
      }

      if (categoryMultiNoteEl) {
        categoryMultiNoteEl.textContent = selectedCategories.length
          ? "מעכשיו ייבחרו מילים רק מתוך הקטגוריות שסימנת כאן."
          : "לא חייבים לבחור רק נושא אחד. אפשר לסמן כמה קטגוריות ולשחק מתוכן יחד.";
      }

      if (clearCategoriesButtonEl) {
        clearCategoriesButtonEl.classList.toggle("hidden", !selectedCategories.length);
      }

      categoryGridEl.innerHTML = unlockedCategories.map((key) => {
        const label = categoryLabels[key];
        const checked = selectedCategories.length
          ? selectedCategories.includes(key)
          : currentSelection.category === key;
        return `
          <label class="category-chip">
            <input type="checkbox" data-category-chip="${key}" ${checked ? "checked" : ""}>
            <span>${categoryIcons[key] || "•"} ${label}</span>
          </label>
        `;
      }).join("");
    }

    function renderSpecialModePills() {
      if (!specialModePillsEl) {
        return;
      }

      [...specialModePillsEl.querySelectorAll(".special-mode-pill")].forEach((button) => {
        button.classList.toggle("active", button.dataset.specialMode === currentSelection.specialMode);
      });
    }

    function renderModeRecords() {
      if (!modeRecordsEl) {
        return;
      }

      const modes = ["single", "competition", "online", "tournament", "party", "survival", "practice"];
      modeRecordsEl.innerHTML = modes.map((mode) => {
        const record = modeRecords[mode];
        if (!record) {
          return `<div class="mode-record"><strong>${getModeLabel(mode)}</strong><br>עדיין אין שיא.</div>`;
        }

        return `<div class="mode-record"><strong>${getModeLabel(mode)}</strong><br>${record.label}</div>`;
      }).join("");
    }

    function renderShop() {
      if (shopCoinsEl) {
        const freezeOwnerText = bonusOwners.freeze ? ` • הקפאה של: ${bonusOwners.freeze}` : "";
        const lifeOwnerText = bonusOwners.life ? ` • חיים של: ${bonusOwners.life}` : "";
        shopCoinsEl.textContent = `מטבעות: ${bonusCoins} • הקפאות: ${bonusInventory.freeze}${freezeOwnerText} • חיים: ${bonusInventory.life}${lifeOwnerText} • דילוג: ${bonusInventory.skip} • מגן: ${bonusInventory.shield}`;
      }

      if (buyFreezeButtonEl) {
        buyFreezeButtonEl.disabled = bonusCoins < 3;
      }

      if (buyLifeButtonEl) {
        buyLifeButtonEl.disabled = bonusCoins < 4;
      }

      if (buySkipButtonEl) {
        buySkipButtonEl.disabled = bonusCoins < 5;
      }

      if (buyShieldButtonEl) {
        buyShieldButtonEl.disabled = bonusCoins < 4;
      }

      renderTopicPacks();
    }

    function renderTopicPacks() {
      if (!packListEl) {
        return;
      }

      packListEl.innerHTML = Object.entries(topicPacks).map(([key, pack]) => {
        const isUnlocked = unlockedTopicPacks.includes(key);
        const wordCount = getPackWordCount(key);
        const categoryText = [...new Set((pack.entries || []).map((entry) => entry.category))]
          .map((category) => `${categoryIcons[category] || "•"} ${categoryLabels[category]}`)
          .join(" • ");

        return `
          <div class="pack-item${isUnlocked ? " active" : ""}">
            <strong>${pack.label}</strong><br>
            <span>${categoryText}</span><br>
            <span>${wordCount} מילים • ${pack.price} מטבעות</span>
            <div class="helper-actions">
              <button class="tiny-button" data-pack-unlock="${key}" type="button" ${isUnlocked || bonusCoins < pack.price ? "disabled" : ""}>
                ${isUnlocked ? "נפתחה" : (pack.price === 0 ? "פתוחה" : `פתח ב-${pack.price}`)}
              </button>
            </div>
          </div>
        `;
      }).join("");
    }

    function unlockTopicPack(packKey) {
      const pack = topicPacks[packKey];
      if (!pack || unlockedTopicPacks.includes(packKey)) {
        return;
      }

      if (bonusCoins < pack.price) {
        setAddWordNote("אין מספיק מטבעות כדי לפתוח את החבילה הזו.", "error");
        return;
      }

      bonusCoins -= pack.price;
      unlockedTopicPacks.push(packKey);
      saveBonusStore();
      saveTopicPacks();
      rebuildWordCollections();
      renderCategoryGrid();
      renderTopicPacks();
      renderShop();
      renderAvatars();
      renderTrophyRoom();
      renderWordManager();
      setAddWordNote(`החבילה "${pack.label}" נפתחה עם ${getPackWordCount(packKey)} מילים.`, "success");
    }

    function renderProfiles() {
      if (!profileListEl || !profileDetailEl) {
        return;
      }

      if (!playerProfiles.length) {
        selectedProfileTabIndex = -1;
      } else if (selectedProfileTabIndex >= playerProfiles.length) {
        selectedProfileTabIndex = playerProfiles.length - 1;
      }

      const activeProfile = selectedProfileTabIndex >= 0 ? playerProfiles[selectedProfileTabIndex] : null;
      const activeStats = activeProfile
        ? (playerStats[activeProfile.name] || { wins: 0, losses: 0, survivalBest: 0 })
        : { wins: 0, losses: 0, survivalBest: 0 };
      const activeStyle = activeProfile
        ? (personalStyles[activeProfile.style || "cheerful"]?.label || "שמח")
        : (personalStyles[selectedStyle]?.label || "שמח");
      const activeProfileAchievements = activeProfile
        ? (achievementStore[getAchievementProfileKey(activeProfile.name)] || [])
        : [...unlockedAchievements];
      const activeRank = getProfileRank(activeStats, activeProfileAchievements.length);
      const rankProgress = getProfileRankProgress(activeStats, activeProfileAchievements.length);
      const totalGames = (activeStats.wins || 0) + (activeStats.losses || 0);
      const winRate = totalGames ? Math.round(((activeStats.wins || 0) / totalGames) * 100) : 0;
      const personality = avatarPersonalities[activeProfile?.avatar || "🙂"] || avatarPersonalities["🙂"];
      const xpInfo = activeProfile ? getXpInfo(activeProfile.name) : getXpInfo(getCurrentProfileName());
      const modeLabels = {
        single: "שחקן יחיד",
        online: "אונליין",
        tournament: "טורניר",
        two: "שני שחקנים",
        competition: "תחרות",
        party: "מסיבה",
        survival: "הישרדות",
        practice: "אימון"
      };

      profileListEl.innerHTML = playerProfiles.length
        ? playerProfiles.slice(0, 10).map((profile, index) => {
          const xpInfo = getXpInfo(profile.name);
          return `
          <button class="profile-tab${selectedProfileTabIndex === index ? " active" : ""}" data-select-profile="${index}" type="button">
            <span class="profile-tab-avatar">${profile.avatar || "🙂"}</span>
            <span class="profile-tab-text">
              <span class="profile-tab-name">${profile.name}</span>
              <span class="profile-tab-meta">רמה ${xpInfo.level} • ${personalStyles[profile.style || "cheerful"]?.label || "שמח"} • ${modeLabels[profile.mode] || "משחק"}</span>
            </span>
          </button>
        `;
        }).join("")
        : '<div class="profile-detail-empty">עדיין אין פרופילים שמורים. שמרו פרופיל חדש כדי שהוא יופיע כאן.</div>';

      if (!profileDetailVisible) {
        profileDetailEl.innerHTML = '<div class="profile-detail-empty">כאן יופיע המידע של הפרופיל שתבחרו. אפשר כבר עכשיו לשמור פרופיל חדש או ללחוץ על פרופיל שמור מהרשימה.</div>';
        return;
      }

      if (!activeProfile) {
        profileDetailEl.innerHTML = '<div class="profile-detail-empty">בחרו פרופיל שמור מהרשימה כדי לראות את המידע שלו למטה.</div>';
        return;
      }

      profileDetailEl.innerHTML = `
        <div class="profile-hero">
          <div class="profile-hero-avatar">${activeProfile.avatar || "🙂"}</div>
          <div>
            <p class="profile-hero-title">${activeProfile.name}</p>
            <p class="profile-hero-subtitle">סגנון אישי: ${activeStyle} • מצב מועדף: ${modeLabels[activeProfile.mode] || "שחקן יחיד"}</p>
            <p class="profile-hero-subtitle">דרגה: ${activeRank.label} • הישגים פתוחים: ${activeProfileAchievements.length}</p>
            <p class="profile-hero-subtitle">אופי: ${personality.title} • ${personality.line}</p>
          </div>
        </div>
        <div class="xp-summary">
          <span class="xp-badge">⚡ רמה ${xpInfo.level} • ${xpInfo.xp} XP</span>
          <div class="profile-progress"><div class="profile-progress-fill" style="width:${xpInfo.progressPercent}%;"></div></div>
        </div>
        <div class="profile-rank-row">
          <div class="profile-rank-head">
            <span class="profile-rank-badge">⭐ ${rankProgress.label}</span>
            <span>${rankProgress.progressText}</span>
          </div>
          <div class="profile-progress">
            <div class="profile-progress-fill" style="width: ${rankProgress.progressPercent}%;"></div>
          </div>
        </div>
        <div class="profile-stat-grid">
          <div class="profile-stat-card">
            <strong>ניצחונות</strong>
            <div class="profile-stat-value">${activeStats.wins || 0}</div>
          </div>
          <div class="profile-stat-card">
            <strong>הפסדים</strong>
            <div class="profile-stat-value">${activeStats.losses || 0}</div>
          </div>
          <div class="profile-stat-card">
            <strong>שיא הישרדות</strong>
            <div class="profile-stat-value">${activeStats.survivalBest || 0}</div>
          </div>
          <div class="profile-stat-card">
            <strong>סה"כ משחקים</strong>
            <div class="profile-stat-value">${totalGames}</div>
          </div>
        </div>
        <div class="profile-stat-grid">
          <div class="profile-stat-card">
            <strong>אחוז הצלחה</strong>
            <div class="profile-stat-value">${winRate}%</div>
          </div>
          <div class="profile-stat-card">
            <strong>נקודות דרגה</strong>
            <div class="profile-stat-value">${activeRank.score}</div>
          </div>
        </div>
        <div class="profile-meta-card">
          <strong>הישגים של הפרופיל</strong>
          ${activeProfileAchievements.length
            ? `<div class="profile-achievement-list">${activeProfileAchievements.map((id) => {
              const item = achievementDefinitions.find((entry) => entry.id === id);
              return item ? `<span class="profile-achievement-chip">${item.icon} ${item.label}</span>` : "";
            }).filter(Boolean).join("")}</div>`
            : "עדיין אין הישגים פתוחים לפרופיל הזה."}
        </div>
        <div class="profile-meta-grid">
          <div class="profile-meta-card">
            <strong>אוואטרים שמורים</strong>
            יחיד: ${activeProfile.singleAvatar || activeProfile.avatar || "🙂"} • שחקן 1: ${activeProfile.playerOneAvatar || activeProfile.avatar || "😎"} • שחקן 2: ${activeProfile.playerTwoAvatar || "🦊"}
          </div>
          <div class="profile-meta-card">
            <strong>שמות שמורים</strong>
            שחקן 1: ${activeProfile.playerOneName || activeProfile.name || "לא הוגדר"} • שחקן 2: ${activeProfile.playerTwoName || "לא הוגדר"}
          </div>
          <div class="profile-meta-card">
            <strong>סטטוס פרופיל</strong>
            הפרופיל הזה שומר סגנון, שמות, הישגים, דרגה והתקדמות אישית משל עצמו.
          </div>
        </div>
        <div class="profile-actions">
          <button class="tiny-button" data-load-profile="${selectedProfileTabIndex}" type="button">טען פרופיל</button>
          <button class="tiny-button" data-edit-profile="${selectedProfileTabIndex}" type="button">ערוך פרופיל</button>
          <button class="tiny-button" data-open-profile="${selectedProfileTabIndex}" type="button">פרופיל מלא</button>
          <button class="tiny-button" data-delete-profile="${selectedProfileTabIndex}" type="button">מחק פרופיל</button>
        </div>
      `;
    }

    function renderSideProfileDock() {}

    function applyPersonalStyle() {
      const style = personalStyles[selectedStyle] || personalStyles.cheerful;
      document.documentElement.style.setProperty("--accent", style.accent);
      document.documentElement.style.setProperty("--accent-2", style.accent2);
      document.documentElement.style.setProperty("--accent-3", style.accent3);
    }

    function renderStylePicker() {
      if (!stylePickerEl) {
        return;
      }

      stylePickerEl.innerHTML = Object.entries(personalStyles).map(([key, style]) => `
        <button class="style-swatch${selectedStyle === key ? " active" : ""}" data-style-key="${key}" type="button">${style.label}</button>
      `).join("");
    }

    function openProfilesPanel() {
      selectedProfileTabIndex = Math.max(0, Math.min(selectedProfileTabIndex, playerProfiles.length - 1));
      renderProfiles();
    }

    function closeProfilesPanel() {
      renderProfiles();
    }

    function syncProfileEditor() {
      if (profileNameInputEl && !profileNameInputEl.value) {
        profileNameInputEl.value = currentSelection.playerOneName || "";
      }
      renderAvatarPickers();
    }

    function renderPlayerStats() {
      if (!playerStatsListEl) {
        return;
      }

      const entries = Object.entries(playerStats)
        .sort(([, a], [, b]) => (b.wins || 0) - (a.wins || 0))
        .slice(0, 5);

      if (!entries.length) {
        playerStatsListEl.innerHTML = `
          <div class="player-stat-item">
            <strong>עדיין אין סטטיסטיקות אישיות.</strong><br>
            אחרי כמה משחקים כאן יופיעו ניצחונות, הפסדים ושיאי הישרדות של כל פרופיל.
          </div>
        `;
        renderProfileLeaderboard();
        renderTrophyRoom();
        return;
      }

      playerStatsListEl.innerHTML = `
        <div class="player-stat-item">
          <strong>מבט מהיר על השחקנים</strong><br>
          כאן רואים את הפרופילים הפעילים ואת הנתונים האישיים הכי חשובים שלהם.
        </div>
      ` + entries.map(([name, stats]) => `
        <div class="player-stat-item">
          <strong>${name}</strong><br>
          ניצחונות: ${stats.wins || 0} • הפסדים: ${stats.losses || 0} • שיא הישרדות: ${stats.survivalBest || 0}
        </div>
      `).join("");

      renderProfileLeaderboard();
      renderTrophyRoom();
    }

    function renderProfileSummary() {
      if (!profileSummaryEl) {
        return;
      }

      const profileName = profileNameInputEl?.value?.trim() || currentSelection.playerOneName || "שחקן יחיד";
      const avatar = currentSelection.singleAvatar || "🙂";
      const stats = playerStats[profileName] || { wins: 0, losses: 0, survivalBest: 0 };
      const styleLabel = personalStyles[selectedStyle]?.label || "שמח";
      const profileAchievements = achievementStore[getAchievementProfileKey(profileName)] || [...unlockedAchievements];
      const rank = getProfileRank(stats, profileAchievements.length);
      const xpInfo = getXpInfo(profileName);
      const totalGames = (stats.wins || 0) + (stats.losses || 0);
      const winRate = totalGames ? Math.round(((stats.wins || 0) / totalGames) * 100) : 0;
      profileSummaryEl.innerHTML = `
        <strong>${avatar} פרופיל נוכחי: ${profileName}</strong><br>
        סגנון אישי: ${styleLabel}<br>
        דרגה: ${rank.label} • הישגים: ${profileAchievements.length}<br>
        XP: ${xpInfo.xp} • רמה ${xpInfo.level}<br>
        ניצחונות: ${stats.wins || 0} • הפסדים: ${stats.losses || 0} • הצלחה: ${winRate}%<br>
        שיא הישרדות: ${stats.survivalBest || 0}<br>
        <div class="profile-tag-list">
          <span class="profile-tag">🏅 ${rank.label}</span>
          <span class="profile-tag">📈 ${winRate}% הצלחה</span>
          <span class="profile-tag">⭐ ${profileAchievements.length} הישגים</span>
        </div>
      `;
    }

    function generateLocalRoomCode() {
      const seed = `${Date.now()}-${currentSelection.playerOneName}-${currentSelection.playerTwoName}-${Math.random()}`;
      return hashString(seed).toString(36).toUpperCase().slice(0, 6).padEnd(6, "X");
    }

    function renderOnlineRoomCard() {
      if (!onlineRoomCardEl) {
        return;
      }
      if (currentSelection.mode !== "online" && !onlineSession.roomId) {
        onlineRoomCardEl.innerHTML = `
          <p class="online-room-title">אונליין אמיתי</p>
          <p class="online-room-text">בוחרים את מצב האונליין, יוצרים חדר אמיתי על השרת, ושולחים קישור לשחקן השני.</p>
          <div class="online-room-code">------</div>
          <p class="online-room-text">כשתעברו למצב הזה, כאן יופיע החדר החי שלכם עם קישור אמיתי.</p>
        `;
        return;
      }
      if (currentSelection.mode === "online" && !onlineSession.created) {
        onlineRoomCardEl.innerHTML = `
          <p class="online-room-title">מוכנים לחדר אונליין?</p>
          <p class="online-room-text">כתבו שני שמות, בחרו את ההגדרות שאתם רוצים, ולחצו על התחלה כדי ליצור חדר אמיתי.</p>
          <div class="online-room-code">------</div>
          <p class="online-room-text">אחרי יצירת החדר תקבלו קישור שאפשר לשלוח ישר לחבר.</p>
        `;
        return;
      }
      onlineRoomCardEl.innerHTML = `
        <p class="online-room-title">${onlineSession.role === "guest" ? "חדר אונליין שהצטרפת אליו" : "חדר אונליין פעיל"}</p>
        <p class="online-room-text">${onlineSession.hostName || currentSelection.playerOneName || "שחקן 1"} מול ${onlineSession.guestName || currentSelection.playerTwoName || "שחקן 2"} • חדר אמיתי שנשמר על השרת.</p>
        <div class="online-room-code">${onlineSession.roomId || localOnlineRoomCode || "------"}</div>
        ${onlineSession.joinUrl && onlineSession.role === "host" ? `
          <p class="online-room-text">זה הקישור לחדר שלך:</p>
          <div class="secret-link-output">${onlineSession.joinUrl}</div>
          <div class="helper-actions"><button class="ghost-button" data-online-copy-link type="button">העתק קישור</button></div>
        ` : ""}
        <p class="online-room-text">${onlineSession.guestJoined ? "השחקן השני כבר נכנס לחדר ואפשר להתחיל לשחק." : "כשהשחקן השני ייכנס לחדר, המצב כאן יתעדכן אוטומטית."}</p>
      `;
    }

    function renderStoryCard() {
      if (!storyCardEl) {
        return;
      }
      const chapterIndex = Math.min(storyChapters.length - 1, storyProgress.chapter || 0);
      const chapter = storyChapters[chapterIndex];
      const progressPercent = Math.round((((storyProgress.completed || 0) % storyChapters.length) / storyChapters.length) * 100);
      storyCardEl.innerHTML = `
        <p class="story-card-title">פרק ${chapterIndex + 1}: ${chapter.title}</p>
        <p class="story-card-text">${chapter.text} • קושי נוכחי: ${difficultyLabels[chapter.difficulty] || "בינוני"}</p>
        <div class="story-progress"><div class="story-progress-fill" style="width:${Math.max(18, progressPercent || 18)}%;"></div></div>
        <p class="story-card-text">בכל פרק מחכות קטגוריות חדשות, תחנות נוספות ודרך ארוכה יותר להשלים. ממש כאן מתחילה המפה המצוירת של המסע.</p>
      `;
    }

    function pushPlayerHistory(name, text) {
      if (!name) {
        return;
      }
      if (!Array.isArray(playerHistory[name])) {
        playerHistory[name] = [];
      }
      playerHistory[name].unshift(text);
      playerHistory[name] = playerHistory[name].slice(0, 8);
      savePlayerHistory();
    }

    function resetGameHistoryOnly() {
      playerHistory = {};
      savePlayerHistory();
      renderProfiles();
      if (profileModalEl?.classList.contains("visible") && selectedProfileTabIndex >= 0) {
        openFullProfile(selectedProfileTabIndex);
      }
      setAddWordNote("היסטוריית המשחקים נמחקה. שאר הנתונים נשארו כמו שהם.", "success");
    }

    function openFullProfile(index) {
      const profile = playerProfiles[index];
      if (!profile || !profileModalEl || !profileModalContentEl || !profileModalTitleEl) {
        return;
      }
      const stats = playerStats[profile.name] || { wins: 0, losses: 0, survivalBest: 0 };
      const achievements = achievementStore[getAchievementProfileKey(profile.name)] || [];
      const rank = getProfileRankProgress(stats, achievements.length);
      const xpInfo = getXpInfo(profile.name);
      const personality = avatarPersonalities[profile.avatar || "🙂"] || avatarPersonalities["🙂"];
      const totalGames = Number(stats.wins || 0) + Number(stats.losses || 0);
      const winRate = totalGames ? Math.round(((stats.wins || 0) / totalGames) * 100) : 0;
      const graphItems = [
        { label: "ניצחונות", value: Math.min(100, (stats.wins || 0) * 10) },
        { label: "אחוז הצלחה", value: winRate },
        { label: "הישגים", value: Math.min(100, achievements.length * 8) },
        { label: "הישרדות", value: Math.min(100, (stats.survivalBest || 0) * 12) }
      ];
      profileModalTitleEl.textContent = `${profile.avatar || "🙂"} ${profile.name}`;
      profileModalContentEl.innerHTML = `
        <div class="profile-modal-grid">
          <section class="profile-modal-section">
            <strong>דרגה נוכחית</strong>
            <div class="profile-rank-row">
              <div class="profile-rank-head">
                <span class="profile-rank-badge">⭐ ${rank.label}</span>
                <span>${rank.progressText}</span>
              </div>
              <div class="profile-progress"><div class="profile-progress-fill" style="width:${rank.progressPercent}%;"></div></div>
            </div>
            <div class="xp-summary">
              <span class="xp-badge">⚡ ${xpInfo.xp} XP • רמה ${xpInfo.level}</span>
              <div class="profile-progress"><div class="profile-progress-fill" style="width:${xpInfo.progressPercent}%;"></div></div>
            </div>
          </section>
          <section class="profile-modal-section">
            <strong>אופי הדמות</strong>
            <div class="profile-history-list">
              <div class="profile-history-item">${personality.title}</div>
              <div class="profile-history-item">${personality.line}</div>
              <div class="profile-history-item">${getAvatarFlavorLineForContext("win", profile.avatar || "🙂")}</div>
            </div>
          </section>
          <section class="profile-modal-section">
            <strong>סטטיסטיקות</strong>
            <div class="profile-history-list">
              <div class="profile-history-item">ניצחונות: ${stats.wins || 0}</div>
              <div class="profile-history-item">הפסדים: ${stats.losses || 0}</div>
              <div class="profile-history-item">שיא הישרדות: ${stats.survivalBest || 0}</div>
              <div class="profile-history-item">מצב מועדף: ${getModeLabel(profile.mode || "single")}</div>
            </div>
            <div class="profile-graph-list">
              ${graphItems.map((item) => `
                <div class="profile-graph-row">
                  <span>${item.label}</span>
                  <div class="profile-graph-track"><div class="profile-graph-fill" style="width:${Math.max(8, item.value)}%;"></div></div>
                </div>
              `).join("")}
            </div>
          </section>
          <section class="profile-modal-section">
            <strong>הישגים פתוחים</strong>
            <div class="profile-achievement-list">
              ${achievements.length
                ? achievements.map((id) => {
                  const item = achievementDefinitions.find((entry) => entry.id === id);
                  return item ? `<span class="profile-achievement-chip">${item.icon} ${item.label}</span>` : "";
                }).join("")
                : '<span class="profile-achievement-chip">עדיין אין הישגים</span>'}
            </div>
            <div class="profile-tag-list">
              <span class="profile-tag">⭐ דרגה: ${rank.label}</span>
              <span class="profile-tag">🎯 מצב מועדף: ${getModeLabel(profile.mode || "single")}</span>
              <span class="profile-tag">🪄 אופי: ${personality.title}</span>
            </div>
          </section>
          <section class="profile-modal-section">
            <strong>בחירות שמורות</strong>
            <div class="profile-history-list">
              <div class="profile-history-item">סגנון אישי: ${personalStyles[profile.style || "cheerful"]?.label || "שמח"}</div>
              <div class="profile-history-item">אוואטר ראשי: ${profile.singleAvatar || profile.avatar || "🙂"}</div>
              <div class="profile-history-item">שחקנים שמורים: ${(profile.playerOneName || profile.name || "שחקן 1")} מול ${profile.playerTwoName || "שחקן 2"}</div>
            </div>
          </section>
          <section class="profile-modal-section full">
            <strong>סיכום פרופיל</strong>
            <div class="profile-achievement-list">
              <span class="profile-achievement-chip">⚡ ${xpInfo.xp} XP</span>
              <span class="profile-achievement-chip">🏅 ${achievements.length} הישגים</span>
              <span class="profile-achievement-chip">🎯 ${winRate}% הצלחה</span>
              <span class="profile-achievement-chip">🛡️ שיא הישרדות ${stats.survivalBest || 0}</span>
            </div>
          </section>
        </div>
      `;
      profileModalEl.classList.add("visible");
    }

    function closeFullProfile() {
      profileModalEl?.classList.remove("visible");
    }

    function renderAmbientLayer() {
      if (!ambientLayerEl) {
        return;
      }
      const paletteByMode = {
        single: ["rgba(255,122,89,0.32)", "rgba(25,183,198,0.28)", "rgba(255,209,102,0.26)", "rgba(125,140,255,0.24)"],
        online: ["rgba(101,208,255,0.34)", "rgba(125,140,255,0.32)", "rgba(255,209,102,0.22)", "rgba(25,183,198,0.28)"],
        tournament: ["rgba(255,209,102,0.32)", "rgba(255,122,89,0.3)", "rgba(25,183,198,0.24)", "rgba(125,140,255,0.22)"],
        party: ["rgba(255,122,89,0.34)", "rgba(255,143,171,0.3)", "rgba(255,209,102,0.28)", "rgba(101,208,255,0.26)"],
        survival: ["rgba(25,183,198,0.3)", "rgba(24,154,98,0.26)", "rgba(255,209,102,0.2)", "rgba(125,140,255,0.22)"]
      };
      const topicParticles = {
        food: "🍔", fruits: "🍎", vegetables: "🥕", drinks: "🥤",
        nature: "🍃", weather: "☁️", sea: "🌊", animals: "🐾",
        sports: "🏅", technology: "💻", space: "🪐", home: "🏠"
      };
      const colors = paletteByMode[currentSelection.mode] || paletteByMode.single;
      const particleCount = currentSelection.specialMode === "story" ? 18 : (currentSelection.mode === "party" ? 20 : 16);
      const particles = Array.from({ length: particleCount }, (_, index) => {
        const left = 4 + (index * 7) % 92;
        const size = 18 + (index % 5) * 10;
        const delay = -(index * 1.1);
        const duration = 10 + (index % 4) * 3;
        const topicIcon = topicParticles[activeTopicKey] || topicParticles[currentSelection.category] || "✨";
        return `<span class="ambient-particle" style="left:${left}%; top:${12 + (index * 6) % 80}%; width:${size}px; height:${size}px; background:${colors[index % colors.length]}; animation-delay:${delay}s; animation-duration:${duration}s;">${index % 3 === 0 ? topicIcon : ""}</span>`;
      }).join("");
      ambientLayerEl.innerHTML = particles;
    }

    function renderStartDashboard() {
      if (!startDashboardEl) {
        return;
      }

      const totalWords = words.length;
      const unlockedCount = unlockedAchievements.size;
      const currentXp = getXpInfo(getCurrentProfileName());
      const greeting = ["בוקר טוב", "צהריים טובים", "ערב טוב"][Math.min(2, Math.floor(new Date().getHours() / 8))] || "ברוכים הבאים";
      startDashboardEl.innerHTML = `
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${greeting}</span>
          <span class="start-dashboard-label">ברכה יומית למסך הבית</span>
        </div>
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${totalWords}</span>
          <span class="start-dashboard-label">מילים במאגר</span>
        </div>
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${playerProfiles.length}</span>
          <span class="start-dashboard-label">פרופילים שמורים</span>
        </div>
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${bonusCoins}</span>
          <span class="start-dashboard-label">מטבעות זמינים</span>
        </div>
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${unlockedCount}</span>
          <span class="start-dashboard-label">הישגים פתוחים</span>
        </div>
        <div class="start-dashboard-card">
          <span class="start-dashboard-value">${currentXp.level}</span>
          <span class="start-dashboard-label">רמת XP</span>
        </div>
      `;
      refreshHelperPanels();
    }

    function getDailyChallengeEntry() {
      const dailyPool = words.filter((entry) => wordLength(entry.word) >= 5);
      if (!dailyPool.length) {
        return null;
      }

      const index = hashString(getTodayKey()) % dailyPool.length;
      return dailyPool[index];
    }

    function renderDailyChallenge() {
      if (!dailyCardEl) {
        return;
      }

      const entry = getDailyChallengeEntry();
      if (!entry) {
        dailyCardEl.innerHTML = "אין כרגע אתגר יומי זמין.";
        return;
      }

      const solvedToday = dailyChallengeState.dateKey === getTodayKey() && dailyChallengeState.wordKey === sanitizeLookupValue(entry.word);
      dailyCardEl.innerHTML = `
        <strong>אתגר יומי</strong><br>
        ${categoryIcons[entry.category] || "🎯"} ${entry.topic} • ${wordLength(entry.word)} אותיות<br>
        ${solvedToday ? "האתגר של היום כבר הושלם." : "מוכן למשחק היומי של היום."}<br>
        <span style="font-size:0.8rem; color: rgba(20, 48, 74, 0.72);">האתגר מתחלף בכל יום.</span>
      `;
    }

    function getWeeklyChallengeEntry() {
      const weeklyPool = words.filter((entry) => wordLength(entry.word) >= 7);
      if (!weeklyPool.length) {
        return null;
      }
      const index = hashString(getWeekKey()) % weeklyPool.length;
      return weeklyPool[index];
    }

    function renderWeeklyChallenge() {
      if (!weeklyCardEl) {
        return;
      }

      const entry = getWeeklyChallengeEntry();
      if (!entry) {
        weeklyCardEl.innerHTML = "אין כרגע אתגר שבועי זמין.";
        return;
      }

      const solvedThisWeek = weeklyChallengeState.weekKey === getWeekKey() && weeklyChallengeState.wordKey === sanitizeLookupValue(entry.word);
      weeklyCardEl.innerHTML = `
        <strong>אתגר שבועי</strong><br>
        ${categoryIcons[entry.category] || "🗓️"} ${entry.topic} • ${wordLength(entry.word)} אותיות<br>
        ${solvedThisWeek ? "האתגר השבועי כבר הושלם." : "מוכן לאתגר הארוך של השבוע."}<br>
        <span style="font-size:0.8rem; color: rgba(20, 48, 74, 0.72);">האתגר מתחלף פעם בשבוע.</span>
      `;
    }

    function getWeeklyMissionDefinitions() {
      return [
        { id: "weekly_win_3", label: "לנצח 3 משחקים השבוע", target: 3, progress: () => Number(weeklyMissionsState.completed.weekly_win_3 || 0), reward: "3 מטבעות" },
        { id: "weekly_story_2", label: "להשלים 2 פרקי סיפור", target: 2, progress: () => Number(weeklyMissionsState.completed.weekly_story_2 || 0), reward: "פתיחת מסע" },
        { id: "weekly_add_2", label: "להוסיף 2 מילים למאגר", target: 2, progress: () => Number(weeklyMissionsState.completed.weekly_add_2 || 0), reward: "2 מטבעות" },
        { id: "weekly_survival_2", label: "לשרוד 2 מילים בהישרדות", target: 2, progress: () => Number(weeklyMissionsState.completed.weekly_survival_2 || 0), reward: "מדליית הישרדות" }
      ];
    }

    function getMonthlyMissionDefinitions() {
      return [
        { id: "monthly_win_10", label: "לנצח 10 משחקים החודש", target: 10, progress: () => Number(monthlyMissionsState.completed.monthly_win_10 || 0), reward: "8 מטבעות" },
        { id: "monthly_story_5", label: "להשלים 5 פרקי סיפור", target: 5, progress: () => Number(monthlyMissionsState.completed.monthly_story_5 || 0), reward: "רקע מיוחד" },
        { id: "monthly_add_8", label: "להוסיף 8 מילים למאגר", target: 8, progress: () => Number(monthlyMissionsState.completed.monthly_add_8 || 0), reward: "5 מטבעות" },
        { id: "monthly_survival_8", label: "לשרוד 8 מילים במצטבר", target: 8, progress: () => Number(monthlyMissionsState.completed.monthly_survival_8 || 0), reward: "תיבת זהב" },
        { id: "monthly_topics_6", label: "לנצח ב-6 נושאים שונים", target: 6, progress: () => Number(monthlyMissionsState.completed.monthly_topics_6 || 0), reward: "מסגרת חדשה" },
        { id: "monthly_tournament_3", label: "להשלים 3 טורנירים", target: 3, progress: () => Number(monthlyMissionsState.completed.monthly_tournament_3 || 0), reward: "10 מטבעות" }
      ];
    }

    function ensureMonthlyMissionState() {
      const monthKey = new Date().toISOString().slice(0, 7);
      if (monthlyMissionsState.monthKey !== monthKey) {
        monthlyMissionsState = { monthKey, completed: {} };
        saveMonthlyMissionsState();
      }
    }

    function advanceMonthlyMission(id, amount = 1) {
      ensureMonthlyMissionState();
      monthlyMissionsState.completed[id] = Number(monthlyMissionsState.completed[id] || 0) + amount;
      saveMonthlyMissionsState();
      renderMonthlyMissions();
    }

    function ensureWeeklyMissionState() {
      const weekKey = getWeekKey();
      if (weeklyMissionsState.weekKey !== weekKey) {
        weeklyMissionsState = { weekKey, completed: {} };
        saveWeeklyMissionsState();
      }
    }

    function advanceWeeklyMission(id, amount = 1) {
      ensureWeeklyMissionState();
      weeklyMissionsState.completed[id] = Number(weeklyMissionsState.completed[id] || 0) + amount;
      saveWeeklyMissionsState();
      renderWeeklyMissions();
      renderTrophyRoom();
    }

    function renderWeeklyMissions() {
      if (!weeklyMissionsListEl) {
        return;
      }
      ensureWeeklyMissionState();
      const missions = getWeeklyMissionDefinitions();
      const doneCount = missions.filter((mission) => mission.progress() >= mission.target).length;
      weeklyMissionsListEl.innerHTML = `
        <div class="mission-item">
          <strong>התקדמות שבועית</strong><br>
          ${doneCount}/${missions.length} משימות הושלמו השבוע<br>
          המשימות מתעדכנות תוך כדי משחק ומראות מה עוד שווה להשלים השבוע.
        </div>
      ` + missions.map((mission) => {
        const progress = Math.min(mission.target, mission.progress());
        const done = progress >= mission.target;
        return `
          <div class="mission-item${done ? " done" : ""}">
            <strong>${mission.label}</strong><br>
            התקדמות: ${progress}/${mission.target} • פרס: ${mission.reward}
          </div>
        `;
      }).join("");
    }

    function renderMonthlyMissions() {
      if (!monthlyMissionsListEl) {
        return;
      }
      ensureMonthlyMissionState();
      const missions = getMonthlyMissionDefinitions();
      const doneCount = missions.filter((mission) => mission.progress() >= mission.target).length;
      monthlyMissionsListEl.innerHTML = `
        <div class="mission-item">
          <strong>התקדמות חודשית</strong><br>
          ${doneCount}/${missions.length} משימות חודשיות הושלמו<br>
          אלה היעדים הארוכים יותר של החודש עם תגמולים גדולים יותר, תיבות פרס ורקעים מיוחדים.
        </div>
      ` + missions.map((mission) => {
        const progress = Math.min(mission.target, mission.progress());
        const done = progress >= mission.target;
        return `
          <div class="mission-item${done ? " done" : ""}">
            <strong>${mission.label}</strong><br>
            התקדמות: ${progress}/${mission.target} • פרס: ${mission.reward}
          </div>
        `;
      }).join("");
    }

    function renderStoryMap() {
      if (!storyMapEl) {
        return;
      }
      if (!storyChapters.length) {
        storyMapEl.innerHTML = '<div class="mission-item">עדיין אין פרקים במפת הסיפור.</div>';
        return;
      }
      const activeChapter = Math.min(storyChapters.length - 1, storyProgress.chapter || 0);
      const totalCompleted = Number(storyProgress.completed || 0);
      const completedPercent = Math.round((Math.min(storyChapters.length, totalCompleted) / storyChapters.length) * 100);
      storyMapEl.innerHTML = `
        <div class="story-journey-head">
          <strong>פרק פעיל: ${activeChapter + 1} מתוך ${storyChapters.length}</strong>
          <div class="story-mini-map">
            ${storyChapters.map((_, index) => `<span class="story-mini-stop${index < activeChapter ? " done" : ""}${index === activeChapter ? " active" : ""}">${index + 1}</span>`).join("")}
          </div>
        </div>
        <div class="mission-item">
          שביל הסיפור מצויר כמו מסע: תחנות שכבר הושלמו, הדגל הנוכחי, ומה מחכה ממש בפרק הבא. הושלמו עד עכשיו ${totalCompleted} תחנות • ${completedPercent}% מהמסע.
        </div>
      ` + storyChapters.map((chapter, index) => {
        const done = index < activeChapter;
        const active = index === activeChapter;
        const chapterDifficulty = difficultyLabels[chapter.difficulty] || "בינוני";
        const chapterIcons = chapter.categories.map((category) => `${categoryIcons[category] || "•"} ${categoryLabels[category] || category}`).join(" • ");
        return `
          <div class="story-node${done ? " done" : ""}${active ? " active" : ""}">
            <span class="story-node-flag">${done ? "🏁 תחנה שהושלמה" : (active ? "🚩 תחנה פעילה" : "🗺️ תחנה בהמשך")}</span>
            <span class="story-node-title">פרק ${index + 1}: ${chapter.title}</span>
            <span class="story-node-text">${chapter.text}</span>
            <div class="story-node-badges">
              <span class="story-node-badge">🎯 קושי ${chapterDifficulty}</span>
              <span class="story-node-badge">${chapterIcons}</span>
            </div>
            <span class="story-node-state">${done ? "✓ הושלם" : (active ? "▶ פעיל עכשיו" : "🔒 בהמשך")}</span>
          </div>
        `;
      }).join("");
    }

    function renderGuideCard() {
      if (!guideCardEl) {
        return;
      }
      const activeAvatar = currentSelection.mode === "single"
        ? (currentSelection.singleAvatar || "🙂")
        : (currentSelection.playerOneAvatar || currentSelection.singleAvatar || "🙂");
      const personality = avatarPersonalities[activeAvatar] || avatarPersonalities["🙂"];
      const chapterIndex = Math.min(storyChapters.length - 1, storyProgress.chapter || 0);
      guideCardEl.innerHTML = `
        <div class="guide-bubble"><strong>${activeAvatar} ${personality.title}</strong><br>${getAvatarFlavorLine("start", activeAvatar)}</div>
        <div class="guide-bubble"><strong>מדריך פתיחה אינטראקטיבי</strong><br>1. בחרו מצב משחק. 2. התאימו נושא וקושי. 3. הביטו ימינה אל האתגרים, הגביעים והטורניר. 4. כשתהיו מוכנים, לחצו על התחלה.</div>
        <div class="guide-bubble"><strong>מה הכי כדאי עכשיו?</strong><br>במצב ${getModeLabel(currentSelection.mode)} שווה לנסות את פרק ${chapterIndex + 1} בסיפור, לפתוח תיבת פרס, או לכוון לעוד XP ותג מיוחד.</div>
      `;
      if (guideMascotEl) {
        guideMascotEl.textContent = activeAvatar;
      }
    }

    function renderProgressMiniMap() {
      if (!progressMiniMapEl) {
        return;
      }
      const profileName = getCurrentProfileName();
      const xpInfo = getXpInfo(profileName);
      const weeklyDone = getWeeklyMissionDefinitions().filter((mission) => mission.progress() >= mission.target).length;
      const favoriteCount = favoriteWords.length;
      progressMiniMapEl.innerHTML = `
        <div class="mission-item">
          <strong>מבט מהיר על ההתקדמות</strong><br>
          רמה, הישגים, פרק נוכחי, משימות שבועיות ומילים אהובות במקום אחד.
        </div>
        <div class="progress-mini-node"><strong>${xpInfo.level}</strong>רמה</div>
        <div class="progress-mini-node"><strong>${unlockedAchievements.size}</strong>הישגים</div>
        <div class="progress-mini-node"><strong>${Math.min(storyChapters.length, (storyProgress.chapter || 0) + 1)}</strong>פרק</div>
        <div class="progress-mini-node"><strong>${weeklyDone}</strong>שבועי</div>
        <div class="progress-mini-node"><strong>${favoriteCount}</strong>אהובות</div>
      `;
    }

    function renderProfileLeaderboard() {
      if (!profileLeaderboardListEl) {
        return;
      }
      const entries = Object.entries(playerStats)
        .map(([name, stats]) => ({
          name,
          wins: Number(stats.wins || 0),
          losses: Number(stats.losses || 0),
          survivalBest: Number(stats.survivalBest || 0),
          rate: (Number(stats.wins || 0) + Number(stats.losses || 0)) ? Math.round((Number(stats.wins || 0) / (Number(stats.wins || 0) + Number(stats.losses || 0))) * 100) : 0
        }))
        .sort((a, b) => (b.wins * 3 + b.survivalBest) - (a.wins * 3 + a.survivalBest))
        .slice(0, 5);

      profileLeaderboardListEl.innerHTML = entries.length
        ? `<div class="leaderboard-item"><strong>דירוג פרופילים</strong><span>כאן רואים מי מוביל כרגע לפי ניצחונות ואחוזי הצלחה.</span></div>` + entries.map((entry, index) => `
          <div class="leaderboard-item">
            <strong>${index + 1}. ${entry.name}</strong>
            <span>${entry.wins} ניצחונות • ${entry.rate}% הצלחה</span>
            <span>שיא הישרדות ${entry.survivalBest}</span>
          </div>
        `).join("")
        : '<p class="leaderboard-empty">עדיין אין שחקנים עם נתונים לשיאי פרופיל. אחרי כמה משחקים יופיע כאן דירוג מסודר.</p>';
    }

    function renderTrophyRoom() {
      if (!trophyRoomEl) {
        return;
      }
      const totalAchievements = unlockedAchievements.size;
      const totalProfiles = playerProfiles.length;
      const totalModesWithRecords = Object.keys(modeRecords).length;
      const totalWeeklyDone = getWeeklyMissionDefinitions().filter((mission) => mission.progress() >= mission.target).length;
      const totalMonthlyDone = getMonthlyMissionDefinitions().filter((mission) => mission.progress() >= mission.target).length;
      const bestPlayer = Object.entries(playerStats).sort(([, a], [, b]) => (Number(b.wins || 0) - Number(a.wins || 0)))[0];
      const packsOpened = unlockedTopicPacks.length;
      trophyRoomEl.innerHTML = `
        <div class="trophy-gallery">
          <div class="trophy-shelf">
            <div class="trophy-shelf-head"><span>אולם האלופים</span><span>✨</span></div>
            <div class="trophy-pedestal-row">
              <div class="trophy-pedestal">
                <span class="icon">🏆</span>
                <strong>${totalAchievements}</strong>
                <span>הישגים פתוחים</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">👑</span>
                <strong>${bestPlayer ? bestPlayer[0] : "ממתין"}</strong>
                <span>${bestPlayer ? `${bestPlayer[1].wins || 0} ניצחונות` : "הגביע עוד פנוי"}</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">🎖️</span>
                <strong>${totalModesWithRecords}</strong>
                <span>מצבים עם שיא</span>
              </div>
            </div>
          </div>
          <div class="trophy-shelf">
            <div class="trophy-shelf-head"><span>מדף האוספים</span><span>🏛️</span></div>
            <div class="trophy-pedestal-row">
              <div class="trophy-pedestal">
                <span class="icon">👤</span>
                <strong>${totalProfiles}</strong>
                <span>פרופילים פעילים</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">📦</span>
                <strong>${packsOpened}</strong>
                <span>חבילות פתוחות</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">⭐</span>
                <strong>${favoriteWords.length}</strong>
                <span>מילים אהובות</span>
              </div>
            </div>
          </div>
          <div class="trophy-shelf">
            <div class="trophy-shelf-head"><span>משימות וזוהר</span><span>🌟</span></div>
            <div class="trophy-pedestal-row">
              <div class="trophy-pedestal">
                <span class="icon">🗓️</span>
                <strong>${totalWeeklyDone}/${getWeeklyMissionDefinitions().length}</strong>
                <span>שבועיות</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">📅</span>
                <strong>${totalMonthlyDone}/${getMonthlyMissionDefinitions().length}</strong>
                <span>חודשיות</span>
              </div>
              <div class="trophy-pedestal">
                <span class="icon">✨</span>
                <strong>${totalAchievements || totalProfiles || totalModesWithRecords ? "פתוח" : "מתמלא"}</strong>
                <span>${totalAchievements || totalProfiles || totalModesWithRecords ? "חדר חי ומתפתח" : "שחקו עוד כדי למלא את החדר"}</span>
              </div>
            </div>
          </div>
        </div>
      `;
    }

    function renderTournamentTree() {
      if (!tournamentTreeEl) {
        return;
      }
      const players = (partyPlayers.length ? partyPlayers : [
        { name: currentSelection.playerOneName || "שחקן 1", avatar: currentSelection.playerOneAvatar || "😎" },
        { name: currentSelection.playerTwoName || "שחקן 2", avatar: currentSelection.playerTwoAvatar || "🦊" }
      ]).slice(0, 8);
      const slots = players.length >= 8 ? 8 : (players.length >= 4 ? 4 : 2);
      const filledPlayers = Array.from({ length: slots }, (_, index) => players[index] || { name: `מקום ${index + 1}`, avatar: "❔" });
      const semifinals = slots === 8
        ? [
          [filledPlayers[0], filledPlayers[1]],
          [filledPlayers[2], filledPlayers[3]],
          [filledPlayers[4], filledPlayers[5]],
          [filledPlayers[6], filledPlayers[7]]
        ]
        : (slots === 4
          ? [
            [filledPlayers[0], filledPlayers[1]],
            [filledPlayers[2], filledPlayers[3]]
          ]
          : [[filledPlayers[0], filledPlayers[1]]]);
      const winners = tournamentBracket.map((match) => match.winner);
      const latestWinner = winners[winners.length - 1] || "ממתינים";
      const championName = partyPlayers
        .map((player, index) => ({ name: player.name, score: partyScores[index] || 0 }))
        .sort((a, b) => b.score - a.score)[0]?.name || latestWinner;
      const renderMatch = (pair, label, index) => {
        const [first, second] = pair;
        const played = winners[index];
        return `
          <div class="tournament-tree-match${played ? " done" : ""}${currentSelection.mode === "tournament" && activeTurn === index ? " active" : ""}">
            <span class="tournament-tree-name">${label}</span>
            <span class="tournament-tree-meta">${first.avatar || "🙂"} ${first.name} מול ${second.avatar || "🙂"} ${second.name}</span>
            <span class="tournament-tree-meta">${played ? `המנצח שהתקדם: ${played}` : "ממתין לתוצאה"}</span>
          </div>
        `;
      };
      const semifinalMarkup = semifinals.map((pair, index) => renderMatch(pair, slots === 8 ? `רבע גמר ${index + 1}` : (semifinals.length > 1 ? `חצי גמר ${index + 1}` : "גמר ישיר"), index)).join("");
      const finalsNeeded = slots === 8 ? 2 : 1;
      const finalTitle = slots === 8 ? "חצי גמר" : "גמר";
      tournamentTreeEl.innerHTML = `
        <div class="mission-item">
          עץ הטורניר מציג את כל הדרך קדימה: ${slots === 8 ? "רבע גמר, חצי גמר" : (slots === 4 ? "חצי גמר" : "דו-קרב ישיר")} ואז גמר ברור בדרך לכתר.
        </div>
        <div class="tournament-tree-column">
          <span class="tournament-tree-title">${slots === 8 ? "רבע גמר" : (slots === 4 ? "חצי גמר" : "פתיחה")}</span>
          ${semifinalMarkup}
        </div>
        <div class="tournament-tree-column">
          <span class="tournament-tree-title">${finalTitle}</span>
          ${Array.from({ length: finalsNeeded }, (_, index) => `
            <div class="tournament-tree-match${winners.length > semifinals.length + index ? " done" : ""}">
              <span class="tournament-tree-name">${finalsNeeded > 1 ? `חצי גמר ${index + 1}` : "משחק גמר"}</span>
              <span class="tournament-tree-meta">${winners.length > index ? `${winners[index] || "ממתין"} כבר עלה שלב` : "שני מנצחים ייפגשו כאן"}</span>
              <span class="tournament-tree-meta">${currentSelection.mode === "tournament" ? "הבראקט מתעדכן חי תוך כדי סיבובים." : "כשתתחילו טורניר, השלב הזה יתמלא."}</span>
            </div>
          `).join("")}
          <div class="tournament-tree-match${winners.length >= semifinals.length ? " done" : ""}">
            <span class="tournament-tree-name">גמר</span>
            <span class="tournament-tree-meta">${winners.length >= semifinals.length ? `${latestWinner} כבר מרגיש את הגביע` : "עדיין אוספים את העולים לגמר"}</span>
          </div>
        </div>
        <div class="tournament-tree-column">
          <span class="tournament-tree-title">אלוף</span>
          <div class="tournament-tree-crown">
            <div>
              <strong>👑 ${partyScores.some((score) => score > 0) ? championName : "הכתר עוד פתוח"}</strong>
              <span>${partyScores.some((score) => score > 0) ? `לוח ניקוד נוכחי: ${partyPlayers.map((player, index) => `${player.name} ${partyScores[index] || 0}`).join(" • ")}` : "כשתתחילו טורניר, כאן יופיע האלוף יחד עם הדרך שלו אל הגמר."}</span>
            </div>
          </div>
        </div>
      `;
    }

    function renderFavoriteWords() {
      if (!favoriteWordsListEl) {
        return;
      }
      const items = favoriteWords
        .map((key) => {
          const [category, wordKey] = key.split(":");
          const match = [
            ...words,
            ...customWordEntries.filter((entry) => entry.favoriteOnly)
          ].find((entry) => entry.category === category && sanitizeLookupValue(entry.word) === wordKey);
          return match ? `<span class="favorite-word-chip">${categoryIcons[match.category] || "⭐"} ${match.word}</span>` : "";
        })
        .filter(Boolean)
        .join("");
      favoriteWordsListEl.innerHTML = items
        ? `<div class="leaderboard-item"><strong>מאגר המילים האהובות שלך</strong><span>${favoriteWords.length} מילים שמורות. אפשר לשחק רק איתן דרך רמת הקושי הייעודית.</span></div>${items}`
        : '<p class="leaderboard-empty">עדיין לא שמרת מילים אהובות. חפש מילה במאגר ולחץ על "שמור" כדי לראות אותה כאן.</p>';
    }

    function renderTopicStats() {
      if (!topicStatsListEl) {
        return;
      }
      const entries = Object.entries(topicStats)
        .map(([category, stats]) => ({
          category,
          wins: Number(stats.wins || 0),
          plays: Number(stats.plays || 0)
        }))
        .sort((a, b) => b.wins - a.wins)
        .slice(0, 5);
      topicStatsListEl.innerHTML = entries.length
        ? `<div class="leaderboard-item"><strong>הנושאים החזקים שלך</strong><span>האחוז מראה כמה הצלחת בכל נושא מתוך מה שכבר שיחקת. למטה רואים גם איפה עוד יש מקום לגדול.</span></div>` + entries.map((entry) => {
          const rate = entry.plays ? Math.round((entry.wins / entry.plays) * 100) : 0;
          return `
            <div class="topic-stat-bar">
              <div class="topic-stat-head">
                <span>${categoryIcons[entry.category] || "•"} ${categoryLabels[entry.category] || entry.category}</span>
                <strong>${rate}%</strong>
              </div>
              <div class="topic-stat-track"><div class="topic-stat-fill" style="width:${Math.max(8, rate)}%;"></div></div>
            </div>
          `;
        }).join("") + (() => {
          const weakest = [...entries].sort((a, b) => ((a.plays ? a.wins / a.plays : 0) - (b.plays ? b.wins / b.plays : 0)))[0];
          return weakest ? `<div class="leaderboard-item"><strong>הנושא שהכי מאתגר אותך כרגע</strong><span>${categoryIcons[weakest.category] || "•"} ${categoryLabels[weakest.category] || weakest.category} • הזדמנות יפה להשתפר.</span></div>` : "";
        })()
        : '<p class="leaderboard-empty">עדיין אין מספיק משחקים כדי להציג נושאים חזקים. אחרי כמה משחקים לפי נושא יופיע כאן דירוג מסודר.</p>';
    }

    function renderPrizeBoxes() {
      if (!prizeBoxListEl) {
        return;
      }
      const wins = Number(playerStats[getCurrentProfileName()]?.wins || 0);
      const boxes = [
        { id: "starter_box", label: "📦 תיבת התחלה", need: 3, reward: "2 מטבעות" },
        { id: "story_box", label: "🎁 תיבת סיפור", need: 6, reward: "רקע מיוחד" },
        { id: "champ_box", label: "👑 תיבת אלוף", need: 10, reward: "5 מטבעות" }
      ];
      prizeBoxListEl.innerHTML = boxes.map((box) => {
        const claimed = Boolean(prizeBoxState.claimed?.[box.id]);
        const unlocked = wins >= box.need;
        return `
          <div class="prize-box">
            <strong>${box.label}</strong>
            <span>${claimed ? "כבר נפתחה בטקס קטן ומנצנץ" : (unlocked ? `מוכנה לפתיחה חגיגית • פרס: ${box.reward}` : `נפתחת אחרי ${box.need} ניצחונות`)}</span>
            ${claimed ? '<button class="tiny-button" type="button" disabled>נפתחה</button>' : (unlocked ? `<button class="tiny-button" data-open-prize-box="${box.id}" type="button">פתח תיבה</button>` : "")}
          </div>
        `;
      }).join("");
    }

    function openPrizeBox(boxId) {
      const wins = Number(playerStats[getCurrentProfileName()]?.wins || 0);
      const rewards = {
        starter_box: () => {
          awardCoins(2);
          return "קיבלת 2 מטבעות.";
        },
        story_box: () => {
          if (!cosmeticInventory.backgrounds.includes("aurora")) {
            cosmeticInventory.backgrounds.push("aurora");
            saveCosmeticInventory();
            renderBackgroundPills();
          }
          return "נפתח לך רקע אורות חדש.";
        },
        champ_box: () => {
          awardCoins(5);
          return "קיבלת 5 מטבעות של אלוף.";
        }
      };
      const requirements = {
        starter_box: 3,
        story_box: 6,
        champ_box: 10
      };

      if (prizeBoxState.claimed?.[boxId]) {
        setAddWordNote("התיבה הזו כבר נפתחה.", "error");
        return;
      }

      if (wins < (requirements[boxId] || 999)) {
        setAddWordNote("עוד אין מספיק ניצחונות כדי לפתוח את התיבה.", "error");
        return;
      }

      prizeBoxState.claimed[boxId] = true;
      savePrizeBoxState();
      const rewardText = rewards[boxId]?.() || "התיבה נפתחה.";
      renderPrizeBoxes();
      renderCosmeticShop();
      renderBackgroundPills();
      setAddWordNote(rewardText, "success");
    }

    function renderCosmeticShop() {
      if (!cosmeticShopListEl || !cosmeticShopCoinsEl) {
        return;
      }
      cosmeticShopCoinsEl.textContent = `מטבעות: ${bonusCoins} • אוואטרים פתוחים: ${cosmeticInventory.avatars.length} • רקעים פתוחים: ${cosmeticInventory.backgrounds.length}`;
      cosmeticShopListEl.innerHTML = cosmeticItems.map((item) => {
        const owned = item.kind === "avatar"
          ? cosmeticInventory.avatars.includes(item.unlockKey)
          : cosmeticInventory.backgrounds.includes(item.unlockKey);
        return `
          <div class="cosmetic-item">
            <div class="cosmetic-item-preview">${item.preview || (item.kind === "avatar" ? "🙂" : "🎨")}</div>
            <div>
              <strong>${item.label}</strong>
              <span>${item.description} • מחיר ${item.price} מטבעות</span>
            </div>
            <button class="tiny-button" data-buy-cosmetic="${item.id}" type="button"${owned ? " disabled" : ""}>${owned ? "פתוח" : "פתח"}</button>
          </div>
        `;
      }).join("");
    }

    function buyCosmeticItem(itemId) {
      const item = cosmeticItems.find((entry) => entry.id === itemId);
      if (!item) {
        return;
      }
      const alreadyOwned = item.kind === "avatar"
        ? cosmeticInventory.avatars.includes(item.unlockKey)
        : cosmeticInventory.backgrounds.includes(item.unlockKey);
      if (alreadyOwned) {
        setAddWordNote("הפריט הזה כבר פתוח אצלך.", "error");
        return;
      }
      if (bonusCoins < item.price) {
        setAddWordNote("אין מספיק מטבעות בשביל הפריט הזה.", "error");
        return;
      }
      bonusCoins -= item.price;
      item.unlock();
      saveBonusStore();
      renderCosmeticShop();
      renderShop();
      renderAvatarPickers();
      renderPartyPlayerFields();
      renderBackgroundPills();
      setAddWordNote(`הפריט "${item.label}" נפתח בהצלחה.`, "success");
    }

    function renderWordManager() {
      if (!wordManagerListEl) {
        return;
      }
      if (wordSearchInputEl) {
        wordSearchInputEl.value = wordSearchTerm;
      }
      const term = sanitizeLookupValue(wordSearchTerm || "");
      const results = words
        .filter((entry) => !term || sanitizeLookupValue(entry.word).includes(term))
        .slice(0, 10);

      wordManagerListEl.innerHTML = results.length
        ? results.map((entry) => {
          const key = `${entry.category}:${sanitizeLookupValue(entry.word)}`;
          const favorite = favoriteWords.includes(key);
          return `
            <div class="leaderboard-item">
              <strong>${entry.word}</strong>
              <span>${categoryIcons[entry.category] || "•"} ${entry.topic}</span>
              <button class="tiny-button" data-favorite-word="${key}" type="button">${favorite ? "מועדפת" : "שמור"}</button>
            </div>
          `;
        }).join("")
        : '<p class="leaderboard-empty">לא נמצאו מילים שמתאימות לחיפוש.</p>';
    }

    function toggleFavoriteWord(wordKey) {
      if (!wordKey) {
        return;
      }
      if (favoriteWords.includes(wordKey)) {
        favoriteWords = favoriteWords.filter((item) => item !== wordKey);
      } else {
        favoriteWords.unshift(wordKey);
        favoriteWords = favoriteWords.slice(0, 20);
      }
      saveFavoriteWords();
      ensureFavoriteDifficultyAvailability();
      renderWordManager();
      renderFavoriteWords();
      renderDifficultyPills();
      renderTrophyRoom();
    }

    function renderMusicState() {
      if (musicStyleSelectEl) {
        musicStyleSelectEl.value = musicStyle;
      }
      if (musicToggleButtonEl) {
        musicToggleButtonEl.textContent = musicEnabled ? "כבה מוזיקה" : "הפעל מוזיקה";
      }
      if (musicStatusEl) {
        const labels = {
          calm: "רגועה",
          playful: "קופצנית",
          dreamy: "חלומית",
          arcade: "ארקייד",
          story: "סיפורית"
        };
        musicStatusEl.textContent = musicEnabled
          ? `מוזיקת ${labels[musicStyle] || "רגועה"} מנוגנת ברקע.`
          : "כרגע שקט. אפשר לבחור סגנון ולהפעיל מוזיקה.";
      }
    }

    function refreshHelperPanels() {
      renderAvatars();
      renderStoryCard();
      renderGuideCard();
      renderStoryMap();
      renderProgressMiniMap();
      renderOnlineRoomCard();
      renderWeeklyMissions();
      renderMonthlyMissions();
      renderPlayerStats();
      renderProfileLeaderboard();
      renderTrophyRoom();
      renderTournamentTree();
      renderCosmeticShop();
      renderWordManager();
      renderFavoriteWords();
      renderTopicStats();
      renderPrizeBoxes();
      renderDailyChallenge();
      renderWeeklyChallenge();
      renderDailyMissions();
      renderDailyWheel();
      renderLoginStreak();
      renderMusicState();
    }

    function renderBonusBuyerOptions() {
      if (!bonusOwnerSelectEl) {
        return;
      }

      let currentValue = currentSelection.bonusBuyer || bonusOwnerSelectEl.value || "player1";

      if (currentSelection.mode === "party" || currentSelection.mode === "tournament") {
        ensurePartyPlayers();
        const partyOptions = partyPlayers.map((player, index) => `
          <option value="party:${index}">${player.name || `משתתף ${index + 1}`}</option>
        `).join("");
        if (!currentValue.startsWith("party:")) {
          currentValue = "party:0";
        }
        bonusOwnerSelectEl.innerHTML = partyOptions;
      } else {
        const playerOneLabel = currentSelection.playerOneName || "שחקן 1";
        const playerTwoLabel = currentSelection.playerTwoName || "שחקן 2";
        bonusOwnerSelectEl.innerHTML = `
          <option value="player1">${playerOneLabel}</option>
          <option value="player2">${playerTwoLabel}</option>
        `;
        if (currentValue.startsWith("party:")) {
          currentValue = "player1";
        }
      }

      bonusOwnerSelectEl.value = currentValue;
      currentSelection.bonusBuyer = currentValue;
    }

    function removeWordFromBank() {
      const category = categorySelectEl.value;
      const wordToRemove = sanitizeStoredWord(addWordInputEl.value);

      if (!wordToRemove) {
        setAddWordNote("כתבו מילה כדי להסיר אותה מהמאגר.", "error");
        addWordInputEl.focus();
        return;
      }

      const previousLength = customWordEntries.length;
      customWordEntries = customWordEntries.filter((entry) => {
        const sameWord = sanitizeLookupValue(entry.word) === sanitizeLookupValue(wordToRemove);
        const sameCategory = category === "all" ? true : entry.category === category;
        return !(!entry.favoriteOnly && sameWord && sameCategory);
      });

      if (customWordEntries.length === previousLength) {
        setAddWordNote("לא נמצאה מילה מותאמת להסרה במאגר שהוספתם.", "error");
        return;
      }

      saveCustomWords();
      rebuildWordCollections();
      addWordInputEl.value = "";
      setAddWordNote(`המילה "${wordToRemove}" הוסרה מהמאגר שהוספתם.`, "success");
    }

    function buyBonusItem(type) {
      const prices = { freeze: 3, life: 4, skip: 5, shield: 4 };
      const price = prices[type];
      if (!price || bonusCoins < price) {
        setAddWordNote("אין מספיק מטבעות לבונוס הזה.", "error");
        return;
      }

      const buyerName = getBonusBuyerName();
      if (["competition", "party", "two"].includes(currentSelection.mode) && bonusOwners[type] && bonusOwners[type] !== buyerName) {
        setAddWordNote(`רק ${bonusOwners[type]} יכול לקנות את הבונוס הזה כרגע.`, "error");
        return;
      }

      bonusCoins -= price;
      bonusInventory[type] += 1;
      if (["competition", "party", "two"].includes(currentSelection.mode)) {
        bonusOwners[type] = buyerName;
      }
      saveBonusStore();
      renderShop();
      updateBonusButtons();
      const labels = {
        freeze: "הקפאת הזמן",
        life: "החיים הנוספים",
        skip: "הדילוג",
        shield: "מגן הטעות"
      };
      setAddWordNote(`${labels[type]} שייך ל-${buyerName}.`, "success");
    }

    function saveCurrentProfile() {
      const profileName = sanitizePlayerName(profileNameInputEl?.value || "")
        || (currentSelection.mode === "party"
          ? (partyPlayers[0]?.name || "מסיבה")
          : (currentSelection.playerOneName || currentSelection.playerTwoName || "שחקן"));
      const profileAvatar = currentSelection.singleAvatar || "🙂";

      if (!profileName) {
        setAddWordNote("אין כרגע שם מתאים לשמירת פרופיל.", "error");
        profileNameInputEl?.focus();
        return;
      }

      playerProfiles = playerProfiles.filter((profile) => profile.name !== profileName);
      playerProfiles.unshift({
        name: profileName,
        avatar: profileAvatar,
        style: selectedStyle,
        mode: currentSelection.mode,
        playerOneName: currentSelection.playerOneName,
        playerTwoName: currentSelection.playerTwoName,
        singleAvatar: currentSelection.singleAvatar,
        playerOneAvatar: currentSelection.playerOneAvatar,
        playerTwoAvatar: currentSelection.playerTwoAvatar
      });
      playerProfiles = playerProfiles.slice(0, 10);
      selectedProfileTabIndex = 0;
      profileDetailVisible = true;
      activeAchievementProfileKey = getAchievementProfileKey(profileName);
      achievementStore[activeAchievementProfileKey] = [...unlockedAchievements];
      saveAchievements();
      saveProfiles();
      renderProfiles();
      renderSideProfileDock();
      renderProfileSummary();
      renderStartDashboard();
      openProfilesPanel();
      setAddWordNote(`הפרופיל של ${profileName} נשמר.`, "success");
    }

    function loadProfile(index) {
      const profile = playerProfiles[index];
      if (!profile) {
        return;
      }

      selectedProfileTabIndex = index;
      profileDetailVisible = true;
      if (profileNameInputEl) {
        profileNameInputEl.value = profile.name || "";
      }
      currentSelection.playerOneName = profile.playerOneName || profile.name || "";
      currentSelection.playerTwoName = profile.playerTwoName || "";
      currentSelection.singleAvatar = profile.singleAvatar || profile.avatar || "🙂";
      currentSelection.playerOneAvatar = profile.playerOneAvatar || profile.avatar || "😎";
      currentSelection.playerTwoAvatar = profile.playerTwoAvatar || "🦊";
      selectedStyle = profile.style || "cheerful";
      applySelectionToControls();
      savePersonalStyle();
      applyPersonalStyle();
      applyVisualTheme(activeTopicKey);
      renderStylePicker();
      renderAvatars();
      renderBonusBuyerOptions();
      setActiveAchievementProfile(profile.name);
      renderProfiles();
      renderSideProfileDock();
      renderProfileSummary();
      renderStartDashboard();
      setAddWordNote(`הפרופיל של ${profile.name} נטען.`, "success");
    }

    function editProfile(index) {
      const profile = playerProfiles[index];
      if (!profile) {
        return;
      }
      loadProfile(index);
      setAddWordNote(`הפרופיל של ${profile.name} נטען לעריכה. שנו ושמרו שוב.`, "success");
    }

    function deleteProfile(index) {
      const profile = playerProfiles[index];
      if (!profile) {
        return;
      }

      delete achievementStore[getAchievementProfileKey(profile.name)];
      if (activeAchievementProfileKey === getAchievementProfileKey(profile.name)) {
        setActiveAchievementProfile("");
      }
      playerProfiles.splice(index, 1);
      selectedProfileTabIndex = Math.max(0, Math.min(selectedProfileTabIndex, playerProfiles.length - 1));
      profileDetailVisible = true;
      saveAchievements();
      saveProfiles();
      renderProfiles();
      renderSideProfileDock();
      renderProfileSummary();
      renderStartDashboard();
      setAddWordNote(`הפרופיל של ${profile.name} נמחק.`, "success");
    }

    function updatePlayerStat(name, updater) {
      if (!name) {
        return;
      }
      const current = playerStats[name] || { wins: 0, losses: 0, survivalBest: 0 };
      playerStats[name] = updater(current);
      savePlayerStats();
      renderPlayerStats();
    }

    function updateTopicStat(categoryKey, resultType) {
      if (!categoryKey || categoryKey === "all") {
        return;
      }
      const current = topicStats[categoryKey] || { plays: 0, wins: 0 };
      topicStats[categoryKey] = {
        plays: current.plays + 1,
        wins: current.wins + (resultType === "win" ? 1 : 0)
      };
      saveTopicStats();
      renderTopicStats();
    }

    function recordResultForPlayers(resultType) {
      let names = currentSelection.mode === "party" || currentSelection.mode === "tournament"
        ? partyPlayers.map((player) => player.name)
        : [currentSelection.playerOneName, currentSelection.playerTwoName].filter(Boolean);

      if ((currentSelection.mode === "party" || currentSelection.mode === "tournament") && resultType === "win") {
        const bestScore = Math.max(...partyScores, 0);
        names = partyPlayers
          .filter((_, index) => partyScores[index] === bestScore)
          .map((player) => player.name);
      }

      if (currentSelection.mode === "single" || currentSelection.mode === "practice" || currentSelection.mode === "survival") {
        names = [currentSelection.playerOneName || "שחקן יחיד"];
      }

      names.filter(Boolean).forEach((name) => {
        updatePlayerStat(name, (current) => ({
          ...current,
          wins: current.wins + (resultType === "win" ? 1 : 0),
          losses: current.losses + (resultType === "lose" ? 1 : 0),
          survivalBest: Math.max(current.survivalBest || 0, currentSelection.mode === "survival" ? competitionScore : 0)
        }));
        awardXp(name, resultType === "win" ? 12 : 4);
        pushPlayerHistory(
          name,
          `${resultType === "win" ? "✅" : "❌"} ${getModeLabel(currentSelection.mode)} • ${resultType === "win" ? "ניצחון" : "הפסד"}${secretWord ? ` • ${secretWord}` : ""}`
        );
      });
      updateTopicStat(activeTopicKey, resultType);
      if (resultType === "win") {
        advanceMonthlyMission("monthly_win_10");
        if (currentSelection.specialMode === "story") {
          advanceMonthlyMission("monthly_story_5");
        }
        if (currentSelection.mode === "survival") {
          advanceMonthlyMission("monthly_survival_8", competitionScore > 0 ? 1 : 0);
        }
      }
      renderProfiles();
    }

    function updateModeRecord(mode, value, label) {
      const currentRecord = modeRecords[mode];
      if (!currentRecord || value > currentRecord.value) {
        modeRecords[mode] = { value, label };
        saveModeRecords();
        renderModeRecords();
      }
    }

    function getActiveAvatar() {
      if (currentSelection.mode === "party") {
        return partyPlayers[activeTurn]?.avatar || currentSelection.singleAvatar || "🙂";
      }

      if (currentSelection.mode === "competition" || currentSelection.mode === "online") {
        return currentSelection.playerOneAvatar || "😎";
      }

      if (currentSelection.mode === "two") {
        return currentSelection.playerTwoAvatar || "🦊";
      }

      return currentSelection.singleAvatar || "🙂";
    }

    function getAvatarPitchMultiplier() {
      const avatar = getActiveAvatar();
      const avatarIndex = Math.max(0, avatarChoices.indexOf(avatar));
      return 0.94 + avatarIndex * 0.012;
    }

    function getAvatarFlavorLine() {
      return getAvatarFlavorLineForContext("idle", getActiveAvatar());
    }

    function getAvatarFlavorLineForContext(context = "idle", avatar = getActiveAvatar()) {
      const personality = avatarPersonalities[avatar] || avatarPersonalities["🙂"];
      const lines = {
        start: {
          "🙂": "בואו נתחיל בכיף. עוד רגע תיפתח מילה חדשה.",
          "😎": "אני כבר מוכן. בואו נראה כמה רחוק תגיעו הפעם.",
          "🤠": "קדימה לשביל של מילים, סיבובים והרבה אומץ.",
          "🦊": "אני מריח ניצחון. רק צריך לפגוע באותיות הנכונות.",
          "🐯": "הפעם נכנסים חזק ובלי לפחד מהקושי.",
          "🐼": "שקט, רוגע, ואז ניחוש מדויק בזמן הנכון.",
          "🦄": "עוד רגע הכול יבריק בקונפטי וצבעים.",
          "🐸": "קפיצה קטנה והופ, כבר נכנסים למשחק.",
          "🐵": "אני מרגיש שמחכה פה סיבוב ממש טוב.",
          "🦁": "מלכים לא מפחדים ממילים קשות.",
          "🐨": "לאט, נעים, ובסוף גם מנצחים.",
          "🐙": "יש לי שמונה דרכים שונות להגיע לפתרון.",
          "🐲": "בואו נבעיר את הלוח הזה.",
          "🦋": "הפעם הכול ירגיש קליל וזוהר."
        },
        win: {
          "🙂": "איזה יופי. זאת בדיוק התחושה שחיכינו לה.",
          "😎": "ידעתי שננצח. זה היה חלק לגמרי.",
          "🤠": "איזה סיבוב יפה. ככה גומרים מסע כמו שצריך.",
          "🦊": "חד, חכם ומדויק. זה היה שלנו.",
          "🐯": "זה היה ניצחון של אלופים.",
          "🐼": "שקט ורגוע, אבל עם סיום מושלם.",
          "🦄": "וואו, זה היה ממש זוהר.",
          "🐸": "קפצנו ישר לניצחון.",
          "🐵": "איזה כיף, אני כבר רוצה עוד סיבוב כזה.",
          "🦁": "מלך אמיתי סוגר סיבוב בדיוק ככה.",
          "🐨": "רגוע אבל לגמרי מנצח.",
          "🐙": "הכול התחבר בדיוק בזמן.",
          "🐲": "האש עבדה. איזה ניצחון.",
          "🦋": "זה נגמר כל כך יפה."
        },
        lose: {
          "🙂": "לא נורא. הסיבוב הבא כבר מחכה לנו.",
          "😎": "פספסנו את זה, אבל נחזור הרבה יותר חדים.",
          "🤠": "גם במסע טוב יש לפעמים עצירה קטנה.",
          "🦊": "הפעם זה ברח, בפעם הבאה נלכוד את זה.",
          "🐯": "הכוח נשאר. עכשיו רק חוזרים חזק יותר.",
          "🐼": "ננשום רגע, וננסה שוב בשקט.",
          "🦄": "הקסם לא נעלם, הוא רק מחכה לסיבוב הבא.",
          "🐸": "נפלה קפיצה אחת, אבל עוד רגע קופצים שוב.",
          "🐵": "אוי, זה כאב קצת, אבל נתקן מהר.",
          "🦁": "אפילו מלכים מפסידים לפעמים.",
          "🐨": "ניקח רגע רגוע ונחזור מחדש.",
          "🐙": "פעם אחת הסתבכנו, בפעם הבאה נצא מזה חלק.",
          "🐲": "האש נרגעה, אבל עוד לא כבתה.",
          "🦋": "ננפנף לזה לשלום ונמשיך הלאה."
        }
      };
      return lines[context]?.[avatar] || personality.line;
    }

    function getDifficultyConfettiBonus() {
      if (currentSelection.specialMode === "kids") {
        return 16;
      }

      if (currentSelection.difficulty === "hard" || currentSelection.specialMode === "extreme") {
        return 12;
      }

      if (currentSelection.difficulty === "easy") {
        return 6;
      }

      return 8;
    }

    function getDifficultyMessage(isHit) {
      if (!isHit) {
        return currentSelection.difficulty === "hard" || currentSelection.specialMode === "extreme"
          ? "אופס, זו טעות שבאמת מרגישים."
          : "לא הפעם, נסה אות אחרת.";
      }

      if (currentSelection.specialMode === "kids") {
        return "מעולה! אות שמחה נפתחה.";
      }

      if (currentSelection.specialMode === "challenge") {
        return isHit ? "פגיעה חדה. מצב אתגר רץ." : "אתגר קשוח, צריך דיוק.";
      }

      if (currentSelection.difficulty === "hard" || currentSelection.specialMode === "extreme") {
        return "יפה מאוד, זו פגיעה חזקה במצב קשה.";
      }

      return "יפה! יש אות נכונה.";
    }

    function getSummaryLines(type) {
      const lines = [
        `מצב: ${getModeLabel(currentSelection.mode)}`,
        `קושי: ${difficultyLabels[currentSelection.difficulty]}${currentSelection.specialMode === "kids" ? " • ילדי" : (currentSelection.specialMode === "challenge" ? " • אתגר" : (currentSelection.specialMode === "extreme" ? " • קשה באמת" : ""))}`,
        `טעויות: ${wrongGuesses}/${maxWrong}`
      ];

      if (currentSelection.mode !== "practice") {
        lines.push(`זמן שנותר: ${formatTime(Math.max(0, remainingTime))}`);
      }

      if (currentSelection.mode === "competition" || currentSelection.mode === "online") {
        lines.push(`ניקוד תחרות: ${competitionScore}`);
      } else if (currentSelection.mode === "tournament") {
        lines.push(`סיבוב טורניר: ${Math.ceil(Math.max(partyRoundCount, 1) / Math.max(1, partyPlayers.length || 1))}/${currentSelection.partyRounds}`);
      } else if (currentSelection.mode === "party") {
        lines.push(`סיבובים: ${currentSelection.partyRounds}`);
      } else if (currentSelection.mode === "survival") {
        lines.push(`ניקוד הישרדות: ${competitionScore}`);
      } else if (type === "win") {
        lines.push(`הרמז ${usedHint ? "נוצל" : "לא נוצל"}`);
      }

      if (shieldActive) {
        lines.push("מגן טעות היה פעיל");
      }

      if (currentSelection.dailyChallenge) {
        lines.push("זה היה אתגר יומי");
      }

      if (currentSelection.weeklyChallenge) {
        lines.push("זה היה אתגר שבועי");
      }

      return lines;
    }

    function renderEndSummary(type) {
      const activeAvatar = currentSelection.mode === "party" || currentSelection.mode === "tournament"
        ? (partyPlayers[activeTurn]?.avatar || currentSelection.singleAvatar || "🙂")
        : (currentSelection.mode === "two" || currentSelection.mode === "competition" || currentSelection.mode === "online"
          ? (currentSelection.playerOneAvatar || "😎")
          : (currentSelection.singleAvatar || "🙂"));
      const personality = avatarPersonalities[activeAvatar] || avatarPersonalities["🙂"];
      const medal = type === "win" ? "🥇" : "🎬";
      const headline = type === "win" ? "רגע של ניצחון" : "עוד סיבוב בסיפור";

      if (endSummaryEl) {
        const heroValue = type === "win"
          ? (currentSelection.mode === "survival" ? competitionScore : Math.max(...partyScores, competitionScore, 1))
          : wrongGuesses;
        endSummaryEl.innerHTML = `
          <div class="end-summary-cinematic">
            <div class="end-summary-hero">
              <div class="end-summary-hero-avatar">${activeAvatar}</div>
              <div class="end-summary-hero-text">
                <strong>${medal} ${headline}</strong>
                <span>${personality.title} • ${type === "win" ? getAvatarFlavorLineForContext("win", activeAvatar) : getAvatarFlavorLineForContext("lose", activeAvatar)}</span>
              </div>
              <div class="end-summary-medal">${type === "win" ? "מדליית סיום" : "חזרה מהירה"}</div>
            </div>
            <div class="end-summary-stats">
              <div class="end-summary-stat">
                <strong>${heroValue}</strong>
                <span>${type === "win" ? "שיא רגעי" : "טעויות"}</span>
              </div>
              <div class="end-summary-stat">
                <strong>${unlockedAchievements.size}</strong>
                <span>הישגים פתוחים</span>
              </div>
              <div class="end-summary-stat">
                <strong>${getXpInfo(getCurrentProfileName()).level}</strong>
                <span>רמת XP</span>
              </div>
            </div>
            <div class="end-summary-grid">
              ${getSummaryLines(type).map((line) => `<div class="end-summary-chip">${line}</div>`).join("")}
            </div>
          </div>
        `;
      }

      if (!partyRankingEl) {
        return;
      }

      if (!["party", "tournament"].includes(currentSelection.mode)) {
        partyRankingEl.innerHTML = "";
        return;
      }

      const ranking = partyPlayers
        .map((player, index) => ({ ...player, score: partyScores[index] || 0 }))
        .sort((a, b) => b.score - a.score);

      const rankingMarkup = ranking.map((player, index) => `
        <div class="party-rank-item${index === 0 ? " top" : ""}">
          <span>${index === 0 ? "🥇 מקום ראשון" : (index === 1 ? "🥈 מקום שני" : (index === 2 ? "🥉 מקום שלישי" : `${index + 1}.`))} • ${player.avatar} ${player.name}</span>
          <strong>${player.score} נק'</strong>
        </div>
      `).join("");

      const bracketMarkup = currentSelection.mode === "tournament"
        ? `
          <div class="tournament-bracket">
            ${tournamentBracket.map((match, index) => `
              <div class="tournament-match">
                <strong>שלב ${index + 1}</strong>
                <span>🏆 ${match.winner} ניצח את ${match.loser}</span>
              </div>
            `).join("") || `
              <div class="tournament-match">
                <strong>עדיין אין תוצאות שמורות</strong>
                <span>ברגע שיסתיימו דו-קרבים, הבראקט יופיע גם כאן.</span>
              </div>
            `}
          </div>
        `
        : "";

      partyRankingEl.innerHTML = `${rankingMarkup}${bracketMarkup}`;
    }

    function showWordResultScreen({
      type,
      icon,
      title,
      subtitle,
      endWord,
      buttonLabel,
      onContinue
    }) {
      if (!endScreenEl) {
        onContinue?.();
        return;
      }

      pendingEndAction = onContinue || null;
      endScreenEl.className = `end-screen visible ${type}`;
      endIconEl.textContent = icon;
      endTitleEl.textContent = title;
      endSubtitleEl.textContent = `${subtitle} ${type === "win" ? getAvatarFlavorLineForContext("win") : getAvatarFlavorLineForContext("lose")}`;
      endWordEl.textContent = endWord;
      renderEndSummary(type);
      endAchievementEl.innerHTML = lastUnlockedAchievements.length
        ? lastUnlockedAchievements.map((id) => {
          const achievement = achievementDefinitions.find((item) => item.id === id);
          return achievement ? `<span class="end-achievement-chip">${achievement.icon} ${achievement.label} +${achievement.coins}</span>` : "";
        }).filter(Boolean).join("")
        : "";
      endButtonEl.textContent = buttonLabel;
    }

    function awardCoins(amount) {
      bonusCoins += amount;
      saveBonusStore();
      renderShop();
      renderStartDashboard();
    }

    function renderLeaderboard() {
      if (!leaderboardListEl) {
        return;
      }

      const topEntries = leaderboard.slice(0, 5);
      if (!topEntries.length) {
        leaderboardListEl.innerHTML = '<p class="leaderboard-empty">עדיין אין שיאים. שחקו תחרות או מסיבה כדי למלא את הטבלה.</p>';
        return;
      }

      leaderboardListEl.innerHTML = topEntries.map((entry, index) => `
        <div class="leaderboard-item">
          <strong>${index + 1}. ${entry.modeLabel}</strong>
          <span>${entry.names}</span>
          <span>${entry.score}</span>
        </div>
      `).join("");
    }

    function saveLeaderboardEntry() {
      if (!["competition", "online", "party", "tournament"].includes(currentSelection.mode)) {
        return;
      }

      const names = currentSelection.mode === "party"
        ? partyPlayers.map((player) => player.name).join(" • ")
        : currentSelection.mode === "online"
          ? `${currentSelection.playerOneName || "שחקן 1"} מול ${currentSelection.playerTwoName || "שחקן 2"} • חדר ${localOnlineRoomCode || "------"}`
        : currentSelection.mode === "tournament"
          ? `${partyPlayers[0]?.name || "שחקן 1"} נגד ${partyPlayers[1]?.name || "שחקן 2"}`
        : `${currentSelection.playerOneName || "שחקן 1"} + ${currentSelection.playerTwoName || "שחקן 2"}`;
      const score = currentSelection.mode === "party"
        ? `${Math.max(...partyScores, 0)} נק'`
        : currentSelection.mode === "online"
          ? `${competitionScore} נק'`
        : currentSelection.mode === "tournament"
          ? `${partyScores[0] || 0}:${partyScores[1] || 0}`
        : `${competitionScore} נק'`;

      leaderboard.unshift({
        mode: currentSelection.mode,
        modeLabel: currentSelection.mode === "party" ? "מסיבה" : (currentSelection.mode === "tournament" ? "טורניר" : (currentSelection.mode === "online" ? "אונליין" : "תחרות")),
        names,
        score,
        value: currentSelection.mode === "party" ? Math.max(...partyScores, 0) : (currentSelection.mode === "tournament" ? Math.max(...partyScores, 0) : competitionScore)
      });

      leaderboard.sort((a, b) => b.value - a.value);
      leaderboard = leaderboard.slice(0, 10);
      saveLeaderboard();
      renderLeaderboard();
    }

    function renderAvatarPicker(container, selected, key) {
      if (!container) {
        return;
      }

      const availableAvatars = avatarChoices.filter((avatar) => cosmeticInventory.avatars.includes(avatar) || avatar === selected);
      container.innerHTML = availableAvatars.map((avatar) => `
        <button class="avatar-option ${avatarAnimationClasses[avatar] || ""}${avatar === selected ? " active" : ""}" data-avatar-key="${key}" data-avatar="${avatar}" type="button" aria-label="${avatarLabels[avatar] || "אוואטר"}">
          <span class="avatar-emoji">${avatar}</span>
          <span class="avatar-label">${avatarLabels[avatar] || "אוואטר"}</span>
        </button>
      `).join("");
    }

    function renderAvatarPickers() {
      renderAvatarPicker(singleAvatarPickerEl, currentSelection.singleAvatar, "singleAvatar");
      renderAvatarPicker(playerOneAvatarPickerEl, currentSelection.playerOneAvatar, "playerOneAvatar");
      renderAvatarPicker(playerTwoAvatarPickerEl, currentSelection.playerTwoAvatar, "playerTwoAvatar");
      renderAvatarPicker(profileAvatarPickerEl, currentSelection.singleAvatar, "singleAvatar");
    }

    function ensurePartyPlayers() {
      const desiredCount = clampPartyPlayerCount(currentSelection.partyPlayerCount);
      currentSelection.partyPlayerCount = desiredCount;
      while (partyPlayers.length < desiredCount) {
        const index = partyPlayers.length + 1;
        partyPlayers.push({
          name: `שחקן ${index}`,
          avatar: avatarChoices[(index - 1) % avatarChoices.length]
        });
      }
      partyPlayers = partyPlayers.slice(0, desiredCount);
    }

    function renderPartyPlayerFields() {
      if (!partyPlayersFieldsEl) {
        return;
      }

      ensurePartyPlayers();
      partyPlayersFieldsEl.innerHTML = partyPlayers.map((player, index) => `
        <div style="margin-top: 12px;">
          <label class="setting-label" for="party-player-name-${index}">שם משתתף ${index + 1}</label>
          <input class="text-input party-player-name" id="party-player-name-${index}" data-party-index="${index}" type="text" maxlength="16" value="${player.name}">
          <div class="avatar-picker party-avatar-picker" data-party-index="${index}">
            ${avatarChoices
              .filter((avatar) => cosmeticInventory.avatars.includes(avatar) || avatar === player.avatar)
              .map((avatar) => `<button class="avatar-option ${avatarAnimationClasses[avatar] || ""}${avatar === player.avatar ? " active" : ""}" data-party-index="${index}" data-party-avatar="${avatar}" type="button"><span class="avatar-emoji">${avatar}</span><span class="avatar-label">${avatarLabels[avatar] || "אוואטר"}</span></button>`).join("")}
          </div>
        </div>
      `).join("");
    }

    function updateBonusButtons() {
      if (freezeButtonEl) {
        const hasFreeze = freezeAvailable || bonusInventory.freeze > 0;
        const canUseFreeze = hasFreeze && canCurrentPlayerUseBonus("freeze");
        freezeButtonEl.classList.toggle("hidden", !canUseFreeze);
        freezeButtonEl.disabled = gameOver || isPaused || currentSelection.mode === "practice" || !canUseFreeze;
        freezeButtonEl.textContent = hasFreeze
          ? `הקפאת זמן${bonusInventory.freeze > 0 ? ` (${bonusInventory.freeze})` : ""}`
          : "הקפאה נוצלה";
      }

      if (lifeButtonEl) {
        const hasLife = extraLifeAvailable || bonusInventory.life > 0;
        const canUseLife = hasLife && canCurrentPlayerUseBonus("life");
        lifeButtonEl.classList.toggle("hidden", !canUseLife);
        lifeButtonEl.disabled = gameOver || !canUseLife || wrongGuesses === 0;
        lifeButtonEl.textContent = hasLife
          ? `חיים נוספים${bonusInventory.life > 0 ? ` (${bonusInventory.life})` : ""}`
          : "חיים נוצלו";
      }

      if (skipButtonEl) {
        const hasSkip = bonusInventory.skip > 0;
        const canUseSkip = hasSkip && canCurrentPlayerUseBonus("skip");
        skipButtonEl.classList.toggle("hidden", !canUseSkip);
        skipButtonEl.disabled = gameOver || !canUseSkip;
        skipButtonEl.textContent = hasSkip ? `דלג מילה (${bonusInventory.skip})` : "אין דילוג";
      }

      if (shieldButtonEl) {
        const hasShield = bonusInventory.shield > 0 || shieldActive;
        const canUseShield = hasShield && canCurrentPlayerUseBonus("shield");
        shieldButtonEl.classList.toggle("hidden", !canUseShield);
        shieldButtonEl.disabled = gameOver || shieldActive || !canUseShield;
        shieldButtonEl.textContent = shieldActive ? "מגן פעיל" : (bonusInventory.shield > 0 ? `מגן טעות (${bonusInventory.shield})` : "אין מגן");
      }
    }

    function useFreezeTime() {
      if ((!freezeAvailable && bonusInventory.freeze <= 0) || currentSelection.mode === "practice" || gameOver || !canCurrentPlayerUseBonus("freeze")) {
        return;
      }

      if (bonusInventory.freeze > 0) {
        bonusInventory.freeze -= 1;
        if (bonusInventory.freeze <= 0) {
          bonusOwners.freeze = "";
        }
        saveBonusStore();
        renderShop();
      } else {
        freezeAvailable = false;
      }
      clearTimer();
      messageEl.textContent = "הזמן קפא ל-10 שניות.";
      messageEl.className = "message guess-hit";
      updateBonusButtons();
      window.clearTimeout(freezeTimerId);
      freezeTimerId = window.setTimeout(() => {
        if (!gameOver && !isPaused) {
          runTimerInterval();
        }
      }, 10000);
    }

    function useExtraLife() {
      if ((!extraLifeAvailable && bonusInventory.life <= 0) || wrongGuesses === 0 || gameOver || !canCurrentPlayerUseBonus("life")) {
        return;
      }

      if (bonusInventory.life > 0) {
        bonusInventory.life -= 1;
        if (bonusInventory.life <= 0) {
          bonusOwners.life = "";
        }
        saveBonusStore();
        renderShop();
      } else {
        extraLifeAvailable = false;
      }
      wrongGuesses = Math.max(0, wrongGuesses - 1);
      messageEl.textContent = "החיים הנוספים הצילו טעות אחת.";
      messageEl.className = "message guess-hit";
      updateStats();
      updateBonusButtons();
    }

    function useSkipWord() {
      if (bonusInventory.skip <= 0 || gameOver || !canCurrentPlayerUseBonus("skip")) {
        return;
      }

      bonusInventory.skip -= 1;
      if (bonusInventory.skip <= 0) {
        bonusOwners.skip = "";
      }
      saveBonusStore();
      renderShop();
      messageEl.textContent = "דילוג הופעל. עוברים למילה חדשה.";
      messageEl.className = "message guess-hit";

      if (currentSelection.mode === "party") {
        startNextPartyRound();
        return;
      }

      if (["competition", "online", "survival"].includes(currentSelection.mode)) {
        startNextCompetitionRound();
        return;
      }

      guessedLetters = new Set();
      wrongGuesses = 0;
      usedHint = false;
      lastGuessResult = null;
      lastGuessedLetter = "";
      pickWord();
      renderWord();
      renderKeyboard();
      updateStats();
    }

    function useShield() {
      if (bonusInventory.shield <= 0 || gameOver || shieldActive || !canCurrentPlayerUseBonus("shield")) {
        return;
      }

      bonusInventory.shield -= 1;
      shieldActive = true;
      if (bonusInventory.shield <= 0) {
        bonusOwners.shield = "";
      }
      saveBonusStore();
      renderShop();
      messageEl.textContent = "מגן טעות הופעל. הטעות הבאה לא תיספר.";
      messageEl.className = "message guess-hit";
      updateBonusButtons();
    }

    function resetAllData() {
      unlockedAchievements = new Set();
      achievementStore = {};
      activeAchievementProfileKey = "regular";
      customWordEntries = [];
      unlockedTopicPacks = ["starter"];
      leaderboard = [];
      modeRecords = {};
      bonusCoins = 0;
      bonusInventory = { freeze: 0, life: 0, skip: 0, shield: 0 };
      bonusOwners = { freeze: "", life: "", skip: "", shield: "" };
      playerProfiles = [];
      playerStats = {};
      dailyChallengeState = {};
      weeklyChallengeState = {};
      dailyMissionsState = { dateKey: "", completed: {} };
      dailyWheelState = { dateKey: "", prize: "", claimed: false };
      weeklyMissionsState = { weekKey: "", completed: {} };
      loginStreakState = { lastDate: "", streak: 0 };
      recentWordKeys = [];
      favoriteWords = [];
      wordSearchTerm = "";
      musicEnabled = false;
      playerXp = {};
      cosmeticInventory = { avatars: [...baseAvatarChoices], backgrounds: [...baseBackgroundChoices] };
      homeIntroSeen = false;
      playerHistory = {};
      storyProgress = { chapter: 0, completed: 0 };
      localOnlineRoomCode = "";
      tournamentBracket = [];
      topicStats = {};
      monthlyMissionsState = { monthKey: "", completed: {} };
      prizeBoxState = { claimed: {} };
      saveAchievements();
      saveCustomWords();
      saveLeaderboard();
      saveModeRecords();
      saveBonusStore();
      saveProfiles();
      savePlayerStats();
      saveDailyChallengeState();
      saveWeeklyChallengeState();
      saveDailyMissionsState();
      saveDailyWheelState();
      saveWeeklyMissionsState();
      saveLoginStreakState();
      saveRecentWords();
      saveTopicPacks();
      savePlayerHistory();
      saveStoryProgress();
      saveFavoriteWords();
      saveMusicPreference();
      savePlayerXp();
      saveCosmeticInventory();
      saveHomeIntroSeen();
      saveTopicStats();
      saveMonthlyMissionsState();
      savePrizeBoxState();
      stopBackgroundMusic();
      rebuildWordCollections();
      populateCategoryOptions();
      setActiveAchievementProfile("");
      renderAchievementBadges();
      renderLeaderboard();
      renderModeRecords();
      renderShop();
      renderProfiles();
      renderPlayerStats();
      renderProfileSummary();
      renderDailyChallenge();
      renderWeeklyChallenge();
      renderDailyMissions();
      renderWeeklyMissions();
      renderDailyWheel();
      renderStoryCard();
      renderStoryMap();
      renderOnlineRoomCard();
      renderProfileLeaderboard();
      renderTrophyRoom();
      renderWordManager();
      renderMusicState();
      renderCosmeticShop();
      renderMonthlyMissions();
      renderFavoriteWords();
      renderTopicStats();
      renderPrizeBoxes();
      renderAmbientLayer();
      renderStartDashboard();
      setAddWordNote("כל הנתונים אופסו.", "success");
    }

    function hashString(value) {
      return [...value].reduce((total, char) => total + char.charCodeAt(0), 0);
    }

    function renderTimePills() {
      if (!timePillsEl) {
        return;
      }

      const selected = String(currentSelection.timeLimit);
      [...timePillsEl.querySelectorAll(".time-pill")].forEach((button) => {
        button.classList.toggle("active", button.dataset.time === selected);
      });
    }

    function renderDifficultyPills() {
      if (!difficultyPillsEl) {
        return;
      }

      const favoritesButton = difficultyPillsEl.querySelector('[data-difficulty="favorites"]');
      const favoritesOption = difficultySelectEl?.querySelector('option[value="favorites"]');
      const showFavorites = hasFavoriteWords();
      favoritesButton?.classList.toggle("hidden", !showFavorites);
      if (favoritesOption) {
        favoritesOption.hidden = !showFavorites;
        favoritesOption.disabled = !showFavorites;
      }
      ensureFavoriteDifficultyAvailability();
      const selected = currentSelection.difficulty;
      [...difficultyPillsEl.querySelectorAll(".difficulty-pill")].forEach((button) => {
        button.classList.toggle("active", button.dataset.difficulty === selected);
      });
    }

    function renderAvatars() {
      const details = modeDetails[currentSelection.mode] || modeDetails.single;
      const playerOneName = currentSelection.playerOneName || "שחקן 1";
      const playerTwoName = currentSelection.playerTwoName || "שחקן 2";
      const singleAvatar = currentSelection.singleAvatar || details.icon;
      const playerOneAvatar = currentSelection.playerOneAvatar || details.icon;
      const playerTwoAvatar = currentSelection.playerTwoAvatar || "🕵️";
      const topicText = currentSelection.mode === "two"
        ? (currentSelection.customTopic || "נושא מותאם")
        : (startScreenEl.classList.contains("hidden")
          ? (activeTopicLabel || "כללי")
          : (currentSelection.category === "all" ? "כל הנושאים" : (categoryLabels[currentSelection.category] || "כללי")));
      const avatars = currentSelection.mode === "two" || currentSelection.mode === "competition" || currentSelection.mode === "online"
        ? [
          `${playerOneAvatar} ${playerOneName}`,
          `${playerTwoAvatar} ${playerTwoName}`
        ]
        : currentSelection.mode === "tournament"
          ? [
            `${partyPlayers[0]?.avatar || playerOneAvatar} ${partyPlayers[0]?.name || playerOneName}`,
            `${partyPlayers[1]?.avatar || playerTwoAvatar} ${partyPlayers[1]?.name || playerTwoName}`
          ]
        : currentSelection.mode === "party"
          ? partyPlayers.map((player) => `${player.avatar} ${player.name}`)
          : [
            `${singleAvatar} ${details.label}`,
            `${categoryIcons[activeTopicKey] || "🎲"} ${topicText}`
        ]
      ;

      const markup = avatars
        .map((item) => {
          const avatar = item.split(" ")[0];
          return `<span class="avatar-pill ${avatarAnimationClasses[avatar] || ""}"><strong>${avatar}</strong><span>${item.split(" ").slice(1).join(" ")}</span></span>`;
        })
        .join("");

      avatarShowcaseEl.innerHTML = markup;
      heroAvatarEl.innerHTML = markup;
      modeChipEl.textContent = `${details.icon} ${details.label}`;
      modeBadgeEl.textContent = `${details.icon} ${details.label}`;
      heroSubtitleEl.textContent = details.subtitle;
      turnBannerEl.hidden = !["party", "competition", "tournament", "online"].includes(currentSelection.mode);
      if (currentSelection.mode === "party") {
        ensurePartyPlayers();
        const activePlayer = partyPlayers[activeTurn] || { name: "שחקן", avatar: "🙂" };
        const visibleRound = Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(partyPlayers.length, 1)));
        turnBannerEl.textContent = `סיבוב ${visibleRound}/${currentSelection.partyRounds} • תור: ${activePlayer.avatar} ${activePlayer.name}`;
      } else if (currentSelection.mode === "tournament") {
        const activePlayer = partyPlayers[activeTurn] || { name: "שחקן", avatar: "🙂" };
        const visibleRound = Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(1, partyPlayers.length)));
        turnBannerEl.textContent = `טורניר ${visibleRound}/${currentSelection.partyRounds} • תור: ${activePlayer.avatar} ${activePlayer.name}`;
      } else if (currentSelection.mode === "competition") {
        turnBannerEl.textContent = `צוות: ${playerOneAvatar} ${playerOneName} + ${playerTwoAvatar} ${playerTwoName}`;
      } else if (currentSelection.mode === "online") {
        turnBannerEl.textContent = `חדר ${onlineSession.roomId || localOnlineRoomCode || "------"} • ${playerOneAvatar} ${playerOneName} מול ${playerTwoAvatar} ${playerTwoName}`;
      }
      renderLiveBoard();
    }

    function renderLiveBoard() {
      if (!liveBoardEl) {
        return;
      }

      if (currentSelection.mode === "tournament") {
        const ranking = partyPlayers
          .map((player, index) => ({ ...player, score: partyScores[index] || 0 }))
          .sort((a, b) => b.score - a.score);
        const maxScore = Math.max(1, currentSelection.partyRounds);
        const visibleRound = Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(partyPlayers.length || 1, 1)));
        liveBoardEl.classList.remove("hidden");
        liveBoardEl.innerHTML = `
          <div class="live-board-header">
            <span>🏆 לוח טורניר חי</span>
            <span class="live-board-subtitle">שלב ${visibleRound}/${currentSelection.partyRounds}</span>
          </div>
          <div class="live-board-grid">
            ${ranking.map((player) => {
              const score = partyScores[partyPlayers.findIndex((entry) => entry.name === player.name)] || 0;
              return `
                <div class="live-board-card${partyPlayers[activeTurn]?.name === player.name ? " active" : ""}">
                  <span class="live-board-name">${player.avatar} ${player.name}</span>
                  <span class="live-board-meta">ניצחונות בסיבובים</span>
                  <span class="live-board-score">${score}</span>
                  <div class="live-board-track"><div class="live-board-fill" style="width:${Math.min(100, Math.round((score / maxScore) * 100))}%;"></div></div>
                </div>
              `;
            }).join("")}
          </div>
          <div class="tournament-bracket">
            ${tournamentBracket.length
              ? tournamentBracket.slice(-8).map((match, index) => `
                <div class="tournament-match">
                  <strong>שלב ${index + 1} • סיבוב ${match.round}</strong>
                  <span>🏆 ${match.winner} ניצח את ${match.loser}</span>
                </div>
              `).join("")
              : `
                <div class="tournament-match">
                  <strong>בראקט בהכנה</strong>
                  <span>כאן תראו את כל הסיבובים שנגמרו ואת מי כל שחקן עבר בדרך לגמר.</span>
                </div>
              `}
          </div>
        `;
        return;
      }

      if (currentSelection.mode === "party") {
        const ranking = partyPlayers
          .map((player, index) => ({ ...player, score: partyScores[index] || 0 }))
          .sort((a, b) => b.score - a.score)
          .slice(0, 4);
        const visibleRound = Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(partyPlayers.length || 1, 1)));
        liveBoardEl.classList.remove("hidden");
        liveBoardEl.innerHTML = `
          <div class="live-board-header">
            <span>🎈 לוח מסיבה חי</span>
            <span class="live-board-subtitle">סיבוב ${visibleRound}/${currentSelection.partyRounds}</span>
          </div>
          <div class="live-board-list">
            ${ranking.map((player, index) => `
              <div class="live-board-row">
                <span>${index === 0 ? "🥇" : (index === 1 ? "🥈" : (index === 2 ? "🥉" : "•"))} ${player.avatar} ${player.name}</span>
                <strong>${player.score} נק'</strong>
              </div>
            `).join("")}
          </div>
        `;
        return;
      }

      if (currentSelection.mode === "competition") {
        liveBoardEl.classList.remove("hidden");
        liveBoardEl.innerHTML = `
          <div class="live-board-header">
            <span>🏁 מצב תחרות</span>
            <span class="live-board-subtitle">כל מילה מוסיפה נקודות לפני שהזמן נגמר</span>
          </div>
          <div class="live-board-grid">
            <div class="live-board-card active">
              <span class="live-board-name">${currentSelection.playerOneAvatar || "😎"} ${currentSelection.playerOneName || "שחקן 1"}</span>
              <span class="live-board-meta">חלק מהצוות המהיר</span>
              <span class="live-board-score">${competitionScore}</span>
            </div>
            <div class="live-board-card">
              <span class="live-board-name">${currentSelection.playerTwoAvatar || "🦊"} ${currentSelection.playerTwoName || "שחקן 2"}</span>
              <span class="live-board-meta">ממשיכים להעלות את הקצב</span>
              <span class="live-board-score">${formatTime(Math.max(0, remainingTime))}</span>
            </div>
          </div>
        `;
        return;
      }

      if (currentSelection.mode === "online") {
        liveBoardEl.classList.remove("hidden");
        liveBoardEl.innerHTML = `
          <div class="live-board-header">
            <span>🛰️ חדר אונליין אמיתי</span>
            <span class="live-board-subtitle">קוד חדר: ${onlineSession.roomId || localOnlineRoomCode || "------"}</span>
          </div>
          <div class="live-board-grid">
            <div class="live-board-card active">
              <span class="live-board-name">${currentSelection.playerOneAvatar || "😎"} ${currentSelection.playerOneName || "שחקן 1"}</span>
              <span class="live-board-meta">${onlineSession.role === "guest" ? "מארח החדר" : "יוצר החדר"}</span>
              <span class="live-board-score">${wrongGuesses}/${maxWrong}</span>
            </div>
            <div class="live-board-card">
              <span class="live-board-name">${currentSelection.playerTwoAvatar || "🦊"} ${currentSelection.playerTwoName || "שחקן 2"}</span>
              <span class="live-board-meta">${onlineSession.guestJoined ? "מחובר לחדר" : "ממתין לקישור"}</span>
              <span class="live-board-score">${formatTime(Math.max(0, remainingTime))}</span>
            </div>
          </div>
        `;
        return;
      }

      if (currentSelection.mode === "survival") {
        liveBoardEl.classList.remove("hidden");
        liveBoardEl.innerHTML = `
          <div class="live-board-header">
            <span>🛡️ הישרדות חיה</span>
            <span class="live-board-subtitle">ממשיכים עד שהריצה נגמרת</span>
          </div>
          <div class="live-board-grid">
            <div class="live-board-card active">
              <span class="live-board-name">${currentSelection.singleAvatar || "🙂"} ריצה נוכחית</span>
              <span class="live-board-meta">מילים שנפתרו ברצף</span>
              <span class="live-board-score">${competitionScore}</span>
            </div>
            <div class="live-board-card">
              <span class="live-board-name">⭐ שיא אישי</span>
              <span class="live-board-meta">הישרדות הכי טובה בפרופיל</span>
              <span class="live-board-score">${playerStats[currentSelection.playerOneName || "שחקן יחיד"]?.survivalBest || 0}</span>
            </div>
          </div>
        `;
        return;
      }

      liveBoardEl.classList.add("hidden");
      liveBoardEl.innerHTML = "";
    }

    function applyVisualTheme(categoryKey = activeTopicKey) {
      const modeSeed = {
        single: 18,
        two: 215,
        competition: 350,
        party: 300,
        practice: 140
      }[currentSelection.mode] || 30;
      const topicSeed = hashString(categoryKey || activeTopicLabel || currentSelection.mode);
      const hueA = (modeSeed + topicSeed) % 360;
      const hueB = (hueA + 42) % 360;
      const hueC = (hueA + 92) % 360;
      const rootStyle = document.documentElement.style;

      rootStyle.setProperty("--theme-a", `hsla(${hueA} 96% 66% / 0.82)`);
      rootStyle.setProperty("--theme-b", `hsla(${hueB} 88% 62% / 0.44)`);
      rootStyle.setProperty("--theme-c", `hsla(${hueC} 90% 66% / 0.34)`);
      rootStyle.setProperty("--bg", `hsl(${(hueB + 10) % 360} 100% 88%)`);
      rootStyle.setProperty("--theme-d", `hsl(${(hueA + 18) % 360} 100% 92%)`);
      rootStyle.setProperty("--theme-panel", `hsla(${hueB} 80% 98% / 0.82)`);
      rootStyle.setProperty("--theme-glow", `hsla(${hueA} 96% 68% / 0.28)`);
      rootStyle.setProperty("--mode-strip", `linear-gradient(135deg, hsl(${hueB} 76% 47%), hsl(${hueC} 74% 62%), hsl(${hueA} 90% 70%))`);
      applyBackgroundStyle();
      renderAmbientLayer();

      gameShellEl.className = `game mode-${currentSelection.mode} difficulty-${currentSelection.difficulty} special-${currentSelection.specialMode}`;
      gameShellEl.dataset.category = categoryKey || "all";
      renderAvatars();
    }

    function applyBackgroundStyle() {
      const rootStyle = document.documentElement.style;
      const presets = {
        sky: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#79d7ff",
          bg: "#8fe1ff"
        },
        sun: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#ffd53d",
          bg: "#ffe56c"
        },
        mint: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#67e3a8",
          bg: "#88f0be"
        },
        rose: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#ff8fb4",
          bg: "#ffacc7"
        },
        lavender: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#b59bff",
          bg: "#c7b3ff"
        },
        peach: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#ffb38a",
          bg: "#ffc8aa"
        },
        lime: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#b7ef4a",
          bg: "#d4ff7b"
        },
        ice: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#b9f2ff",
          bg: "#d5f8ff"
        },
        coral: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#ff7f6f",
          bg: "#ffb0a6"
        },
        white: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#ffffff",
          bg: "#ffffff"
        },
        black: {
          a: "transparent",
          b: "transparent",
          c: "transparent",
          d: "#111111",
          bg: "#111111"
        },
        candy: {
          a: "rgba(255, 86, 145, 0.82)",
          b: "rgba(255, 209, 102, 0.54)",
          c: "rgba(84, 196, 255, 0.42)",
          d: "#ffd5e9",
          bg: "#fff0c8"
        },
        ocean: {
          a: "rgba(38, 183, 255, 0.82)",
          b: "rgba(0, 193, 193, 0.52)",
          c: "rgba(125, 140, 255, 0.36)",
          d: "#cceeff",
          bg: "#b7e7ff"
        },
        sunset: {
          a: "rgba(255, 122, 89, 0.84)",
          b: "rgba(255, 179, 71, 0.58)",
          c: "rgba(255, 143, 171, 0.36)",
          d: "#ffd6b3",
          bg: "#ffe3c2"
        },
        forest: {
          a: "rgba(34, 197, 94, 0.84)",
          b: "rgba(132, 204, 22, 0.56)",
          c: "rgba(16, 185, 129, 0.36)",
          d: "#d7f8c8",
          bg: "#e2ffd3"
        },
        berry: {
          a: "rgba(244, 114, 182, 0.84)",
          b: "rgba(251, 113, 133, 0.54)",
          c: "rgba(192, 132, 252, 0.38)",
          d: "#ffd5f2",
          bg: "#ffe0f8"
        },
        aurora: {
          a: "rgba(34, 197, 94, 0.84)",
          b: "rgba(6, 182, 212, 0.58)",
          c: "rgba(139, 92, 246, 0.42)",
          d: "#d9fff6",
          bg: "#d7f8ff"
        },
        fire: {
          a: "rgba(255, 90, 54, 0.88)",
          b: "rgba(255, 159, 28, 0.62)",
          c: "rgba(255, 209, 102, 0.4)",
          d: "#ffd0b1",
          bg: "#ffe1a6"
        },
        dream: {
          a: "rgba(125, 211, 252, 0.84)",
          b: "rgba(192, 132, 252, 0.56)",
          c: "rgba(249, 168, 212, 0.42)",
          d: "#e8dbff",
          bg: "#ffe0f2"
        },
        tropical: {
          a: "rgba(0, 194, 168, 0.86)",
          b: "rgba(138, 201, 38, 0.58)",
          c: "rgba(255, 202, 58, 0.42)",
          d: "#d7ffe6",
          bg: "#fff1b8"
        },
        "night-pop": {
          a: "rgba(29, 53, 87, 0.92)",
          b: "rgba(67, 97, 238, 0.6)",
          c: "rgba(76, 201, 240, 0.4)",
          d: "#ccd7ff",
          bg: "#c1d5ff"
        }
      };

      const preset = presets[selectedBackgroundStyle];
      if (selectedBackgroundStyle === "theme") {
        if (selectedStyle === "ocean") {
          rootStyle.setProperty("--theme-a", "rgba(38, 183, 255, 0.82)");
          rootStyle.setProperty("--theme-b", "rgba(20, 184, 166, 0.48)");
          rootStyle.setProperty("--theme-c", "rgba(125, 211, 252, 0.34)");
          rootStyle.setProperty("--theme-d", "#c8f1ff");
          rootStyle.setProperty("--bg", "#bdeaff");
        } else if (selectedStyle === "sunset") {
          rootStyle.setProperty("--theme-a", "rgba(249, 115, 22, 0.82)");
          rootStyle.setProperty("--theme-b", "rgba(239, 68, 68, 0.46)");
          rootStyle.setProperty("--theme-c", "rgba(253, 186, 116, 0.34)");
          rootStyle.setProperty("--theme-d", "#ffd9bf");
          rootStyle.setProperty("--bg", "#ffe8d2");
        }
        renderAmbientLayer();
        return;
      }

      if (!preset) {
        return;
      }

      rootStyle.setProperty("--theme-a", preset.a);
      rootStyle.setProperty("--theme-b", preset.b);
      rootStyle.setProperty("--theme-c", preset.c);
      rootStyle.setProperty("--theme-d", preset.d);
      rootStyle.setProperty("--bg", preset.bg);
      renderAmbientLayer();
    }

    function renderBackgroundPills() {
      if (!backgroundPillsEl || !backgroundTabsEl) {
        return;
      }

       const solidStyles = ["sky", "sun", "mint", "rose", "lavender", "peach", "lime", "ice", "coral", "white", "black"];
       const gradientStyles = ["candy", "ocean", "sunset", "forest", "berry", "aurora", "fire", "dream", "tropical", "night-pop"];

      [...backgroundTabsEl.querySelectorAll("[data-bg-tab]")].forEach((button) => {
        button.classList.toggle("active", button.dataset.bgTab === selectedBackgroundTab);
      });

      [...backgroundPillsEl.querySelectorAll(".background-pill")].forEach((button) => {
        button.classList.toggle("active", button.dataset.bgStyle === selectedBackgroundStyle);
        const style = button.dataset.bgStyle;
        const isVisible = selectedBackgroundTab === "theme"
          ? style === "theme"
          : (selectedBackgroundTab === "solid"
            ? solidStyles.includes(style)
            : gradientStyles.includes(style));
        const isOwned = style === "theme"
          || solidStyles.includes(style)
          || cosmeticInventory.backgrounds.includes(style);
        button.classList.toggle("hidden", !isVisible);
        button.disabled = !isOwned && style !== "theme";
        button.style.opacity = !isOwned && style !== "theme" ? "0.42" : "";
      });
    }

    function updatePauseButton() {
      if (!pauseButtonEl) {
        return;
      }

      pauseButtonEl.disabled = gameOver || loadingScreenEl.classList.contains("visible") || startScreenEl.classList.contains("hidden") === false;
      pauseButtonEl.classList.toggle("paused", isPaused);
      pauseButtonEl.textContent = isPaused ? "המשך משחק" : "עצור משחק";
    }

    function loadAchievements() {
      try {
        const saved = JSON.parse(localStorage.getItem("hangman_achievements") || "{}");
        if (Array.isArray(saved)) {
          achievementStore = { regular: saved };
        } else {
          achievementStore = saved || {};
        }
      } catch {
        achievementStore = {};
      }
      setActiveAchievementProfile(activeAchievementProfileKey);
    }

    function saveAchievements() {
      try {
        achievementStore[activeAchievementProfileKey] = [...unlockedAchievements];
        localStorage.setItem("hangman_achievements", JSON.stringify(achievementStore));
      } catch {
        // Ignore storage failures in local file contexts.
      }
    }

    function renderAchievementBadges() {
      const selectedTab = renderAchievementBadges.selectedTab || "all";
      const categoryMap = {
        all: () => true,
        story: (item) => ["daily_hero", "weekly_hero", "word_collector"].includes(item.id),
        tournament: (item) => ["tournament_winner", "party_star", "competition_master"].includes(item.id),
        survival: (item) => ["survivor", "flawless", "no_hint"].includes(item.id),
        kids: (item) => ["first_win", "training_finish"].includes(item.id)
      };
      const visibleAchievements = achievementDefinitions.filter((item) => (categoryMap[selectedTab] || categoryMap.all)(item));
      const unlocked = visibleAchievements.filter((item) => unlockedAchievements.has(item.id));
      const locked = visibleAchievements.filter((item) => !unlockedAchievements.has(item.id));
      const renderBadgeList = (items, isUnlocked) => items.map(({ id, icon, label, coins }) => {
        const badgeClass = isUnlocked ? "badge unlocked" : "badge locked";
        const checkMark = isUnlocked ? '<span class="badge-check">✓</span>' : "";
        return `<span class="${badgeClass}">${checkMark}${icon} ${label} • ${coins} מטבעות</span>`;
      }).join("");
      achievementBadgesEl.innerHTML = `
        <div class="achievement-tabs">
          <button class="achievement-tab${selectedTab === "all" ? " active" : ""}" data-achievement-tab="all" type="button">הכול</button>
          <button class="achievement-tab${selectedTab === "story" ? " active" : ""}" data-achievement-tab="story" type="button">סיפור</button>
          <button class="achievement-tab${selectedTab === "tournament" ? " active" : ""}" data-achievement-tab="tournament" type="button">טורניר</button>
          <button class="achievement-tab${selectedTab === "survival" ? " active" : ""}" data-achievement-tab="survival" type="button">הישרדות</button>
          <button class="achievement-tab${selectedTab === "kids" ? " active" : ""}" data-achievement-tab="kids" type="button">ילדים</button>
        </div>
        <div class="mission-item">
          <strong>מבט מסודר על ההישגים</strong><br>
          הטאב הפעיל מסנן את הרשימה כדי שתראו בנפרד את הסיפור, הטורניר, ההישרדות או מצב הילדים.
        </div>
        <div class="achievement-section">
          <div class="achievement-section-title"><span>פתוחים</span><span>${unlocked.length}</span></div>
          <div class="achievement-grid">${renderBadgeList(unlocked, true) || '<span class="badge locked">עדיין אין הישגים פתוחים</span>'}</div>
        </div>
        <div class="achievement-section">
          <div class="achievement-section-title"><span>נעולים</span><span>${locked.length}</span></div>
          <div class="achievement-grid">${renderBadgeList(locked, false)}</div>
        </div>
      `;
    }

    function unlockAchievements(ids) {
      const newlyUnlocked = [];
      let awardedCoins = 0;

      ids.forEach((id) => {
        if (!unlockedAchievements.has(id)) {
          unlockedAchievements.add(id);
          newlyUnlocked.push(id);
          const definition = achievementDefinitions.find((item) => item.id === id);
          awardedCoins += definition?.coins || 0;
        }
      });

      if (newlyUnlocked.length) {
        saveAchievements();
        renderAchievementBadges();
        if (awardedCoins > 0) {
          awardCoins(awardedCoins);
        }
        awardXp(getCurrentProfileName(), newlyUnlocked.length * 10);
      }

      lastUnlockedAchievements = newlyUnlocked;
      renderStartDashboard();
    }

    function matchesDifficulty(word, difficulty) {
      const length = wordLength(word);

      if (difficulty === "easy") {
        return length >= 3 && length <= 5;
      }

      if (difficulty === "hard") {
        return length >= (currentSelection.specialMode === "extreme" ? 9 : 8);
      }

      return length >= 5 && length <= 8;
    }

    function getFilteredWords() {
      let pool = words;
      const selectedCategories = getSelectedCategoryKeys();

      if (currentSelection.difficulty === "favorites") {
        const favoriteKeys = new Set(favoriteWords);
        pool = [
          ...words.filter((entry) => favoriteKeys.has(`${entry.category}:${sanitizeLookupValue(entry.word)}`)),
          ...customWordEntries.filter((entry) => entry.favoriteOnly && favoriteKeys.has(`${entry.category}:${sanitizeLookupValue(entry.word)}`))
        ];
      }

      if (selectedCategories.length && selectedCategories.length !== Object.keys(categoryLabels).length) {
        pool = pool.filter((entry) => selectedCategories.includes(entry.category));
      }

      if (currentSelection.specialMode === "kids") {
        const kidsCategories = new Set(["animals", "birds", "fruits", "vegetables", "food", "colors", "games"]);
        pool = pool.filter((entry) => kidsCategories.has(entry.category) && wordLength(entry.word) <= 6);
      }

      if (currentSelection.specialMode === "challenge") {
        pool = pool.filter((entry) => wordLength(entry.word) >= 6);
      }

      if (currentSelection.specialMode === "extreme") {
        pool = pool.filter((entry) => wordLength(entry.word) >= 8);
      }

      const byDifficulty = currentSelection.difficulty === "favorites"
        ? pool
        : pool.filter((entry) => matchesDifficulty(entry.word, currentSelection.difficulty));
      const unlockedPool = byDifficulty.filter((entry) => {
        const key = `${entry.category}:${sanitizeLookupValue(entry.word)}`;
        return !usedWordKeys.has(key) && !recentWordKeys.includes(key);
      });

      if (unlockedPool.length) {
        return unlockedPool;
      }

      if (byDifficulty.length) {
        return byDifficulty;
      }

      if (pool.length) {
        return pool;
      }

      const fallbackByDifficulty = currentSelection.difficulty === "favorites"
        ? pool
        : words.filter((entry) => matchesDifficulty(entry.word, currentSelection.difficulty));
      return fallbackByDifficulty.length ? fallbackByDifficulty : words;
    }

    function setMode(mode) {
      if (mode !== "online" && onlineSession.roomId) {
        resetOnlineSession();
      }
      currentMode = mode;
      currentSelection.mode = mode;
      const isTwoPlayers = mode === "two";
      const isCompetition = mode === "competition";
      const isOnline = mode === "online";
      const isTournament = mode === "tournament";
      const isParty = mode === "party";
      const isSurvival = mode === "survival";
      const isSingleLike = mode === "single" || mode === "competition" || mode === "online" || mode === "tournament" || mode === "practice" || mode === "party" || isSurvival;
      const usesTimer = mode !== "practice" && !isSurvival && !isTournament;
      const usesPlayerNames = isTwoPlayers || isCompetition || isOnline || isTournament || isParty;

      singlePlayerBtn.classList.toggle("active", mode === "single");
      twoPlayerBtn.classList.toggle("active", isTwoPlayers);
      competitionModeBtn.classList.toggle("active", mode === "competition");
      onlineModeBtn.classList.toggle("active", isOnline);
      tournamentModeBtn.classList.toggle("active", isTournament);
      partyModeBtn.classList.toggle("active", isParty);
      survivalModeBtn.classList.toggle("active", isSurvival);
      practiceModeBtn.classList.toggle("active", mode === "practice");
      singleCategorySettingEl.classList.toggle("hidden", !isSingleLike);
      singleDifficultySettingEl.classList.toggle("hidden", !isSingleLike);
      singleAvatarSettingEl.classList.toggle("hidden", isCompetition || isOnline || isParty || isTournament);
      customSettingsEl.classList.toggle("hidden", !usesPlayerNames);
      partyPlayerConfigEl.classList.toggle("hidden", !(isParty || isTournament));
      fixedPlayerConfigEl.classList.toggle("hidden", isParty || isTournament);
      twoPlayerWordSettingsEl.classList.toggle("hidden", !isTwoPlayers);
      bonusOwnerSettingEl?.classList.toggle("hidden", !(isTwoPlayers || isCompetition || isTournament || isParty));
      timeSettingEl.classList.toggle("hidden", !usesTimer);
      partyRoundsSettingEl.classList.toggle("hidden", !(isParty || isTournament));
      const roundsLabelEl = partyRoundsSettingEl?.querySelector('label[for="party-rounds-input"]');
      if (roundsLabelEl) {
        roundsLabelEl.textContent = isTournament
          ? "מספר דו-קרבים בטורניר (מ-1 עד 50)"
          : "מספר סיבובים (מ-1 עד 50)";
      }
      categorySelectEl.disabled = !isSingleLike;
      difficultySelectEl.disabled = !isSingleLike;
      timeSelectEl.disabled = !usesTimer;
      partyRoundsInputEl.disabled = !(isParty || isTournament);
      specialModePillsEl?.classList.toggle("hidden", !isSingleLike);
      setStartNote(
        isTwoPlayers
          ? "במצב שני שחקנים שחקן אחד מזין מילה ונושא, והשני מנחש."
          : (mode === "competition"
            ? "במצב תחרות שני שחקנים מתחרים על כמה שיותר מילים בזמן."
            : (mode === "online"
              ? "במצב אונליין אמיתי יוצרים חדר על השרת ושולחים קישור לשחקן השני."
            : (mode === "tournament"
              ? "במצב טורניר אפשר להגדיר 2, 4 או 8 משתתפים ולקבל בראקט ברור עד לאלוף."
            : (mode === "party"
              ? "במצב מסיבה כל אחד מקבל תור ונקודות משלו."
            : (mode === "survival"
              ? "במצב הישרדות ממשיכים עד שמפסידים, וצוברים שיא מילים."
            : (mode === "practice"
              ? "במצב אימון אין מגבלת זמן ואפשר להתאמן בשקט."
              : "במצב שחקן יחיד תיבחר מילה אקראית לפי ההגדרות."))))))
      );
      setAddWordNote();
      if (isParty) {
        renderPartyPlayerFields();
      }
      renderBonusBuyerOptions();
      renderTimePills();
      renderSpecialModePills();
      renderCategoryGrid();
      if (mode === "online" && !localOnlineRoomCode) {
        localOnlineRoomCode = onlineSession.roomId || "";
      }
      renderAvatars();
      refreshHelperPanels();
      applyVisualTheme(
        isTwoPlayers
          ? (topicLookup[sanitizeTopicValue(currentSelection.customTopic)] || sanitizeTopicValue(currentSelection.customTopic) || "custom")
          : currentSelection.category
      );
      updatePauseButton();
    }

    function populateCategoryOptions() {
      ensureUnlockedCategorySelection();
      const unlockedCategories = getUnlockedCategories();
      const options = [`<option value="all">כל הנושאים</option>`]
        .concat(
          unlockedCategories.map((value) => `<option value="${value}">${categoryIcons[value] || "•"} ${categoryLabels[value]}</option>`)
        );

      categorySelectEl.innerHTML = options.join("");
      categorySelectEl.value = currentSelection.category;
    }

    function syncSelectionFromControls() {
      currentSelection.category = categorySelectEl.value;
      currentSelection.selectedCategories = [...categoryGridEl.querySelectorAll("[data-category-chip]:checked")].map((input) => input.dataset.categoryChip);
      currentSelection.difficulty = difficultySelectEl.value;
      currentSelection.timeLimit = Number(timeSelectEl.value);
      currentSelection.partyRounds = clampPartyRounds(partyRoundsInputEl.value);
      currentSelection.partyPlayerCount = clampPartyPlayerCount(partyPlayerCountInputEl?.value);
      currentSelection.playerOneName = sanitizePlayerName(playerOneInputEl.value);
      currentSelection.playerTwoName = sanitizePlayerName(playerTwoInputEl.value);
      currentSelection.bonusBuyer = bonusOwnerSelectEl?.value || "player1";
      currentSelection.singleAvatar = currentSelection.singleAvatar || "🙂";
      currentSelection.playerOneAvatar = currentSelection.playerOneAvatar || "😎";
      currentSelection.playerTwoAvatar = currentSelection.playerTwoAvatar || "🦊";
      currentSelection.customWord = sanitizeCustomWord(customWordInputEl.value);
      currentSelection.customTopic = sanitizeCustomTopic(customTopicInputEl.value);
      if (currentSelection.specialMode === "challenge") {
        currentSelection.timeLimit = 90;
      }
      renderTimePills();
      renderDifficultyPills();
      renderSpecialModePills();
    }

    function applySelectionToControls() {
      categorySelectEl.value = currentSelection.category;
      difficultySelectEl.value = currentSelection.difficulty;
      timeSelectEl.value = String(currentSelection.timeLimit);
      partyRoundsInputEl.value = String(currentSelection.partyRounds);
      if (partyPlayerCountInputEl) {
        partyPlayerCountInputEl.value = String(currentSelection.partyPlayerCount);
      }
      playerOneInputEl.value = currentSelection.playerOneName;
      playerTwoInputEl.value = currentSelection.playerTwoName;
      if (bonusOwnerSelectEl) {
        bonusOwnerSelectEl.value = currentSelection.bonusBuyer;
      }
      customWordInputEl.value = currentSelection.customWord;
      customTopicInputEl.value = currentSelection.customTopic;
      setMode(currentSelection.mode);
      renderPartyPlayerFields();
      renderTimePills();
      renderDifficultyPills();
      renderSpecialModePills();
      renderCategoryGrid();
      renderAvatarPickers();
      renderBonusBuyerOptions();
    }

    function setStartNote(message, isAlert = false) {
      startNoteEl.textContent = message;
      startNoteEl.classList.toggle("alert", isAlert);
    }

    function showStartValidation(message, field) {
      setStartNote(message, true);
      playSound("validation");
      field?.focus();
    }

    function pickWord() {
      if (currentSelection.mode === "two") {
        const word = currentSelection.customWord;
        secretWord = word;
        activeTopicLabel = currentSelection.customTopic || "שני שחקנים";
        descriptiveHint = `זה רמז למילה שהשחקן השני בחר. יש בה ${wordLength(word)} אותיות.`;
        hint = `נושא: ${activeTopicLabel}`;
        activeTopicKey = topicLookup[sanitizeTopicValue(activeTopicLabel)] || sanitizeTopicValue(activeTopicLabel) || "custom";
        applyVisualTheme(activeTopicKey);
        return;
      }

      if (currentSelection.dailyChallenge) {
        const choice = getDailyChallengeEntry();
        if (choice) {
          secretWord = choice.word;
          descriptiveHint = buildHint(choice);
          activeTopicLabel = choice.topic;
          hint = currentSelection.specialMode === "extreme" ? "נושא: מוסתר" : `נושא: ${activeTopicLabel}`;
          activeTopicKey = choice.category || "all";
          rememberWord(choice);
          applyVisualTheme(activeTopicKey);
          return;
        }
      }

      if (currentSelection.weeklyChallenge) {
        const choice = getWeeklyChallengeEntry();
        if (choice) {
          secretWord = choice.word;
          descriptiveHint = buildHint(choice);
          activeTopicLabel = choice.topic;
          hint = currentSelection.specialMode === "extreme" ? "נושא: מוסתר" : `נושא: ${activeTopicLabel}`;
          activeTopicKey = choice.category || "all";
          rememberWord(choice);
          applyVisualTheme(activeTopicKey);
          return;
        }
      }

      if (currentSelection.specialMode === "story") {
        const chapter = storyChapters[Math.min(storyChapters.length - 1, storyProgress.chapter || 0)];
        const chapterPool = words.filter((entry) => chapter.categories.includes(entry.category) && matchesDifficulty(entry.word, chapter.difficulty));
        const choice = (chapterPool.length ? chapterPool : getFilteredWords())[Math.floor(Math.random() * (chapterPool.length ? chapterPool.length : getFilteredWords().length))];
        usedWordKeys.add(`${choice.category}:${sanitizeLookupValue(choice.word)}`);
        rememberWord(choice);
        secretWord = choice.word;
        descriptiveHint = `${chapter.text} ${buildHint(choice)}`;
        activeTopicLabel = `${choice.topic} • ${chapter.title}`;
        hint = `נושא: ${currentSelection.specialMode === "extreme" ? "מוסתר" : activeTopicLabel}`;
        activeTopicKey = choice.category || currentSelection.category || "all";
        applyVisualTheme(activeTopicKey);
        return;
      }

      const pool = getFilteredWords();
      const choice = pool[Math.floor(Math.random() * pool.length)];
      usedWordKeys.add(`${choice.category}:${sanitizeLookupValue(choice.word)}`);
      rememberWord(choice);
      secretWord = choice.word;
      descriptiveHint = buildHint(choice);
      activeTopicLabel = choice.topic;
      hint = currentSelection.specialMode === "extreme" ? "נושא: מוסתר" : `נושא: ${activeTopicLabel}`;
      activeTopicKey = choice.category || currentSelection.category || "all";
      applyVisualTheme(activeTopicKey);
    }

    function renderWord() {
      wordEl.innerHTML = "";

      for (const letter of secretWord) {
        const slot = document.createElement("div");
        slot.className = "letter-slot";
        const normalizedLetter = normalizeLetter(letter);
        const isVisible = !isHebrewLetter(letter) || guessedLetters.has(normalizedLetter) || gameOver;
        slot.textContent = isVisible ? letter : "";

        if (isVisible && lastGuessResult === "hit" && normalizedLetter === lastGuessedLetter) {
          slot.classList.add("reveal");
        }

        wordEl.appendChild(slot);
      }
    }

    function renderKeyboard() {
      keyboardEl.innerHTML = "";

      keyboardRows.forEach((rowLetters) => {
        const row = document.createElement("div");
        row.className = "keyboard-row";

        rowLetters.forEach((letter) => {
          const button = document.createElement("button");
          button.type = "button";
          button.className = "key";
          button.textContent = letter;
          button.disabled = gameOver || isPaused || guessedLetters.has(letter);

          if (guessedLetters.has(letter)) {
            button.classList.add(wordIncludesLetter(secretWord, letter) ? "correct" : "wrong");
          }

          if (letter === lastGuessedLetter && lastGuessResult) {
            button.classList.add(lastGuessResult === "hit" ? "guess-hit" : "guess-miss");
          }

          button.addEventListener("click", () => handleGuess(letter));
          row.appendChild(button);
        });

        keyboardEl.appendChild(row);
      });
    }

    function replayAnimation(element, className) {
      if (!element) {
        return;
      }

      element.classList.remove(className);
      void element.offsetWidth;
      element.classList.add(className);

      const clearClass = () => {
        element.classList.remove(className);
      };

      element.addEventListener("animationend", clearClass, { once: true });
    }

    function triggerUiTap(element) {
      if (!element) {
        return;
      }

      replayAnimation(element, "ui-tap");
    }

    function triggerUiFieldFlash(element) {
      if (!element) {
        return;
      }

      replayAnimation(element, "ui-field-flash");
    }

    function launchConfetti(amount = 22) {
      if (!celebrationEl) {
        return;
      }

      const colors = ["#ff7a59", "#19b7c6", "#ffd166", "#189a62", "#ff8fab", "#6dd3ff"];
      const pieces = Array.from({ length: amount }, (_, index) => {
        const piece = document.createElement("span");
        piece.className = "confetti-piece";
        piece.style.left = `${6 + (index % Math.max(8, Math.ceil(amount / 2))) * (88 / Math.max(7, Math.ceil(amount / 2)))}%`;
        piece.style.background = colors[index % colors.length];
        piece.style.setProperty("--x-start", `${(index % 2 === 0 ? -1 : 1) * (10 + index * 2.4)}px`);
        piece.style.setProperty("--y-start", "8px");
        piece.style.setProperty("--x-end", `${(index - amount / 2) * 15}px`);
        piece.style.setProperty("--y-end", `${-96 - (index % 5) * 20}px`);
        piece.style.setProperty("--spin", `${(index % 2 === 0 ? 1 : -1) * (170 + index * 22)}deg`);
        return piece;
      });

      celebrationEl.replaceChildren(...pieces);

      requestAnimationFrame(() => {
        pieces.forEach((piece) => piece.classList.add("pop"));
      });

      window.setTimeout(() => {
        celebrationEl.replaceChildren();
      }, 1100);
    }

    function updateStats() {
      wrongCountEl.textContent = `${wrongGuesses} / ${maxWrong}`;
      scoreEl.textContent = (currentSelection.mode === "competition" || currentSelection.mode === "online")
        ? String(competitionScore)
        : (currentSelection.mode === "party"
          ? partyScores.join(":")
          : (currentSelection.mode === "tournament"
            ? `${partyScores[0] || 0}:${partyScores[1] || 0}`
            : (currentSelection.mode === "survival" ? String(competitionScore) : "-")));
      usedLettersEl.textContent = guessedLetters.size ? [...guessedLetters].join(" ,") : "-";
      hintEl.textContent = hintVisible && usedHint ? descriptiveHint : hint;
      hintEl.classList.add("open");
      hintButtonEl.disabled = isPaused || gameOver || currentSelection.specialMode === "extreme" || currentSelection.specialMode === "challenge";
      hintButtonEl.textContent = currentSelection.specialMode === "extreme"
        ? "אין רמז במצב קשה באמת"
        : (currentSelection.specialMode === "challenge"
          ? "אין רמז במצב אתגר"
        : (!usedHint
          ? "פתח רמז"
          : (hintVisible ? "סגור רמז" : "פתח רמז")));
      updatePauseButton();
      updateBonusButtons();

      // עדכון כרטיס נושא חזותי
      const topicHintCard = document.getElementById("topic-hint-card");
      const topicHintIcon = document.getElementById("topic-hint-icon");
      const topicHintLabel = document.getElementById("topic-hint-label");
      if (topicHintIcon && topicHintLabel) {
        const icon = categoryIcons[activeTopicKey] || "🎲";
        const label = activeTopicLabel || "כללי";
        topicHintIcon.textContent = icon;
        topicHintLabel.textContent = label;
        // אנימציה מחדש
        if (topicHintCard) {
          topicHintCard.style.animation = "none";
          topicHintCard.offsetHeight;
          topicHintCard.style.animation = "";
        }
      }

      parts.forEach((part) => {
        const step = Number(part.dataset.step);
        part.classList.toggle("visible", step <= wrongGuesses);
      });

      renderLiveBoard();
    }

    function getUnrevealedLetters() {
      const unrevealed = [...new Set([...secretWord]
        .filter((letter) => isHebrewLetter(letter))
        .map((letter) => normalizeLetter(letter))
        .filter((letter) => !guessedLetters.has(letter)))];

      return unrevealed;
    }

    function finishWin() {
      const achievementsToUnlock = ["first_win"];
      const streakBonus = streakCount >= 2 ? 2 : 0;

      if (!usedHint) {
        achievementsToUnlock.push("no_hint");
      }

      if (wrongGuesses === 0) {
        achievementsToUnlock.push("flawless");
      }

      if (currentSelection.mode === "practice") {
        achievementsToUnlock.push("training_finish");
      }

      if (["competition", "online"].includes(currentSelection.mode)) {
        gameOver = true;
        clearTimer();
        competitionScore += 1 + streakBonus;
        streakCount += 1;

        if (competitionScore >= 3) {
          achievementsToUnlock.push("competition_master");
        }

        unlockAchievements(achievementsToUnlock);
        advanceWeeklyMission("weekly_win_3");
        messageEl.textContent = "מדהים! עוברים למילה הבאה.";
        messageEl.className = "message win";
        replayAnimation(wordEl, "burst");
        replayAnimation(messageEl, "guess-hit");
        launchConfetti(36 + getDifficultyConfettiBonus());
        playSound("win");
        renderKeyboard();
        updateStats();
        showWordResultScreen({
          type: "win",
          icon: currentSelection.mode === "online" ? "🛰️" : "🎉",
          title: currentSelection.mode === "online" ? "חדר האונליין התקדם!" : "מילה אחת הושלמה!",
          subtitle: currentSelection.mode === "online"
            ? `${currentSelection.playerOneName || "שחקן 1"} מול ${currentSelection.playerTwoName || "שחקן 2"} עלו עוד נקודה בחדר ${localOnlineRoomCode || "------"}.`
            : `${currentSelection.playerOneName || "שחקן 1"} ו-${currentSelection.playerTwoName || "שחקן 2"} פתרו עוד מילה.`,
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "למילה הבאה",
          onContinue: () => {
            hideEndScreen();
            startNextCompetitionRound();
          }
        });
        return;
      }

      if (currentSelection.mode === "party") {
        gameOver = true;
        clearTimer();
        partyScores[activeTurn] += 1 + streakBonus;
        streakCount += 1;
        if (partyScores[activeTurn] >= 3) {
          achievementsToUnlock.push("party_star");
        }
        unlockAchievements(achievementsToUnlock);
        advanceWeeklyMission("weekly_win_3");
        messageEl.textContent = `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} לקח את הסיבוב!`;
        messageEl.className = "message win";
        replayAnimation(wordEl, "burst");
        replayAnimation(messageEl, "guess-hit");
        launchConfetti(40 + getDifficultyConfettiBonus());
        playSound("win");
        renderKeyboard();
        updateStats();
        showWordResultScreen({
          type: "win",
          icon: "🌟",
          title: `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} ניצח במילה!`,
          subtitle: "עוד רגע עוברים למשתתף הבא במסיבה.",
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "למשתתף הבא",
          onContinue: () => {
            hideEndScreen();
            startNextPartyRound();
          }
        });
        return;
      }

      if (currentSelection.mode === "tournament") {
        gameOver = true;
        clearTimer();
        partyScores[activeTurn] += 1;
        const opponentIndex = (activeTurn + 1) % Math.max(1, partyPlayers.length);
        tournamentBracket.push({
          round: Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(1, partyPlayers.length))),
          winner: partyPlayers[activeTurn]?.name || "שחקן",
          loser: partyPlayers[opponentIndex]?.name || "שחקן"
        });
        unlockAchievements(["tournament_winner"]);
        advanceWeeklyMission("weekly_win_3");
        messageEl.textContent = `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} ניצח בסיבוב הטורניר!`;
        messageEl.className = "message win";
        replayAnimation(wordEl, "burst");
        replayAnimation(messageEl, "guess-hit");
        launchConfetti(34 + getDifficultyConfettiBonus());
        playSound("win");
        renderKeyboard();
        updateStats();
        showWordResultScreen({
          type: "win",
          icon: "👑",
          title: `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} לקח את הסיבוב`,
          subtitle: "עוד רגע עוברים לסיבוב הבא בטורניר.",
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "לסיבוב הבא",
          onContinue: () => {
            hideEndScreen();
            startNextTournamentRound();
          }
        });
        return;
      }

      if (currentSelection.mode === "survival") {
        gameOver = true;
        clearTimer();
        competitionScore += 1 + streakBonus;
        streakCount += 1;
        if (competitionScore >= 3) {
          achievementsToUnlock.push("survivor");
        }
        unlockAchievements(achievementsToUnlock);
        if (competitionScore >= 2) {
          advanceWeeklyMission("weekly_survival_2");
        }
        messageEl.textContent = "שורדים עוד מילה. ממשיכים מיד.";
        messageEl.className = "message win";
        replayAnimation(wordEl, "burst");
        replayAnimation(messageEl, "guess-hit");
        launchConfetti(28 + getDifficultyConfettiBonus());
        playSound("win");
        renderKeyboard();
        updateStats();
        showWordResultScreen({
          type: "win",
          icon: "🛡️",
          title: "עוד מילה שרדתם!",
          subtitle: `סך הכול שרדתם ${competitionScore} מילים עד עכשיו.`,
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "להמשיך לשרוד",
          onContinue: () => {
            hideEndScreen();
            startNextCompetitionRound();
          }
        });
        return;
      }

      gameOver = true;
      clearTimer();
      messageEl.textContent = "כל הכבוד! גילית את המילה.";
      messageEl.className = "message win";
      renderWord();
      renderKeyboard();
      updateStats();
      unlockAchievements(achievementsToUnlock);
      replayAnimation(wordEl, "burst");
      replayAnimation(messageEl, "guess-hit");
      awardCoins(currentSelection.mode === "two" ? 1 : 2);
      completeDailyMission("win_one");
      advanceWeeklyMission("weekly_win_3");
      updateModeRecord(currentSelection.mode, 1, "ניצחון מלא במשחק אחד");
      recordResultForPlayers("win");
      if (currentSelection.specialMode === "story") {
        storyProgress.completed = (storyProgress.completed || 0) + 1;
        storyProgress.chapter = Math.min(storyChapters.length - 1, storyProgress.chapter + 1);
        saveStoryProgress();
        renderStoryCard();
        renderStoryMap();
        advanceWeeklyMission("weekly_story_2");
      }
      if (currentSelection.dailyChallenge) {
        unlockAchievements(["daily_hero"]);
        dailyChallengeState = { dateKey: getTodayKey(), wordKey: sanitizeLookupValue(secretWord) };
        saveDailyChallengeState();
        renderDailyChallenge();
      }
      if (currentSelection.weeklyChallenge) {
        unlockAchievements(["weekly_hero"]);
        weeklyChallengeState = { weekKey: getWeekKey(), wordKey: sanitizeLookupValue(secretWord) };
        saveWeeklyChallengeState();
        renderWeeklyChallenge();
      }
      launchConfetti((currentSelection.mode === "two" ? 44 : 58) + getDifficultyConfettiBonus());
      playSound("win");
      showEndScreen("win");
    }

    function finishLoss(reason = `הפעם הפסדת. המילה הייתה: ${secretWord}`) {
      gameOver = true;
      clearTimer();
      messageEl.textContent = reason;
      messageEl.className = "message lose";
      renderWord();
      renderKeyboard();
      updateStats();
      playSound("lose");
      replayAnimation(messageEl, "guess-miss");
      replayAnimation(hangmanBoxEl, "guess-miss");
      replayAnimation(hangmanFigureEl, "explode");
      updateModeRecord(currentSelection.mode, 0, "המשחק הסתיים בלי ניצחון");
      recordResultForPlayers("lose");
      window.clearTimeout(endScreenTimer);
      endScreenTimer = window.setTimeout(() => showEndScreen("lose"), 900);
    }

    function finishCompetition() {
      gameOver = true;
      clearTimer();
      window.clearTimeout(freezeTimerId);
      messageEl.textContent = currentSelection.mode === "online"
        ? `חדר האונליין הסתיים עם ${competitionScore} מילים.`
        : `התחרות הסתיימה עם ${competitionScore} מילים.`;
      messageEl.className = competitionScore > 0 ? "message win" : "message lose";
      renderWord();
      renderKeyboard();
      updateStats();
      if (competitionScore >= 3) {
        unlockAchievements(["competition_master"]);
      } else {
        lastUnlockedAchievements = [];
      }
      if (competitionScore > 0) {
        awardCoins(competitionScore);
        advanceWeeklyMission("weekly_win_3");
        updateModeRecord(currentSelection.mode === "online" ? "online" : "competition", competitionScore, `${competitionScore} מילים ${currentSelection.mode === "online" ? "בחדר אונליין" : "בתחרות"}`);
        recordResultForPlayers("win");
        launchConfetti(48 + getDifficultyConfettiBonus());
        playSound("win");
        saveLeaderboardEntry();
        showEndScreen("win");
      } else {
        playSound("lose");
        updateModeRecord(currentSelection.mode === "online" ? "online" : "competition", 0, currentSelection.mode === "online" ? "חדר אונליין ללא נקודות" : "תחרות ללא נקודות");
        recordResultForPlayers("lose");
        saveLeaderboardEntry();
        showEndScreen("lose");
      }
    }

    function finishParty() {
      gameOver = true;
      clearTimer();
      window.clearTimeout(freezeTimerId);
      messageEl.textContent = "המסיבה הסתיימה!";
      messageEl.className = "message win";
      awardCoins(Math.max(...partyScores, 0));
      updateModeRecord("party", Math.max(...partyScores, 0), `שיא מסיבה: ${Math.max(...partyScores, 0)} נק'`);
      recordResultForPlayers("win");
      saveLeaderboardEntry();
      showEndScreen("win");
    }

    function finishTournament() {
      gameOver = true;
      clearTimer();
      messageEl.textContent = "הטורניר הסתיים!";
      messageEl.className = "message win";
      awardCoins(Math.max(...partyScores, 0) + 2);
      updateModeRecord("tournament", Math.max(...partyScores, 0), `שיא טורניר: ${partyPlayers.map((player, index) => `${player.name} ${partyScores[index] || 0}`).join(" • ")}`);
      recordResultForPlayers("win");
      saveLeaderboardEntry();
      showEndScreen("win");
    }

    function startNextCompetitionRound() {
      if (!["competition", "online", "survival"].includes(currentSelection.mode) || (["competition", "online"].includes(currentSelection.mode) && remainingTime <= 0)) {
        finishCompetition();
        return;
      }

      transitionWithLoading(() => {
        guessedLetters = new Set();
        wrongGuesses = 0;
        usedHint = false;
        hintVisible = false;
        lastGuessResult = null;
        lastGuessedLetter = "";
        celebrationEl?.replaceChildren();
        hangmanFigureEl?.classList.remove("explode");
        wordEl.classList.remove("burst");
        hangmanBoxEl.classList.remove("guess-miss");
        messageEl.classList.remove("guess-hit", "guess-miss");
        pickWord();
        renderWord();
        renderKeyboard();
        updateStats();
      }, {
        duration: 120,
        title: currentSelection.mode === "survival" ? "הישרדות נמשכת..." : "מילה חדשה בדרך...",
        subtitle: currentSelection.mode === "survival" ? "אין עצירה, ממשיכים לשרוד." : "ממשיכים ישר לסיבוב הבא."
      });
    }

    function startNextPartyRound() {
      ensurePartyPlayers();
      if (currentSelection.mode !== "party" || remainingTime <= 0 || partyRoundCount >= currentSelection.partyRounds * partyPlayers.length) {
        finishParty();
        return;
      }

      partyRoundCount += 1;
      activeTurn = (activeTurn + 1) % partyPlayers.length;
      transitionWithLoading(() => {
        guessedLetters = new Set();
        wrongGuesses = 0;
        usedHint = false;
        hintVisible = false;
        lastGuessResult = null;
        lastGuessedLetter = "";
        celebrationEl?.replaceChildren();
        hangmanFigureEl?.classList.remove("explode");
        wordEl.classList.remove("burst");
        hangmanBoxEl.classList.remove("guess-miss");
        messageEl.classList.remove("guess-hit", "guess-miss");
        pickWord();
        renderWord();
        renderKeyboard();
        updateStats();
        renderAvatars();
      }, {
        duration: 130,
        title: "עוברים למשתתף הבא...",
        subtitle: `${(partyPlayers[activeTurn] || { name: "משתתף" }).name}, זה התור שלך.`
      });
    }

    function startNextTournamentRound() {
      ensurePartyPlayers();
      if (currentSelection.mode !== "tournament" || partyRoundCount >= currentSelection.partyRounds * Math.max(1, partyPlayers.length)) {
        finishTournament();
        return;
      }

      partyRoundCount += 1;
      activeTurn = (activeTurn + 1) % Math.max(1, partyPlayers.length);
      transitionWithLoading(() => {
        guessedLetters = new Set();
        wrongGuesses = 0;
        usedHint = false;
        hintVisible = false;
        lastGuessResult = null;
        lastGuessedLetter = "";
        celebrationEl?.replaceChildren();
        hangmanFigureEl?.classList.remove("explode");
        wordEl.classList.remove("burst");
        hangmanBoxEl.classList.remove("guess-miss");
        messageEl.classList.remove("guess-hit", "guess-miss");
        pickWord();
        renderWord();
        renderKeyboard();
        updateStats();
        renderAvatars();
      }, {
        duration: 130,
        title: "עוברים לסיבוב הבא בטורניר...",
        subtitle: `${(partyPlayers[activeTurn] || { name: "שחקן" }).name}, זה הרגע שלך בדרך לגביע.`
      });
    }

    function checkGameState() {
      if (wordSolved(secretWord)) {
        finishWin();
        return;
      }

      if (currentSelection.mode === "practice") {
        return;
      }

      if (wrongGuesses >= maxWrong && currentSelection.mode === "party") {
        streakCount = 0;
        gameOver = true;
        clearTimer();
        renderKeyboard();
        updateStats();
        playSound("lose");
        replayAnimation(messageEl, "guess-miss");
        replayAnimation(hangmanBoxEl, "guess-miss");
        replayAnimation(hangmanFigureEl, "explode");
        showWordResultScreen({
          type: "lose",
          icon: "🌧️",
          title: `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} לא הצליח במילה הזו`,
          subtitle: "לא נורא, במסיבה תמיד מגיע תור חדש.",
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "למשתתף הבא",
          onContinue: () => {
            hideEndScreen();
            startNextPartyRound();
          }
        });
        return;
      }

      if (wrongGuesses >= maxWrong && currentSelection.mode === "tournament") {
        const opponentIndex = (activeTurn + 1) % Math.max(1, partyPlayers.length);
        partyScores[opponentIndex] += 1;
        tournamentBracket.push({
          round: Math.max(1, Math.ceil(Math.max(partyRoundCount, 1) / Math.max(1, partyPlayers.length))),
          winner: partyPlayers[opponentIndex]?.name || "שחקן",
          loser: partyPlayers[activeTurn]?.name || "שחקן"
        });
        gameOver = true;
        clearTimer();
        renderKeyboard();
        updateStats();
        playSound("lose");
        replayAnimation(messageEl, "guess-miss");
        replayAnimation(hangmanBoxEl, "guess-miss");
        replayAnimation(hangmanFigureEl, "explode");
        showWordResultScreen({
          type: "lose",
          icon: "🌧️",
          title: `${(partyPlayers[activeTurn] || { name: "שחקן" }).name} פספס את הסיבוב`,
          subtitle: `${(partyPlayers[opponentIndex] || { name: "שחקן" }).name} לקח את הסיבוב הזה בבראקט.`,
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "המשך טורניר",
          onContinue: () => {
            hideEndScreen();
            startNextTournamentRound();
          }
        });
        return;
      }

      if (wrongGuesses >= maxWrong && (currentSelection.mode === "competition" || currentSelection.mode === "online")) {
        streakCount = 0;
        gameOver = true;
        clearTimer();
        renderKeyboard();
        updateStats();
        playSound("lose");
        replayAnimation(messageEl, "guess-miss");
        replayAnimation(hangmanBoxEl, "guess-miss");
        replayAnimation(hangmanFigureEl, "explode");
        showWordResultScreen({
          type: "lose",
          icon: "🌧️",
          title: "המילה הזו הוחמצה",
          subtitle: currentSelection.mode === "online" ? "מכאן עוברים ישר לסיכום חדר האונליין." : "מכאן עוברים ישר לסיכום התחרות.",
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: currentSelection.mode === "online" ? "לסיכום החדר" : "לסיכום התחרות",
          onContinue: () => {
            hideEndScreen();
            finishCompetition();
          }
        });
        return;
      }

      if (wrongGuesses >= maxWrong && currentSelection.mode === "survival") {
        gameOver = true;
        clearTimer();
        renderKeyboard();
        updateStats();
        playSound("lose");
        replayAnimation(messageEl, "guess-miss");
        replayAnimation(hangmanBoxEl, "guess-miss");
        replayAnimation(hangmanFigureEl, "explode");
        showWordResultScreen({
          type: "lose",
          icon: "🌧️",
          title: "כאן מסע ההישרדות נעצר",
          subtitle: "עוד רגע תראו את סיכום ההישרדות שלכם.",
          endWord: `המילה הייתה: ${secretWord}`,
          buttonLabel: "לסיכום ההישרדות",
          onContinue: () => {
            hideEndScreen();
            finishLoss(`הפעם הפסדת. המילה הייתה: ${secretWord}`);
          }
        });
        return;
      }

      if (wrongGuesses >= maxWrong) {
        finishLoss(`הפעם הפסדת. המילה הייתה: ${secretWord}`);
      }
    }

    function showEndScreen(type) {
      if (!endScreenEl) {
        return;
      }

      endScreenEl.className = `end-screen visible ${type}`;

      let title = "";
      let subtitle = "";
      let endWord = "";

      if (type === "win") {
        endIconEl.textContent = currentSelection.mode === "competition" ? "🏁" : (currentSelection.mode === "online" ? "🛰️" : (currentSelection.mode === "party" ? "👑" : (currentSelection.mode === "tournament" ? "🏆" : "🎉")));
        if (currentSelection.mode === "competition") {
          title = "התחרות הסתיימה!";
          subtitle = `${currentSelection.playerOneName || "שחקן 1"} מול ${currentSelection.playerTwoName || "שחקן 2"} פתרו ${competitionScore} מילים בזמן.`;
          endWord = `שיא בסבב הזה: ${competitionScore}`;
      } else if (currentSelection.mode === "online") {
          title = "החדר האונליין הושלם!";
          subtitle = `${currentSelection.playerOneName || "שחקן 1"} מול ${currentSelection.playerTwoName || "שחקן 2"} סיימו בהצלחה את חדר ${onlineSession.roomId || localOnlineRoomCode || "------"}.`;
          endWord = `המילה בחדר הייתה: ${secretWord}`;
        } else if (currentSelection.mode === "tournament") {
          const bestScore = Math.max(...partyScores, 0);
          const winners = partyPlayers.filter((_, index) => partyScores[index] === bestScore);
          title = winners.length > 1 ? "יש תיקו בטורניר!" : `${winners[0]?.name || "שחקן"} הוא אלוף הטורניר!`;
          subtitle = partyPlayers.map((player, index) => `${player.name}: ${partyScores[index] || 0}`).join(" • ");
          endWord = `בבראקט נרשמו ${tournamentBracket.length} דו-קרבים.`;
        } else if (currentSelection.mode === "party") {
          const bestScore = Math.max(...partyScores, 0);
          const winners = partyPlayers.filter((_, index) => partyScores[index] === bestScore);
          title = winners.length > 1
            ? "יש תיקו במסיבה!"
            : `${winners[0]?.name || "שחקן"} ניצח במסיבה!`;
          subtitle = partyPlayers.map((player, index) => `${player.name}: ${partyScores[index] || 0}`).join(" | ");
          endWord = `לוח ניקוד: ${partyScores.join(":")}`;
        } else if (currentSelection.mode === "survival") {
          title = "הישרדות הסתיימה בניצחון!";
          subtitle = `שרדתם ${competitionScore} מילים רצופות.`;
          endWord = `המילה האחרונה הייתה: ${secretWord}`;
        } else if (currentSelection.mode === "two") {
          title = "ניחשתם את המילה, ניצחתם!";
          subtitle = `${currentSelection.playerTwoName || "שחקן 2"} ניצח. החזירו ל-${currentSelection.playerOneName || "שחקן 1"} כדי להזין מילה ונושא חדשים.`;
          endWord = `המילה הייתה: ${secretWord}`;
        } else {
          title = "ניחשתם את המילה, ניצחתם!";
          subtitle = "כל הכבוד, שחקו שוב.";
          endWord = `המילה הייתה: ${secretWord}`;
        }
      } else {
        endIconEl.textContent = "🌧️";
        title = currentSelection.mode === "competition" ? "התחרות נגמרה" : (currentSelection.mode === "online" ? "חדר האונליין נסגר" : (currentSelection.mode === "tournament" ? "הטורניר נגמר" : "לא נורא, אולי בפעם הבאה תצליחו"));
        subtitle = currentSelection.mode === "competition"
          ? `${currentSelection.playerOneName || "שחקן 1"} ו-${currentSelection.playerTwoName || "שחקן 2"} הגיעו ל-${competitionScore} מילים הפעם.`
          : (currentSelection.mode === "online"
            ? `${currentSelection.playerOneName || "שחקן 1"} מול ${currentSelection.playerTwoName || "שחקן 2"} סיימו את חדר ${onlineSession.roomId || localOnlineRoomCode || "------"}.`
          : (currentSelection.mode === "tournament"
            ? `${partyPlayers[0]?.name || "שחקן 1"}: ${partyScores[0] || 0} • ${partyPlayers[1]?.name || "שחקן 2"}: ${partyScores[1] || 0}`
          : (currentSelection.mode === "survival"
            ? `הצלחתם לשרוד ${competitionScore} מילים לפני ההפסד.`
          : (currentSelection.mode === "two"
            ? `אולי בפעם הבאה ${currentSelection.playerTwoName || "שחקן 2"} יצליח.`
            : "נסו שוב במשחק הבא."))));
        endWord = `המילה הייתה: ${secretWord}`;
      }

      endTitleEl.textContent = title;
      endSubtitleEl.textContent = `${subtitle} ${type === "win" ? getAvatarFlavorLineForContext("win") : getAvatarFlavorLineForContext("lose")}`;
      endWordEl.textContent = endWord;
      renderEndSummary(type);
      endAchievementEl.innerHTML = lastUnlockedAchievements.length
        ? lastUnlockedAchievements.map((id) => {
          const achievement = achievementDefinitions.find((item) => item.id === id);
          return achievement ? `<span class="end-achievement-chip">${achievement.icon} ${achievement.label} +${achievement.coins}</span>` : "";
        }).filter(Boolean).join("")
        : "";
      endButtonEl.textContent = currentSelection.mode === "two" ? "מילה חדשה" : (currentSelection.mode === "tournament" ? "טורניר חדש" : "שחק שוב");
    }

    function hideEndScreen() {
      if (!endScreenEl) {
        return;
      }

      pendingEndAction = null;
      endScreenEl.className = "end-screen";
    }

    function hideStartScreen() {
      startScreenEl.classList.add("hidden");
    }

    function showLoadingScreen(options = {}) {
      const details = modeDetails[currentSelection.mode] || modeDetails.single;
      loadingScreenEl.dataset.mode = currentSelection.mode;
      loadingTitleEl.textContent = options.title || details.loadingTitle;
      loadingSubtitleEl.textContent = options.subtitle || (currentSelection.mode === "two"
        ? `${currentSelection.playerTwoName || "שחקן 2"}, זה הזמן שלך להתחיל לנחש.`
        : (currentSelection.mode === "competition"
          ? `${currentSelection.playerOneName || "שחקן 1"} ו-${currentSelection.playerTwoName || "שחקן 2"}, התחרות מתחילה עכשיו.`
          : (currentSelection.mode === "online"
            ? `${currentSelection.playerOneName || "שחקן 1"} ו-${currentSelection.playerTwoName || "שחקן 2"}, חדר ${onlineSession.roomId || localOnlineRoomCode || "------"} מוכן.`
          : (currentSelection.mode === "tournament"
            ? `${(partyPlayers[activeTurn] || { name: currentSelection.playerOneName || "שחקן 1" }).name}, הטורניר מתחיל איתך.`
          : (currentSelection.mode === "party"
            ? `מתחילים עם ${(partyPlayers[activeTurn] || { name: "שחקן" }).name}.`
            : details.loadingSubtitle)))));
      loadingScreenEl.classList.add("visible");
    }

    function hideLoadingScreen() {
      loadingScreenEl.classList.remove("visible");
    }

    function showNightIntroScreen() {
      if (!nightIntroScreenEl) {
        return;
      }
      nightIntroVisible = true;
      hideLoadingScreen();
      hideEndScreen();
      startScreenEl.classList.add("hidden");
      nightIntroScreenEl.classList.add("visible");
    }

    function hideNightIntroScreen() {
      if (!nightIntroScreenEl) {
        return;
      }
      nightIntroVisible = false;
      nightIntroScreenEl.classList.remove("visible");
      nightModeEnabled = false;
      document.body.classList.remove("night-mode");
      if (nightModeButtonEl) {
        nightModeButtonEl.textContent = "מצב לילה";
      }
      saveNightMode();
      showStartScreen();
    }

    function transitionWithLoading(callback, options = {}) {
      window.clearTimeout(loadingTimer);
      hideEndScreen();
      showLoadingScreen(options);
      loadingTimer = window.setTimeout(() => {
        hideLoadingScreen();
        callback();
      }, options.duration || 120);
    }

    function showStartScreen() {
      window.clearTimeout(loadingTimer);
      window.clearTimeout(freezeTimerId);
      clearTimer();
      if (onlinePollId) {
        window.clearInterval(onlinePollId);
        onlinePollId = null;
      }
      isPaused = false;
      nightIntroVisible = false;
      closeProfilesPanel();
      hideLoadingScreen();
      hideEndScreen();
      nightIntroScreenEl?.classList.remove("visible");
      applySelectionToControls();
      startScreenEl.classList.remove("hidden");
      messageEl.textContent = getAvatarFlavorLineForContext("start");
      messageEl.className = "message";
      applyVisualTheme(currentSelection.category);
      updatePauseButton();
      updateBonusButtons();
      renderShop();
    }

    function handleGuess(letter) {
      const normalizedLetter = normalizeLetter(letter);

      if (gameOver || isPaused || guessedLetters.has(normalizedLetter)) {
        return;
      }

      if (currentSelection.mode === "online" && onlineSession.active && onlineSession.role === "guest") {
        sendOnlineGuess(normalizedLetter);
        return;
      }

      guessedLetters.add(normalizedLetter);

      const isHit = wordIncludesLetter(secretWord, normalizedLetter);
      let shieldBlocked = false;
      lastGuessedLetter = normalizedLetter;
      lastGuessResult = isHit ? "hit" : "miss";

      if (!isHit) {
        if (shieldActive) {
          shieldActive = false;
          shieldBlocked = true;
          messageEl.textContent = "המגן הציל את הטעות הזאת.";
          messageEl.className = "message guess-hit";
        } else {
          wrongGuesses = currentSelection.mode === "practice"
            ? Math.min(maxWrong, wrongGuesses + 1)
            : wrongGuesses + 1;
        }
      }

      if (!shieldBlocked) {
        messageEl.textContent = getDifficultyMessage(isHit);
        messageEl.className = "message";
      }

      renderWord();
      renderKeyboard();
      updateStats();

      replayAnimation(messageEl, isHit ? "guess-hit" : "guess-miss");
      playSound(isHit ? "correct" : "wrong");

      if (isHit) {
        replayAnimation(wordEl, "burst");
        launchConfetti((currentSelection.mode === "competition" || currentSelection.mode === "online" ? 28 : 22) + getDifficultyConfettiBonus());
      } else {
        replayAnimation(hangmanBoxEl, "guess-miss");
      }

      checkGameState();
    }

    function useHint() {
      if (isPaused || gameOver || currentSelection.specialMode === "extreme" || currentSelection.specialMode === "challenge") {
        return;
      }

      if (!usedHint) {
        usedHint = true;
        hintVisible = true;
        completeDailyMission("use_hint");
      } else {
        hintVisible = !hintVisible;
      }

      lastGuessedLetter = "";
      lastGuessResult = "hit";
      messageEl.textContent = hintVisible ? "הרמז נפתח." : "הרמז נסגר.";
      messageEl.className = "message";
      updateStats();
      replayAnimation(hintEl, "ui-field-flash");
      replayAnimation(messageEl, "guess-hit");
      playSound("correct");
    }

    function startGame() {
      window.clearTimeout(loadingTimer);
      window.clearTimeout(freezeTimerId);
      if (currentSelection.mode === "online" && onlineSession.active && onlineSession.role === "guest") {
        guessedLetters = new Set();
        wrongGuesses = 0;
        gameOver = false;
        isPaused = false;
        usedHint = false;
        hintVisible = false;
        lastGuessResult = null;
        lastGuessedLetter = "";
        hideLoadingScreen();
        hideEndScreen();
        renderWord();
        renderKeyboard();
        updateStats();
        renderAvatars();
        renderOnlineRoomCard();
        renderStoryCard();
        renderAmbientLayer();
        startOnlinePolling();
        return;
      }
      guessedLetters = new Set();
      wrongGuesses = 0;
      gameOver = false;
      isPaused = false;
      usedHint = false;
      hintVisible = false;
      freezeAvailable = false;
      extraLifeAvailable = false;
      shieldActive = false;
      streakCount = 0;
      competitionScore = 0;
      ensurePartyPlayers();
      partyScores = Array.from({ length: partyPlayers.length }, () => 0);
      partyRoundCount = currentSelection.mode === "party" || currentSelection.mode === "tournament" ? 1 : 0;
      tournamentBracket = [];
      activeTurn = 0;
      gameTimeLimit = currentSelection.timeLimit;
      lastUnlockedAchievements = [];
      lastGuessResult = null;
      lastGuessedLetter = "";
      window.clearTimeout(endScreenTimer);
      clearTimer();
      celebrationEl?.replaceChildren();
      hideLoadingScreen();
      hideEndScreen();
      hangmanFigureEl?.classList.remove("explode");
      wordEl.classList.remove("burst");
      hangmanBoxEl.classList.remove("guess-miss");
      messageEl.classList.remove("guess-hit", "guess-miss");
      pickWord();
      messageEl.textContent = "בחר אות מהמקלדת או לחץ על כפתור רמז.";
      if (currentSelection.specialMode === "kids") {
        messageEl.textContent = "מצב ילדי פעיל. מתחילים בכיף, ברוגע ובחיוך גדול.";
        shieldActive = true;
        if (currentSelection.mode !== "practice") {
          gameTimeLimit = currentSelection.timeLimit + 60;
        }
        const kidsReveal = getUnrevealedLetters()[0];
        if (kidsReveal) {
          guessedLetters.add(kidsReveal);
        }
      } else if (currentSelection.specialMode === "story") {
        const chapter = storyChapters[Math.min(storyChapters.length - 1, storyProgress.chapter || 0)];
        messageEl.textContent = `מצב סיפור פעיל. פרק ${Math.min(storyChapters.length, (storyProgress.chapter || 0) + 1)}: ${chapter.title}`;
      } else if (currentSelection.specialMode === "challenge") {
        messageEl.textContent = "מצב אתגר פעיל. 90 שניות, בלי רמזים, ומילים קשות יותר.";
      } else if (currentSelection.specialMode === "extreme") {
        messageEl.textContent = "מצב קשה באמת פעיל. אין רמז והנושאים מוסתרים.";
      } else {
        messageEl.textContent = getAvatarFlavorLineForContext("start");
      }
      messageEl.className = "message";
      renderWord();
      renderKeyboard();
      startTimer();
      updateStats();
      updatePauseButton();
      updateBonusButtons();
      renderAvatars();
      renderOnlineRoomCard();
      renderStoryCard();
      renderAmbientLayer();
    }

    function startConfiguredGame() {
      syncSelectionFromControls();
      currentSelection.dailyChallenge = false;
      currentSelection.weeklyChallenge = false;

      if (currentSelection.mode === "party" || currentSelection.mode === "tournament") {
        ensurePartyPlayers();
        if (partyPlayers.some((player) => !sanitizePlayerName(player.name))) {
          showStartValidation(currentSelection.mode === "tournament" ? "כתבו שמות לכל משתתפי הטורניר כדי להתחיל." : "כתבו שמות לכל המשתתפים במסיבה כדי להתחיל.", partyPlayerCountInputEl);
          return;
        }
      }

      if (["competition", "online"].includes(currentSelection.mode) && (!currentSelection.playerOneName || !currentSelection.playerTwoName)) {
        showStartValidation("כתבו שמות לשני השחקנים כדי להתחיל.", !currentSelection.playerOneName ? playerOneInputEl : playerTwoInputEl);
        return;
      }

      if (currentSelection.mode === "two") {
        if (wordLength(currentSelection.customWord) < 2 && !currentSelection.customTopic) {
          showStartValidation("כתבו גם מילה וגם נושא כדי להתחיל במצב שני שחקנים.", customWordInputEl);
          return;
        }

        if (wordLength(currentSelection.customWord) < 2) {
          showStartValidation("כתבו מילה בעברית כדי להתחיל במצב שני שחקנים.", customWordInputEl);
          return;
        }

        if (!currentSelection.customTopic) {
          showStartValidation("כתבו גם נושא כדי להתחיל במצב שני שחקנים.", customTopicInputEl);
          return;
        }

        const knownTopics = wordTopicLookup.get(sanitizeLookupValue(currentSelection.customWord));
        const normalizedTopic = sanitizeTopicValue(currentSelection.customTopic);

        if (!knownTopics) {
          showStartValidation("הנושא לא מתאים למילה שכתבת.", customTopicInputEl);
          return;
        }

        if (!knownTopics.has(normalizedTopic)) {
          showStartValidation("הנושא לא מתאים למילה שכתבת.", customTopicInputEl);
          return;
        }
      }

      if (currentSelection.mode === "online") {
        createOnlineRoom();
        renderOnlineRoomCard();
        return;
      }

      hideStartScreen();
      transitionWithLoading(() => {
        startGame();
      }, { duration: 150 });
    }

    document.addEventListener("keydown", (event) => {
      primeAudio();
      if (nightIntroVisible) {
        event.preventDefault();
        hideNightIntroScreen();
        return;
      }

      if (startScreenEl && !startScreenEl.classList.contains("hidden")) {
        return;
      }

      if (event.repeat || isTypingField(event.target) || isPaused) {
        return;
      }

      const pressedLetter = normalizeLetter(event.key);

      if (!alphabet.includes(pressedLetter)) {
        return;
      }

      event.preventDefault();
      handleGuess(pressedLetter);
    });

    document.addEventListener("click", (event) => {
      primeAudio();
      const tappable = event.target.closest("button, .category-chip, .select, .text-input");
      if (!tappable) {
        return;
      }

      playUiSoundForElement(tappable);
      triggerUiTap(tappable);
      if (tappable.classList.contains("category-chip")) {
        triggerUiFieldFlash(tappable);
      }
    });

    document.addEventListener("input", (event) => {
      const field = event.target.closest("input[type='text'], input[type='number'], .text-input");
      if (!field) {
        return;
      }

      triggerUiFieldFlash(field);
    });

    document.addEventListener("change", (event) => {
      const field = event.target.closest("select, input[type='checkbox']");
      if (!field) {
        return;
      }

      const flashTarget = field.closest(".category-chip") || field;
      triggerUiFieldFlash(flashTarget);
    });

    document.addEventListener("focusin", (event) => {
      const field = event.target.closest("button, .text-input, .select, .avatar-picker, .style-picker");
      if (!field) {
        return;
      }

      if (field.classList.contains("avatar-picker") || field.classList.contains("style-picker")) {
        triggerUiFieldFlash(field);
        return;
      }

      if (field.matches("button")) {
        triggerUiTap(field);
        return;
      }

      triggerUiFieldFlash(field);
    });

    document.addEventListener("pointerdown", () => {
      primeAudio();
    }, { passive: true });
    document.addEventListener("touchstart", () => {
      primeAudio();
    }, { passive: true });

    singlePlayerBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("single"); });
    twoPlayerBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("two"); });
    competitionModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("competition"); });
    onlineModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("online"); });
    tournamentModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("tournament"); });
    partyModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("party"); });
    survivalModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("survival"); });
    practiceModeBtn.addEventListener("click", () => { currentSelection.dailyChallenge = false; currentSelection.weeklyChallenge = false; setMode("practice"); });
    timePillsEl?.addEventListener("click", (event) => {
      const button = event.target.closest(".time-pill");

      if (!button) {
        return;
      }

      currentSelection.timeLimit = Number(button.dataset.time);
      timeSelectEl.value = String(currentSelection.timeLimit);
      renderTimePills();
    });
    categorySelectEl.addEventListener("change", () => {
      syncSelectionFromControls();
      currentSelection.selectedCategories = [];
      setAddWordNote();
      renderCategoryGrid();
      applyVisualTheme(categorySelectEl.value);
    });
    categoryGridEl?.addEventListener("change", () => {
      syncSelectionFromControls();
      if (currentSelection.selectedCategories.length) {
        currentSelection.category = "all";
        categorySelectEl.value = "all";
      }
      renderCategoryGrid();
      applyVisualTheme(currentSelection.selectedCategories[0] || currentSelection.category);
    });
    clearCategoriesButtonEl?.addEventListener("click", () => {
      currentSelection.selectedCategories = [];
      renderCategoryGrid();
      applyVisualTheme(currentSelection.category);
    });
    addWordButtonEl?.addEventListener("click", addWordToBank);
    favoriteWordButtonEl?.addEventListener("click", saveWordToFavoritesOnly);
    removeWordButtonEl?.addEventListener("click", removeWordFromBank);
    onlineRoomCardEl?.addEventListener("click", (event) => {
      const copyButton = event.target.closest("[data-online-copy-link]");
      if (!copyButton) {
        return;
      }
      copyOnlineRoomLink();
    });
    addWordInputEl?.addEventListener("keydown", (event) => {
      if (event.key === "Enter") {
        event.preventDefault();
        addWordToBank();
      }
    });
    difficultySelectEl.addEventListener("change", syncSelectionFromControls);
    difficultyPillsEl?.addEventListener("click", (event) => {
      const button = event.target.closest(".difficulty-pill");
      if (!button) {
        return;
      }

      currentSelection.difficulty = button.dataset.difficulty;
      difficultySelectEl.value = currentSelection.difficulty;
      renderDifficultyPills();
    });
    specialModePillsEl?.addEventListener("click", (event) => {
      const button = event.target.closest(".special-mode-pill");
      if (!button) {
        return;
      }

      currentSelection.specialMode = button.dataset.specialMode;
      if (currentSelection.specialMode === "kids") {
        currentSelection.difficulty = "easy";
        difficultySelectEl.value = "easy";
      }
      if (currentSelection.specialMode === "story") {
        currentSelection.difficulty = storyChapters[Math.min(storyChapters.length - 1, storyProgress.chapter || 0)].difficulty;
        difficultySelectEl.value = currentSelection.difficulty;
      }
      if (currentSelection.specialMode === "challenge") {
        currentSelection.difficulty = "hard";
        difficultySelectEl.value = "hard";
        currentSelection.timeLimit = 90;
        timeSelectEl.value = "60";
      }
      if (currentSelection.specialMode === "extreme") {
        currentSelection.difficulty = "hard";
        difficultySelectEl.value = "hard";
      }
      renderDifficultyPills();
      renderSpecialModePills();
    });
    timeSelectEl.addEventListener("change", () => {
      syncSelectionFromControls();
      renderTimePills();
    });
    partyRoundsInputEl.addEventListener("change", () => {
      currentSelection.partyRounds = clampPartyRounds(partyRoundsInputEl.value);
      partyRoundsInputEl.value = String(currentSelection.partyRounds);
    });
    partyPlayerCountInputEl?.addEventListener("change", () => {
      currentSelection.partyPlayerCount = clampPartyPlayerCount(partyPlayerCountInputEl.value);
      partyPlayerCountInputEl.value = String(currentSelection.partyPlayerCount);
      renderPartyPlayerFields();
      renderBonusBuyerOptions();
      renderAvatars();
      renderShop();
    });
    partyPlayersFieldsEl?.addEventListener("input", (event) => {
      const input = event.target.closest(".party-player-name");
      if (!input) {
        return;
      }

      const index = Number(input.dataset.partyIndex);
      if (!partyPlayers[index]) {
        return;
      }

      partyPlayers[index].name = sanitizePlayerName(input.value) || `שחקן ${index + 1}`;
      renderBonusBuyerOptions();
      renderAvatars();
      renderShop();
    });
    partyPlayersFieldsEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-party-avatar]");
      if (!button) {
        return;
      }

      const index = Number(button.dataset.partyIndex);
      if (!partyPlayers[index]) {
        return;
      }

      partyPlayers[index].avatar = button.dataset.partyAvatar;
      renderPartyPlayerFields();
      renderBonusBuyerOptions();
      renderAvatars();
      renderShop();
    });
    playerOneInputEl.addEventListener("input", () => {
      syncSelectionFromControls();
      renderBonusBuyerOptions();
      renderAvatars();
      renderSideProfileDock();
      renderOnlineRoomCard();
      renderShop();
    });
    playerTwoInputEl.addEventListener("input", () => {
      syncSelectionFromControls();
      renderBonusBuyerOptions();
      renderAvatars();
      renderSideProfileDock();
      renderOnlineRoomCard();
      renderShop();
    });
    bonusOwnerSelectEl?.addEventListener("change", () => {
      syncSelectionFromControls();
      renderShop();
    });
    customTopicInputEl.addEventListener("input", () => {
      syncSelectionFromControls();
      applyVisualTheme(topicLookup[sanitizeTopicValue(customTopicInputEl.value)] || sanitizeTopicValue(customTopicInputEl.value) || "custom");
    });
    [singleAvatarPickerEl, playerOneAvatarPickerEl, playerTwoAvatarPickerEl, profileAvatarPickerEl].forEach((picker) => {
      picker?.addEventListener("click", (event) => {
        const button = event.target.closest(".avatar-option");
        if (!button) {
          return;
        }

        currentSelection[button.dataset.avatarKey] = button.dataset.avatar;
        renderAvatarPickers();
        renderAvatars();
        renderSideProfileDock();
      });
    });
    customWordInputEl.addEventListener("input", syncSelectionFromControls);
    startButtonEl.addEventListener("click", () => {
      ensureAudioContext();
      startConfiguredGame();
    });
    nightModeButtonEl?.addEventListener("click", () => {
      nightModeEnabled = !nightModeEnabled;
      document.body.classList.toggle("night-mode", nightModeEnabled);
      if (nightModeButtonEl) {
        nightModeButtonEl.textContent = nightModeEnabled ? "בטל מצב לילה" : "מצב לילה";
      }
      saveNightMode();
      if (nightModeEnabled) {
        showNightIntroScreen();
      } else if (nightIntroVisible) {
        nightIntroVisible = false;
        nightIntroScreenEl?.classList.remove("visible");
        showStartScreen();
      }
    });
    saveProfileButtonEl?.addEventListener("click", saveCurrentProfile);
    profilesButtonEl?.addEventListener("click", openProfilesPanel);
    closeProfilesButtonEl?.addEventListener("click", closeProfilesPanel);
    profileListEl?.addEventListener("click", (event) => {
      const selectButton = event.target.closest("[data-select-profile]");
      if (selectButton) {
        const nextIndex = selectButton.dataset.selectProfile === "default"
          ? -1
          : Number(selectButton.dataset.selectProfile);
        if (selectedProfileTabIndex === nextIndex) {
          profileDetailVisible = !profileDetailVisible;
        } else {
          selectedProfileTabIndex = nextIndex;
          profileDetailVisible = true;
        }
        renderProfiles();
        return;
      }

      const loadButton = event.target.closest("[data-load-profile]");
      if (loadButton) {
        loadProfile(Number(loadButton.dataset.loadProfile));
        return;
      }

      const editButton = event.target.closest("[data-edit-profile]");
      if (editButton) {
        editProfile(Number(editButton.dataset.editProfile));
        return;
      }

      const deleteButton = event.target.closest("[data-delete-profile]");
      if (deleteButton) {
        deleteProfile(Number(deleteButton.dataset.deleteProfile));
        return;
      }

      const openButton = event.target.closest("[data-open-profile]");
      if (openButton) {
        openFullProfile(Number(openButton.dataset.openProfile));
      }
    });
    profileDetailEl?.addEventListener("click", (event) => {
      const loadButton = event.target.closest("[data-load-profile]");
      if (loadButton) {
        loadProfile(Number(loadButton.dataset.loadProfile));
        return;
      }

      const editButton = event.target.closest("[data-edit-profile]");
      if (editButton) {
        editProfile(Number(editButton.dataset.editProfile));
        return;
      }

      const deleteButton = event.target.closest("[data-delete-profile]");
      if (deleteButton) {
        deleteProfile(Number(deleteButton.dataset.deleteProfile));
        return;
      }

      const openButton = event.target.closest("[data-open-profile]");
      if (openButton) {
        openFullProfile(Number(openButton.dataset.openProfile));
      }
    });
    closeProfileModalButtonEl?.addEventListener("click", closeFullProfile);
    profileModalEl?.addEventListener("click", (event) => {
      if (event.target === profileModalEl) {
        closeFullProfile();
      }
    });
    packListEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-pack-unlock]");
      if (!button) {
        return;
      }
      unlockTopicPack(button.dataset.packUnlock);
    });
    stylePickerEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-style-key]");
      if (!button) {
        return;
      }
      selectedStyle = button.dataset.styleKey;
      savePersonalStyle();
      applyPersonalStyle();
      applyVisualTheme(activeTopicKey);
      renderStylePicker();
      renderProfiles();
      renderProfileSummary();
      renderStartDashboard();
    });
    backgroundTabsEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-bg-tab]");
      if (!button) {
        return;
      }
      selectedBackgroundTab = button.dataset.bgTab;
      if (selectedBackgroundTab === "theme") {
        selectedBackgroundStyle = "theme";
        saveBackgroundStyle();
        applyVisualTheme(activeTopicKey);
      }
      renderBackgroundPills();
    });
    backgroundPillsEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-bg-style]");
      if (!button) {
        return;
      }
      selectedBackgroundStyle = button.dataset.bgStyle;
      selectedBackgroundTab = selectedBackgroundStyle === "theme"
        ? "theme"
        : (["sky", "sun", "mint", "rose", "lavender", "peach", "lime", "ice", "coral", "white", "black"].includes(selectedBackgroundStyle) ? "solid" : "gradient");
      saveBackgroundStyle();
      applyVisualTheme(activeTopicKey);
      renderBackgroundPills();
    });
    dailyChallengeButtonEl?.addEventListener("click", () => {
      currentSelection.dailyChallenge = true;
      currentSelection.weeklyChallenge = false;
      setMode("single");
      hideStartScreen();
      transitionWithLoading(() => {
        startGame();
      }, {
        duration: 150,
        title: "פותחים את האתגר היומי...",
        subtitle: "מילה אחת מיוחדת ליום הזה."
      });
    });
    weeklyChallengeButtonEl?.addEventListener("click", () => {
      currentSelection.dailyChallenge = false;
      currentSelection.weeklyChallenge = true;
      setMode("single");
      hideStartScreen();
      transitionWithLoading(() => {
        startGame();
      }, {
        duration: 150,
        title: "פותחים את האתגר השבועי...",
        subtitle: "מילה מיוחדת שמתחלפת פעם בשבוע."
      });
    });
    dailyWheelButtonEl?.addEventListener("click", spinDailyWheel);
    wordSearchInputEl?.addEventListener("input", (event) => {
      wordSearchTerm = event.target.value.trim();
      renderWordManager();
    });
    wordManagerListEl?.addEventListener("click", (event) => {
      const favoriteButton = event.target.closest("[data-favorite-word]");
      if (!favoriteButton) {
        return;
      }
      toggleFavoriteWord(favoriteButton.dataset.favoriteWord);
    });
    prizeBoxListEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-open-prize-box]");
      if (!button) {
        return;
      }
      openPrizeBox(button.dataset.openPrizeBox);
    });
    cosmeticShopListEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-buy-cosmetic]");
      if (!button) {
        return;
      }
      buyCosmeticItem(button.dataset.buyCosmetic);
    });
    musicStyleSelectEl?.addEventListener("change", (event) => {
      musicStyle = event.target.value || "calm";
      saveMusicPreference();
      if (musicEnabled) {
        startBackgroundMusic();
      } else {
        renderMusicState();
      }
    });
    musicToggleButtonEl?.addEventListener("click", toggleBackgroundMusic);
    musicTestButtonEl?.addEventListener("click", testBackgroundMusic);
    achievementBadgesEl?.addEventListener("click", (event) => {
      const button = event.target.closest("[data-achievement-tab]");
      if (!button) {
        return;
      }
      renderAchievementBadges.selectedTab = button.dataset.achievementTab || "all";
      renderAchievementBadges();
    });
    resetButton.addEventListener("click", () => {
      ensureAudioContext();
      transitionWithLoading(() => {
        startGame();
      }, {
        duration: 120,
        title: "מכינים משחק חדש...",
        subtitle: "רק רגע ומתחילים שוב."
      });
    });
    pauseButtonEl?.addEventListener("click", () => {
      ensureAudioContext();
      setPaused(!isPaused);
    });
    hintButtonEl.addEventListener("click", useHint);
    freezeButtonEl?.addEventListener("click", useFreezeTime);
    lifeButtonEl?.addEventListener("click", useExtraLife);
    skipButtonEl?.addEventListener("click", useSkipWord);
    shieldButtonEl?.addEventListener("click", useShield);
    buyFreezeButtonEl?.addEventListener("click", () => buyBonusItem("freeze"));
    buyLifeButtonEl?.addEventListener("click", () => buyBonusItem("life"));
    buySkipButtonEl?.addEventListener("click", () => buyBonusItem("skip"));
    buyShieldButtonEl?.addEventListener("click", () => buyBonusItem("shield"));
    homeEnterButtonEl?.addEventListener("click", () => {
      homeIntroSeen = true;
      saveHomeIntroSeen();
      homeOverlayEl?.classList.add("hidden");
    });
    switchModeButtonEl.addEventListener("click", () => {
      transitionWithLoading(() => {
        showStartScreen();
      }, {
        duration: 110,
        title: "חוזרים להגדרות...",
        subtitle: "עוד רגע בוחרים מצב חדש."
      });
    });
    resetDataButtonEl?.addEventListener("click", resetAllData);
    endButtonEl?.addEventListener("click", () => {
      ensureAudioContext();

      if (pendingEndAction) {
        const action = pendingEndAction;
        pendingEndAction = null;
        action();
        return;
      }

      if (currentSelection.mode === "two") {
        transitionWithLoading(() => {
          showStartScreen();
        }, {
          duration: 110,
          title: "מכינים מילה חדשה...",
          subtitle: "מחזירים למסך ההגדרות."
        });
        return;
      }

      transitionWithLoading(() => {
        startGame();
      }, {
        duration: 120,
        title: "מתחילים שוב...",
        subtitle: "עוד שנייה והמילה הבאה כאן."
      });
    });
    endHomeButtonEl?.addEventListener("click", () => {
      hideEndScreen();
      showStartScreen();
      homeIntroSeen = true;
      homeOverlayEl?.classList.add("hidden");
    });

    document.getElementById("whatsapp-share-btn")?.addEventListener("click", () => {
      const won = !endScreenEl?.classList.contains("lose");
      const word = secretWord || "?";
      const mistakes = wrongGuesses;
      const maxW = maxWrong;
      const topic = activeTopicLabel || "כללי";
      const icon = categoryIcons[activeTopicKey] || "🎲";
      const hearts = Array(maxW).fill("").map((_, i) => i < (maxW - mistakes) ? "🟢" : "🔴").join("");
      const msg = won
        ? `🎉 ניצחתי באיש תלוי!\n\n📝 המילה: ${word}\n${icon} נושא: ${topic}\n❤️ ${hearts}\n✅ נשארו לי ${maxW - mistakes} חיים\n\nבוא תנסה גם אתה! 🕹️`
        : `😅 הפסדתי באיש תלוי...\n\n📝 המילה הייתה: ${word}\n${icon} נושא: ${topic}\n❤️ ${hearts}\n\nבוא תנסה להכות אותי! 🕹️`;
      const url = `https://wa.me/?text=${encodeURIComponent(msg)}`;
      window.open(url, "_blank");
    });

    loadCustomWords();
    loadTopicPacks();
    loadRecentWords();
    rebuildWordCollections();
    loadAchievements();
    loadLeaderboard();
    loadModeRecords();
    loadBonusStore();
    loadProfiles();
    loadPlayerStats();
    loadPlayerHistory();
    loadDailyChallengeState();
    loadWeeklyChallengeState();
    loadLoginStreakState();
    loadDailyMissionsState();
    loadWeeklyMissionsState();
    loadDailyWheelState();
    loadStoryProgress();
    loadFavoriteWords();
    loadPlayerXp();
    loadCosmeticInventory();
    loadTopicStats();
    loadMonthlyMissionsState();
    loadPrizeBoxState();
    loadMusicPreference();
    loadHomeIntroSeen();
    loadPersonalStyle();
    loadBackgroundStyle();
    loadNightMode();
    maybeAdvanceLoginStreak();
    populateCategoryOptions();
    renderAchievementBadges();
    renderLeaderboard();
    renderModeRecords();
    renderShop();
    renderProfiles();
    renderSideProfileDock();
    renderStylePicker();
    renderBackgroundPills();
    applyPersonalStyle();
    renderPlayerStats();
    renderProfileSummary();
    renderStoryCard();
    renderOnlineRoomCard();
    renderAmbientLayer();
    renderStartDashboard();
    renderDailyChallenge();
    renderWeeklyChallenge();
    renderDailyMissions();
    renderWeeklyMissions();
    renderMonthlyMissions();
    renderLoginStreak();
    renderDailyWheel();
    renderGuideCard();
    renderStoryMap();
    renderProgressMiniMap();
    renderProfileLeaderboard();
    renderTrophyRoom();
    renderTournamentTree();
    renderCosmeticShop();
    renderWordManager();
    renderFavoriteWords();
    renderTopicStats();
    renderPrizeBoxes();
    renderMusicState();
    setMode("single");
    if (musicEnabled) {
      startBackgroundMusic();
    }
    renderAvatarPickers();
    renderTimePills();
    renderDifficultyPills();
    homeOverlayEl?.classList.toggle("hidden", homeIntroSeen);
    renderSpecialModePills();
    renderCategoryGrid();
    updateTimerDisplay();
    renderKeyboard();
    updateStats();
    updateBonusButtons();
    applyIncomingOnlineRoom();
  </script>
</body>
</html>
